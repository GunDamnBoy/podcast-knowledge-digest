# MAINTENANCE — 節目知識庫・維護說明

**這份文件是給「要修改這套系統」的人（或 Claude）看的，不是給執行每日產出的人看的。**
每日產出請看 `AGENT_BRIEF.md`。

想改動任何東西時，最快的方式是開一個新對話輸入 `/podcast-maintain`，或直接說「讀 MAINTENANCE.md」。

**分工原則（2026-08-03 確立）**：`AGENT_BRIEF.md` 只寫**現在的規格與判斷規則**，本檔負責**為什麼會變成這樣**——事故經過、誤判過程、被否決的選項。brief 是每日排程每天完整讀一次的東西，塞事故史會讓它越長越慢，而且執行者根本不需要知道當初誰誤判了什麼。歷史寫在這裡，brief 用一行指過來。

---

## 1. 這套系統由哪些東西定義

| # | 檔案／位置 | 角色 | 誰會讀它 |
|---|---|---|---|
| 1 | `~/podcast-knowledge-digest/AGENT_BRIEF.md` | **完整規格**：23 檔節目清單、全文來源、podfetch 管線、內容規格、資料格式、發布流程 | 每日排程在第 0 步完整讀過 |
| 2 | 排程任務 `podcast-digest-daily` 的 SKILL.md | **執行骨架**：流程順序與分支判斷。事實細節指向 brief 章節，不重抄——**唯一例外是第 3 步的子代理任務卡**（子代理讀不到 brief，卡上抄了第 3／4 節的必要規格，**改內容規格時兩邊都要改**，healthcheck 檢查不到這種同步） | 排程觸發時直接執行 |
| 3 | `~/podcast-knowledge-digest/index.html` | 前端外殼：CSS、渲染邏輯、節目徽章 | 瀏覽器 |
| 4 | `~/podcast-knowledge-digest/data/*.json` | 每日內容 | `index.html` |
| 5 | `~/.podfetch/`（腳本、`config.json`、`shows.json`、`state.json`、`logs/`） | 01:00 的轉錄管線，由 launchd `com.kenny.podfetch` 觸發 | 排程排錯時會讀 |
| 6 | `~/podcast-transcripts/<date>/` | podfetch 產出的逐字稿與 `manifest.json` | 排程第 1 步 |
| 7 | `~/.podfetch/healthcheck.py` | 健康檢查（對受檢系統唯讀，只寫自己的 `metrics.csv`） | 維護時第一步就跑 |
| 8 | `~/.podfetch/fix-schedule.sh` | 把 launchd 時刻寫回 01:00 的驗證／還原工具 | 只在確認 plist 被改動時 |
| 9 | 本檔 `MAINTENANCE.md` | 維護說明＋事故與決策檔案 | 維護者 |

**最重要的一條規則：第 1 項與第 2 項是一組兩份，改任一邊都必須同步另一邊。**
兩者不同步時，排程會拿到互相矛盾的指示，而且不會報錯。

排程 SKILL.md 沒有版本控制，只有 `AGENT_BRIEF.md` 在 git 裡。**brief 才是真正的來源。**

---

## 2. 修改的標準流程

0. **先存快照**：`bash ~/.podfetch/snapshot.sh "開工前"`。repo 外的檔案沒有 git，改壞或被覆寫只能靠它還原（見第 12 節）。收工前再存一份「收工後」。
1. **跑 `python3 ~/.podfetch/healthcheck.py`**，把機械式檢查一次做完（它會順便把當日指標寫進 `metrics.csv`）。
2. 讀 `AGENT_BRIEF.md`（全部）與排程 SKILL.md（`list_scheduled_tasks` 取得 `path` 後 Read）。
3. **先比對兩者是否已經不同步**，有的話先修好再談新需求。
4. 改 `AGENT_BRIEF.md`。
5. 用 `mcp__scheduled-tasks__update_scheduled_task` 同步排程 prompt。
6. 在 brief 第 8 節加變更紀錄；**事故經過與被否決的選項寫進本檔第 7 節**，不要寫進 brief。
7. 若動到節目清單，`~/.podfetch/shows.json` 與 `index.html` 的 `.ep.s-<key>::before`／`.b-<key>`／深色模式那一組都要補。
8. 收工前再跑一次 `healthcheck.py`，並用子代理獨立比對 brief 與 SKILL.md。
9. **不要跑任何 git 指令**（含 `git status`）——`com.kenny.dashpush` 每 180 秒自動推送，跑 git 會留下 `.git/index.lock` 擋住推送。要看推送鏈用 `cat .git/refs/heads/main`、`tail .git/logs/refs/remotes/origin/main`，這些是唯讀指令，安全。

---

## 3. 健康檢查

```
python3 ~/.podfetch/healthcheck.py
```

對受檢的系統唯讀、不碰 git；**唯一會寫的檔案是它自己的 `~/.podfetch/metrics.csv`**（每日指標，同日重跑覆寫該列）。在 Cowork 的 Linux 沙箱裡也能跑（會自動偵測掛載點）。檢查項目：

- `data/*.json` 可解析、`index.json` 由新到舊、`days` 與實際檔案雙向對應、`episodeCount` 與實際集數相符
- 資料用到的 `showKey` 是否都有色條／徽章／深色模式 CSS，且都沿用 `shows.json` 的鍵值
- podfetch 當日日誌**是否有 `01:0x` 的執行**（掃全檔「視窗：」行，**不是只看第一行**——手動執行會把排程擠出檔案開頭，08-10 修）、是 0 集還是異常收束、`state.json` 的 `last_run_utc` 新鮮度
- 最新逐字稿目錄的 `OK`／`DEGRADED`／`FAILED` 統計
- 推送鏈 local vs origin（唯讀 `cat`）
- **最新一份資料檔裡的 `⚠︎` 佔位集數**（下次抓到全文要重寫，不可被去重擋掉）
- **線上狀態**：帶 cache-buster 抓線上 `index.json` 比對 `updatedLabel` 與 `days[0].date`。**沙箱連不到 GitHub 會顯示 WARN 而非 PASS**，要在 Mac 上跑才驗得到這一項
- **節目數量**：brief 宣稱的檔數與 `shows.json` 是否一致
- **brief 引用的 `config.json` 數值是否與實際設定相符**——這一項專門抓 2026-08-03 那類「下文改了、上文漏改」的內部矛盾

結束碼 0 = 通過或僅有 WARN，1 = 有 FAIL。

---

## 4. 已知的坑（踩過才寫進來的，不要再踩一次）

- **「當天沒有目錄」≠「podfetch 掛了」。** 0 集時 podfetch 正常結束但不建立目錄。照 brief 第 2 節的三段排查：(1) 資料夾有沒有連線 → (2) 讀當天日誌，停在異常處就是失效；**但看到 `沒有新集數。` 不可以直接判成真的 0 集——那一格在 2026-08-15 被證明會漏掉整天 10 集，必須用帶 `&cb=` 的 iTunes 查詢做一次外部對照**（見第 7 節與 brief 第 2 節） → (3) **當天日誌根本不存在時先別判失效**，讀 `logs/` 裡最新那份「視窗：」行的時間戳（**掃全檔，不要只看第一行**——手動執行會把排程擠出檔案開頭），沒有任何一次 `[01:` 就是執行時刻漂到日報後面，不是 podfetch 死了。第 (3) 格的修法與失效完全不同，講錯會讓人往錯方向修。
- **「日誌時間戳對不上」有兩種成因，三項都要查完，順序是 plist 優先。** (a) plist 的時刻值不對 → `/usr/libexec/PlistBuddy -c "Print :StartCalendarInterval" ~/Library/LaunchAgents/com.kenny.podfetch.plist`，應為 `Hour = 1`；(b) 機器睡著、launchd 延後補跑 → `pmset -g custom` 看 AC Power 的 `sleep` 是否為 0、`pmset -g sched` 看有沒有排定喚醒。**先查 plist**，它最便宜最確定，而且「整點加一秒」（如 `[07:00:01]`）正是準時觸發的特徵、直接指向 (a)。
- **除錯時先問「這個值今天有沒有被人動過」。** 在正在被修改的系統上，讀到的現況不等於事發時的狀況——2026-08-03 就是這樣把修改後的 plist 當成修改前的證據，記錯了一次根因（見第 7 節）。
- **`pmset` 電源設定不存在於任何設定檔裡。** 重灌或換機不會遷移，且在「只靠電池」與「完全關機」兩種情況下會失效。使用習慣一改（晚上關機、不插電），01:00→03:00 的時序保證就沒了。**它與 plist 是兩件獨立的事**：plist 決定幾點跑，`pmset` 決定那個時刻機器醒不醒著。
- **驗證上線一定要帶 cache-buster。** 裸網址與 `raw.githubusercontent.com` 都會回舊快取（實測回到三天前），且要同時確認 `updatedLabel` 是本次執行時間，只看日期會被騙。
- **`showKey` 一律沿用 `~/.podfetch/shows.json` 的鍵值**，不要在網站端另取名字，否則徽章永遠對不上。23 檔節目**全部**定義了 CSS（`index.html` 共 **26 組**，另含三組已移出現役但歷史資料仍在站上的 `capitalallocators`／`bg2`／`breakdowns`）。08-11 起沒有節目走預設藍；新增節目時若忘了補 CSS，`healthcheck.py` 的「showKey CSS」會 WARN。**移出節目時 CSS 不刪**，見第 5 節開頭。
- **iTunes lookup 的 US 商店快取嚴重過期**，尤其 All-In，而且 limit 越小快取越舊。**第一線解法是一律帶 `&cb=<時間戳>`**（`podfetch.py` 的 `itunes_lookup()` 已內建，手動查詢要自己加）——快取綁在確切的查詢字串上，換任何變動參數就繞開。**要換商店只能用查詢參數 `&country=GB`；路徑前綴 `itunes.apple.com/gb/lookup`／`/au/` 在本環境回空白內容，本檔與 brief 過去寫的那條退路實際上不存在**（2026-08-15 實測）。2026-08-15 因為缺 `cb` 而整天回報「沒有新集數」，實際有 10 集，見第 7 節。
- **Substack 的 `/api/v1/archive` 也會回過期快取（2026-08-05）。** Dwarkesh 與 Latent Space 首次取回的最新一筆分別停在 6/08 與 7/08，要帶 cache-buster 才拿到當天。**失效的樣子是「這檔今天沒有新集數」**，於是安靜退回機器轉錄。與 iTunes US 商店是同一類問題，凡是「清單型 API」都要先懷疑快取。
- **`web_fetch` 約 104,700 字元上限，超過會截斷且不報錯（2026-08-05）。** Latent Space 那集官方稿只取到 73%。取長逐字稿後要比對末尾時間戳與 `trackTimeMillis`，不足就用 podfetch 補齊末段並在 `source` 標明兩個來源各涵蓋哪一段。
- **`ft.com` 已進入 `web_fetch` 封鎖清單（2026-08-06，HTTP 403）。** FT／Unhedged 只剩 Chrome 一條路，**2026-08-08 已在無人值守排程中實測成功**。另外**入口網址連錯兩次**：`ft.com/unhedged` 是電子報存檔、不含 podcast 集數；08-08 改成的 `ft.com/the-economics-show` **只列姊妹節目自己的集數，一樣不含 Unhedged**。**實際可用的入口是 FT 站內搜尋 `ft.com/search?q="<原文標題>"`，而且每集有兩個頁面，要取標題帶 `Transcript:` 前綴的那一個**（另一個集數頁正文僅約 2,700 字元，很容易被誤判成付費牆）。Unhedged 的 feed 也會放姊妹節目的重播。
- **講者標記會被 Gemini 誤植，而完整度檢查抓不到（2026-08-06）。** 實例：完整度 1.01、狀態 OK，但中段起把主持人標成片頭廣告旁白者。`shows.json` 的主持人名單只是提示。**這是繼 503、0 字回傳之後的第四種失敗態，而且是唯一一種「所有機械檢查都顯示正常」的**。
- **改防護機制時要問「它在最壞情況下會怎麼失敗」。** 典型形態是正常路徑有效、極端路徑放大傷害（08-04 修 503 造成 08-06 的 Lite 被永久除名；08-03 修時序造成根因誤判）。**次數與完整清單一律以第 12 節登記簿的第一類 bullet 為準，不要在這裡另記一個數字**——本行過去寫死「連續兩次」，後來與登記簿脫鉤。
- **「收工後」快照要在**最後一次**改動之後取，否則快照鏈會出現空窗（2026-08-16）。** 08-16 那場的「收工後」快照取於 18:27，但 `08-16 ⑤`（第 5.5 步、回報第 16 項、成本模型訂正）是在那之後才寫進 `SKILL.md`——**於是當晚稽核時，SKILL.md 唯一的還原點是舊的**，若那時檔案遺失，⑤ 那一輪的工作就沒了。**而且症狀會偽裝成規格漂移**：對讀「brief 宣稱已同步 vs 快照查無實據」，看起來就像 SKILL.md 沒改。當晚由使用者貼回正本、以 `2026-08-16-233000` 補存還原。**「改了 SKILL.md 就存快照」這條規則本身沒問題，錯在把快照當成「收工的儀式」而不是「每次改動的收尾」。**
- **`snapshot.sh` 曾經會安靜產生「空快照」（2026-08-16 修）。** 它的 `BASE` 原本寫死 `$HOME/.podfetch`、**沒有掛載點偵測**，所以在 Cowork 沙箱裡（`$HOME` 不是 `/Users/kenny`）每個檔案都印「（缺）」，然後**照樣建目錄、寫 MANIFEST、回報「快照完成」**——那筆會出現在 `--list` 裡，看起來就是一個正常的還原點。**而 repo 外的檔案沒有 git，快照是唯一還原點。** 已補上與 `healthcheck.py` 的 `resolve()` 同一套順序（先找 `/sessions/*/mnt`、再退回家目錄），並加了守衛：**一個檔都沒抄到就自刪目錄、印錯誤、回非 0**。`SKILL.md` 單獨缺仍屬正常（`~/Documents` 是 macOS TCC 保護區，沙箱看不到）。**教訓：兩支腳本讀同一組路徑，只有一支做了偵測——「另一支也需要嗎」這個問題當時沒有人問。**
- **`update_scheduled_task` 的 `prompt` 是整份覆寫，不是局部編輯。** 送出前一定要先 Read 目前的 SKILL.md，以它為基礎改。2026-08-04 就這樣蓋掉了另一場維護的四項改動（見第 7 節）。**「我記得的版本」在多場維護之間一定會過期**，而 SKILL.md 不在 git 裡，沒有任何機制會擋。
- **brief 說「已同步進 SKILL.md」不等於真的同步。** 紀錄與實況會脫鉤，而紀錄看起來更可信。子代理比對時要專門檢查這一項。
- **`web_fetch` 對 RSS／XML 一律回 `[binary data]`**，不要指望直接讀 feedUrl。
- **FT 存取失效時不會報錯**，只會安靜退回付費牆。每次都要檢查正文長度（Unhedged 通常 5,000 字元以上）。
- **YouTube 字幕自 2026-08-02 起全面失效**，只當最後手段，每集最多試 5 分鐘。操作要領見第 8 節。
- **Word 報告絕對不能存進 repo**（那是 public repo），要寫到暫存輸出資料夾再用 `present_files` 交付。
- **前一晚的美東晚間集數必定順延到隔天那一版**，這是 03:00 時段的設計取捨，不是故障，不要去追。
- **brief 內部會自打架。** 邊做邊改時習慣在下文補新值卻漏改上文，2026-08-03 一次抓到四處。`healthcheck.py` 的「brief vs config」就是為此而寫，但它只涵蓋數值型設定，敘述性矛盾仍要靠子代理比對。

---

## 4B. 基礎設施完整參考（自 brief 第 6 節移入，2026-08-08）

brief 第 6 節只留每日執行需要的，完整版在這裡：

- **Repo（本機）**：`~/podcast-knowledge-digest`（家目錄下，不要放 `~/Documents`，macOS TCC 會擋背景程式）。
- **GitHub**：`GunDamnBoy/podcast-knowledge-digest`，Public，Pages（Source ＝ GitHub Actions）。
- **推送認證**：remote URL 內嵌 fine-grained PAT（只授權此 repo、Contents 讀寫），存於本機 `.git/config`。換 token：產新 PAT → `git -C ~/podcast-knowledge-digest remote set-url origin https://<新PAT>@github.com/GunDamnBoy/podcast-knowledge-digest.git` → 撤舊。
- **背景推送腳本**：`~/.dashpush/auto-push.sh`，多 repo 版，依序處理 `advisory-knowledge-hub` 與 `podcast-knowledge-digest`，launchd `com.kenny.dashpush` 每 180 秒觸發。
- **podfetch 排程**：launchd `com.kenny.podfetch` 每天 01:00；`~/.podfetch/fix-schedule.sh` 可把 plist 寫回 01:00（驗證／還原用，不是例行修復）。
- **電源設定**：見第 9 節（`pmset -c sleep 0` 主保障＋00:55 排定喚醒後備、AlDente 否決理由、FileVault 單點）。
- **每日轉檔**：`~/.podfetch/json2docx.py` 把 `data/YYYY-MM-DD.json` 轉成 Word（python-docx；沙箱用 `pip install python-docx --break-system-packages`）。**內容以 JSON 為唯一來源**，不要讓 LLM 重寫一遍。

---

## 5. 新增一檔節目的完整步驟

> **移出一檔節目走同一張表，但第 4 步反過來：`index.html` 的三組 CSS 一律保留不刪**（2026-08-16 立）。歷史日檔仍然帶著舊 `showKey`，刪掉色碼那些集數就會掉回預設藍。`healthcheck.py` 的 showKey 檢查早就寫明「歷史檔案含已移除的節目是正常的」，只比對**最新一份**——所以留著不會製造 FAIL。目前有三組屬於這種狀態：`capitalallocators`、`bg2`、`breakdowns`。
> **另外第 6 步有可能「不需要動」**：一進一出時總數不變，SKILL.md 的 `description` 就不必改。**這種時候要在登記簿明寫「因總數不變而未動」**——不然事後看，「不必動」與「忘了動」長得一模一樣。

1. `AGENT_BRIEF.md` 第 1 節：加進節目清單，含 AppleID、官方逐字稿來源（若有）、排序位置。
2. `~/.podfetch/shows.json`：加上 `appleId`／`name`／`hosts`（主持人名單會餵給轉錄 prompt，讓 Gemini 寫真名而不是 `Speaker A`）。
3. `~/.podfetch/config.json` 的 `show_priority`：插到適當位置（額度不足時先犧牲排在後面的）。
4. `index.html`：補 `.ep.s-<key>::before`、`.b-<key>`、`html[data-theme="dark"] .b-<key>` 三處。**撞色要三處分別檢查**——08-10 只查了色條就宣稱查過，結果徽章文字色與現役的 `unhedged` 只差 12（RGB 距離），等於同一個顏色。實務門檻：**與最近鄰的 RGB 距離至少 25，低於 20 一定要換。**
5. **`README.md` 兩處**：開頭的「偵測 N 檔節目」與「追蹤的節目」清單。**這一步 08-10 漏了**——數字改了、清單沒補，而 `healthcheck.py` 只比對 brief 與 `shows.json`，**看不到 README**。它是 Public repo 唯一會被外部讀者看到的檔案。
6. 排程 SKILL.md：節目數變動一定會動到 `description`（「N 檔財經 Podcast」）；流程或排序規則有變才動 prompt 本體。
7. brief 第 8 節加變更紀錄，`MAINTENANCE.md` 第 12 節登記簿補一列。
8. 跑 `healthcheck.py` 確認 showKey 三項檢查都 PASS。**注意它涵蓋不到 README 與撞色**，那兩項只能靠這張清單。

---

## 6. 目前已知待辦與觀察中的事項

> **已結案（2026-08-06 移出待辦）**：plist `Hour = 1` 與 `pmset` 那組電源設定都已驗證通過——08-04、08-05、08-06 連續三天日誌開頭都是 `[01:00:00]`。若哪天又偏晚，照第 4 節的三項排查（**plist 優先**），不要重新當成未驗證的新問題。
>
> 當初留的退路仍然有效，記在這裡備用：**若時序保證連續失效**，就別再靠它，改成讓日報容忍 podfetch 尚未跑完（短暫等待後重讀，或改用前一天的 manifest 並在回報中標明）。

- **FT 的 syndication cookie 仍是脆弱依賴。** Chrome 路徑本身已於 2026-08-08 在無人值守排程中實測成功，但 cookie 會過期、失效時安靜退回付費牆，長期解法是登入真正的 FT 帳號。
- **YouTube 字幕退援路徑實質上已死**，目前只剩 podfetch、官方逐字稿、FT 三條路。若 podfetch 出問題，退援能力比看起來薄。
- **資料夾連線不保證跨工作階段留存**，排程要能自我修復（已寫進 SKILL.md）。
> **已結案（2026-08-08）**：變更紀錄膨脹問題已處理——歸檔到本檔第 11 節，brief 只留當日條目，歸檔規則寫死在 brief 第 8 節末尾（每次維護開工時執行）。
- **Gemini 免費層額度會被 Google 無預警調整，而我們沒有任何偵測機制。** 08-05 就發生了（Flash 3.5／3.6 的 RPD 從 20 砍到 10），是使用者主動截圖 Console 才發現。`healthcheck.py` 檢查不到——它只比對 brief 與 `config.json`，兩邊都是我們自己寫的。**建議每隔一兩週看一次 AI Studio Console 的 Rate limits 頁**，或想辦法讓 podfetch 把實際回傳的額度資訊記進日誌。
- **重試放大率約 2.5 倍，值得盯著。** 08-05 預估 17 個請求實際打出 42 次。目前靠 Lite 的 500 RPD 吸收，但若哪天 Lite 也被砍，就得回頭檢視三種重試（503、字數不足、輸出截斷）的觸發條件。
- **GitHub Actions／Pages 部署端沒有任何主動監測。** 08-07 卡了至少兩小時，只能靠人去看 Actions 分頁。`healthcheck.py` 現在會偵測「線上落後本地」，但**偵測不到原因**——workflow 沒觸發、跑失敗、還是 Pages 佇列塞住，是三種不同的修法。
- **講者標記誤植目前只能靠人工校正把關**，機械檢查抓得到格式異常與統計異常，抓不到「合理人名張冠李戴」（見第 7 節）。這是目前最主要的品質風險。
- **補了檢查之後要問「它涵蓋到鏈的哪一段為止」。** 已經連續兩次是同一條鏈往下游挪一格再出事：08-03 是 push 沒跑，08-07 是 push 跑了但 deploy 沒跑。
- **在沙箱裡匯入 `podfetch.py` 做測試，會在沙箱家目錄建出假的 `~/.podfetch/logs/`。** 它的 `log()` 是模組層級行為，只要 import 就會建目錄。`healthcheck.py` 的 `resolve()` 已改為優先認掛載點，但**測試完仍要記得 `rm -rf /sessions/*/.podfetch`**，否則後續工具可能抓錯路徑。2026-08-08 就因此看到一整排假故障。
- **程式碼裡的常數完全在機械檢查的涵蓋範圍之外。** `healthcheck.py` 只比對 brief 與 `config.json`，而 `WORDS_PER_MIN`（現為 200，已改過三代 130→165→200）這種寫在 `podfetch.py` 裡的常數改了之後，brief 與警告字串都可能漏改——08-06 就發現「用 165 計算卻對使用者報 130」，而且那句話會寫進 front matter 與 manifest。**寫進使用者可見輸出的數字一律引用常數，不要寫死。** 值得考慮把這類常數移進 `config.json` 讓檢查涵蓋得到。
- **✅ 已結案（2026-08-16 當晚）：第 5.5 步確實在正本裡。** 稽核時 18:27 的「收工後」快照裡搜不到 `5.5`／`第 16 項`／`eff_tokens`，一度懷疑 `08-16 ⑤` 宣稱已同步但實際沒改。**使用者貼回正本後確認：第 5.5 步、回報第 16 項、成本模型訂正全部都在。****所以那不是規格漂移，是快照落後一輪**——真正的問題在下一條。
- **觀察點記分板積壓（2026-08-16 量測）。** 56 條裡 **47 條卡在「觀察中」（84%）**，只有 9 條曾被改判，**最舊一條停在 07-29、18 天沒動**；而新增是每天 3 條、連續 12 天沒斷。也就是「附加」一直有做、「改判」是斷續的（近 6 天有 4 天做了）。`healthcheck.py` 現在會把這三個數字印出來，**但刻意不報警**——積壓算不算問題要人看，讓它天天亮只會變成背景噪音。**盯的是趨勢：觀察中佔比若持續往上，就是回訪流於形式。**
- **brief 違反自己第 9 行訂的分工規則（2026-08-16 稽核，未處理）。** 第 9 行寫「本檔只寫現在的規格與判斷規則，事故經過一律寫在 `MAINTENANCE.md` 第 7 節」，但實測第 1–7 節有 79 行帶事故／沿革標記、**合計佔第 1–7 節字元的 43%**（這些行同時含規則與敘事，43% 是可切分的上界），全檔共 128 個日期標記；第 8 節另佔 9.3%（**此為本場改動前的量測；記下這一筆之後第 8 節本身變厚，已升到約 12%——自我指涉的數字要標時點**）。**規則是對的，沒有被執行。** 本輪刻意不動——明天 8/17 已經有六個變因要驗（本線四個 ⏳ ＋ 投顧線兩個），再疊上大面積下推就分不清是誰造成的。
- **`healthcheck.py` 的 brief 一致性檢查只涵蓋數值**。敘述性的自相矛盾（例如退援順序寫兩套）目前只能靠子代理比對抓，值得再想有沒有機械化的辦法。
> **已結案（2026-08-16）**：08-15 留下的三條待辦都已處理，改動見第 12 節 `08-16 ①②`。三條原文與結果如下，**留著是因為它們示範了「先問正常情況會不會也觸發」怎麼落到實作**：
> - 「0 集既是合法結果、也是失效態的症狀，目前無法區分」→ 加了 `discover()` 的健全性對照。**當初的顧慮是「門檻要用該節目自己的最長間隔而不是固定天數」**，實作時改成更省的做法：不挑特定節目，而是取**全部 23 檔所有集數裡最新的那一筆**與 `stale_feed_hours`（72）比。這樣既不必為每檔維護一組間隔，也讓門檻的依據變成一句可驗證的話——「23 檔裡有每個交易日都發的 Bloomberg，全部同時安靜三天不會發生」。零額外請求。
> - 「`discover()` 回空仍無條件推進 `last_run_utc`」→ 0 集一律不推進。當初列的兩個選項（不推進／推進到視窗起點＋一小段）取了前者，因為後者要多一個「一小段是多長」的參數，而它沒有可驗證的依據。
> - 「BG2 與 Business Breakdowns 更新頻率明顯下降」→ 08-16 移出，換成 Prof G Markets（週一至五每日）與 Money Stuff（每週五），總數維持 23 檔。**當初寫「若持續數月無更新再考慮」，實際提前動手的理由是使用者要的是頻率與討論度，不是等它們正式停更。**

---

## 7. 事故與決策檔案

### 2026-08-15　日誌說「沒有新集數」，那句話本身是對的——錯的是它看到的資料

08-15 凌晨 01:00，podfetch 準時執行、正常結束、日誌兩行：`視窗：08-13 16:30 → 08-14 17:00 (UTC)`、`沒有新集數。`。前一天有 12 集，這天 0 集。實際上窗口內有 10 集。

**根因在一行沒人懷疑過的字串組裝。** `itunes_lookup()` 組出的網址是固定的：同一支 AppleID、同一個 limit，就永遠是同一個 URL。iTunes 把回應**綁在確切的查詢字串**上快取，那份快取當時停在 07-31、落後 15 天。HTTP 回 200、JSON 結構完全正常、`discover()` 逐集比對 `releaseDate` 之後合理地判定「都比視窗起點舊」，於是回空陣列。`discover()` 的錯誤處理只在 `itunes_lookup()` 丟例外時寫 `! <key> 查詢失敗`——沒有例外，就沒有任何一行異常。

**為什麼三段排查抓不到。** 第 2 格明寫「有『沒有新集數。』就是真的 0 集，一切正常」。那條規則在設計時是對的：0 集日確實常見（週末尤其）。但它預設了「查詢結果可信」，而這正是本次失效的那一層。**「日誌說正常」與「真的正常」在這個失效態下不可區分**，任何只讀日誌的排查都會判成綠燈。當天定案靠的是外部對照——拿同一支 AppleID 帶與不帶 `&cb=` 各查一次，All-In 分別回 07-31 與 08-13。

**唯一的外顯訊號是執行時間。** 前一天光是輪詢 23 檔就花了 9 秒（`01:00:00` → `01:00:09`），這天從開始到「沒有新集數」只有 1 秒。23 次跨太平洋的 HTTP 查詢不可能在 1 秒內跑完，但**沒有任何機制在看這個數字**。

**它還會製造永久缺口，而且缺口的大小取決於失效持續多久。** `discover()` 回空時，程式仍**無條件推進** `last_run_utc`（`if not eps:` 分支裡就寫在 `log()` 下一行）。下一次的視窗起點因此是「這次的結束時刻減 30 分鐘」，那被跳過的 24.5 小時再也不會被涵蓋。當天最後一集發布於 `16:17`、新起點是 `16:30`，**差 13 分鐘全部落在窗口外**。復原是把 `last_run_utc` 回捲到前一次的值再重跑；72 小時 lookback 上限給的緩衝是兩個排程週期，超過就真的救不回來。

**教訓一：對外部 API 的「同一個請求會拿到同一份答案」是一個假設，不是保證。** brief 早在 08-05 就為 Substack 的 `/api/v1/archive` 寫過「一律要帶 `&cb=`」，甚至在同一段裡註明「與 iTunes 的 US 商店快取是同一類問題」——**知道是同一類問題，卻只在其中一邊套用解法**。跨系統的同型缺陷要一次掃完，不要只補當時踩到的那一個。

**教訓二：文件裡沒被執行過的解法，不算解法。** brief 從 08-03 起就寫著「換 GB 或 AU 商店重查」並附上 `itunes.apple.com/gb/lookup` 的寫法，還註明「實測 GB／AU 是即時的」。08-15 實測，**路徑前綴形式在本環境回空白內容**——那條退路在需要它的那天是不存在的。正確寫法是查詢參數 `&country=GB`。這與 08-14 學到的「最先該驗證的是最根本、最不像會錯的那個前提」是同一件事的另一面：**寫進文件的退路，要在不需要它的時候先跑一次**。

**教訓三：靜默失效要留下可被機器看見的痕跡。** 這次能被抓到，是因為有人覺得「12 集 → 0 集」不合理而去外部對照。若當天恰好是週末、0 集本來就合理，這 24.5 小時會無聲消失，而且沒有任何一份紀錄會顯示它曾經存在。**0 集是一個合法結果，也是一個失效態的症狀，兩者目前無法區分**——已列入第 6 節待辦。

### 2026-08-14　修對層次的那一次，以及一個看了三天沒看見的盲區

**連續三天測同一條規則，前兩次失敗、第三次成功，差別在改的層次不同。**

| 日期 | 改了什麼 | 重寫比例 | 每集成本 |
|---|---|---|---|
| 08-11 | 把「先估篇幅再落筆」寫進規格 | 3/5 | 57.6 萬 |
| 08-12 | 加強措辭、寫進任務卡 | 3/5 | 54.9 萬 |
| 08-14 | **把規劃單位從字數換成段數** | **0/12** | **28.3 萬** |

**前兩次改的是「要求」，第三次改的是「執行者手上的工具」。** 08-12 已經記過這個判斷（規則失效時先問它依賴的量測可不可靠），08-14 是它的驗收：段數達標 11/12、平均 2.6 次工具呼叫、無一集超過 5 次、成本減半。**這條規則現在有效，接下來就不要再動它了**——三次修改同一條規則，前兩次都是因為在錯的層次施力。

順帶把 08-08 那句「新架構是線性成長，集數多時才顯優勢」也驗了：12 集這天固定開銷佔比 20.1% → 10.4%。

---

**同一天發現的盲區比上面那件事更值得記，因為它已經存在很久而且完全隱形。**

TIP837 的回報裡有一句「時間戳最大到 227 分遠超 78 分鐘片長，章節切分改用主題轉換判定」。掃全庫 72 集之後：

| 日期 | 節目 | 完整度 | status | 時間戳溢出 |
|---|---|---|---|---|
| 08-11 | 20VC | 0.91 | **OK** | **+1888%**（最大 1,192 分／片長 59 分） |
| 08-14 | TIP837 | 0.98 | **OK** | **+224%** |
| 08-08 | Bloomberg | 1.19 | OK | +65% |
| 08-05 | Pivot | 1.14 | OK | +61% |

**兩集的時間軸整個崩壞，完整度正常、status 是 OK，沒有任何機制看見。** 08-11 那天的回報只說「20VC 有 21 條整段錯置訊號」——**講者檢查抓到的是副作用，時間軸崩壞本身隱形。**

**它為什麼要緊**：時間戳決定摘譯的章節切分。08-14 那集是子代理自己發現異常才改用主題轉換分節的——**又一次「執行者靠自覺補上規格缺口」，這是這套系統反覆出現的形態，每次都代表規格缺一條路。**

**修法與 08-07 講者檢查同構**：獨立欄位 `timestamp_notes`，不進 `warnings`、不影響 `status`。理由一樣——時間軸壞掉不代表內容缺漏（那兩集完整度 0.91／0.98），併進去會用一個錯的理由把好集數標成降級。

**但第二條規則差點重演 08-07 的雜訊問題。** 初版只看「大間隙」（相鄰時間戳超過 90 秒），對 72 集報 15 集——因為**長篇獨白與真跳段長得一模一樣**（時間戳只在換人時出現）。那正是講者檢查初版「九集報八集」的同一種失敗。改成**「間隙佔比 >30% 且完整度 <0.85」兩個訊號同時成立**之後，全庫只報 7 集，而唯一被間隙條件抓到的（08-14 MIB，22 分鐘無戳、完整度 0.73）正是子代理獨立確認確有兩處跳段的那一集。

**這裡有一個可以推廣的教訓**：我第一版把「與完整度一起看」寫進了警告訊息的文字裡，叫讀的人自己去對照。**能寫成條件的判斷就不要寫成叮嚀**——叮嚀會被忽略，而且每天叮嚀一次就變成雜訊。

---

**第四件事，也是這五天最該記的一件：我 debug 了四次，從來沒驗證過最根本的那個前提。**

`eff_tokens_k` 從 08-09 加入起連續空白六天。修正史：

| 次數 | 假設 | 結果 |
|---|---|---|
| 08-10 | 路徑是 `~/.claude/projects` | 錯（那是 CLI 的路徑） |
| 08-10 | 路徑是 `$TMPDIR/claude-hostloop-plugins/*/projects` | 錯 |
| 08-14 | 路徑是 `~/Library/.../local-agent-mode-sessions/*/*/*/.claude/projects` | **路徑對了**，但 70 個檔案沒一個落在 02:30–06:00 |
| 08-14 | mtime 窗口不可靠（App 續讀會推後），改用內容指紋 | 還是沒中 |

第五次才做對的事：`grep -rl "<排程 prompt 的指紋>" ~/Library/Application\ Support/Claude --include="*.jsonl"`——**唯一命中的是維護對話本身**（指紋出現在裡面，只因為我在那裡貼過排程 prompt）。

**結論：`local-agent-mode-sessions/` 底下只有互動式對話，排程執行根本不在本機留 transcript。** 四次修正全都建立在「那個檔案存在」這個從沒被驗證的前提上。**我從第二層（路徑對不對）開始查，把第一層（東西存在嗎）當成已知。**

> **這個結論在 2026-08-16 被修正了一半，界線現在是清楚的。** 08-15 的用量分析發現**沙箱掛載點讀得到 transcript**（`$HOME/mnt/.claude/projects/<專案目錄>/<uuid>.jsonl`，子代理在 `<uuid>/subagents/agent-*.jsonl`），那份報告的所有數字都是直接解析而得、無一估算。它留下一個可驗證的檢查點：「排程執行是否同樣留在這個位置」。**08-16 對帳完畢，答案是：**
> - **每個工作階段都能讀到自己的 transcript**，就在自己的沙箱掛載點裡。08-15 那份報告做得到，是因為那天日報產出、事故排查、維護全在同一場對話裡。
> - **但讀不到別場的。** 08-16 實測：本場的 `.claude/projects/` 底下只有本場那一個 `.jsonl`；`/sessions/` 下另有 70 多個其他工作階段的掛載點，**全部 `Permission denied`**。
> - **08-14 那個「掃 `~/Library` 找不到」的結論也沒錯**，它掃的是 Mac 上的互動對話目錄，與沙箱掛載點是兩個地方。三個看似矛盾的觀察其實同時成立。
>
> **所以已經確定的是：「只有當事人讀得到自己的」，事後任何人都救不回來**（沙箱隨工作階段銷毀）。08-16 的 `eff_tokens_k` 就是這樣永久遺失的。
>
> **但還有一件事沒有被驗證，不要把它當成已知（2026-08-16 標記）。** 兩份用量分析都寫「排程執行不留 transcript」，**而兩者的證據都只證明了「從別場讀不到」**——沒有人驗證過「排程執行在**它自己的**沙箱裡有沒有 `.jsonl`」，因為那個沙箱從外面看不見。**這正是 08-14 那個五次失敗的同型錯誤：把「我找不到」當成「它不存在」。**
> **唯一能回答的辦法是讓排程執行自己試一次**：在收工前 `ls` 自己的 `$HOME/mnt/.claude/projects/`，找到就量測並把三個數字寫進回報，找不到就在回報寫「找不到」。**兩種結果都有價值**——找得到就此不必人工填；找不到才算真正確認「排程不留 transcript」，此後可以放心寫死人工填。
> **若確認找得到，自我量測有一個必須標明的偏差**：讀檔當下最後幾個回合還沒寫進去，數字是**下限**。
**修法不是刪掉欄位，而是換來源。** 資料一直都在——每天的 token 分析報告有，只是機器讀不到。改成維護時人工抄入，`healthcheck.py` 只負責不要洗掉人工值、沒填時提醒。回填五天之後基線終於完整：

| 日期 | 集數 | 逐字稿 | 加權 token | 每集 |
|---|---|---|---|---|
| 08-09 | 5 | 412 KB | 9,700K | 1,940K |
| 08-11 | 5 | 490 KB | 2,883K | 577K |
| 08-12 | 5 | 369 KB | 2,746K | 549K |
| 08-14 | 12 | 656 KB | 3,399K | **283K** |

**「量不到就刪掉」與「留著空欄位」都是錯的答案**——前者丟掉真實存在的資料，後者假裝在追蹤。第三條路是問「資料在哪裡？」而不是「程式為什麼讀不到？」

---

**第三件事：基線有洞，而且洞是靜默的。** `metrics.csv` 缺 08-13 那一列——指標只在「跑 healthcheck 的當天」寫入，沒跑維護的日子就沒有紀錄。而 08-13 其實有 3 集、**其中一集 DEGRADED**，完全沒進基線也沒被任何人看到。healthcheck 現在會掃 `data/` 與逐字稿目錄的聯集回填缺漏日期（本次補了四天）。**一份有洞的基線在做趨勢比較時會安靜地誤導人**——這正是這個檔案存在的目的所要避免的事。

### 2026-08-12　規則沒有失效，是它依賴的量測失效了

**08-11 加了「先估篇幅再落筆」，08-12 五集仍有三集重寫——比例完全沒變（3/5）。** 成本 274 萬對 288 萬持平，但那天的逐字稿少了 25%，換算下來**每 KB 成本反而上升**。表面看是規則沒被遵守。

**實際上是規則要求執行者做一件它做不到的事。** 掃全庫 13 個日檔 74 集，比對子代理自報的 `chars` 與正文實際字元數：

- 偏差中位數 **0%**
- 範圍 **−38% 到 +81%**
- **42% 的集數偏差 20% 以上**

**這個分布的形狀本身就是答案。** 中位數 0 代表沒有系統性偏差——不是子代理習慣性高估或低估，而是**每一集用的尺都不一樣**（有的把英數詞組算一個單位、有的算字元、有的算法不明）。**一把無偏但誤差 ±40% 的尺，比一把有偏但穩定的尺更沒用**：有偏可以校正，雜訊不行。

當天三個實例把後果講得很完整：Dwarkesh 自報 5,900 而實際 8,709、Pivot 自報 5,948 而實際 8,045——**兩集都自以為在區間內，實際超上界近三成**；In Good Company 自報 1,789 而實際 1,087——**自以為超標所以壓縮了兩輪，實際上它一直低於下界**。三集都在「照規則行事」，三集都做錯，而且錯的方向不同。

**修法有兩層，缺一不可。**

**第一層是把記錄面的尺固定住**：`chars` 改由組檔時的 python 機械計算並覆寫，子代理填 `0` 即可。這解決了「事後判斷有沒有超規格」的問題。

**但這不解決寫作當下的問題**——子代理手上沒有 python。所以**第二層是把規劃單位換成它量得到的東西：段數。** 全庫 1,546 個段落的中位數是 198 字元（四分位 150–251），換算後每個層級都有對應的目標段數。**段數是下筆前就數得準的，字元數不是。**

**這件事的一般形態值得記住：一條規則失效時，先問它依賴的量測可不可靠，再問執行者有沒有遵守。** 08-11 那次我把原因歸給「寫作紀律」並加強措辭，隔天比例一模一樣——**加強一條建立在壞量測上的規則，只會讓執行者更用力地做錯事。**

**同一天的第二件事：重派壓縮比原本還貴。** ILTB 那集因 API 中斷後重派，**重派那次 36.2 萬，比原本那次 20.8 萬還高**，即使它完全沒讀逐字稿、只讀 41 KB 的既有 JSON——壓縮改寫本身就是反覆讀寫的工作。兩次合計佔全場 20.8%。因此加了成本門檻：**超上界 50% 以內直接收下並註明，超過才重派**。理由是 brief 本來就允許「超上限、說明理由」這種降級——**花 36 萬 token 去消滅一個被允許的降級，是拿成本換一個規格上並不要求的整齊。**

### 2026-08-11　三天前那句無法驗證的宣稱，今天有答案了

**08-08 做完 token 架構改造時寫下「粗估降 60–75%」，並在 08-09 自己標註「那是估算不是量測，至今無法驗證」。今天它被驗證了：同樣五集，970 萬 → 288 萬，降 70%。** 而且今天的逐字稿還多 19%（490 KB vs 412 KB），子代理佔比從 84% 降到 62%。

**成立的是那個機制判斷，不是那個數字。** 08-09 的分析結論是「成本的驅動是回合數不是資料量」——今天 Acquired 一集 310 KB 逐字稿（第二大的 4.5 倍）只花了 1.8 倍成本，正好反過來佐證同一件事。**降幅來自 08-09 加的工具呼叫預算，不是 08-08 的架構本身**；08-08 只做到「把資料隔離出去」，那是必要不充分。

**但這個勝利同時暴露了一個設計衝突。** 今天五集有四集超出呼叫預算，主因是**三集初稿超出字數上界後重寫**（Acquired 14,255→6,583）。而每重寫一輪，整份逐字稿就被重新計費一次——**寫作規格（字數上界）與成本規格（呼叫預算）在長節目上互相打架，而任務卡沒有告訴子代理該怎麼調解。** 子代理選擇了「先滿足字數、再吃成本」，那是合理的優先序，但兩者本來不必二選一：**先列骨架、先分配字數，就能一次寫到位。** 這條已寫進規格。

**另外三件當天記下的事：**

**① 子代理自報的呼叫次數會低報，而且低報幅度不小。** 自報 4／7／6／9／11，系統實測 5／7／6／11／18。Acquired 差了 7 次。回報第 13 項現在拿這個數字當成本監控依據，**所以規格已改為明講「這是下限不是實數」**——一個會系統性偏低的指標，如果沒人知道它偏低，就會拿它做錯決策。

**② 記分板的種子是我自己污染的。** 08-10 回填時把後記段落照單全收，連「以上內容為節目摘譯與整理，非投資建議」這種免責文句都收進去了。結果 08-11 六條改判裡有四條在處理這些雜訊——**記分板的價值在於「哪些判斷後來被證明對了」，混進不可能有真假的條目就只是稀釋它。** 已刪 3 條純免責條目並加收錄標準：新增前先問「這句話三個月後有可能被證明是錯的嗎」。

**③ 又一次「執行者靠自覺補上規格缺口」。** Acquired 273 分鐘，子代理自主判斷不去抓官方稿（`web_fetch` 上限會靜默截斷、拼接不切實際），判斷完全正確——**但規格裡沒有這條路**。這是 08-10 記過的同一種形態：當回報裡出現「這是我的自主判斷」，那就是規格缺條款的訊號。已寫成明確規則（>120 分鐘的 A 類直接用 podfetch 稿）。

### 2026-08-10　三個規則同時失效，但只有一個看得出來

**08-10 只有 2 集，而且是同一支 113 秒的〈Our Town〉預告**——Bloomberg News 把它同時推進 Bloomberg Surveillance 與 Odd Lots 兩個 feed。逐字稿合計 5 KB，是有紀錄以來最薄的一天。

**表面問題是重複，真正的問題是三條規則同時在同一天沒接住，而且只有第一條會被人看見。**

**① 去重比錯了欄位。** 現行規則讀「前一份日檔的 `url` 與 `title`」。這兩集**同一天**（不是跨日）、`url` 因 feed 不同而不同、日檔 `title` 是兩個子代理各自擬的中譯所以也不同——**三個條件全部避開**。而 manifest 裡它們的原文 `title` 都是 `"Introducing: Our Town"`、`durationMs` 都是 `113000`，**唯一能認出同源的兩個欄位，剛好都不在比對範圍內**。

這不是規則寫壞，是**規則的立論範圍寫死了**：當時 brief 寫的是「窗口重疊的代價是**相鄰兩天**會重複命中同一集」（該句已於 08-10 改寫）——去重從一開始就只為「跨日」設計，同日同源不在它的世界觀裡。**當一條規則附了它自己的理由，那個理由就是它的邊界；邊界外的同類問題不會觸發任何警訊。**

**② 字數下界沒有例外條款。** 素材只有 301 字，下界是 1,200 字。主代理寫了 986／501 字並把理由塞進 `source` 欄位——處置正確，但**規格裡沒有這條路**。原本只規定「超上限要說明」，低於下界完全沒寫，回報清單十三項也沒有對應項。**制度上，寫太短與偷懶在事後完全分不出來。**

**③ 子代理門檻不存在。** SKILL.md 第 3 步「每集派一個子代理」是無條件的，唯一豁免是「拿不到全文」。主代理當天判斷 2.5 KB 的逐字稿不值得派——省了數十萬 token，但那是臨場判斷，**明天換一個執行者就不會這樣做**。

**共同形態**：三件事都是「**主代理靠自覺補上了規格的缺口**」。當天回報看起來很正常，正是因為它補得好。**如果那天執行者剛好沒想到，這三個缺口會以三種完全不同的症狀出現（重複集數、灌水、成本暴增），而且都不會被歸因到同一個原因。** 教訓：**看到執行者「偏離規格但做對了」時，那是規格缺條款的訊號，不是執行者發揮得好。**

**還查出一件更難堪的事，而且當天修了兩次都沒修好**：08-09 才加的 token 量測，`eff_tokens_k` 連續兩天都是空的。第一次歸因是路徑——`measure_session_tokens()` 讀 `~/.claude/projects`，那是 Claude Code CLI 的路徑，而每日排程跑在 Cowork。改成同時搜 `$TMPDIR/claude-hostloop-plugins/<hash>/projects/` 之後在 Mac 上實測，**還是空的**。

**這裡的教訓不是「猜錯路徑」，是「猜錯沒有症狀」。** 留空看起來像「還沒有資料」，不像「壞了」——所以它可以安靜地跑很多天，而 `metrics.csv` 那三個欄位一直在那裡，讓人以為基線正在累積。**一個回傳空值的量測，跟沒有量測是同一件事，但它看起來像有。**

修法因此改變方向：不再繼續猜路徑，而是**讓失敗自己出聲**。量測不到時「每日指標」改報 WARN 並印出區分得出三種原因的訊息（找不到根目錄／根目錄下沒有 `.jsonl`／有檔案但都不在 02:30–06:00 窗口，最後一種附上最近三筆 mtime）。**下一次執行就會直接告訴我們該往哪裡查，不必再猜。**

### 2026-08-09　成本的驅動因素是回合數，不是資料量

**08-09 是子代理架構的首次實跑，結論一半成立一半不成立。**

成立的部分：424 KB 逐字稿**一個字都沒進主對話**，主對話只佔全程 16%，其中檔案操作僅 4.8%、五張摘要卡合計 2.1%。隔離設計是有效的。

不成立的部分：**五個子代理吃掉 84%**，平均每集 160 萬，最貴的 All-In 一集 225 萬。

**真正的原因用一個量測就講完了**：All-In 那個子代理跑了 70 次工具呼叫，而該集逐字稿只有 1,201 行——**Read 的上限是 2,000 行，一次就讀得完**。所以那 70 次不是被迫分頁，是讀完之後又做了 68 件事。最可能是為了滿足「金句必須是逐字稿實際出現的發言」而反覆 grep 核對，**但全文早已在它的 context 裡，那些核對一次都不必要**。

**核心機制**：逐字稿一進 context，之後每一次工具呼叫都把那 120 KB 再送一遍。70 次 ≒ 8.2 MB 重讀。**舊架構是「一份大 context 被重讀很多次」，新架構變成「五份小 context 各自被重讀很多次」，並沒有自動變便宜。**

**但方向仍然對**：新架構的成本是線性的（五集分五個 context，彼此不加乘），舊架構十集塞同一個 context 是平方式成長。**集數越多，新架構的優勢才顯現**——08-09 只有五集，可能還沒過損益兩平點。

**教訓有兩層**：

1. **優化前要先確認成本模型。** 我當初假設「資料量大＝貴」，所以把資料隔離出去；實際的模型是「context × 回合數」，隔離資料只解決了一半。**真正的槓桿是壓回合數**，而那需要在任務卡裡明講，因為子代理不知道自己每多一次呼叫要付什麼代價。
2. **沒有量測就不要寫降幅。** 08-08 的 changelog 寫「粗估降 60–75%」，那是估算，而且**因為舊架構從未被量測過，它至今無法驗證**。`metrics.csv` 已補上 token 欄位，但基線要從今天才開始累積。**估算要標明是估算。**

### 2026-08-09　指標校準：讓「正常」等於 1.0

起因是 08-09 有兩集標 `DEGRADED`，去查完整度發現是 1.44 與 1.53。掃過**單 token 與片語級跳針都是 0**——不是跳針，是 All-In 那類四人交叉搶話的節目本來就講到 238 字/分。

> **這裡先訂正一個當天寫錯、隔一輪才發現的因果（08-09 ②）**：那兩集的 `DEGRADED` **不是**完整度超標造成的，wpm 也**不決定** status。`podfetch.py` 的 `status` 只看 `warnings`，而 warnings 只有三個來源：字數低於 `MIN_RATIO`、段級語速偏離**本集中位數**（`REL_RATIO`／`HIGH_RATIO`，不看 wpm）、跳針剔除——**完整度 1.4 這種「超標」在任何路徑都不會產生 warning**。那兩集的降級來自段級相對檢查。**上修 wpm 反而讓段級重試門檻變嚴。** 所以底下這段校準的價值在「讓顯示的完整度可讀」，不在「減少 DEGRADED」。教訓：**改指標前先確認它實際接到哪條分支**，文件互相對讀查不出這種矛盾。

17 檔節目的實測語速中位數約 **200** 字/分，範圍 122–218（排除跳針污染樣本）。**原本全域設 165 的後果是：幾乎每一集的完整度都顯示 1.2–1.3，「超標」變成常態。** 當一個指標天天報異常，它就不再是訊號——真出問題時沒人分得出來。

修法：全域上修到 200，並讓 `shows.json` 可用 `"wpm"` 逐檔覆寫（已為 7 檔設定，如 ILTB 140、In Good Company 160、Odd Lots 185）。校準後 15 檔全部落在 0.87–1.09。

**教訓：閾值型指標要定期用實際分布回頭校準。** 這與 08-07 講者檢查那次是同一個道理——初版對 9 集報 8 集，等於雜訊產生器。**設計檢查時要問「正常情況會不會也觸發」，而且要在累積足夠樣本後回頭再問一次。**

### 2026-08-08　token 成本結構：問題不在讀了多少，在讀進哪個 context

**背景**：每日排程是一條長對話，**每次工具呼叫都會重送整個 context**。實測成本結構：10 集逐字稿 617 KB（≈15–20 萬 token）讀進主 context，之後每次 Write／bash 呼叫都再付一次；brief 68.8 KB 同理；寫完 229 KB 的 JSON 後，同一份內容又被 LLM 重寫成 82 頁 Word。

**三個修法與其原理**：

1. **子代理分集**：token 計費的關鍵不是「讀了多少」而是「讀進哪個 context、之後還有幾次呼叫」。逐字稿在子代理 context 只被讀一次就結束（子代理只做 3–4 次工具呼叫），成品走檔案系統回主流程，主 context 只收摘要卡。**資料量沒變，重複計費的次數變了。**
2. **json2docx 腳本**：LLM 輸出比輸入貴數倍，「同一份內容寫兩次」是最貴的浪費。凡是機械轉換（JSON→docx）都該是腳本，LLM 只寫一次事實來源。
3. **brief 瘦身**：固定輸入（每天讀＋每次呼叫重送）的膨脹是複利型成本。變更紀錄歸檔規則寫死在 brief 第 8 節末尾——**每次維護把兩天前的條目移到本檔第 11 節**，否則 08-06 那次「重構成效兩天被吃回去」會重演。

**品質取捨（要誠實記著）**：交叉觀察過去是主代理讀完全部逐字稿後寫的，現在改用每集摘要卡（5–8 條具名論點）合成。**主代理再也看不到任何一句原文**——若某天交叉觀察品質明顯變淺，第一個該懷疑的就是摘要卡的資訊密度，修法是加厚摘要卡格式，不是退回讀全文。

**風險（新架構未實測）**：子代理並行寫 `/tmp/ep-*.json` 的組檔正確性、A 類節目在子代理裡抓官方稿的成功率（子代理有 web_fetch，但 Chrome 類工具未驗證——FT 已例外由主代理處理）、失敗重派的行為。**明天 03:00 是第一次實跑，後天要看回報第 12 項。**

### 2026-08-08　指標在最需要它的時候是反向的

**現象**：兩集標 `DEGRADED`，完整度分別是 **3.36** 與 **1.93**——看起來像「內容超標」，實際上是**內容嚴重缺漏**。The Compound 有一行是 28,122 個重複的「I」，Hard Fork 有 9,521 個「let's」。

**`spoken_words()` 直接數空白分隔的 token，跳針全部計入。** 於是失效越嚴重、數字越漂亮。**這比沒有指標更糟**——`completeness` 正是日報用來判斷「這集能不能信」的依據，而它在唯一需要它的場合給出相反的答案。

`HIGH_RATIO` 的逐段檢查有把它們標成 `DEGRADED`（機制有作用），所以不是完全沒訊號；**但寫進 front matter 與 manifest 的數字是垃圾**，而下游讀的是那個數字。

**修法**：新增 `collapse_loops()`，計字前把連續重複 20 次以上的 token 壓成一次，並單獨在 `warnings` 記一筆剔除量。實測 3.36 → 1.10、1.93 → 1.10，與人工評估吻合。

**教訓：設計指標時要問「這個指標在它要偵測的失效態下，會不會失效得比被偵測的東西更嚴重」。** 字數在「內容太少」時是好指標，在「內容重複到爆」時方向完全相反——而後者恰恰是 LLM 轉錄最典型的失效之一。

### 2026-08-08　時間戳雙重偏移：prompt 自己製造了誘因

**現象**：合併後的時間軸會往回跳。第 2 段標成 40:00–59:52，實際是 20:00–39:52；同一集的第 3 段卻是對的。

**根因**：prompt 寫「Segment 2 of 4, covering approximately 20:00-40:00 of the full episode」——**等於告訴 Gemini 這段的絕對位置，它就照絕對時間輸出**，而 `offset_timestamps()` 又加了一次 20 分鐘。

**關鍵在於它不一致**：同一次執行、同一集，第 2 段用絕對、第 3 段用相對。**所以只改 prompt 擋不住**——LLM 對格式指示的遵守率不是 100%，凡是「靠它守規矩」的地方都要有機械式的兜底。

**修法**：兩層。(1) 拿掉 prompt 裡的絕對時間範圍（那是誘因），改為明確要求從 00:00 起算；(2) 合併前看該段第一個時間戳離 0 近還是離 offset 近，取近的那個解釋——兩種輸出都能自動歸位。

**連帶影響**：08-07 回報的「`Speaker 7` 連續 20 分鐘」其實是這個造成的假象，不是講者標記問題。**一個 bug 會在別的檢查裡偽裝成另一種 bug。**

### 2026-08-07　驗證鏈只驗到一半，最後一段斷了卻報綠燈

**現象**：08-07 的日報寫檔、commit、push 全部完成（`local == origin` 同雜湊 `b542611`），`healthcheck.py` 的「推送鏈」報 PASS，**但網站兩小時後仍停在 8/6**。

**根因不是任何元件壞掉，是驗證範圍畫錯了。** 完整的發布鏈有五段：

```
寫檔 → commit → push → GitHub Actions → Pages
       └────── healthcheck 只驗到這裡 ──────┘
```

08-03 的 `auto-push.sh` 事故之後，我們得到「靜默必須是可辨識的狀態」這個結論，也確實補了 `updatedLabel` 驗證——**但只補在日報的執行流程裡，沒有補進 healthcheck**。於是每天早上巡檢會看到全綠，而網站是舊的。

**修法**：`healthcheck.py` 新增「線上狀態」檢查，帶 cache-buster 抓線上 `index.json` 比對。沙箱連不到 GitHub（403 tunnel），此時顯示 WARN 並提示到 Mac 上跑，**不會誤報成 PASS**。

**教訓：補了一個檢查之後，要問「它涵蓋到鏈的哪一段為止」。** 這次是同一個教訓在同一條鏈上往下游挪一格又發生一次——第一次是 push 沒跑，第二次是 push 跑了但 deploy 沒跑。**每次只補到「上次出事的那一格」，下一格就會是下一次的事故。**

### 2026-08-07　講者標記誤植：機器檢查抓得到什麼、抓不到什麼

**現象**：九集全部 `status=OK`、完整度正常，卻有五集的講者標記需要人工校正。失敗型態各異：整段標錯人（All-In 開頭 13 分鐘的 Jason 被標成 Chamath）、廣告與電台台呼被歸給主持人（MiB）、拆成 Speaker 1/2/3/4 且中途翻轉（MacroVoices）、三位來賓合併成一個（20VC）、人名誤植但代名詞對不上（Bloomberg 的「Secretary Yellen」配 he/his）。

**為什麼危險**：錯的名字會直接變成**對真實人物的不實陳述**——跨節目交叉觀察寫「誰主張什麼」時尤其嚴重。而且它比缺名字難發現得多：`Speaker 2` 一眼就知道要查證，一個合理的人名不會引起任何懷疑。

**兩道修法**：

1. **轉錄 prompt 改為保守標記**。預設 `Speaker N`，只有明確依據才寫真名；廣告與台呼標 `Announcer`；標籤不得中途改名。核心那句話直接寫進 prompt：*a wrong name is far worse than no name*。
2. **`check_speaker_labels()` 做機械式的可疑訊號偵測**，寫進 manifest `warnings`。

**校準過程本身是重點。** 初版用「名字不在 `shows.json` 的主持人名單裡」當訊號，結果 9 集報 8 集——**因為來賓本來就不在名單上**。一個對 89% 的樣本都響的檢查等於沒有檢查。改成偵測「標籤格式異常」（把整句台詞或廣告文案當人名）後降到 5 集，每一則都對應實際問題。**設計檢查時要先問「正常情況會不會也觸發」**，否則做出來的是雜訊產生器。

**明確的能力邊界，寫下來免得以後誤以為有覆蓋**：抓不到「合理人名張冠李戴」。All-In 那個案例的標籤是真實存在的主持人、格式正常、佔比正常，機械上完全看不出問題。**這一類只能靠讀內容時的上下文校正**（誰被直呼名字、誰在問問題、開場說了誰不在）。

### 2026-08-06　暫時性錯誤產生永久性後果，把修法的效果整個抵銷

**現象**：`prefer_lite` 上線後的第一天，Console 顯示 Gemini 3.6 Flash **22/10 RPD**（超限、跳出「You have reached a rate limit」），而三個 500 RPD 的 Flash-Lite 只用了 8、3、少量。**看起來像 `prefer_lite` 沒生效。**

**但它生效了。** 日誌第 15 行的池子順序確實是 Lite 在前。真正發生的事在後面 77 秒：

```
[01:01:23] gemini-3.5-flash-lite    本次停用（暫時過載）→ 改用 3.1-flash-lite
[01:02:04] gemini-3.1-flash-lite    本次停用（暫時過載）→ 改用 flash-lite-latest
[01:02:40] gemini-flash-lite-latest 本次停用（暫時過載）→ 改用 flash-latest
```

一次過載尖峰，把三個高額度模型全部打下線。之後 13 段裡有 10 段落在 10 RPD 的 Flash 身上。

**根因是 08-04 修 503 時我自己寫的**：把 `ModelOverloaded`（503）和 `DailyQuotaForModel`、`ModelUnavailable`（404）併進同一個 handler，全部 `EXHAUSTED.add(m)`。而 `EXHAUSTED` 在程式裡的宣告就寫著「本次執行中已確定日額度用完的模型」——**我把暫時性錯誤丟進了一個語意上專屬於永久性失效的集合**。

**修法**：拆成兩個 handler。503 走新的 `OVERLOADED` 冷卻表（預設 300 秒後自動回池），404 與日額度維持永久除名。`current_model()` 同時檢查兩者；若池內全在冷卻，等最早到期的那個而不是判定失敗。

**修法本身又需要兩道護欄，而且第一道還不夠**（08-06 當天的回歸檢查抓到）：「只冷卻不除名」讓輪換失去自然終點，供應端持續過載時唯一的收束方式是把當天所有額度燒到 429。先加了輪換次數上限 `max(4, len(pool) * 3)`，但**次數不等於時間**——每次輪換都可能等一輪 300 秒冷卻，18 次就超過一小時，而窗口只有兩小時。因此再加上單集牆鐘上限 `episode_budget_seconds`（1200 秒）。**同一個修法連續兩輪都少考慮一個維度。**

**教訓有兩層，第二層才是重點**：

1. **暫時性錯誤與永久性失效需要不同的資料結構，不只是不同的處置。** 共用一個集合就等於把語意也合併了。
2. **這是「修法本身製造了新失效模式」的第二次**——08-04 修 503 造成 08-06 的 Lite 被除名；更早 08-03 修時序造成的誤判也是同一類。**改動防護機制時，要問「這個修法在最壞情況下會怎麼失敗」，而不只是「它能不能解決眼前這個症狀」。** 兩次都是修法在正常路徑上有效、在極端路徑上反而放大傷害。

**還有一個觀測面的教訓**：08-05 我看到 Lite 的 TPM 是 51.27K，讀成「輪換正常分流」；08-06 Lite 的 TPM 只有 1.15K，才看得出它是被踢出去而不是在工作。**單看「Lite 有在用」不足以證明它在承擔主要負載，要看相對比例。**

### 2026-08-05　整份取代蓋掉了另一場維護的成果

**這是流程事故，不是系統事故，但代價一樣真實。**

08-03 有一場「同步巡檢」改了排程 SKILL.md 四個地方（給 iTunes 子代理補上 US 商店快取規則、補上第 7 節合規入口、回報清單補「寫不進 repo」、三段排查補第 3 格）。**08-04 我用 `update_scheduled_task` 送出一份完整 prompt 時，把那四項全部蓋掉了**，而且完全沒有察覺——`prompt` 是整份取代，不是局部編輯。

**為什麼沒被發現**：brief 第 8 節那則變更紀錄寫著「已補上排程 SKILL.md ⋯」，讀 brief 的人會相信它已經在了。**紀錄與實況脫鉤，而紀錄看起來更可信。** 是 08-05 的子代理獨立比對才抓出來（問題 7、8）。

**教訓有三層**：

1. **`update_scheduled_task` 的 `prompt` 是整份覆寫。** 送出前必須先 Read 目前的 SKILL.md，以它為基礎修改，不能拿自己記憶中的版本重寫。維護 skill 已經寫了這條警語，我還是踩了——**因為我以為自己就是上一個改它的人**。
2. **多場維護在同一份無版本控制的檔案上工作時，「我記得的版本」一定會過期。** 排程 SKILL.md 不在 git 裡，沒有任何機制會阻止覆蓋。
3. **變更紀錄宣稱「已同步」不等於真的同步。** 子代理比對要特別檢查：brief 說補進 SKILL.md 的東西，SKILL.md 裡真的有嗎。這一項已加進維護 skill 的比對清單。

**修法**：四項全部還原，並與 08-05 的新增一起寫回。

### 2026-08-05　兩個修法都成立，但 Google 把地板抽掉了

**驗證結果**：08-05 的 podfetch `[01:00:00]` 準時，**7 集全部拿到逐字稿**（08-04 是 3/6 失敗），只有 ILTB 一集 DEGRADED。Flash-Lite 的 TPM 從 1.2K 跳到 51.27K，證明 503 輪換確實在分流。⚠︎ 重寫規則也走通了：Bloomberg 日圓那集補回 6,012 字，跨檔搬移與 `index.json` 同步都正確。healthcheck 0 FAIL。

**但同一份 Console 數據顯示 Flash 3.5／3.6 的 RPD 從 20 掉到 10。** 修法生效的同一天，它所依賴的前提被砍半。

**這暴露了一個先前沒看清的量級問題**：08-05 預估 17 個請求，實際打出 **42 次**——放大約 2.5 倍。來源是三種重試疊加（503 換模型前 2 次、字數不足重試、輸出截斷重試）。在 Flash 有 20 RPD 時這個放大率還撐得住，砍到 10 就撐不住了。

**修法**：模型池順序反轉為 Lite 優先。**踩到的坑是「改設定沒有用」**——`build_model_pool` 一律先 `take(heavy)` 再 `take(lite)`，池子順序與 `model_preference` 無關，必須改程式。新增 `prefer_lite` 開關讓它可切換回去。

**教訓：外部額度是會變的前提，不是常數。** brief 把實測額度寫成表格是對的，但沒有任何機制會在它變動時提醒——是使用者主動截圖才發現。`healthcheck.py` 也檢查不到（它只比對 brief 與 `config.json`，兩邊都是我們自己寫的）。**目前只能靠人定期看 Console。**

**另一個獨立的失效模式**：ILTB 那集第 1/4 段回傳 **0 字**，重試後仍空——不是 503、不是額度，是模型直接吐空。字數檢查抓到並標記 DEGRADED，機制照設計運作。這是第三種失敗態，值得記著。

### 2026-08-04　503 死守重試，把 Flash 額度燒光卻放著 Lite 不用

**現象**：08-04 首跑 6 集，3 集 FAILED，日誌結尾是 `HTTP 503 This model is currently experiencing high demand`。

**光看日誌會得到錯誤結論**——它長得像「Google 那邊當機，無能為力」。要看 AI Studio Console 的當日數據才知道不是：

| 模型 | RPD | TPM |
|---|---|---|
| Gemini 3.6 Flash | **21/20 超限** | 30.8K/250K（12%） |
| Gemini 3.5 Flash | **21/20 超限** | 37.7K/250K（15%） |
| Gemini 3.5 Flash Lite | 9/500 | 1.2K/250K |
| Gemini 3.1 Flash Lite | 1/500 | 17/250K |

**兩個 Flash 各 20 RPD 全部燒光（合計 42 次請求，遠超日誌估計的 15 次），而有 500 RPD 的兩個 Lite 只用了 9 次和 1 次。** 多出來的請求就是重試——重試同樣計入 RPD。

**根因在程式碼**：舊版 `transcribe_one` 只有 429 日額度會丟 `DailyQuotaForModel` 觸發模型輪換；500／503 走的是 `time.sleep(20 * (retry + 1))` 然後 `continue`，**在同一個模型上把 5 次重試耗完（約 200 秒）後直接 `RuntimeError`，整集判定 FAILED**。於是變成一邊燒 Flash 的稀缺額度、一邊放著 491 次 Lite 額度不用。

**當時的修法**：新增 `ModelOverloaded`，503 重試兩次就丟出，接進既有的輪換路徑（與 404／日額度同一個 handler）。那 3 集幾乎確定換個模型就會成功。

> **⚠︎ 這個修法在兩天後被推翻。** 「與 404／日額度同一個 handler」正是 08-06 事故的根因——503 因此讓模型永久除名。現行做法是 503 走獨立的 `OVERLOADED` 冷卻表，見上一篇。**本段保留的是當時的判斷，不是現行規格。**

**教訓有兩層**：

1. **「暫時性錯誤」與「額度耗盡」需要不同的處置，混在一起會兩邊都做錯。** 對前者該換資源，對後者才該退讓。
2. **只看應用層日誌會誤判成外部故障。** 日誌說「模型忙」是真的，但「我們把重試打在最稀缺的資源上」只有從供應商端的用量數據才看得出來。**排查配額類問題時，應用層日誌不足以定案，要去看 Console 的實際用量。**

### 2026-08-04　去重會把自己補回來的集數擋掉

3 集轉錄失敗時，日報照規則寫成 `⚠︎ 全文摘譯待補` 的佔位。而 podfetch 失敗時**刻意不推進 `last_run_utc`**，下次執行必定重抓同一集——這是正確的設計。

但去重規則比對 `url`／`title`，會把佔位當成「已收錄」而略過，**補回來的完整逐字稿因此永遠進不了網站，而且毫無徵兆**。兩個各自正確的機制組合出一個必然發生的漏洞。

**修法**：`source` 以 `⚠︎` 開頭的集數不算已收錄，拿到全文要重寫並跨檔搬移。同時明訂 `⚠︎` 是機器讀的標記，不可改寫成別的格式。`healthcheck.py` 也加了「待補全文」檢查主動列出這些集數。

**教訓：兩個都對的規則放在一起可能是錯的。** 這類漏洞在單獨檢視任一機制時都看不出來，只有推演「A 發生之後 B 會怎樣」才會浮現。

### 2026-08-03　把「修改後的狀態」誤讀成「修改前的證據」

**這裡原本有一篇「podfetch 延後補跑」的事故紀錄，已於同日第四次巡檢刪除，因為它的根因判定是錯的。** 保留這則短紀錄，是因為它示範的判斷失誤本身值得記住。

**現象**：08-03 的 podfetch 日誌開頭是 `[07:00:01]` 而非 `[01:00:0x]`。

**當時的判定**：執行 `PlistBuddy -c "Print :StartCalendarInterval"` 印出 `Hour = 1`，據此認定「plist 一直都對」，把根因歸給機器睡眠導致 launchd 延後補跑。

**實情**：當天 07:00 的 plist 就是 `Hour = 7`——那也是當時 brief 第 6 節白紙黑字記載的設計值——是**當日稍晚才由使用者改成 1**。調查時讀到的 `Hour = 1` 是修改後的狀態，卻被當成修改前的證據。`[07:00:01]` 這種「整點加一秒」正是 `StartCalendarInterval` 準時觸發的特徵，本來就指向設定值。

**教訓**：在一個「正在被修改」的系統上除錯，**現況不等於事發時的狀況**。查任何設定前先問一句「這個值今天有沒有被人動過」；若動過，它只能證明現在，不能證明當時。旁證要從不會被回溯改寫的東西上找——日誌時間戳、檔案 mtime、當時版本的文件。

**`pmset` 那組電源設定與這件事無關，獨立保留**：01:00 的工作在會睡眠的 Mac 上本來就需要喚醒與不睡眠保障，那部分的決策是對的，見第 9 節。

### 2026-08-03　`auto-push.sh` 靜默失效

8/2 18:20 腳本被改成只含 `REPO="$HOME/advisory-knowledge-hub"` 的單一 repo 版，本 repo 從此完全脫離自動推送。**整個失效過程沒有任何外顯徵兆**——launchd 回報 exit 0、`push.log` 沒有新行（原版在「無變更」時直接 `exit 0` 且不留紀錄）、`data/` 檔案照常寫入、排程任務照常回報成功，只有網站悄悄停在舊版。發現方式是比對 `.git/logs/HEAD` 最後一次 commit 的時間戳與檔案 mtime。

修復後的腳本改為多 repo 迴圈、以 `continue` 而非 `exit` 跳過個別 repo、**無變更時也寫入 log**，並在推送成功後記錄 HEAD 短雜湊。

**教訓：「靜默」必須是可辨識的狀態，不能與正常運作無法區分。** 這條原則後來也用在別處——日報的 `notifyOnCompletion`、podfetch 的字數檢查、FT 的正文長度檢查，全都是同一件事的不同版本。也因為這次，`AGENT_BRIEF.md` 第 5 節的上線驗證必須比對 `updatedLabel` 而不能只看 `days[0].date`（事故當天 `days[0].date` 早就是當天日期，光看它會被騙過去）。

### 2026-08-03　執行時段從 09:00 改到 03:00

**動機**：一是希望起床就看得到成果；二是與 `advisory-dashboard-daily` 拉開 token 用量（那條線 cron 設 07:30，實際帶 jitter 約 07:35 觸發）。

**代價是結構性的**：台北 03:00 ＝ 美東前一天 15:00（EDT），而主要節目全部集中在台北 04:00–06:30 落地（Bloomberg TV 04:01、Bloomberg Money 04:25、MiB 05:00、All-In 06:23）。這些在 03:00 那一刻還沒發布，所以每天的日報固定收不到前一晚的美東晚間集數。All-In 因此大約在發布後 21 小時才進日報，而不是原本的 3 小時。podfetch 在 01:00 跑，看到的更早（美東 13:00 為止），13:00–15:00 EDT 之間發布的同樣順延一版。

**但沒有任何集數會遺失**：podfetch 視窗以 `last_run_utc` 為起點，日報又會去重，順延的集數下一版必定收得到。

### 2026-08-02　YouTube 字幕全面失效，改建 podfetch 管線

當天 YouTube 轉錄稿在本機 Chrome 上全面失效——面板可開啟但內容永遠停在載入中，`timedtext` 回傳空字串，InnerTube `get_transcript` 回 `FAILED_PRECONDITION`。在兩支不相干影片上重現，確認為環境層級故障，當日六集全部沒有逐字稿。

**事故暴露的真正問題是架構**：全文取得被綁死在「Chrome 開著 ＋ YouTube 已登入 ＋ YouTube 沒改版」三個條件上，任一失效就整天報廢，而且是安靜失效。

**修法**：改用 podcast 原始 MP3 ＋ Gemini API 轉錄。iTunes Lookup 的每個 `podcastEpisode` 都帶 `episodeUrl`（直接 MP3 網址），20 檔全部確認可取得——**這套系統從一開始就不需要 YouTube**。副作用是拿到了 YouTube 自動字幕從來給不了的維度：`shows.json` 預先寫入主持人名單，轉錄 prompt 要求 Gemini 用真名，跨節目交叉觀察因此能具體到人。

### 2026-08-02　Gemini 免費層：瓶頸是 RPD 不是 TPM

> **以下是 2026-08-02 當時的實測值，不是現行額度。** Google 已於 08-05 把 Flash 3.5／3.6 的 RPD 從 20 砍到 10（Lite 仍為 500，倍數因此從 25 倍變成 50 倍），模型池順序也已反轉為 Lite 優先。**現行數字見 `AGENT_BRIEF.md` 第 2 節與本節 08-05 那篇。** 保留這篇是因為「瓶頸是 RPD 不是 TPM」這個判斷本身仍然成立，而且它是後續所有設計的起點。

從 AI Studio Console 實測（同一專案內）：

| 模型 | RPM | TPM | RPD |
|---|---|---|---|
| Flash（2.5／3／3.5／3.6） | 5 | 250K | **20** |
| Flash-Lite 2.5 | 10 | 250K | 20 |
| **Flash-Lite 3.1／3.5** | 15 | 250K | **500** |

一般 Flash 每個模型每天只有 20 個請求，而 TPM 上限 250K 實際只用到 12%（約 30K）。**這推翻了直覺——正確策略是「少而大」的請求，不是「多而小」。** 因此 MP3 一律走 Files API（上傳免費、不計入 RPD，也繞開 inline base64 的 20MB 上限），並組成多模型輪替池。

新版 Flash-Lite 的日額度是一般 Flash 的 **25 倍**，是整個設計裡最重要的一個數字。**不要把 Lite 名額拿掉**，那等於自願放棄 25 倍額度。

**但段長最後不是由 RPD 決定，而是由輸出上限決定。** Lite 有 500 RPD 之後 RPD 就不再是瓶頸，真正咬人的是 `maxOutputTokens`：30 分鐘音檔的逐字稿約 6,000 字≒8,000+ token，很容易撞天花板。所以段長收斂到 20 分鐘（約 4,000 字），425 分鐘的一天約 22 個請求。

其他實測結論：`gemini-flash-latest` 目前對應 Gemini 3.6 Flash，屬 20 RPD 那一組；`gemini-2.5-flash` 已對新專案關閉（回 404）；`gemini-2 flash`／`gemini-3.1 pro`／`gemini-2.5 pro` 在免費層是 0/0。初次測試撞 rate limit 的兩個原因：自動挑到 preview 模型（免費額度比穩定版嚴格得多），以及三個請求並行瞬間撞 TPM。

**`max_output_tokens` 必須明確指定，這是最容易重蹈的坑。** 預設約 8,192，而 Gemini 3.x 的 thinking token 也算進輸出預算，實測導致多段被腰斬（一致卡在 5,600–6,200 字，最慘一段只吐出 6 個字）。同時以 `thinkingConfig.thinkingBudget = 0` 關掉 thinking——逐字轉錄不需要推理。

**模型池會在執行中「瘦身」，這是正常的。** `state.json` 的 `model_pool` 記的是當次跑完後仍可用的模型，不是設定值。08-02 的實際軌跡：`gemini-3-flash-preview`（429）→ `gemini-2.5-flash`（404）→ `gemini-2.5-flash-image`（額度滿）→ `gemini-flash-latest`（跑一段後用盡）→ 最後三個 Flash-Lite 完成全部六集。**看到 `model_pool` 只剩 Lite 不代表設定壞了**，代表溢流機制正常運作。

**額度用完時的行為**：429 會照 Google 回傳的 `retryDelay` 等待；判定為日額度耗盡則丟 `QuotaExhausted` 停止本次執行，**已完成的段落留在 `~/.podfetch/cache/`，下次直接沿用不重跑**，未完成的集數不寫進 `seen`、`last_run_utc` 也不推進，下次視窗仍涵蓋得到。若要加量再考慮付費，約 US$0.12／小時音檔。

### 2026-08-02　切段改用純 Python

原本用 ffmpeg，但這台 Mac 沒有 Homebrew——為了一個切檔動作要求安裝套件管理器不合理。改為解析 MPEG Layer III 的 frame 標頭、在 frame 邊界切檔，不重新編碼，因此零外部相依。第一個 frame 若是 Xing／Info／VBRI 標頭會跳過：它記的是整個原檔的長度，複製到第一段會讓解碼器誤判時長。若系統剛好有 ffmpeg 則優先使用（順便降到 16kHz 單聲道 32kbps，上傳量小很多），失敗時自動退回內建切檔器。

早期的 9 MiB 單段上限是 inline base64 時代的產物（膨脹約 1.34 倍須低於 20MB 請求上限），改走 Files API 後已放寬到 `max_chunk_mb: 48`。

### 2026-08-02　固定窗口造成無法察覺的缺口

podfetch 舊版用固定 26 小時窗口，漏跑一天就會產生缺口——7/31 22:53 到 8/1 15:24 之間整段掉出去，而且完全沒有徵兆。新版改為**以 `last_run_utc` 為起點**往前推 30 分鐘重疊，上限 72 小時。**不要改回固定窗口。**

（日報那一側仍是 26 小時窗口，因為它有去重機制兜底；兩者的窗口邏輯不同是刻意的。）

---

## 8. YouTube 字幕操作要領（2026-08-02 起已知失效，僅存查）

真的掉進這條退援路徑時才需要看。每集最多試 5 分鐘。

**先找到影片**：用 `https://www.youtube.com/results?search_query=%22<完整標題>%22`（加引號精確比對）比逐頁捲頻道 `/videos` 快得多。用 `read_page` 帶 `ref_id` 取得結果的 `href` 拿到 `watch?v=` 連結，並核對片長與 Apple 的 `trackTimeMillis` 是否吻合。

**取字幕的實際可行順序**：

1. `navigate` 到 `watch` 頁，等 4 秒
2. `find`「`...更多內容`」按鈕並點擊，展開影片說明，等 3 秒
3. 用 `computer` 的 `scroll` 往下捲約 10–12 格，直到出現標題「**字幕記錄**」與藍色按鈕「**顯示轉錄稿**」。先 `screenshot` 確認位置
4. **用座標點擊**那顆「顯示轉錄稿」按鈕，等 6 秒
5. 按 `Home` 回頁面頂端，等 3 秒，再 `get_page_text`
6. 「字幕記錄／搜尋轉錄稿」之後、到片尾台詞之前的整段（含時間戳）即為逐字稿；移除時間戳行後合併

**已知的坑**：

- 用 `find` 拿到的「顯示轉錄稿」ref 直接點**經常沒有反應**（頁面上有兩個同名節點，且影片播放會讓描述自動收合）。要捲到該區塊、用座標點才穩。
- 影片右上角的「⋯」選單**沒有**轉錄稿選項，只有下載與檢舉，別浪費時間。
- 想用 `javascript_tool` 讀 `ytInitialPlayerResponse.captions` 再 fetch 字幕檔：**行不通**。YouTube 有 Trusted Types，`DOMParser` 會被擋；改用 `&fmt=json3` 回傳空字串（baseUrl 需要 POT token）。乖乖走 UI。
- 純音訊型 Podcast 影片（如 Bloomberg Surveillance）一樣有自動字幕，流程相同。

**該集根本不在 YouTube 上**也確實會發生。2026-07-31 補跑時，Bloomberg 的 *Reacting to PCE, GDP, and Kevin Warsh*（42:23）全站搜尋無結果，官方播放清單當日只有四則且顯示「已隱藏無法播放的影片」。此時不要硬湊內容，照 brief 第 2 節退援順序最後一層處理。

---

## 9. 執行環境：機器與電源（2026-08-03 確立）

第 4 節提過 `pmset` 電源設定不存在於任何設定檔裡。本節把整個環境依賴一次寫清楚——這些同樣不存在於設定檔中，換機或重灌不會遷移。

**決策**：把這台舊 MacBook Pro 當常時開機的伺服器——放在家裡、全天開機、插著電、**不裝任何第三方電源管理軟體**。

### 每天需要醒著的時間窗口

| 時間（台北） | 事件 | 需要什麼 |
|---|---|---|
| **01:00** | **`com.kenny.podfetch`** | Mac 醒著，約 30–45 分鐘（六集） |
| **03:00** | **`podcast-digest-daily`** | Mac 醒著、Claude 桌面版開著 |
| 07:35 | `advisory-dashboard-daily`（另一條線） | Mac 醒著、Chrome 開著 |
| 至 11:30 | 該線跑完 | 同上，持續 |

**是從 01:00 到 11:30 的連續十個半小時，不是「時間到醒一下」。** 排定喚醒只解決 01:00 那個起點；喚醒後若閒置，macOS 會再睡回去，03:00 就接不到。這個風險是結構性的、與任何一次具體事故無關——`sleep 0` 就是為它而設。

### 已套用的設定

```bash
sudo pmset -c sleep 0          # 插電時永不睡眠（螢幕仍可關）
sudo pmset -c disksleep 0
sudo pmset -c womp 1           # 允許網路喚醒
sudo pmset repeat wakeorpoweron MTWRFSU 00:55:00   # 保險：萬一仍睡著
```

另加一項：

```bash
sudo pmset -b sleep 30    # 電池模式改 30 分鐘
```

電池模式原為 `sleep 1`，代表電源瞬斷切到電池後一分鐘就睡——**這會直接打斷正在跑的 01:00 轉錄或 03:00 日報**。30 分鐘可撐過短暫停電。

**2026-08-03 實測驗證通過**：AC Power 為 `sleep 0`、`disksleep 0`、`womp 1`、`displaysleep 10`；Battery Power 為 `sleep 30`；`pmset -g ps` 顯示 `AC Power`；`pmset -g sched` 顯示 `wakepoweron at 0:55AM every day`。

**`autorestart` 無法驗證，不要當成保障。** `sudo pmset -c autorestart 1` 執行未報錯，但 `pmset -g custom` 的 AC Power 清單**不會列出這個鍵**，無從確認是否生效。Apple Silicon 機種已移除此鍵（預設即復電自動開機），Intel 機種應會列出。要確認機型跑 `uname -m`（`arm64` ＝ Apple Silicon、`x86_64` ＝ Intel）。**工作假設一律採保守版：停電當天需人工介入，該日產出視為會缺。**

`sleep 0` 是主要保障，`repeat wakeorpoweron` 是後備。**兩者都要留**：只有後備而沒有主要保障時，機器會在 00:55 醒來、閒置後又睡回去，03:00 的日報就接不到。這是結構性風險，不繫於任何一次具體事故。

### 為什麼不用 AlDente（或任何充電管理軟體）

AlDente 有三個功能會讓 Mac **在插著電時改用電池供電**：Discharge、Sailing Mode、Calibration Mode。電源來源一變成電池，macOS 就改套用 `pmset -b` 並積極睡眠，`-c sleep 0` 完全不生效。

否決的真正理由不是「它會出錯」，而是**它的失效方式是安靜的**——與第 7 節 `auto-push.sh` 那起事故同一種模式：失效狀態與正常運作在現場無法區分。代價是電池長期停在 100%；這台是舊機且定位為固定式伺服器，判斷為可接受。macOS 內建的「最佳化電池充電」可保持開啟（只延後充電、不放電，無害）。

### 其他前提

- **蓋子必須打開**，除非接外接螢幕。闔蓋且無外接螢幕時一定會睡。
- **Claude 桌面版加進登入項目**；投顧那條還需要 Chrome 維持開啟與登入。
- **避免半夜自動重開機**，建議自動更新設成「僅下載、手動安裝」。
- **若啟用 FileVault，重開機需人工輸入密碼**，`autorestart` 救不回無人值守情境。已知單點，尚未解決。
- 全天開機要注意散熱，不要塞在密閉空間。

### 驗證

```bash
pmset -g custom     # AC Power 段的 sleep 應為 0
pmset -g sched      # 應有 repeat wakeorpoweron 00:55:00
pmset -g ps         # 應顯示 "AC Power"
```

`healthcheck.py` 的「podfetch 時刻」若持續 PASS（日誌開頭是 `[01:`），代表這套設定有效。

---

## 10. 不要做的事

- 不要憑印象編造節目內容，也不要編造引述。沒有逐字稿就不寫金句。
- 0 集時不要產生空檔案、不要動 `index.json`、不要產 Word 報告。
- 不要把 Word 報告寫進 repo。
- 不要跑 git 指令。
- 不要把 `~/podcast-transcripts` 移進 repo（Public repo ＋ 付費來源逐字稿 ＝ 著作權問題），就算加 `.gitignore` 也不要。
- 不要只改 brief 或只改排程 SKILL.md 其中一邊。
- 不要把事故經過寫進 brief——寫進本檔第 7 節。

---

## 11. 變更紀錄歸檔（自 `AGENT_BRIEF.md` 第 8 節移入）

**為什麼歸檔**：brief 每天被排程完整讀一次，變更紀錄一度膨脹到佔全檔 68%（46.6 KB），而執行者不需要歷史。第 8 節只留最近一次維護當天的條目，其餘依日期由新到舊收在這裡。維護時要查「當初為什麼這樣改」，先看第 7 節的事故檔案，再看這裡的逐日紀錄。

### 2026-08-15

- **iTunes 查詢沒有 cache-buster，造成一整天的「假零集」——這是本系統第一次出現「主路徑安靜漏抓當天全部集數」。** podfetch 01:00 準時執行、**1 秒內結束**、日誌寫「沒有新集數。」，而窗口內實際有 10 集。根因：`itunes_lookup()` 的網址是固定字串，iTunes 把回應綁在**確切的查詢字串**上快取，US 商店回傳停在 07-31（落後 15 天），HTTP 仍是 200，`discover()` 因此一集都沒收到、也不寫任何錯誤行。**修法是在網址加 `&cb=<unix 時間戳>`。** brief 第 1 節已同步 URL 範本、`&cb=` 的必要性、以及「換商店要用 `&country=GB`，路徑前綴 `/gb/`／`/au/` 在本環境回空白內容」這條訂正——**舊版寫的 `/gb/` 是錯的，照著做只會拿到空白**。
- **這個失效態的可觀測特徵要記住**：唯一的外顯訊號是**執行時間從 9 秒掉到 1 秒**（23 檔網路查詢不可能 1 秒跑完），而 brief 第 2 節「三段排查」的第 2 格會直接把「沒有新集數。」判成一切正常。**「日誌說正常」與「真的正常」在這個失效態下分不開，只能靠外部對照**——當天是拿同一支 AppleID 帶與不帶 `cb` 各查一次才定案。
- **而且它會製造永久缺口。** `discover()` 回空時仍**無條件推進** `last_run_utc`，下次窗口起點因此跳過那 24.5 小時。當天最後一集發布於 16:17、新起點是 16:30，**差 13 分鐘全部落在窗口外**。復原方式是把 `last_run_utc` 回捲再重跑，10 集全數救回。
- **FT 入口第二次寫錯，這次連「已更正的版本」也是錯的。** 08-08 把入口從 `ft.com/unhedged` 改成 `ft.com/the-economics-show`，但後者只列 The Economics Show 自己的集數、**一樣不含 Unhedged**。可用的入口是 FT 站內搜尋 `ft.com/search?q="<原文標題>"`，而且**每集有兩個頁面，要取標題帶 `Transcript:` 前綴的那一個**——另一個集數頁正文僅約 2,700 字元，很容易被誤判成「付費牆擋住」而白白退層。brief 第 1、2 節已同步。
- **FT 全文改由子代理抓，不再進主 context。** 舊寫法是「約 45K 字元會進主 context 一次，這是工具限制下的已接受成本」；現在主代理只做定位與驗證（用 `javascript_tool` 回傳長度與付費牆檢查，輸出僅數十字元），再把 URL 交給子代理 `get_page_text`。**23,924 字元完全沒有進主對話**，那條「已接受成本」不必再接受。
- **三處與實況脫鉤的敘述一併訂正**：①ILTB 語速兩處打架（brief 第 1 節寫 122，第 2 節與 `shows.json` 都是 140）；②檔名慣例把窗口寫成「台北 01:00 至 03:00」，實際**兩端都由 podfetch 決定**（實測 00:30 → 隔日 01:00、24.5 小時，終點是 podfetch 的執行時刻而非日報的）；③brief 開頭宣稱「讀了這份就能完整重現整套系統」，但三條成本紀律只存在於 SKILL.md，已改為明確指路而非默默不提。

### 2026-08-14

- **「用段數規劃」成立，而且效果比預期大。** 連續三天測同一條規則：08-11 寫進措辭（3/5 重寫）、08-12 加強措辭（3/5 重寫）、08-12 晚間把規劃單位從字數換成段數 → **08-14 十二集零重寫**，段數達標 11/12（唯一超標的 GS Exchanges 是官方逐字稿密度高的允許情形）。**每集成本 54.9 萬 → 28.3 萬，減半**；子代理平均 2.6 次工具呼叫、無一集超過 5 次。**這條規則現在有效，沒有新證據就不要再動它**——連續三次修改同一條規則已經證明，前兩次都因為改錯了層次而白費。
- **順帶驗證「新架構是線性成長、集數多才顯優勢」**（08-08 寫下、一直沒機會測）：12 集這天，固定開銷（指令與工具清單）佔比從 08-12 的 20.1% 降到 **10.4%**，攤薄效果清楚。
- **新增時間軸檢查 `check_timestamps()`，這是一個存在很久卻從沒人看見的盲區。** 08-11 的 20VC 最大時間戳跑到 1,192 分鐘（片長 59 分，**溢出 1888%**）、08-14 的 TIP837 跑到 253 分（片長 78 分，**溢出 224%**）——兩集完整度分別是 0.91 與 0.98、status 都是 OK，**三天來沒有任何機制看見它**。訊號寫進**獨立的 `timestamp_notes`／`timestampNotes`，不影響 `status`**（時間軸壞掉不等於內容缺漏，併進 `warnings` 會用錯的理由標降級——08-07 講者檢查踩過這個坑）。詳見第 2 節（維度定義）與第 3 節（兩條門檻）。
  **第二條規則刻意設成「兩個訊號同時成立」**：只看大間隙會對 72 集報 15 集，因為長篇獨白與真跳段長得一樣；加上「完整度 <0.85」之後全庫只報 7 集，而唯一被抓到的間隙案例（08-14 MIB，22 分鐘無戳、完整度 0.73）正是子代理獨立確認確有兩處跳段的那一集。**「與完整度一起看」這件事應該由程式做，不是寫進訊息叫人自己判斷。**
- **回歸複驗（子代理）抓到 9 處，全修。** 最嚴重的兩處都是**已經記過的同型錯誤又犯一次**：①08-12 的變更紀錄以 changelog 體例留在 `MAINTENANCE.md` 第 7 節、沒進第 11 節——**這正是 08-09 抓過的 08-08 同型錯，防它的規則就寫在本節末尾**；②第 12 節那段列舉又對不上（08-12 那列已翻 ✅ 卻沒進第三類 bullet），**連續五輪、四種排列**，所以把「改字與改表必須同一次做完」改寫成三步檢查清單。其餘七處：`podfetch.py` 新註解裡的例子數字（1,114／56 應為 1,192／59）與三份文件不符、`_ANY_TS` 不吃縮排時間戳（同檔其他正則都吃，帶縮排的稿會靜默回空）、`TOKEN_NOTE` 被回填日覆寫（今天的量測失敗原因指向 08-13）、檔頭設計清單沒有時間軸那條、README 只寫兩種檢查、brief 第 3 節有句子被新區塊擠斷、`TS_DARK_GAP=90` 的定義沒寫進 brief。
- **token 量測的路徑終於用 `find` 定位出來，不再猜。** 真實位置是 `~/Library/Application Support/Claude/local-agent-mode-sessions/<帳號>/<工作區>/local_<階段>/.claude/projects/<專案>/<uuid>.jsonl`，子代理在同層的 `<uuid>/subagents/agent-*.jsonl`——**掃描邏輯本來就對得上，錯的只有根目錄那一行 glob**。前兩次分別猜成 `~/.claude/projects` 與 `$TMPDIR/claude-hostloop-plugins/`，`eff_tokens_k` 因此連續五天空白。**猜兩次不中就該停下來去量，而不是猜第三次。**（此欄仍只在 Mac 上跑 healthcheck 時才有值，沙箱看不到 `~/Library`。）
  **最後的結論是這三欄根本量不到，已改為人工填入。** 用 `grep` 掃遍 `~/Library/Application Support/Claude` 之後確認：**`local-agent-mode-sessions/` 底下只有互動式對話，排程執行不在本機留 transcript。** 五天四次修正（兩次猜路徑、一次改判別法、一次加指紋）**全都建立在一個從沒被驗證的前提上——那個檔案存在**。
  **但資料一直都在**，每天的 token 分析報告有，只是機器讀不到。所以改成**維護時人工從當日報告抄進 `metrics.csv`**（加權總量千位／子代理數／子代理總回合數），`healthcheck.py` 只負責「不要把人工填的值洗掉」並在沒填時提醒。已回填 08-09／08-10／08-11／08-12／08-14 五天，**整條優化曲線終於在同一張表上**：每集成本 1,940K → 577K → 549K → **283K**。
  **教訓**：debug 一條鏈時，最先該驗證的是**最根本、最不像會錯的那個前提**——我從第二層（路徑）開始查，把第一層（檔案存在嗎）當成已知。
  **路徑修好之後露出的第二層問題也值得記，那是同一種毛病**：原本用「mtime 落在 02:30–06:00」判斷哪一個 transcript 是排程執行，實測 70 個檔案**沒有一個**落在窗口內——**mtime 是「最後一次寫入」，桌面 App 之後重開或續讀同一個 session 都會把它往後推**。已改用**內容指紋**（排程 prompt 開頭那句只會出現在排程執行的 transcript 裡）＋**檔內 `timestamp`** 判日期，mtime 窗口留作退援並在訊息中標明走了哪一條。**時間是外部條件，內容才是「這個檔案是什麼」的證據。**
- **`skill_kb` 這一欄從來沒有被填過任何一天**（15 列全空）——它讀 `~/Documents/`，而沙箱看不到那裡，維護幾乎都在沙箱跑。**刻意不改成從別處猜**：空值至少誠實，填一個猜來的大小會讓這一欄看起來有在追蹤。要它有值就得在 Mac 的終端機直接跑 healthcheck。
- **`healthcheck.py` 會回填 `metrics.csv` 缺漏的日子。** 指標原本只在「跑 healthcheck 的當天」寫入，**沒跑維護的日子就沒有那一列**——08-13 就這樣缺了，而那天其實有 3 集、其中一集 DEGRADED，完全沒進基線。現在掃 `data/` 與逐字稿目錄的聯集回填（本次補了 07-29／07-30／07-31／08-13）。**回填列的 `brief_kb`／`skill_kb` 一律留空**，那兩個是「當下的檔案大小」，回填舊日期時填今天的值會是假的。

### 2026-08-12

- **找到 08-11 那條「先估篇幅再落筆」失效的根因：尺是浮動的。** 08-12 五集仍有三集重寫（與前一天同樣是 3/5），成本 274 萬 vs 288 萬持平，但逐字稿少了 25%——**每 KB 成本反而上升**。實測全庫 13 個日檔 74 集，子代理自報的 `chars` 與正文實際字元數：**偏差中位數 0%，範圍 −38% 到 +81%，42% 的集數偏差 20% 以上**。這不是系統性偏差而是**雜訊**——一把無偏但誤差 ±40% 的尺，比一把有偏但穩定的尺更沒用。當天實例：Dwarkesh 自報 5,900／實際 8,709、Pivot 自報 5,948／實際 8,045（兩集都自以為在區間內，Pivot 實際超上界 23.8%、Dwarkesh 34.0%），而 In Good Company 自報 1,789／實際 1,087（自以為超標所以壓縮了兩輪，實際一直低於下界）。**在一把會浮動的尺上「先估篇幅」是不可能執行的。**
- **`chars` 改由組檔時機械計算並覆寫**（第 4 節）。子代理現在填 `0` 即可，不必也不應該自己數。**歷史 13 個日檔已一併重算，74 集中 42 集的值有變。**
- **寫作規格改用「段數」規劃，不用字數**（第 3 節）。字數的唯一定義寫死為「`sections[].paragraphs[]` 串接後的字元數」，同時給每個層級對應的目標段數（6–9／10–15／13–20／20–33），依據是全庫 1,546 個段落的中位數 **198 字元**（四分位 150–251）。**段數是子代理下筆時數得準的，字元數不是**——把規劃單位換成它量得到的東西，規則才可能被執行。
- **回歸複驗（子代理）抓到 8 處，全修**：最嚴重的是**「用段數規劃」只改了 SKILL.md、brief 第 3 節仍留著被廢掉的「查出目標字數／分配每節字數」**——一組兩份漏改一邊，而且留下的正是本輪判定為「要求執行者做一件它做不到的事」的那條；任務卡自己打架（一處說「填估計值」、兩處說「填 0」）；MAINTENANCE 第 12 節的 08-11 列未結案（仍寫「四項修正待 08-12 驗證」，而 08-12 已證偽其中一項）、第二類 bullet 未納入 08-11、「連續三輪」未更新；另有歸檔範圍、回報第 14 項單位、50% 邊界重疊、150–250 vs 150–251、超上界百分比概括等五處。
- **`index.html` 卡片顯示的「摘譯 N 字」語意已改變**（顯示的就是 `chars`）：74 集中 42 集的數字變了，且它的意義從「子代理自報字數，每集一把尺」變成「正文字元數」。**沒有改 `index.html`（那是紅線）**，但記在這裡以免下次有人比對舊截圖時困惑。
- **中斷重派加成本門檻**（排程 SKILL.md 第 3 步）：已留下有效 JSON 但超上界時，**超出 50% 以內直接收下並在 `source` 註明，超過才重派壓縮**。08-12 的 ILTB 中斷後重派，**重派那次 36.2 萬比原本那次 20.8 萬還貴**，即使它完全沒讀逐字稿——壓縮改寫本身就是反覆讀寫的工作，兩次合計佔當天全場 20.8%。**超上界是本檔已經允許、只要說明理由的降級；花 36 萬 token 去消滅一個被允許的降級並不划算。**（那次超了 79%，依新規則仍該重派，當天處置正確。）

### 2026-08-11

- **token 優化終於有量測答案：同樣五集，970 萬 → 288 萬，降 70%，而且逐字稿還多 19%（490 KB vs 412 KB）。** 這個問題從 08-08 寫下「粗估降 60–75%」以來一直無法驗證，現在實測落在同一區間。子代理佔比從 84% 降到 62%。**「成本的驅動是回合數不是資料量」這個判斷成立**，工具呼叫預算是有效的那一刀。
- **集層級時限與整場硬停通過實測，餘裕很大。** Acquired 273 分鐘、14 段、45,237 字，從 01:06:57 跑到 01:18:41 只用 **11 分 44 秒**（單集上限 60 分）；整場 18 分 41 秒（整場上限 100 分）。**沒有誤殺，也證明 3600／6000 不算保守過頭。**
- **任務卡加「先估篇幅再落筆」（第 3 節同步）。** 08-11 四集超出呼叫預算，主因是三集初稿超上界後重寫（Odd Lots 8,257→6,434、Acquired 14,255→6,583、Bloomberg 3,642→2,983）——**每重寫一輪，整份逐字稿就被重新計費一次**。寫作規格與成本預算在長節目上會互相打架，解法是先列骨架並分配字數，不是事後刪。**理想工具呼叫數仍是 2 次（Read → Write）**，「想骨架」是中間的思考步驟、不佔呼叫。
- **補齊 `acquired`／`bg2`／`lennys` 三組 CSS，現役 23 檔全部齊備（`index.html` 共 24 組）。** 08-11 healthcheck 第一次真的 WARN——Acquired 卡片走了預設藍。三處撞色分別檢查，色條最近鄰 Δ47–64、深色最近鄰 Δ61 以上。
- **記分板加收錄標準，並清掉 3 條純免責條目（44 → 41）。** 08-10 回填種子時把後記段落照單全收，連「以上內容非投資建議」都進了記分板，**08-11 六條改判裡有四條在清這些雜訊**。新規則：只收可驗證的預測或檢驗點，免責聲明、來源標註、管線自檢一律不收。
- **回報第 13 項註明呼叫次數是自報下限。** 08-11 實測：自報 4／7／6／9／11，系統實際 5／7／6／11／18——Acquired 差了 7 次。**這個數字現在是成本監控的依據，但它會低報**，判斷時要記得。
- **回歸複驗（子代理）抓到 8 處，全修**：登記簿那兩列的 ⚠️ 沒跟著內文翻正（**只改字沒改表，與前兩輪方向相反、形態相同，已是連續第三輪**）、逐場登記表被一個空行切成兩張且 08-10 ② 錯位、`obs-20260810-3` 這條純管線自檢被判「應驗」而 brief 正好拿同句型當反例、任務卡「3 次工具呼叫」只枚舉得出 2 次真呼叫、120 分鐘規則沒進到子代理任務卡與第 1 節、收錄標準 brief 四類 SKILL 三類、README 未同步、observations 的 `note` 鍵未進格式範例。
- **超長節目不取官方稿寫進規格（第 2 節）**：片長超過 120 分鐘的 A 類節目直接用 podfetch 稿。`web_fetch` 約 104,700 字元的單次上限會靜默截斷，兩小時以上的官方稿必然超出數倍。08-11 的 Acquired 是子代理自主做了這個判斷、判斷正確，**只是規格裡當時沒有這條路**——又一次「執行者靠自覺補上規格缺口」。

### 2026-08-10（第三場，程式碼審查與知識庫新功能）

- **修掉 `episode_budget_seconds` 的語意謊言。** 它名為單集上限，實際上設在「每段、每次重試」裡、且只在過載分支檢查——乾淨跑永遠不會觸發（所以從沒被發現），極端路徑無上界：2 小時集理論最壞 4 小時。現在整集只設一次、每段開始前檢查，值同步加大到 3600（2 小時集乾淨跑要 10–20 分，1200 會把會成功的集殺成 FAILED）。**另加整場執行上限 `run_budget_seconds: 6000`**：02:40 後不再開始新集數——03:00 日報要讀 manifest，寧可缺集也不能讓它讀到不完整的檔案。
- **快取段落加品質門檻。** 舊版只驗「>200 bytes」，被截斷的壞段寫進 cache 後，下次接續會被無條件吸收，**該段內容永久缺失且無徵兆**。現在讀 cache 套用與現場相同的 0.55× 門檻；**只丟棄一次**（`.redo` 標記），重轉後仍不足就照收並記 warning——否則語速慢於基準的節目會每天重轉同一段燒 RPD。
- **模型探測結果 72 小時內沿用。** 探測發真實請求、計入 RPD——每天 6–10 個、佔全日 25–30%，其中 3 個以上打在只有 10 RPD 的 Flash 池上，而結果早就存進 state 只是從不回讀。防線：執行中有模型 404 就標 `force_reprobe`，隔天強制重測全池。
- **config.json 讀取失敗改為中止。** 舊行為靜默退回程式內建預設值——而內建的 `segment_seconds` 預設是 1800，正是被回滾過的值。「用錯參數跑完」比「不跑」難發現得多；不跑會被 healthcheck 的日誌檢查抓到。內建預設值也全部對齊 config 現值。
- **healthcheck 三處修正**：推送鏈補 packed-refs fallback（git gc 之後 loose refs 會消失，否則變成每天假 WARN）；01:00 判定改掃全檔的「視窗：」行（維護時手動跑過一次就會把排程執行擠出第一行，誤報 FAIL 還把人引去查 plist）；`podfetch_requests` 改名 **`segments_done`**——它數的是完成段數不是 API 請求數，08-05 實測請求數是段數的 2.5 倍，舊名會讓人拿它估 RPD 而系統性低估。
- **網站四個新功能**：①**觀察點記分板**（`#/obs`）——每天觀察後記的可驗證觀察點進 `data/observations.json` 追蹤（已回填 41 條種子），日報每天回訪改判，讓知識庫從「摘要」變成「有記憶的判斷追蹤」；②**來賓索引**（`#/guests`）——按來賓彙整歷次出現，新 schema 加結構化 `guests` 欄位，舊集數退回解析自由文字；③**品質與主持人顯示**——新 `quality` 欄位（完整度／status／講者警示）進閱讀器 meta 表格，`hosts` 欄位存在至今從未顯示過，一併補上；④**主題標籤**——新 `topics` 欄位（15 個受控詞表、每集 1–3 個），先累積資料，跨日主題頁之後再建。
- **前端搜尋效能**：歷史日檔（不可變）拿掉 `no-cache`，只有今天那份保留；搜尋文本快取（`__txt`），不再每次按鍵重建。天數多了才會顯現的差距，趁小先修。
- schema 三個新欄位（`guests`／`topics`／`quality`）與觀察點格式已寫進第 4 節；任務卡同步（詞表兩邊一致）；回報清單擴到 15 項（第 15 項＝記分板本日動了什麼）。`snapshot.sh` 時間戳改到秒級並防同秒覆蓋（分鐘級時同一分鐘兩次快照會靜默互蓋——對一個因覆寫事故而誕生的備份工具而言不可接受）。
- **回歸複驗（子代理）抓到 9 處，全修**：①`run_deadline` 起算點原在模型探測**之後**——重新探測的那一天（最慢的一天）門檻會往後漂十幾分鐘，已移到程式啟動即起算；②第 4 節「每天只需要動兩個檔」與記分板每日回寫直接矛盾（**照 brief 字面可以合理略過回訪**，記分板的價值會被規格自己關掉），已改「三個檔」並補目錄樹；③「看日誌第一行」的判準只修了機器端，brief／MAINTENANCE／SKILL 五處人工指示全留舊，已全改為掃「視窗：」行；④觀察點種子檔補上 `verdict`／`verdictDate` 欄位（brief 格式範例與實檔曾不一致——「補欄位」與「改欄位」是兩種不同動作）；⑤README 補兩個新頁與 observations.json；⑥nav chip 原本只在 manifest 載入成功後才綁事件，失敗時看起來正常但點不動，改為無條件綁定＋來賓頁防呆；⑦healthcheck 01:00 判定的分支順序調整（全檔掃描結果優先於第一行格式）；⑧snapshot.sh 改動補進本紀錄；⑨`segments_done` 說明補「快取段不計入」。

### 2026-08-10

- **新增 We Study Billionaires（TIP），節目數 22 → 23。** 加它的唯一理由是**週末補檔**：週一那一版涵蓋美東週六中午到週日中午，現有 22 檔在這個窗口幾乎沒有供給（08-10 只有 2 集，還是同一支預告）。TIP 一週兩檔、**其中一檔固定在週日**（實查 08-09／08-02／07-26 皆為週日發布，UTC 00:00 ＝ 美東週六 20:00，**落在窗口內**）。六處都已補：brief 第 1 節、`shows.json`、`config.json` 的 `show_priority`（插在 `iltb` 之後）、`index.html` 三組 CSS、`README.md` 的節目清單、排程 SKILL.md 的 `description`。**語速未校準**，先沿用全域 200，累積幾集後再決定要不要補 `wpm`。
  **配色第一版選錯，已換掉。** 初版 `#a16207` 只檢查了色條就宣稱查過撞色——實際上徽章文字色與現役的 `unhedged`（`#a1560a`）**RGB 距離只有 12**，等於同一個顏色；深色模式也與 All-In 撞在同一片金色。**「補的時候三處都要補」那句話，撞色檢查也要三處分別做。** 現用 `#701a75`（梅紫），最近鄰是 `pivot` Δ60／深色 `nopriors` Δ26。
- **`healthcheck.py` 的 token 量測改成「失敗要出聲」。** 08-10 修了路徑之後在 Mac 上實測，`eff_tokens_k` **仍然是空的**——第二次猜路徑又猜錯。**真正的問題不是猜錯，是猜錯這件事沒有任何症狀**：留空看起來像「還沒有資料」而不是「壞了」，所以它安靜地跑了兩天。現在量測失敗會讓「每日指標」變成 WARN 並印出原因（找不到根目錄／根目錄下沒有 `.jsonl`／有檔案但都不在 02:30–06:00，後者會附上最近三筆 mtime）。**加了量測就要順手驗證它回傳非空值，這是第二次踩同一個坑。**
- **新增「同日同源去重」（第 1 節）。** 08-10 當天兩集是**同一支 113 秒的〈Our Town〉預告**，分別被 Bloomberg Surveillance 與 Odd Lots 推送——**同一天、不同 url、不同中譯標題**，三個條件全部避開了現行去重。而唯一能認出同源的兩個欄位（manifest 原文 `title` 與 `durationMs`）**剛好都不在比對範圍內**。新規則比對這兩個欄位、兩者都相同才合併，保留 `show_priority` 較前者並在其 `meta` 加「同源推送」。**失敗方向刻意選在漏判那一側**：多留一集只是重複，誤刪會弄丟內容。
- **補上「低於字數下界」的例外與回報義務（第 3 節）。** 原本規格只規定超上限要說明，**低於下界完全沒有條款**——08-10 那兩集逐字稿只有 301／303 字，下界卻是 1,200 字，主代理寫了 986／501 字並自行把理由塞進 `source` 欄位。**依賴自覺的紀律不是紀律**，現在明訂「素材撐不起下界時以素材為準」，同時列為回報第 14 項。
- **子代理改為有門檻：逐字稿小於 10 KB 的集數由主代理直接處理**（排程 SKILL.md 第 3 步）。08-10 兩份逐字稿各 2.5 KB，派子代理的固定開銷（整包指令送進新 context）遠大於處理內容本身。當天主代理是臨場判斷不派，**寫進規格才會穩定生效**。
- **修 SKILL.md 的退援順序（第 2 步）。** 原本寫「官方稿 → podfetch → FT → YouTube」，把 FT 排成獨立的第 3 層；實際上 **FT 流程取得的就是 Unhedged 的官方逐字稿（第 1 層）**，失效時退回 podfetch 而非跳到 YouTube。同一份文件裡順序宣告與操作指示互相打架。
- **統一「26 小時」的說法（第 1 節）。** 原本第 1 節寫「固定 26 小時」、第 2 節的設計原則又寫「以 `last_run_utc` 為準，不是固定窗口」——**字面互相否定**。實測 08-10 的窗口是 24.5 小時，兩種描述都不吻合。現在一律以 `last_run_utc` 為準，26 小時只當說明用的近似值。
- **修 token 量測讀錯路徑。** `healthcheck.py` 的 `measure_session_tokens()` 只找 `~/.claude/projects`，那是 Claude Code CLI 的路徑，**而每日排程跑在 Cowork**，transcript 在 `$TMPDIR/claude-hostloop-plugins/<hash>/projects/` 底下。結果 `eff_tokens_k` 連續兩天都是空的——**加了量測卻沒量到，比沒加更糟，因為 `metrics.csv` 看起來已經在收集基線了。** 現在同時搜尋多個候選根目錄（用 glob，因為 hash 會變）。

### 2026-08-09

- **子代理任務卡加上工具呼叫預算，這是目前最有效的成本控制點。** 08-09 首次實跑，五個子代理吃掉全程 84%，最貴一集 225 萬——**不是因為逐字稿大，是因為它跑了 70 次工具呼叫**。實測所有逐字稿最長 1,201 行，Read 上限 2,000 行，**每一份都在一次呼叫內讀得完**，所以那 70 次不是被迫分頁，是讀完之後又做了 68 件事（多半是為了核對金句而反覆 grep／重讀）。**逐字稿一進 context，之後每一次呼叫都把那 120 KB 再送一遍。** 任務卡現在明訂：Read 一次不帶 offset → Write 一次，**目標 2 次、上限 5 次**，並明講「全文已在你的 context 裡，核對不要再讀第二次」。摘要卡末行回報實際次數，回報清單第 13 項收集，超過 5 次要點名。
- **`metrics.csv` 補上 `eff_tokens_k`／`subagents`／`agent_turns` 三欄。** 08-08 寫的「粗估降 60–75%」**是估算不是量測**，至今無法驗證，因為舊架構從未被量測過。`healthcheck.py` 現在會解析當日排程 session 的 transcript 算加權 token（重讀 0.1x、寫入 2x、產出 5x）。**選 session 時只認 02:30–06:00 之間結束的**——維護用的互動對話往往比排程執行大好幾倍，挑錯就等於量錯。沙箱讀不到實機路徑時留空、不報錯。
- **語速基準改為 per-show 並上修全域值**（`WORDS_PER_MIN` 165 → 200，`shows.json` 可用 `"wpm"` 覆寫，已為 7 檔設定）。起因是查 08-09 兩集的完整度（1.44／1.53），掃過**單 token 與片語級跳針都是 0**——不是跳針，只是 All-In 那類四人搶話的節目本來就講到 238 字/分（**那兩集的 `DEGRADED` 另有原因，見下一條**）。17 檔實測中位數約 200，**原本的 165 讓幾乎每集完整度都顯示 1.2–1.3，「超標」變成常態，這個數字失去可讀性**。校準後 15 檔全部落在 0.87–1.09。
- **回歸複驗抓到六處**（子代理獨立比對）。**其中兩處是我當天改動的副作用**（`metrics.csv` 擴欄後舊列錯位、把 wpm 校準寫成一個程式上不成立的因果），**兩處是 08-08 歸檔留下的**（規則被跟著搬走、08-08 紀錄根本沒進第 11 節），**其餘是既有缺陷**：
  - **`metrics.csv` 表頭擴為 15 欄，但 08-02～08-08 的舊列仍是 12 欄**，整份檔案無法按欄名解析——**而它存在的唯一目的就是當可解析的基線**。`healthcheck.py` 現在重寫時會把短列補空欄。
  - **歸檔時把規則跟著紀錄一起搬走了。** 「brief 與 SKILL.md 是一組兩份、改任一邊都要同步」這條規則原在本節末尾，08-08 隨變更紀錄被移進 `MAINTENANCE.md`，**於是 brief 整整一天沒有任何一句要求雙邊同步**——而 08-04 的整份覆寫事故正是這類漏抄造成的。已寫回本節並標明不要刪。**歸檔時要分辨哪些是紀錄、哪些是規則。**
  - **08-08 的三段變更紀錄根本沒進 `MAINTENANCE.md` 第 11 節**，而是以 changelog 體例留在第 7 節（事故檔案）裡，使本節「已歸檔至第 11 節」成為假陳述。已移正。
  - **`healthcheck.py` 的「brief vs config」會靜默跳過抓不到的鍵，卻仍回報「N 個都對得上」。** `episode_budget_seconds` 在 brief 寫成「`key`（現為 1200 秒）」，正則吃不到，等於從未被檢查。現在改為 WARN 列出哪些鍵找不到可比對的數值，brief 也補上 `key: 值` 形式。**「檢查數」與「實查數」脫鉤是最會讓人安心的一種失效。**
  - 順帶修掉：`podfetch.py` 常數上方兩代前的舊註解（還寫著「原本設 130」與已過期的 Market Huddle 168）、brief 第 6 節指向 `MAINTENANCE.md` 第 4 節（實際在 4B）、`healthcheck.py` 與 brief 都還宣稱它「不寫任何檔案」（它會寫 `metrics.csv`）。
- **訂正一個當天自己寫錯的因果：`wpm` 不決定 `DEGRADED`。** 初稿寫「基準過低導致 `DEGRADED` 失去訊號價值」，讀 `podfetch.py` 才發現 status 只看 `warnings`，而完整度「超標」在任何路徑都不會產生 warning——那兩集的降級來自段級相對檢查。**上修 wpm 反而讓段級重試門檻變嚴。** 教訓：**改指標前先確認它接到哪條分支**，文件與文件互相對讀查不出這種矛盾，只有讀程式才會發現。

### 2026-08-08（第三次，版本控制與指標基線）

- **建立 `~/.podfetch/snapshot.sh`，補上 repo 外檔案的回溯能力。** repo 內的 brief／MAINTENANCE／index.html 有 git，但**改動最頻繁的檔案全在 repo 外且零版本紀錄**——`podfetch.py` 這一週至少改 8 次、排程 `SKILL.md` 至少 9 次而且**被整份覆寫過一次**（08-04），當時無法還原、只能靠子代理事後比對發現。腳本存八個檔案＋`MANIFEST.txt`（SHA 與大小），支援 `--list`／`--diff`，還原就是 `cp` 回去。**`gemini.key` 與 `state.json` 刻意排除。** 維護流程第 0 步改為「開工先快照」（`MAINTENANCE.md` 第 2 節）。
- **`healthcheck.py` 每次執行自動累積每日指標到 `~/.podfetch/metrics.csv`**（12 欄：集數、狀態分布、逐字稿 KB、產出 JSON KB、brief KB、SKILL KB、podfetch 請求數、音檔分鐘、講者訊號數）。**動機是今天的 token 優化只能「估算」降幅**——從來沒有量測過任何一天，沒有基線的優化只能自稱有效。已回填 08-02 起的歷史；**08-08 是舊架構最後一天，也是新架構的對照基準**（10 集／逐字稿 617 KB／產出 229 KB／29 個請求）。同日重跑覆寫該列。
- **新增 `MAINTENANCE.md` 第 12 節「版本登記簿」**：13 場維護的速查表——動了哪些檔案、想解決什麼、**後來成立嗎**。最後一欄標出「修法本身製造新問題」的案例，動防護機制前要先看。（建立當時是三次，現況以第 12 節那張表為準。）第 11 節仍是敘事體的來龍去脈，兩者互補。

### 2026-08-08（第二次，token 優化）

- **每日排程改為「子代理分集處理」架構，這是本系統成本結構的根本改變。** 主對話是一條長對話，每次工具呼叫都重送整個 context——過去 10 集逐字稿（617 KB ≈ 15–20 萬 token）讀進主 context 後被重複計費數十次。現在：主代理只讀 manifest 與 brief，**每集派一個子代理**（同一則訊息並行派出）在各自 context 讀逐字稿、把該集 JSON 寫進 `/tmp/ep-NN.json`、只回傳十行摘要卡；主代理用摘要卡寫交叉觀察、用 python 合併暫存檔組出 `data/` 兩檔。**逐字稿與完整集數 JSON 從此不進主對話。** 交叉觀察改以摘要卡合成（每卡含 5–8 條具名論點），品質取捨已評估。
- **Word 報告改由 `~/.podfetch/json2docx.py` 從 JSON 機械轉出**。過去同一份內容 LLM 寫兩次（JSON 229 KB ＋ 82 頁 docx），現在第二次是免費的腳本轉檔。已用 08-08 真實資料驗證：10 集、528 段落、81K 字元，跨節目觀察與金句齊全。
- **本檔瘦身**：變更紀錄只留最近兩天、其餘歸檔至 `MAINTENANCE.md` 第 11 節（歸檔規則寫在第 8 節末尾）；第 6 節維護專用內容（PAT 換發、電源設定完整指令、launchd 細節）移至 `MAINTENANCE.md` 第 4B／9 節，只留每日執行需要的。68.8 KB → 44.6 KB（規格本體 20.9 KB；當日紀錄 22.7 KB 會在下次維護歸檔）。
- **三項合計的降幅：當時只能粗估 60–75%，而那是估算不是量測**——沒有任何一天的實際數字可比。08-09 首次實跑後回頭檢視，這個數字**至今仍無法驗證**，因為舊架構從未被量測過。`metrics.csv` 已於 08-09 補上 `eff_tokens_k` 欄位，往後才會有真基線。**寫下未經量測的降幅是這次的教訓：估算要標明是估算。**


### 2026-08-08

- **修掉一個讓完整度指標「反向」的缺陷。** Gemini 在長音檔上會整行跳針（08-08 實例：The Compound 一行 28,122 個重複的「I」、Hard Fork 9,521 個「let's」），而 `spoken_words()` 直接數 token，跳針全部計入——於是**內容嚴重缺漏的失效態顯示成「超標 336%」**。`completeness` 是日報用來判斷該集能不能信的依據，這個數字在最需要它的時候是錯的。新增 `collapse_loops()`：計字前把「連續重複 20 次以上的同一 token」壓成一次，並在 `warnings` 單獨記一筆剔除了多少字。**實測還原成 1.10 與 1.10，與人工評估吻合。**
- **修掉時間戳的雙重偏移。** prompt 原本告訴 Gemini「本段涵蓋整集的 20:00–40:00」，它就照絕對時間輸出，而 `offset_timestamps()` 又加一次 → 第 2 段標成 40:00–59:52、實際是 20:00–39:52。**而且同一集裡時而遵守時而不遵守**（第 3 段是對的），所以合併後時間軸會往回跳。**光改 prompt 擋不住**，已加機械式偵測：看該段第一個時間戳離 0 近還是離 offset 近，取近的那個解釋；同時把 prompt 裡那句誘因拿掉、改為明確要求從 00:00 起算。08-08 的「`Speaker 7` 連續 20 分鐘」就是這個造成的假象——**一個 bug 會在別的檢查裡偽裝成另一種 bug**。
- **修正 FT 的入口網址**（第 1 節）。`ft.com/unhedged` 是 Unhedged **電子報存檔、完全不含 podcast 集數**，過去一直寫錯；正確入口是 `ft.com/the-economics-show`。08-08 是 FT 路徑第一次在無人值守排程中跑完並成功（45,181 字元、無付費牆）。同時記下 **Unhedged 的 feed 會放姊妹節目的重播**，遇到要標明原始播出日。
- 補 `hardfork` CSS，`index.html` 現為 20 組；**補色時要檢查撞色**——第一版讓它用到 `markethuddle` 的橘紅。現役 22 檔中仍缺 `bg2`／`acquired`／`lennys` 三組。
- **`speakerNotes` 上線第一天就發揮作用**：10 集有 8 集報警，其中 Pivot 的標籤在中段廣告後整體對調，照標籤直接引用會把 Kara Swisher 的話掛到希拉蕊．柯林頓頭上。**這正是這個欄位存在的理由**，也再次確認機械檢查只能提示可疑、判斷仍要靠讀內容。
- **回歸檢查抓到八處，含我這次改動製造的三個瑕疵**（子代理獨立比對）：
  - **時間戳偵測的判定線太鬆**。原本用「離 off 比離 0 近」，判定線落在 `off/2`——第 2 段 `off=1200` 時門檻只有 **10:00**，只要段首有十分鐘廣告或音樂，整段就會被誤判成絕對時間而跳過位移，時間軸倒退 20 分鐘。改為要求**貼近** off（`ABS_TS_TOLERANCE = 180` 秒）。
  - **跳過位移的分支直接 `return text`，不再正規化格式**。模型若寫 `[105:30]` 這種三位數分鐘，下游 `SPEAKER_LINE` 匹配不到，會誤報成「裸時間戳」。改為把 offset 歸零後照樣走一次格式化。
  - **`collapse_loops()` 把跳針從段級語速也扣掉了，等於默默關掉 `HIGH_RATIO` 對單 token 跳針的偵測**。新增 `loop_segs` 記錄哪幾段跳針，並寫進警告文字——**位置資訊不能因為換了偵測方式就丟掉**。
  - 順帶修掉：`transcribe_one()` 殘留未使用的 `start_s`／`end_s` 參數與死掉的 `hhmm()`；`episode_budget_seconds` 補進 `healthcheck.py` 的 `CONFIG_KEYS`（現檢查 10 個）。
  - **`healthcheck.py` 的 `resolve()` 改為先找掛載點、再找家目錄。** 在沙箱裡把 `podfetch.py` 匯入測試時，它的 `log()` 會在沙箱家目錄建出一個空的 `~/.podfetch/logs/`，原本的順序會抓到那個假目錄，然後報出一整排假故障（讀不到 `shows.json`、執行時刻變成測試當下的時間）。**驗證工具本身被測試行為污染，這是最難察覺的一種假警報。**
  - 文件面：第 3 節仍寫「四種訊號寫進 `warnings`」（實際是五種、且 08-07 已拆到 `speaker_notes`）；跳針字數 28,127／9,527 應為 **28,122／9,521**（第 3 節自己要求「數字一律不四捨五入」）；「08-07 的 Speaker 7」應為 08-08；第 2 節的 front matter 欄位清單漏了 `showKey`／`segments`／`words_per_minute`；**跳針剔除只寫在變更紀錄、沒進第 2 節規格本體**（執行者照 SKILL.md 只會讀第 2 節）；`MAINTENANCE.md` 的 CSS 組數與健康檢查清單未跟上；**「FT 從未在無人值守實測過」這句過期敘述同時存在於四個地方**，今天才真正跑通。

### 2026-08-07

- **`healthcheck.py` 新增「線上狀態」檢查**。08-07 網站停在前一天，而推送鏈檢查報 PASS——**因為它只比對 `local == origin`，而完整的鏈是「寫檔 → commit → push → Actions → Pages」**。前三段對不代表最後一段有跑。新檢查帶 cache-buster 抓線上 `index.json`，比對 `updatedLabel` 與 `days[0].date`；連不到網路時顯示 WARN 並提示到 Mac 上跑，不會誤報綠燈。**這是 08-03 auto-push 事故的同一個教訓往下游挪了一格。**
- **轉錄 prompt 改為保守標記講者**：預設 `Speaker N`，**只有拿到明確依據（自我介紹、被直呼、主持人點名）才寫真名**；廣告與台呼標 `Announcer`，不再歸給主持人；同一段內標籤不得中途改名或重新編號；並明講「錯的名字遠比沒有名字糟——它是對真實人物的不實陳述，而且事後極難發現」。
- **新增 `check_speaker_labels()`**，把四種可疑訊號寫進 manifest `warnings`：標籤格式異常（把台詞或廣告文案當人名）、全集只有一個講者、泛稱佔比 ≥40% 卻混著真名、單一講者連續獨佔 ≥15 分鐘。**用 08-07 的九集實測校準過**：初版對 9 集報 8 集（等於沒有訊號，因為「名字不在主持人名單裡」對每一位來賓都會響），改用「格式異常」而非「不在名單」後降到 5 集，每一則都對應實際發生的問題。
  - **明確的能力邊界**：抓不到「合理人名張冠李戴」。08-07 的 All-In 把開頭 13 分鐘的 Jason Calacanis 整段標成 Chamath Palihapitiya——標籤本身完全合理，只是錯的。這一類只能靠讀內容時的上下文校正。
- **修正兩個官方稿來源的取法**（第 1 節）：GS Exchanges 的 slug **不是節目標題的直譯**，拼錯會回空頁而不是 404、看起來像「沒有官方稿」；MacroVoices 官方稿**通常落後一週以上**，當集退回 podfetch 是正常的，不是失效。
- **額度表改為「10–20，會浮動」**。08-05 從 20 掉到 10、08-07 又回到 20。**Lite 優先的理由是「500 遠大於 10–20」這個量級差，不是某個特定數字**，不要拿浮動的值去做設計判斷。
- **字數規格補上來源密度的例外**：官方逐字稿沒有廣告與口頭贅語，同片長的實質內容多三到五成，超出層級上限一成左右是合理的——**不要為了壓進區間而砍掉整段論點**。
- 補 `gsx`／`nopriors` 兩組 CSS，`index.html` 現為 19 組；現役 22 檔中仍缺 `bg2`／`hardfork`／`acquired`／`lennys` 四組。
- **回歸檢查抓到我這次改動的三個瑕疵，一併修掉**（子代理獨立比對）：
  - **講者訊號原本會直接把 `status` 打成 `DEGRADED`**，因為它和完整度訊號共用同一個 `warnings` list。實測 08-07 九集會有五集誤降，而**唯一真正標錯人的 All-In 反而零訊號**——降級的分佈與實際品質相反，而且 `DEGRADED` 的定義是「完整度不足」，寫進網站 `source` 的降級理由會是假的。已拆成獨立的 `speaker_notes`／`speakerNotes` 欄位，不影響 `status`。
  - **`SPEAKER_LINE` 正則只認 `[MM:SS]`**，而合併後超過一小時的行是 `[H:MM:SS]`——一小時以上的節目後半整段不進統計。已改為兩種都吃。
  - **泛稱分類用行首錨定**，`*Speaker 3` 會被歸進「真名」，同一個標籤在不同訊號裡身分互相矛盾。已改為子字串比對。
  - 順帶新增第五個訊號「**裸時間戳**」：有時間戳但整行沒有講者標記。08-07 的 MiB 有 **71%（167／234 行）**是這樣，而完整度 1.00、狀態 OK——正是使用者回報的「1:00–1:19 整段無標記」，先前完全看不出來。
- 同步 `README.md`（原本宣稱「逐字稿標的是真實姓名而非講者 A」，與保守標記直接矛盾）、`MAINTENANCE.md` 的 CSS 組數與健康檢查項目清單、`config.json` 的額度註解。

### 2026-08-06（第二次，回歸複驗）

- **修掉一個會寫進逐字稿的錯數字**：`WORDS_PER_MIN` 早已改為 **165**，但字數不足的警告字串仍寫死「130 字/分基準」——**那句會進 front matter 與 manifest**，等於用 165 計算卻對使用者報 130。已改為引用常數本身。第 2 節與程式碼註解也一併更正。**`healthcheck.py` 抓不到這一類**：`WORDS_PER_MIN` 是程式常數、不在 `config.json` 裡，「brief vs config」涵蓋不到。
- **輪換上限補上時間維度**（`episode_budget_seconds: 1200`）。昨天只加了次數上限，但**每次輪換都可能等一輪 300 秒冷卻，18 次就超過一小時**，而 podfetch 01:00 到日報 03:00 只有兩小時窗口。20 分鐘的話五集全撞上也只花 100 分鐘。
- **修正規格與程式碼不符**：昨天寫「撞到就讓該段失敗」，實際上 `RuntimeError` 會穿出分段迴圈使**整集** `FAILED`。已據實改寫，並說明為何可接受（快取保留、`last_run_utc` 不推進、下次接續）。
- **拆開計數器**：`rotations` 原本同時計過載輪換與額度輪換，卻只有過載分支檢查上限。改為 `overload_rotations` 只計過載，額度輪換天然受池子大小限制。
- **`ModelOverloaded` 的 docstring 還留著被推翻的舊語意**（「走與 404／日額度相同的輪換路徑」），註解與實作互相矛盾，已改寫並加上「不要再併回去」的警語。
- **`MAINTENANCE.md` 第 6 節清掉兩項已完成的待辦**：plist 與 `pmset` 已連續三天（08-04／05／06）驗證通過，卻還掛在待辦且措辭是未來式。FT 那條也更新為反映封鎖清單。
- 順手修掉變更紀錄的四處小錯：根因日期記成 08-05（應為 08-04）、「八處」底下只有七條、分層表邊界在紀錄裡還是舊的重疊寫法、「本節設定清單」實指第 2 節。

### 2026-08-06

- **修掉 08-04 我自己引進的 bug：503 不再讓模型永久除名。**（`ModelOverloaded` 是 08-04 加的，當時錯在把它接進 404／日額度那個 handler；08-05 的改動是 `prefer_lite` 與額度表，與此無關。） 新增 `OVERLOADED` 冷卻表（`config.json` 的 `overload_cooldown_seconds: 300`），500／503 改為冷卻 5 分鐘後自動回池；`EXHAUSTED` 回歸原設計，只放日額度用盡與 404。**08-06 的實況是：三個 500 RPD 的 Lite 在 77 秒內被一次過載尖峰全數永久除名，剩下 10 RPD 的 Flash 扛完 13 段裡的 10 段，把 3.6 Flash 燒到 22/10。** `prefer_lite` 本身生效了，是冷卻語意錯誤把它的效果整個抵銷掉。經過見 `MAINTENANCE.md` 第 7 節。
- **摘譯字數改為依節目長度分層**（第 3 節）：未滿 15 分 1,200–1,800／15–30 分 2,000–3,000／30–55 分 2,500–4,000／55 分以上或 10 個以上獨立主題 4,000–6,500（邊界含下界、不含上界）。**原本一律 2,000–3,000 每天都被違反**——57–65 分鐘的節目壓到 3,000 字必然砍掉整段內容，執行者只能超規格後在回報裡解釋。**天天被違反的規格不是規格。** 上限與「不要灌水」的紀律仍在，超出所屬層級才需要說明。
- **`ft.com` 已進入本環境 `web_fetch` 的封鎖清單**（HTTP 403 `URL is on blocklist`）。第 2 節退援第 3 層改為明寫「只能走 Chrome」，並標註**這條路徑從未在無人值守的排程中實測過**，卡住就退回 podfetch 那層並在回報中明講，不要空等。
- **回歸檢查抓到的其餘七處一併修掉**（子代理獨立比對）：
  - **第 2 節「四個不要改的設計」第 4 項還寫著舊語意**（「走與 404／日額度相同的輪換路徑」）——那正是這次事故的根因寫法。**規格與同一份檔案裡的變更紀錄互相牴觸，而且規格那半段跟程式碼不符**，已改寫。
  - **第 0 節仍寫死「約 2,000–3,000 字」**，與第 3 節的分層表直接打架，而且它在整份文件最前面、執行者會先撞到被廢掉的那條。`README.md` 同一句也一併改。
  - **我這次的改動製造了一個新問題**：503 改成「只冷卻不除名」後，`while True` 沒有輪換上限，供應端持續過載時本段會在池內無限輪轉，**唯一的自然終點是把當天所有額度燒到 429**。已加上 `len(pool) * 3` 的上限，撞到就讓該段失敗。
  - 日誌的「改用 X」原本只排除 `EXHAUSTED`、沒排除冷卻中的模型，會指錯下一個模型——**08-06 的根因正是靠這串日誌反推出來的**，誤導會直接影響下次排查。
  - `overload_cooldown_seconds` 補進第 2 節的設定清單與 `healthcheck.py` 的 `CONFIG_KEYS`。
  - 分層表的邊界值重疊（15／30／55 各有兩個答案），已改為「含下界、不含上界」。
  - `MAINTENANCE.md` 第 7 節 08-04 那篇的修法已被推翻卻沒有時效標註，已比照 08-02 額度表加註。
- **新增「講者標記誤植」的寫作紀律**（第 3 節）。08-06 實例：完整度 1.01、狀態 OK，但 Gemini 從中段起把主持人標成片頭廣告旁白者，而開場已說明該主持人請假。**這是字數檢查抓不到的失敗態**——`shows.json` 的主持人名單只是提示、不保證歸屬正確。金句與立場歸屬要交叉驗證，判定不了就寫節目名。SKILL.md 的主動回報清單也補上這一項（現為十項）。

### 2026-08-05

- **Google 把新版 Flash 的免費層 RPD 從 20 砍到 10**（3.5／3.6；2.5 Flash 仍是 20，Flash-Lite 3.1／3.5 仍是 500）。第 2 節額度表已更新。
- **模型池順序反轉為 Lite 優先**（`config.json` 新增 `prefer_lite: true`，`podfetch.py` 的 `build_model_pool` 依此決定 `take()` 順序）。**注意：光改 `model_preference` 沒有用**——舊版一律先 `take(heavy)` 再 `take(lite)`，池子順序與偏好清單無關，必須改程式。08-05 實測預估 17 個請求實際打出 42 次（重試放大約 2.5 倍），Flash 兩個模型雙雙超限而 Lite 只用 8 次；Lite 完整度 1.14–1.16 與 Flash 無可辨識差距。
- **補兩個會安靜失敗的官方稿陷阱**（第 1 節）：Substack `/api/v1/archive` 回過期快取（Dwarkesh、Latent Space 分別停在 6/08 與 7/08，要帶 cache-buster）；`web_fetch` 約 104,700 字元上限會截斷長逐字稿且不報錯（Latent Space 只取到 73%，末段以 podfetch 補齊）。**兩者失效的樣子都是「今天沒有官方稿」，於是安靜退回機器轉錄。**
- 補 `dwarkesh`／`iltb`／`pivot` 三組 CSS，`index.html` 現為 17 組。
- **還原被覆寫的排程 SKILL.md 四項改動**（08-03 第五次巡檢加的：iTunes 子代理的 US 商店快取規則、第 7 節合規入口、回報清單的「寫不進 repo」、三段排查第 3 格）。**08-04 我用 `update_scheduled_task` 送出完整 prompt 時把它們蓋掉了**——`prompt` 是整份取代不是局部編輯，而我以為自己就是上一個改它的人。經過與教訓見 `MAINTENANCE.md` 第 7 節，維護 skill 已加入對應的檢查步驟。
- **修掉子代理比對抓到的其餘不一致**：`config.json` 的 `_comment_quota` 仍寫舊的 20 RPD（與同檔 `_comment_pool` 打架）；`MAINTENANCE.md` 第 7 節 08-02 那篇的額度表沒標明是當時值、讀起來像現行規格；MAINTENANCE 多處仍寫 20 檔／9 組 CSS；「三個不要改的設計」下面有四項、「三個已知陷阱」下面有五條；本節變更紀錄未由新到舊排序；Masters in Business 被誤接在 Compound 的說明結尾而不在 B 類清單裡。

### 2026-08-04（首次實跑後的修正）

- **`pmset` 那套設定驗證成功**：08-04 的 podfetch 日誌開頭是 `[01:00:00]`，準時執行。08-03 的延後補跑問題確認解決。
- **修掉一個會靜默吃掉集數的去重缺陷（本次最重要）。** 08-04 有 3 集因 Gemini HTTP 503 轉錄失敗，日報照規則寫成 `⚠︎ 全文摘譯待補` 的佔位。但 podfetch 失敗時不推進 `last_run_utc`，下次執行必定重抓同一集——**而去重規則會比對 `url`／`title` 把它當成「已收錄」而略過，補回來的完整逐字稿因此永遠進不了網站，且毫無徵兆**。這是設計本身保證會發生的組合，不是偶發。已在第 1 節加入例外：`source` 以 `⚠︎` 開頭的集數不算已收錄，拿到全文要重寫並跨檔搬移。同時明訂 **`⚠︎` 是機器讀的標記，不可改寫成別的格式**。
- **`healthcheck.py` 兩處誤報，都是這次實跑才暴露的**：(1) 缺 CSS 原本判 FAIL，但 brief 明訂那只是視覺不一致、功能正常，改為 WARN；(2) showKey 命名檢查原本拿全部歷史資料比對 `shows.json`，於是移除節目後會產生**永久性假警報**（歷史檔案裡的 `capitalallocators` 永遠都在），改為只檢查最新一份，並把僅存在於歷史的鍵值單獨列為預期狀態。
- **新增「待補全文」檢查**：列出最新一份裡 `⚠︎` 開頭的集數，提醒下次要重寫。
- 補上 `oddlots` 與 `capitalallocators` 的 CSS（前者是現役節目卻一直沒定義，08-04 第一次出現；後者為了讓歷史資料正常顯示），`index.html` 現為 14 組。
- **`podfetch.py` 新增 `ModelOverloaded`：500／503 重試兩次就換模型。** 從 AI Studio Console 的當日數據反推才看清根因——兩個 Flash 的 RPD 都爆到 21/20，而 Flash-Lite 只用了 9/500 與 1/500，503 才是主要錯誤（約 22 次）。**舊版只有 429 日額度會觸發輪換，503 則在同一模型上把 5 次重試燒完再判定整集失敗**；由於重試也計入 RPD，等於一邊燒掉 Flash 額度、一邊放著 491 次 Lite 額度不用。順帶確認 TPM 只用到 12–15%，brief 原本「瓶頸是 RPD 不是 TPM」的判斷成立。
- 08-04 那兩集 Capital Allocators 佔位已從 `data/2026-08-04.json` 移除（空殼內容，且節目本次下架不會再補），該日 6 集改為 4 集，`index.json` 同步。

### 2026-08-03（第五次，同步巡檢）

- **撤銷「podfetch 延後補跑」這個根因判定，`MAINTENANCE.md` 該篇事故紀錄已刪除。** 該篇宣稱 08-03 日誌是 `[07:00:01]` 的原因是機器睡眠、且「plist 一直都是 `Hour = 1`」。實情相反：**當天 plist 就是 `Hour = 7`**（那也是重整前本檔第 6 節記載的設計值），是當日稍晚才由使用者改成 1；調查者讀到的 `Hour = 1` 是修改後的狀態，卻被當成修改前的證據。`[07:00:01]` 這種「整點加一秒」正是 `StartCalendarInterval` 準時觸發的特徵，不是喚醒補跑。**`pmset` 那組電源設定獨立保留**——01:00 的工作在會睡眠的 Mac 上確實需要它，那部分的決策沒有問題（第 6 節、`MAINTENANCE.md` 第 9 節）。
- **時間戳排查順序改為 plist 優先**（原為 `pmset -g custom` → `pmset -g sched` → plist）。plist 最便宜也最確定，且「整點加一秒」的特徵直接指向設定值。同時加上一條通則：**先查到的那一項若已經被人改過，很容易把「修改後的狀態」誤讀成「修改前的證據」**，所以三項都要看完。
- **補上排程 SKILL.md 給 iTunes 子代理的指示裡缺的「US 商店快取」規則。** 子代理讀不到本檔，而該規則只寫在本檔第 1 節，實測會導致退援路徑漏抓 All-In（US 商店回報最新只到 7/18，GB 商店為 7/31）。同時把「怎麼換國別商店」寫成具體網址格式，不再只說「改用 GB／AU」。
- **SKILL.md 補上指向第 7 節「合規與語氣」的入口**。第三次重整時漏了，導致來源標註、付費來源只做重點濃縮、非投資建議這些會影響交付內容的規則沒有任何入口。
- **修掉四處內部矛盾**：窗口接縫敘述與 `last_run_utc` 自癒機制對撞（已釐清為「只有走 iTunes 退援路徑時 26 小時才是硬邊界」）、檔名慣例的舉例仍停留在 09:00 時代、Unhedged 同時被放在退援第 1 層與第 3 層（已說明 FT 流程就是它取得官方稿的方式）、Masters in Business 在 A 類表格說「新集數改走 B」但 B 類清單裡沒有它。
- **釐清 `limit=8` 與「limit 越小快取越舊」的拉扯**：`limit=8` 是起手式，發現回傳過舊就換國別商店重查。
- **SKILL.md 的主動回報清單由七項增為八項**，補上「寫不進 repo `data/`」——該情境的處置本來就寫在本檔第 5 節，但沒進封閉列舉的回報清單，等於會安靜地不被講出來。
- **補上時刻漂移守衛的死角**：守衛原本掛在「今天的日誌」上，但若 podfetch 的時刻漂到 03:00 之後，日報執行當下今天的日誌必然不存在，會直接落進「podfetch 失效」分支，守衛永遠不會觸發。已在三段排查加入第 3 格：今天的日誌不存在時，**先讀 `logs/` 裡最新那一份的開頭時間戳**來區分「時刻漂移」與「真失效」，兩者的修法完全不同。

### 2026-08-03（第四次，節目異動）

- **移除 Capital Allocators，加入 Business Breakdowns（1559120677）、In Good Company（1614211565）、The Compound and Friends（1456467014）。節目數 20 → 22。** Capital Allocators 本身完全正常（812 集，當天還發了新集），移除是取捨不是汰換：它談的是機構配置者與 LP-GP 關係，在這份清單裡最不直接可用。
- **補上的是一個真正的缺口：個股／公司基本面拆解。** 原本 20 檔裡沒有任何一檔在做這件事——ILTB 是投資人訪談，不是公司拆解。Business Breakdowns 每集拆一家公司的商業模式、單位經濟、護城河、估值與風險，來賓多是實際持有部位的 buy-side。
- 三檔都已加進 `shows.json`、`config.json` 的 `show_priority`，`index.html` 也補了三組 CSS（現為 12 組）。
- **驗證候選時踩到 brief 早就警告過的坑，值得記一筆**：Business Breakdowns 的節目層 `releaseDate` 顯示 2026-05-29，看起來停更兩個月；拉集數層才發現最新是 07-27。**節目層那個日期就是第 1 節說的「舊資料，不要誤判」**，差點因此把一檔活著的節目判死。挑選節目時一律要看 `entity=podcastEpisode` 的結果。
- **另外發現 iTunes 的 `search` 端點透過 `web_fetch` 回空字串，`lookup` 正常。** 找新節目的 AppleID 不能靠 `search`，要用 WebSearch 或直接從 Apple Podcasts 網址取 id。已補進第 1 節的已知陷阱。

### 2026-08-03（第三次，結構重整）

- **本檔與 `MAINTENANCE.md` 重新分工**：brief 只留規格與判斷規則，事故經過／誤判過程／被否決的選項全部搬到 `MAINTENANCE.md` 第 7 節「事故與決策檔案」，YouTube 操作要領搬到第 8 節。**動機是本檔已膨脹到 42 KB 而排程每天要完整讀一次**，其中很大一部分是執行者根本不需要知道的歷史；而且同一件事在兩處各寫一份，反而讓「哪個是現行規格」變得不清楚。
- **排程 SKILL.md 改為流程骨架**：只留執行順序與分支判斷，事實細節指向本檔章節，不再重抄。**這是為了根除「漏抄型」同步 bug**——08-03 第二次巡檢抓到的四處缺漏全部屬於這一類，只要兩份文件都在描述同一批事實，這種 bug 就會無限重演。保留了「讀不到 brief 時仍能自我修復連線」的最小集合，不會因為瘦身而失去韌性。
- **新增 `~/.podfetch/healthcheck.py`**：把每次維護都要手動重跑的機械式檢查集中成一支唯讀腳本（JSON 可解析與排序、`days` 與檔案雙向對應、`episodeCount` 相符、showKey 三處 CSS 與 `shows.json` 對齊、podfetch 日誌時間戳與收束狀態、逐字稿降級統計、推送鏈、**brief 引用的 `config.json` 數值是否與實際一致**）。最後一項專門抓 08-03 那類「下文改了、上文漏改」的內部矛盾。
- **開啟排程的 `notifyOnCompletion`**：0 集日不動 `index.json`、不產 Word 報告，網站毫無變化，先前完全無法區分「今天沒東西」與「排程根本沒跑」。這是這套系統反覆中招的靜默失效模式，補上通知作為最低限度的心跳。
- **修正去重的措辭**：原寫「前一天的 json」，但 0 集日不產檔，昨天的檔案可能不存在。改為「`data/` 裡日期最大的那一份」。
- **重構後的回歸檢查抓到九處問題，一併修掉**（子代理獨立比對）：
  - **給 iTunes 子代理的指示缺了完成任務所需的定義。** SKILL.md 把「窗口判定」外包給子代理，但子代理讀不到 brief，而 26 小時窗口、`releaseDate` 是 UTC、台北 ＝ UTC+8、Bloomberg 要用 `limit=20` 全都只寫在 brief 裡。**瘦身的代價就在這裡：指向章節對主代理有效，對子代理無效**，凡是外包出去的步驟，定義必須隨指示一起傳。
  - 「窗口邊緣又拿不到全文的集數不收錄」原本沒進 SKILL.md 的主動回報清單（那是封閉列舉的六項），被排除的集數不會出現在 manifest 狀態裡，等於靜默掉一集。已補為第 4 項。
  - **本檔與 `MAINTENANCE.md` 對「日誌時間戳不對」的第一步互相牴觸**：brief 寫「不要建議去改 plist」，MAINTENANCE 寫「兩種成因都要驗、先驗 plist」。前者把一次性的查證結果固化成永久前提，是錯的——已改為三項依序全查（順序於第四次修訂再調整為 plist 優先）。
  - **`pmset -c sleep 0` 這個主要保障原本完全不在 brief**，只有後備的 `repeat wakeorpoweron`。實際需要的是 01:00 到中午的連續清醒，只靠排定喚醒會在閒置後睡回去、03:00 接不到。同時刪掉「唯一一個不在設定檔裡的依賴」這句——蓋子、登入項目、充電管理軟體等前提同樣不在設定檔裡。
  - `~/.podfetch/cache/` 補進元件表（額度中斷時的續跑機制，排錯時看不到會困惑）。
  - `config.json` 的 `show_priority` 與 `model_preference` 補進說明，並註明 `healthcheck.py` 只檢查數值型、這兩個清單型的檢查不到。
  - `MAINTENANCE.md` 第 7 節一處章節引用指錯（「第 5 節」實指 brief 第 5 節），以及 07:30／07:35 的不一致（cron 設 07:30，帶 jitter 實際約 07:35）。
  - **`README.md` 整份過期且是 Public repo 的門面**：仍寫「每個平日早上 08:30」「其餘節目讀 YouTube 官方頻道字幕」，檔案結構也沒有 `MAINTENANCE.md`。已改寫為現行實況。brief 第 4 節的目錄結構圖原本也沒列 `README.md`，一併補上——**兩份文件的檔案清單互不涵蓋對方，是這次才發現的盲點**。

### 2026-08-03（第二次，維護巡檢）

- **修正 podfetch 與日報的時序倒置**：podfetch 實際跑在 07:00 而非 01:00，日報 cron 改 03:00 後會每天搶先執行、誤判成「podfetch 失效」而掉進已死的 YouTube 退援。修法是把 plist 的 `StartCalendarInterval` 從 `Hour = 7` 改為 `Hour = 1`。
- **第 2 節加入「時刻漂移」守衛**：排查時要順手確認日誌開頭時間戳是 `[01:`，不是就在當日回報中明講。
- **`pmset` 電源設定登記為基礎設施依賴**（第 6 節）。它與 plist 時刻是兩件獨立的事：plist 決定「幾點跑」，`pmset` 決定「那個時刻機器醒不醒著」，兩者都要對。
- **補齊 `index.html` 四組節目徽章**（`latentspace`／`lex`／`mib`／`twentyvc`），這四個 showKey 早已出現在資料裡卻一直走預設藍。同時要求 showKey 一律沿用 `shows.json` 的鍵值。
- **統一 YouTube 嘗試上限為 5 分鐘**（當時的 YouTube 操作章節寫 8 分鐘，與其他三處牴觸；該章節已於第三次重整搬到 `MAINTENANCE.md` 第 8 節）。
- **修掉本檔內部自打架的過期數值**：20 分鐘段／Files API（原寫 25 分鐘／base64）、`max_chunk_mb: 48`（原寫 9 MiB）、每日請求數（該數值已不列在本檔，改由 `config.json` 註解與 `MAINTENANCE.md` 第 7 節記載）、「每天（含週末）」（原寫「每個平日」）、26 小時（原殘留「26／74 小時」）。
- **補上排程 SKILL.md 缺的四條規則**：A 類節目仍須抓官方稿（原本的「不要開 Chrome」會讓官方稿永遠拿不到）、podfetch 主路徑同樣要去重、寫不進 repo 時的退援、YouTube 登入前提。

### 2026-08-03（第一次）

- **執行時段從 09:00 改到 03:00**，確立「前一晚美東晚間集數順延一版」的取捨。
- **資料夾連線不保證跨工作階段留存**：排程執行中讀不到就用 `request_cowork_directory` 自我修復。
- **確立「當天沒有目錄 ≠ podfetch 掛了」的三段排查。**
- **iTunes lookup 已知陷阱**（US 商店快取、RSS 回 `[binary data]`）。
- **驗證上線必須帶 cache-buster**，且要確認 `updatedLabel`。

### 2026-08-02

- **改以本機 podfetch 管線為主要全文來源**（第 2 節）：01:00 抓音檔經 Gemini API 轉錄，產出帶講者姓名的逐字稿。跨節目交叉觀察因此可以具體到人。
- **YouTube 字幕全面失效**，降為最後手段。

---

## 12. 版本登記簿（速查表）

第 11 節是敘事體，回答「為什麼」；這一節是速查表，回答「**動了哪些檔案、能還原到哪裡、當時想解決什麼、後來成立嗎**」。找歷史先看這裡定位，再回第 11 節讀細節。

### 回溯機制（2026-08-08 建立）

**repo 內的 `AGENT_BRIEF.md`／`MAINTENANCE.md`／`README.md`／`index.html`／`data/` 有 git。** 但改動最頻繁的檔案全在 repo 外、**在 2026-08-08 之前完全沒有版本紀錄**——`podfetch.py` 這一週至少改了 8 次，排程 `SKILL.md` 至少 9 次而且**被整份覆寫過一次**（08-04），當時無法還原、只能靠子代理事後比對發現。

```bash
bash ~/.podfetch/snapshot.sh "開工前"      # 每次維護開工與收工各存一份
bash ~/.podfetch/snapshot.sh --list        # 列出所有快照
bash ~/.podfetch/snapshot.sh --diff A B    # 比對兩份
cp ~/.podfetch/snapshots/<時間戳>/podfetch.py ~/.podfetch/   # 還原
```

存 `podfetch.py`／`healthcheck.py`／`json2docx.py`／`snapshot.sh`／`fix-schedule.sh`／`config.json`／`shows.json`／`SKILL.md`，附 `MANIFEST.txt` 記 SHA 與大小。**`gemini.key` 與 `state.json` 刻意排除**（金鑰不入快照；執行狀態不是設定，存它只是雜訊）。

> **在 Cowork 沙箱裡跑這支腳本會缺 `SKILL.md`**（沙箱看不到 `~/Documents/`），要在 Mac 的終端機跑才完整。`snapshots/2026-08-08-1852/` 就是這樣產生的不完整快照，內附說明檔。

### 指標基線（`~/.podfetch/metrics.csv`，每次跑 healthcheck 自動追加）

**動機**：08-08 做了一次大幅 token 優化，卻只能「估算」降幅，因為從來沒量測過任何一天。**沒有基線，任何優化都只能自稱有效。** 同一天重跑會覆寫該日那列。

欄位（15 欄，2026-08-09 由 12 擴為 15）：`date`／`episodes`／`ok`／`degraded`／`failed`／`transcript_kb`／`output_json_kb`／`brief_kb`／`skill_kb`／`segments_done`（08-10 前叫 `podfetch_requests`，但它數的是完成段數不是 API 請求數——實測請求是段數的 2.5 倍，舊名會讓人拿它估 RPD 而低估）／`podfetch_minutes`／`speaker_flags`／**`eff_tokens_k`／`subagents`／`agent_turns`**

**擴欄時舊列會少欄。** 表頭改成 15 欄而歷史列還是 12 欄，整份檔案就無法按欄名解析——而這份檔案存在的唯一目的就是當可解析的基線。`healthcheck.py` 現在每次重寫時會把短列補空欄到與表頭等長（2026-08-09 由子代理抓到）。

> **08-15 那一列不可與其他日子直接比較（2026-08-16 註記）。** 它記的是 `31891/15/186`——每集 319 萬，是 08-14（28.3 萬）的十倍。**那不是成本回頭惡化，是事故當天的維護成本混進了產出成本**：那天要做假零集判定、外部對照、`state.json` 回捲、重跑 podfetch，再補完 10 集的日報。**15 個子代理裡只有 10 個是日報本業**（3,492K／68 回合），另外 5 個是集數盤點與文件稽核的一次性成本（5,166K），**比整個日報摘譯還貴**。
> **可比的數字是摘譯部分的每集 349K**（3,492K ÷ 10 集），與 08-14 的 283K 同一量級——**段數規劃那條紀律在事故日仍然有效**（10 集零重寫、子代理平均 2.4 次呼叫）。畫成本趨勢時用這個數，有效序列是 08-09 194 萬 → 08-11 57.6 萬 → 08-12 54.9 萬 → 08-14 28.3 萬 → 08-15 34.9 萬（摘譯部分）。
> **`eff_tokens_k` 於 08-16 由 28425 改為 31891。** 同一場工作階段量了兩次，中間只隔 14 個回合就多出 3,466K（+12%）——**那 14 回合的輸出只佔新增成本的 6%，其餘 94% 是重讀對話本身**。填表一律用**最後一次量測**，並記住這一欄的語意是「整場工作階段」，不是「日報產出」。

> **成本模型：驅動成本的是「每回合帶進多少新內容」，不是回合序號（2026-08-16 訂正）。**
> **這一條在 24 小時內被推翻過一次，經過本身就是教訓。** 08-15 的用量分析從**一段 14 個回合**的觀察（平均 248K，是當時全場平均的 1.8 倍）推論出「成本隨回合數接近平方成長」。08-16 把樣本拉到 250 個回合重算，**結論不成立**——每 50 回合的平均是 46K → 173K → 45K → 78K → 142K → 66K，**尖峰狀，不是單調上升**。
>
> 把每回合拆成三種成分才看得懂：
>
> | 成分 | 加權 | 佔比 | 行為 |
> |---|---|---|---|
> | 快取寫 ×2 | 13,224K | **54.2%** | **尖峰狀**——新內容進場時才發生 |
> | 快取讀 ×0.1 | 7,089K | 29.0% | 穩定上升（每回合 15K → 48K，約 3 倍） |
> | 輸出 ×5 | 4,106K | 16.8% | 與寫作量相關 |
>
> - **對話變長確實會墊高地板**（context 從 164K 升到 486K），**但它被 ×0.1 壓住，只佔 29%。**
> - **主導的是快取寫（54.2%），由「這一回合有多少新內容進場」決定。** 兩個尖峰段分別對應「派發 15 個子代理」與「大量檔案讀寫」。
> - **空轉的回合其實很便宜**（101–150 段每回合只有 45K）。所以「回合數」是個好用的代理指標，只因為多數回合都會帶進新內容——**它不是機制本身**。
>
> **現行四條紀律全部仍然成立，但其中一條的理由要改**：「一件事做完就開新對話」不是因為平方成長，而是因為長對話墊高每回合地板（3 倍），且新任務會把舊任務的紀錄一起帶進快取。另外三條（逐字稿不進主對話、子代理呼叫預算、先估篇幅不要重寫）在新模型下**理由更強**——它們擋的正是最貴的那 54.2%。
>
> **方法論教訓：拿一段 14 回合的觀察去推「成長曲線的形狀」，樣本不足以支撐那個結論。** 要判斷曲線形狀，最少要看完整區間的分段平均；要判斷成本來源，要拆成分而不是看分組——**只看分組（回覆／子代理／bash）會完全看不出「尖峰狀」這個性質**。

已回填 08-02 起的歷史。**08-08 是舊架構的最後一天，也是新架構的對照基準**：10 集、逐字稿 617 KB、產出 JSON 229 KB、brief 45 KB、podfetch 29 個請求、470 分鐘音檔、8 集有講者訊號。08-09 起是子代理架構的第一筆，但 08-08 那列沒有 `eff_tokens_k`（欄位是隔天才加的），**所以新舊架構仍然沒有可直接相減的 token 基線**——只能從 08-09 起往後累積。

### 逐場登記

| 日期 | 主題 | 動到的檔案 | 想解決什麼 | 後來成立嗎 |
|---|---|---|---|---|
| 08-02 | 建 podfetch 管線 | `podfetch.py`（新）、`config.json`、`shows.json`、brief | YouTube 字幕全面失效，全文取得綁死在 Chrome＋登入＋沒改版三個條件上 | ✅ 沿用至今；副產品是講者姓名 |
| 08-03 ①② | 改 03:00、三段排查、cache-buster | brief、SKILL.md | 起床就看得到；與投顧線拉開 token | ✅ 但代價是美東晚間集數順延一版 |
| 08-03 ③ | **規格與事故史分家** | brief（42→32 KB）、`MAINTENANCE.md`、`healthcheck.py`（新） | brief 每天被完整讀一次，塞滿執行者用不到的歷史 | ⚠️ 方向對，但變更紀錄兩天內把成效吃回去，08-08 才用歸檔規則根治 |
| 08-03 ④ | 節目異動 22 檔 | brief、`shows.json`、`config.json`、`index.html` | 移除 Capital Allocators，補個股基本面拆解的缺口 | ✅ |
| 08-03 ⑤ | 同步巡檢 | brief、SKILL.md | **撤銷 08-03 的根因誤判**（plist 當時就是 `Hour = 7`） | ✅ 教訓：讀到的現況不等於事發時狀況 |
| 08-04 | 503 換模型 | `podfetch.py` | 503 死守重試把 Flash 額度燒光，3 集全失敗 | ❌ **修法本身造成 08-06 事故**——503 併進 `EXHAUSTED` 讓 Lite 被永久除名 |
| 08-05 | Lite 優先 | `podfetch.py`、`config.json`、brief | Flash RPD 被砍半，稀缺資源排最前面等於開場燒光 | ✅ 但當天被 08-04 的冷卻語意抵銷，08-06 才真正生效 |
| 08-06 ① | **503 改冷卻不除名** | `podfetch.py`、`config.json` | 一次過載尖峰 77 秒內把三個 500 RPD 的 Lite 全部永久除名 | ✅ 08-07 實測 Lite 承擔 82% 請求 |
| 08-06 ② | 回歸複驗 | `podfetch.py`、`healthcheck.py`、brief | 修法沒有輪換上限，最壞會把當天額度全燒光 | ✅ 加了次數＋時間雙上限 |
| 08-07 | 講者保守標記＋線上檢查 | `podfetch.py`、`healthcheck.py`、brief、SKILL.md | 五檔節目講者誤植；推送鏈 PASS 但網站沒更新 | ✅ 08-08 抓到 Pivot 標籤對調（會把主持人的話掛到前國務卿頭上） |
| 08-08 ① | 跳針與時間戳 | `podfetch.py`、brief | 完整度指標在跳針失效態下**反向**（3.36 實為 1.10） | ✅ 實測還原吻合人工評估 |
| 08-08 ② | **token 優化** | SKILL.md（重寫）、`json2docx.py`（新）、brief（68.8→45 KB） | 逐字稿在主 context 被重複計費數十次；同一份內容 LLM 寫兩次 | ✅ **架構成立，降幅已於 08-11 證實**。08-09 實測：逐字稿確實一個字都沒進主對話（檔案操作僅 4.8%），但**五個子代理吃掉全程 84%**——成本從「一份大 context 被重讀很多次」變成「五份小 context 各自被重讀很多次」。好處是改為線性成長，集數多時才顯優勢。**08-11 實測：同樣五集 970 萬 → 288 萬，降 70%，落在當初粗估的 60–75% 區間內。** 但降幅主要來自 08-09 加的呼叫預算，本列的架構隔離是必要不充分 |
| 08-08 ③ | 版本控制與基線 | `snapshot.sh`（新）、`healthcheck.py`、本節 | repo 外的檔案零版本紀錄；優化降幅只能估算 | ✅ 快照機制此後每場都用到（08-10 靠它確認只有 SKILL.md 有變）；基線於 08-11 首次派上用場 |
| 08-08 ④ | FT 入口「更正」（**2026-08-15 事後補登**） | brief、SKILL.md | `ft.com/unhedged` 是電子報存檔、不含 podcast 集數 | ❌ **改成了另一個同樣不含 Unhedged 的頁面**（`ft.com/the-economics-show` 只列姊妹節目自己的集數），08-15 實際去跑才發現，正解是站內搜尋＋選 `Transcript:` 頁。**當時完全沒登記在本表**，只在 brief 第 1 節留下「（2026-08-08 更正）」五個字——**沒進登記簿的改動，等於沒有人會回頭驗收它** |
| 08-09 ① | 呼叫預算、token 量測、語速校準 | SKILL.md（任務卡）、`healthcheck.py`、`podfetch.py`、`shows.json`、brief、本檔 | 子代理回合數失控（單集 70 次呼叫）；降幅無法量測；完整度基準過低使 1.2–1.3 變常態 | ✅ **呼叫預算 08-11 受測並證實有效**（與 08-08 ② 合計降 70%）；⚠️ 但 **token 量測根本沒生效**，路徑寫成 CLI 的 `~/.claude/projects`，連兩天空值，08-10 才修 |
| 08-09 ② | 回歸複驗 | brief、`podfetch.py`、`healthcheck.py`、本檔 | 上一輪自己寫錯的因果與三處歸檔／擴欄後遺症 | ⚠️ **又是修法本身留下問題**：把 wpm 校準說成「修好 DEGRADED」（程式上不成立）、擴欄後歷史列錯位、歸檔時把「兩份一組要同步」這條規則一起搬走 |
| 08-10 ① | 同源去重、下界例外、子代理門檻、退援層號、26 小時統一、token 量測出聲 | brief、SKILL.md、`healthcheck.py`、本檔 | 同一支音檔被兩個 feed 推送而佔滿當日；規格缺「低於下界」與「低量日」條款；FT 被誤列成獨立的退援層；26 小時與 `last_run_utc` 字面互斥；token 量測讀錯路徑 | ✅ 08-11 起未再出現同源重複、退援層號混用或低於下界未回報；**前三個缺口共同形態是「主代理靠自覺補上規格的缺口」**，見第 7 節 |
| 08-10 ② | **新增 TIP（22→23 檔）** | brief、`shows.json`、`config.json`、`index.html`、`README.md`、SKILL.md（description）、本檔 | 週一那一版涵蓋美東週末，現有節目在該窗口幾乎零供給 | ⚠️ 節目本身待驗（08-11 起看它是否真的落進窗口、語速未校準）；**流程面已知失誤兩處**：README 節目清單漏補、撞色只查了三處中的一處，兩者都不在 `healthcheck.py` 涵蓋範圍，已補進第 5 節步驟表 |
| 08-10 ③ | **程式碼審查＋知識庫新功能** | `podfetch.py`、`config.json`、`healthcheck.py`、`snapshot.sh`、`index.html`、`data/observations.json`（新）、brief、SKILL.md、本檔 | 單集時限實為無上界（2 小時集最壞 4 小時）；壞快取永久固化；探測每天燒 Flash 池 30%；config 壞損靜默用錯參數；知識庫沒有記憶（觀察點寫完就丟、來賓查不到、品質看不到） | ✅ 08-11 實測通過（273 分鐘集只跑 11 分 44 秒，遠低於 60 分上限；無誤殺）。**時限語意與快取門檻都是防護機制**，改前已按第 12 節慣例逐項寫下最壞情況 |
| 08-10 ④ | **維護 skill 本身改版** | podcast-maintain skill（經 `save_skill` 覆寫；**它不在 snapshot.sh 涵蓋範圍**，版本只存在 Cowork 帳號端）、本檔 | skill 裡的過期清單直接造成了 08-10 ② 的 README 漏補（「四處」實為八步）；「連續三次」寫死的次數已腐化；缺這兩天新增的七個同步比對點與三條驗證教訓 | ✅ 08-11 起連續多場依新版流程執行，未再出現「照過期清單漏步驟」。**設計原則改為「指向單一事實來源，不再複製清單」**——skill 指向第 5 節步驟表與第 12 節登記簿，而不是自己再抄一份 |
| 08-11 | **首次量測驗收＋四項修正** | brief、SKILL.md、`index.html`、`data/observations.json`、本檔 | 呼叫預算與字數上界互相打架（三集寫完再壓縮）；acquired 無 CSS 實際撞到；記分板種子含免責雜訊；自報呼叫次數低報；超長節目不取官方稿無規格依據 | ⚠️ **驗收那半成立、修正那半有一項被證偽**。成立：08-08＋08-09 的 token 優化實測降 70%（970 萬→288 萬）、集層級時限實測 11 分 44 秒遠低於 60 分上限。**證偽：「先估篇幅再落筆」隔天重寫比例一模一樣（3/5）**——根因是它依賴的字數量測本身不可靠，08-12 改為以段數規劃 |
| 08-12 | **量測面的根因修正** | brief、SKILL.md、`data/*.json`（13 檔重算）、本檔 | 「先估篇幅」連兩天失效（3/5 集重寫），根因是子代理自報字數的誤差範圍 −38%～+81%；中斷重派比原本還貴 | ✅ **08-14 驗收通過**：12 集零重寫（前兩天都是 3/5）、段數達標 11/12、每集成本減半。**這是第一次修的是「規則依賴的量測」而不是規則本身，也是第一次一次就成功** |
| 08-14 | **段數規劃驗收＋時間軸檢查** | `podfetch.py`、`healthcheck.py`、brief、SKILL.md、本檔 | 驗收 08-12 的段數改動；時間戳崩壞三天無人看見；metrics.csv 缺沒跑維護的日子 | ✅ **驗收通過**：12 集零重寫、每集成本減半（54.9→28.3 萬）。**時間軸檢查已於 08-15 首次實戰命中**——MIB 那集 39% 片長落在大間隙且完整度 0.73，雙訊號同時成立，子代理獨立確認確有兩處跳段，全日 10 集無誤報；metrics 回填亦正常 |
| 08-15 | **iTunes cache-buster＋文件同步** | `podfetch.py`、`state.json`（回捲）、brief、SKILL.md、本檔 | 主路徑安靜漏抓當天全部 10 集（假零集）；FT 入口二度寫錯；子代理比對出 12 處文件不同步 | ✅ cache-buster 那半當天修完即救回全部 10 集，**08-16 的排程執行再度確認**：23 檔輪詢耗時 **10 秒**（事故當天是 1 秒），且視窗起點 `08-14 23:43Z` 早於前一次終點 `08-15 00:13Z`、有 30 分鐘重疊無缺口——**手動回捲與隔天排程正確銜接**。**這是第一次「主路徑」而非退援路徑失效**，唯一外顯訊號只有執行時間 9 秒→1 秒。⏳ **FT 入口那半是第三個版本，尚未在無人值守排程中跑過**（`ft.com/search?q="<原文標題>"` → 取標題帶 `Transcript:` 前綴的頁面）——**前兩個版本都是「更正過但沒驗證過」，這個版本目前也還在同一個狀態**，下次 Unhedged 有新集數時要在回報中確認走通了沒 |
| 08-16 ① | **0 集兩道防線** | `podfetch.py`、`config.json`（`stale_feed_hours`）、`healthcheck.py`（`CONFIG_KEYS`）、brief、SKILL.md、本檔 | 08-15 事故的兩個獨立成因：`discover()` 回空仍無條件推進 `last_run_utc`（暫時性失效變永久缺口）；「0 集」與「查詢壞了」在日誌上完全無法區分 | ⏳ **待實戰**。離線模擬四情境全對（事故日 15.2 天觸發／真 0 集 0.6 天通過／長週末 1.9 天通過／查無集數觸發）。**動防護機制前照慣例先問最壞情況**：健全性對照零額外請求、只印警告不改流程；不推進 `last_run_utc` 的最壞代價是真 0 集日重複查一次。**兩者都不會在極端路徑放大傷害，這是刻意設計的** |
| 08-16 ② | **換掉兩檔低頻節目（仍 23 檔）** | brief、`shows.json`、`config.json`、`index.html`、`README.md`、本檔 | BG2 掉到約每季一集、Business Breakdowns 約每兩個月，brief 仍把兩者當常態節目（第 6 節 08-15 已列為觀察中） | ⏳ **待實戰**。移出 BG2／Business Breakdowns，補進 Prof G Markets（1744631325，週一至五每日）與 Money Stuff（1739582836，每週五）；**兩檔的節奏都用帶 `&cb=` 的 lookup 逐集核實過，不是憑印象**。**流程面新增一條通則：移出節目時 `index.html` 的 CSS 一律保留**（歷史集數還在站上，刪了會掉色），已寫進第 5 節與 brief 第 4 節。SKILL.md 的 `description` 因總數不變而未動——**這是第 5 節第 6 步第一次不需要動，要留意「不必動」與「忘了動」在事後看起來一樣** |
| 08-16 ③ | 重播規則一般化 | brief（第 4 節通則）、SKILL.md（第 2 步、任務卡、回報第 14 項） | 「feed 放重播」原本只寫在 Unhedged 那一格，而新加的 Money Stuff 會發 `Re-Run:` 前綴的重播 | ⏳ **待實戰**。**第一版只改了 SKILL.md、brief 端沒立通則，卻已在 brief 第 8 節宣稱「兩邊都改了」**，由複驗抓出後補上——見 `08-16 ④` |
| 08-16 ⑤ | **排程自我量測探測＋成本模型訂正** | SKILL.md（新增第 5.5 步、回報第 16 項、開頭成本段）、brief、`metrics.csv`（08-15 改 31891）、本檔 | ①`eff_tokens_k` 五次自動化失敗後結論成「排程不留 transcript」，**但五次都是從別場去找**，沒人驗證過排程能不能讀自己的；②08-15 從 14 個回合推論的「成本平方成長」被 250 回合的重算推翻 | ⏳ **待明天第一次回報**。這一步的設計重點是**兩種結果都有價值**：找得到就不必人工填，連續找不到才算真正結案。**已寫死兩個防呆**：數字是下限要標明、找不到不准換路徑猜第二次（08-14 五連猜的教訓）。成本模型部分已即時訂正三處文件。**✅ 同步狀態已於 2026-08-16 當晚結案**：稽核時 18:27 快照裡查無第 5.5 步，一度懷疑宣稱不實；使用者貼回正本後確認**三項改動全部都在**（第 5.5 步、回報第 16 項、成本模型訂正）。**問題出在快照時機不是規格**——「收工後」快照取於 18:27、⑤ 在那之後才寫，已以 `2026-08-16-233000` 補存。**產出面仍為 ⏳，等明天第一次回報第 16 項** |
| 08-16 ④ | 回歸複驗 | brief、SKILL.md、`healthcheck.py`、本檔 | 上面三列自己製造的新問題 | ⚠️ **又是修法本身留下問題，這次是第六次。** 6 處中 3 處源於今天：`last_run_utc` 的語意從「上次執行時刻」改成「下次視窗起點」之後，**brief、SKILL.md、`healthcheck.py` 共四個地方仍在用舊語意讀它**（最嚴重的是「假零集要回捲」與新行為在同一段落裡打架，照做會把視窗起點多推一格）；重播規則宣稱同步但只同步了一半；`capitalallocators` 走預設藍那句與新寫的 26 組矛盾。另 2 處是舊帳：`MAINTENANCE.md` 第 4 節的 CSS 組數還是 24。**教訓：改一個欄位的語意時，要搜的是所有「讀」它的地方，不是所有「寫」它的地方。** |
| 08-16 ⑥ | **`snapshot.sh` 掛載點偵測＋記分板機械把關** | `snapshot.sh`、`healthcheck.py`（`check_observations`）、brief 第 1 節、本檔第 4／6 節、SKILL.md 第 4 步（**待使用者貼上**） | ①`snapshot.sh` 在沙箱裡會**安靜產生空快照**，而它是 repo 外檔案的唯一還原點；②`observations.json` 每天要動卻零機械把關；③「同源推送 `meta`」規則存在但沒有執行者；④「退援路徑做不了同源去重」只寫在 SKILL.md | ⏳ **待實戰。** **動防護機制前照慣例先問最壞情況**：`snapshot.sh` 的守衛只在「一個檔都沒抄到」時觸發（Mac 上除非 `.podfetch` 整個空掉，否則不可能），觸發時是自刪＋非 0 退出，不會蓋掉任何既有快照；`check_observations` **刻意只用單一訊號**（兩個 ISO 日期字串比對，無雜訊來源），並在註解裡寫明為什麼不套「兩個獨立訊號」那條規則——**0 集日不產日檔所以不會誤報**，六個分支已用暫存假資料逐一驗過（含 0 集日）。積壓數字只印不報警。**本場未動 brief 的 43% 事故敘事**，理由是明天已有六個變因要驗。**⚠ 本場的修法自己也留下三個問題，當場被複驗子代理抓出並修掉**——①`check_observations()` 原本用 `elif` 鏈，**非法 status 會把「落後 15 天」整條蓋掉**（次要的資料衛生檢查排在主要目的前面）；②`data/` 零日檔時回 PASS，等於什麼都沒驗到卻報綠燈；③函式 docstring 寫「回訪沒做、只附加不改判、整步跳過都會出聲」，**實際只擋住第一種**（實測當天新增 3 條、改判 0 條，照樣 PASS）。**第七次了，而且這次是在同一場之內被抓到的**——差別在於複驗被指定去讀程式分支，而不是只對讀文件。**另補一件本場才發現的事**：SKILL.md 的還原點當時是舊的（見上一列與第 4 節新增的坑），已請使用者貼回正本並以 `2026-08-16-233000` 補存，快照鏈恢復完整 |

**讀這張表的方式**：「後來成立嗎」那一欄的 ❌ 與 ⚠️ 是最有價值的部分，但**兩種標記代表兩件不同的事，不要混為一談**。另有第三種標記 **⏳＝本場剛做完、還沒有實戰資料**（2026-08-16 起使用）——**它不是 ✅ 的弱化版，是「下一場維護的第一件事就是回來結掉它」**。舊做法是先寫 ✅ 再等打臉，08-11 的「先估篇幅」就是這樣自稱成立了一天。以下三類只收已經結案的列：

- **修法本身製造新問題**（動防護機制前一定要先看這幾筆）：`08-04`（❌，503 併進 `EXHAUSTED` → 08-06 事故）、`08-03 ①②`（✅，根因誤判，由 `08-03 ⑤` 撤銷）、`08-06 ①`（✅，當下缺輪換上限，由 `08-06 ②` 補）、`08-09 ②`（⚠️，文件層三種副作用）、`08-08 ④`（❌，FT 入口從 `ft.com/unhedged`「更正」成 `ft.com/the-economics-show`，而後者**同樣不含 Unhedged 集數**，直到 `08-15` 才發現——**「更正過」不等於「驗證過」**，兩個版本都沒有在當下實際跑一次；該列是 08-15 事後補登的）、`08-16 ④`（⚠️，`last_run_utc` 的語意改了，但四個「讀」它的地方沒跟著改，其中一句還與新行為寫在同一段落裡；**2026-08-16 稽核補記：同一天的第二次子代理複驗又抓到 10 處，其中 3 處源自 ④ 那一輪本身**——宣稱已同步進 SKILL.md 的第 5.5 步與成本模型改寫都查無實據，且 ④ 自己的「6 處中 3 處源於今天」逐條歸屬實為 5 處。**「複驗」這個動作本身也需要被複驗**）。**六次。**
- **方向對但成效未證實或被吃回去**：`08-03 ③`（⚠️，瘦身兩天內被變更紀錄吃回去）、`08-11`（⚠️，「先估篇幅再落筆」隔天實測完全無效——**方向對，但它依賴的量測是壞的**，08-12 才把單位換成段數）。
- **原本未證實、後來被實測證明成立**：`08-12`（段數規劃，08-14 一次驗收通過——**這是唯一一筆「一次就成功」**）、`08-14`（時間軸檢查，08-15 首次實戰命中：MIB 那集雙訊號同時成立、子代理獨立確認確有兩處跳段，當日 10 集零誤報——**「兩個獨立訊號同時成立」這個設計原則第二次被證明有效**）、`08-08 ②` 與 `08-09 ①` 這一組——架構隔離加上呼叫預算，**08-11 實測同樣五集 970 萬 → 288 萬（降 70%）**，落在當初粗估的 60–75% 區間內，兩列的標記已同步翻為 ✅。`08-09 ①` 的 token 量測那半仍未生效（路徑猜錯兩次，08-10 改為失敗會出聲），該列因此保留一個 ⚠️。**這是登記簿上第一筆從 ⚠️ 翻正的紀錄**：優化的方向判斷對了，但要等到有量測才知道對在哪一段。

**這三類與 ⚠️ 標記不是一一對應的**，不要用「⚠️ 有幾列」去核對「發生幾次」——第一類有些列標的是 ✅（問題在後續那一列才被抓出來，本列的修法當下成立）。**改標記或加新列時，三個 bullet 都要重讀一次，而且改字與改表必須同一次做完。** 08-09 ②、08-10、08-11、08-12、08-14 **連續五輪**抓到這段列舉與表對不上：08-09 ②與 08-10 是只改表沒改字；08-11 反過來只改字沒改表（宣稱「第一筆翻正」時表上一格都沒動）；08-12 寫了新列與新敘事，卻沒回頭把 08-11 那列結案；08-14 把 08-12 那列翻成 ✅ 了，卻沒把它加進第三類。**五輪、四種排列，所以把它變成檢查清單而不是叮嚀——每次動這張表，依序做完三件事再收工：①把上一列結案（本場驗證了它嗎？）②新增本場那一列 ③三個 bullet 逐條重讀，看有沒有列該進來或該移類。**

**08-09 那次的形態和前三次不同，值得單獨記：** 前三次都是「程式的修法在極端路徑放大傷害」，這次是**文件層的三種副作用**——歸檔搬走了規則、擴欄留下錯位的舊列、以及最隱蔽的一種：**把改動的理由寫成一個程式上不成立的因果**。前兩者靠對讀文件抓得到，第三者不行，只有回去讀程式分支才會發現。**所以每次改「指標」或「門檻」，驗證時要指定子代理去確認那個值實際接到哪條分支。**

---

