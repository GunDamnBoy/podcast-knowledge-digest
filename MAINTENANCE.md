# MAINTENANCE — 節目知識庫・維護說明

**這份文件是給「要修改這套系統」的人（或 Claude）看的，不是給執行每日產出的人看的。**
每日產出請看 `AGENT_BRIEF.md`。

想改動任何東西時，最快的方式是開一個新對話輸入 `/podcast-maintain`，或直接說「讀 MAINTENANCE.md」。

**分工原則（2026-08-03 確立）**：`AGENT_BRIEF.md` 只寫**現在的規格與判斷規則**，本檔負責**為什麼會變成這樣**——事故經過、誤判過程、被否決的選項。brief 是每日排程每天完整讀一次的東西，塞事故史會讓它越長越慢，而且執行者根本不需要知道當初誰誤判了什麼。歷史寫在這裡，brief 用一行指過來。

---

## 1. 這套系統由哪些東西定義

| # | 檔案／位置 | 角色 | 誰會讀它 |
|---|---|---|---|
| 1 | `~/podcast-knowledge-digest/AGENT_BRIEF.md` | **完整規格**：各節目的全文來源與取得方式、podfetch 管線、內容規格、資料格式、發布流程。**現役節目名單本身不在裡面**——唯一的家是 `~/.podfetch/shows.json` | 每日排程在第 0 步完整讀過 |
| 2 | 排程任務 `podcast-daily-300` 的 SKILL.md | **執行骨架**：流程順序與分支判斷，**正本是 `kb-core/scripts/podcast/DIGEST-PROMPT.md`，排程那份是整份複製的副本**。<br>**2026-08-23 訂正**：本列原本寫舊 taskId，且「唯一例外是第 3 步的子代理任務卡（抄了 brief 第 3／4 節）」整句已過期——任務卡 08-22 起是獨立檔 `kb-core/scripts/podcast/preamble.md`，而數字的家是 `anchors.json` 不是 brief 第 3／4 節。 | 排程觸發時直接執行 |
| 3 | `~/podcast-knowledge-digest/index.html` | 前端外殼：CSS、渲染邏輯、節目徽章 | 瀏覽器 |
| 4 | `~/podcast-knowledge-digest/data/*.json` | 每日內容 | `index.html` |
| 5 | `~/.podfetch/`（腳本、`config.json`、`shows.json`、`state.json`、`logs/`） | 01:00 的轉錄管線，由 launchd `com.kenny.podfetch` 觸發 | 排程排錯時會讀 |
| 6 | `~/podcast-transcripts/<date>/` | podfetch 產出的逐字稿與 `manifest.json` | 排程第 1 步 |
| 7 | `~/kb-core/scripts/podcast/healthcheck.py` | 健康檢查（對受檢系統唯讀，只寫自己的 `metrics.csv`） | 維護時第一步就跑 |
| 8 | `~/kb-core/scripts/podcast/fix-schedule.sh` | 把 launchd 時刻寫回 01:00 的驗證／還原工具 | 只在確認 plist 被改動時 |
| 9 | 本檔 `MAINTENANCE.md` | 維護說明＋事故與決策檔案 | 維護者 |

**最重要的一條規則：第 1 項與第 2 項是一組兩份，改任一邊都必須同步另一邊。**
兩者不同步時，排程會拿到互相矛盾的指示，而且不會報錯。

排程 SKILL.md 沒有版本控制，只有 `AGENT_BRIEF.md` 在 git 裡。**brief 才是真正的來源。**

---

## 2. 修改的標準流程

0. ~~**先存快照**~~ **這一步 2026-08-22 已退役，不要再跑**（2026-08-23 訂正：本行一直沒改，而 `MAIN.md` 早就寫「不再需要快照」——**同一套文件裡兩條相反的第 0 步**）。理由：`snapshot.sh` 抄的八個目標有七個現在住在 kb-core、有 git，`com.kenny.kbcorepush` 每 300 秒自動 commit＋push；唯一在 git 之外的 `~/.podfetch/metrics.csv` 它從來沒抄過。**腳本還在，但只會出聲、不建立任何東西**——照舊跑它會印「快照完成」，讓人以為還原點存在。要還原或看歷史：`cd ~/kb-core && git log -- scripts/podcast/`。
1. **跑 `python3 ~/kb-core/scripts/podcast/healthcheck.py`**，把機械式檢查一次做完（它會順便把當日指標寫進 `metrics.csv`）。
2. 讀 `AGENT_BRIEF.md`（全部 —— **讀完要確認回傳沒有截斷提示**，它曾兩次撐破 `Read` 上限而截斷點在權威區之前，見第 6 節）與排程 SKILL.md（`list_scheduled_tasks` 取得 `path` 後 Read）。
3. **先比對兩者是否已經不同步**，有的話先修好再談新需求。
4. 改 `AGENT_BRIEF.md`。
5. 用 `mcp__scheduled-tasks__update_scheduled_task` 同步排程 prompt。
6. 在 brief 第 8 節加變更紀錄；**事故經過與被否決的選項寫進本檔第 7 節**，不要寫進 brief。
7. 若動到節目清單，`~/.podfetch/shows.json` 與 `index.html` 的 `.ep.s-<key>::before`／`.b-<key>`／深色模式那一組都要補。
8. 收工前再跑一次 `healthcheck.py`，並用子代理獨立比對 brief 與 SKILL.md。
9. **不要跑任何 git 指令**（含 `git status`）——**`com.kenny.kbpublish.podcast` 每 60 秒**掃 `~/outbox/podcast/` 自動推送，跑 git 會留下 `.git/index.lock` 擋住推送。（**2026-08-30 訂正**：這裡原本寫 `com.kenny.dashpush` 每 180 秒，**那支 08-20 就退場了**。`MAIN.md` 08-21 訂正、`AGENT_BRIEF` 第 6 節 08-23 訂正，**而維護流程自己這一份留到 08-30** —— 同一個錯誤在三份文件裡各活了不同長度，因為每次都只修被指出來的那一處。）要看推送鏈用 `cat .git/refs/heads/main`、`tail .git/logs/refs/remotes/origin/main`，這些是唯讀指令，安全。

---

## 3. 健康檢查

```
python3 ~/kb-core/scripts/podcast/healthcheck.py
```

對受檢的系統唯讀、不碰 git；**唯一會寫的檔案是它自己的 `~/.podfetch/metrics.csv`**（每日指標，同日重跑覆寫該列）。在 Cowork 的 Linux 沙箱裡也能跑（會自動偵測掛載點）。檢查項目：

- `data/*.json` 可解析、`index.json` 由新到舊、`days` 與實際檔案雙向對應、`episodeCount` 與實際集數相符
- 資料用到的 `showKey` 是否都有色條／徽章／深色模式 CSS，且都沿用 `shows.json` 的鍵值
- podfetch 當日日誌**是否有 `01:0x` 的執行**（掃全檔「視窗：」行，**不是只看第一行**——手動執行會把排程擠出檔案開頭，08-10 修）、是 0 集還是異常收束、`state.json` 的 `last_run_utc` 新鮮度
- 最新逐字稿目錄的 `OK`／`DEGRADED`／`FAILED` 統計
- 推送鏈 local vs origin（唯讀 `cat`）
- **最新一份資料檔裡的 `⚠︎` 佔位集數**（下次抓到全文要重寫，不可被去重擋掉）
- **線上狀態**：帶 cache-buster 抓線上 `index.json` 比對 `updatedLabel` 與 `days[0].date`。**沙箱連不到 GitHub 會顯示 WARN 而非 PASS**，要在 Mac 上跑才驗得到這一項
- **節目數量**：brief 宣稱的檔數與 `shows.json` 是否一致
- **brief 引用的 `config.json` 數值是否與實際設定相符**——這一項專門抓 2026-08-03 那類「下文改了、上文漏改」的內部矛盾

結束碼 0 = 通過或僅有 WARN，1 = 有 FAIL。

---

## 4. 已知的坑（踩過才寫進來的，不要再踩一次）

- **「當天沒有目錄」≠「podfetch 掛了」。** 0 集時 podfetch 正常結束但不建立目錄。照 brief 第 2 節的三段排查：(1) 資料夾有沒有連線 → (2) 讀當天日誌，停在異常處就是失效；**但看到 `沒有新集數。` 不可以直接判成真的 0 集——那一格在 2026-08-15 被證明會漏掉整天 10 集，必須用帶 `&cb=` 的 iTunes 查詢做一次外部對照**（見第 7 節與 brief 第 2 節） → (3) **當天日誌根本不存在時先別判失效**，讀 `logs/` 裡最新那份「視窗：」行的時間戳（**掃全檔，不要只看第一行**——手動執行會把排程擠出檔案開頭），沒有任何一次 `[01:` 就是執行時刻漂到日報後面，不是 podfetch 死了。第 (3) 格的修法與失效完全不同，講錯會讓人往錯方向修。
- **「日誌時間戳對不上」有兩種成因，三項都要查完，順序是 plist 優先。** (a) plist 的時刻值不對 → `/usr/libexec/PlistBuddy -c "Print :StartCalendarInterval" ~/Library/LaunchAgents/com.kenny.podfetch.plist`，應為 `Hour = 1`；(b) 機器睡著、launchd 延後補跑 → `pmset -g custom` 看 AC Power 的 `sleep` 是否為 0、`pmset -g sched` 看有沒有排定喚醒。**先查 plist**，它最便宜最確定，而且「整點加一秒」（如 `[07:00:01]`）正是準時觸發的特徵、直接指向 (a)。
- **判斷節目是否停更，一律去讀那一集的原文標題，不要從日報回報的中文摘述推論（2026-08-18）。** 08-18 的回報第 14 項只寫「（節目停更公告）」五個字，維護端據此推論 Prof G Markets 收攤、還準備把兩天前才做的 `08-16 ②` 判為 ❌。**實際去讀日檔才發現原文是 `Prof G Markets Is Taking A Break — You Should Too`——兩週暑假，8/31 就回歸。** 「停更」在中文裡同時涵蓋「暫停」與「停播」，摘述沒有錯，是讀的人選了嚴重的那一個意思。**這與本檔反覆記的「查不到 ≠ 不存在」是同一個形態的鏡像：二手描述 ≠ 一手事實，而二手描述往往更好讀，所以更容易被當成結論。** 節目異動的成本很高（第 5 節八步、動四個檔），**動手前一定要回日檔或 feed 看原文標題**。
- **除錯時先問「這個值今天有沒有被人動過」。** 在正在被修改的系統上，讀到的現況不等於事發時的狀況——2026-08-03 就是這樣把修改後的 plist 當成修改前的證據，記錯了一次根因（見第 7 節）。
- **`pmset` 電源設定不存在於任何設定檔裡。** 重灌或換機不會遷移，且在「只靠電池」與「完全關機」兩種情況下會失效。使用習慣一改（晚上關機、不插電），01:00→03:00 的時序保證就沒了。**它與 plist 是兩件獨立的事**：plist 決定幾點跑，`pmset` 決定那個時刻機器醒不醒著。
- **驗證上線一定要帶 cache-buster。** 裸網址與 `raw.githubusercontent.com` 都會回舊快取（實測回到三天前），且要同時確認 `updatedLabel` 是本次執行時間，只看日期會被騙。
- **`showKey` 一律沿用 `~/.podfetch/shows.json` 的鍵值**，不要在網站端另取名字，否則徽章永遠對不上。現役節目與 `index.html` 色碼的差集由 `healthcheck.py` 的「showKey CSS」現算，**不要在這裡抄一份數字**。**移出節目時 CSS 不刪**，見第 5 節開頭。
  > **2026-08-20 訂正**：這一段原本寫「新增節目時若忘了補 CSS，`healthcheck.py` 的『showKey CSS』會 WARN」——**那句是錯的，而且錯了很久**。那條檢查比的是「資料**用到**的 showKey」，新節目還沒出過集數時它不在裡面，所以檢查對「加了節目卻忘了加色」**唯一會發生的那段期間**是瞎的。08-20 加 `fwdguidance` 時 CSS 三條全漏，這支腳本當天跑過、判 PASS。已補上反方向（`shows.json` 現役 − `index.html` 色條），現在那句才成立。
  > **一句「有東西在守」的話寫錯，比沒寫更糟——它會讓人不再去看。**
- **iTunes lookup 的 US 商店快取嚴重過期**，尤其 All-In，而且 limit 越小快取越舊。**第一線解法是一律帶 `&cb=<時間戳>`**（`podfetch.py` 的 `itunes_lookup()` 已內建，手動查詢要自己加）——快取綁在確切的查詢字串上，換任何變動參數就繞開。**要換商店只能用查詢參數 `&country=GB`；路徑前綴 `itunes.apple.com/gb/lookup`／`/au/` 在本環境回空白內容，本檔與 brief 過去寫的那條退路實際上不存在**（2026-08-15 實測）。2026-08-15 因為缺 `cb` 而整天回報「沒有新集數」，實際有 10 集，見第 7 節。
- **Substack 的 `/api/v1/archive` 也會回過期快取（2026-08-05）。** Dwarkesh 與 Latent Space 首次取回的最新一筆分別停在 6/08 與 7/08，要帶 cache-buster 才拿到當天。**失效的樣子是「這檔今天沒有新集數」**，於是安靜退回機器轉錄。與 iTunes US 商店是同一類問題，凡是「清單型 API」都要先懷疑快取。
- **`web_fetch` 約 104,700 字元上限，超過會截斷且不報錯（2026-08-05）。** Latent Space 那集官方稿只取到 73%。取長逐字稿後要比對末尾時間戳與 `trackTimeMillis`，不足就用 podfetch 補齊末段並在 `source` 標明兩個來源各涵蓋哪一段。
- **`ft.com` 已進入 `web_fetch` 封鎖清單（2026-08-06，HTTP 403）。** FT／Unhedged 只剩 Chrome 一條路，**2026-08-08 已在無人值守排程中實測成功**。另外**入口網址連錯兩次**：`ft.com/unhedged` 是電子報存檔、不含 podcast 集數；08-08 改成的 `ft.com/the-economics-show` **只列姊妹節目自己的集數，一樣不含 Unhedged**。**實際可用的入口是 FT 站內搜尋 `ft.com/search?q="<原文標題>"`，而且每集有兩個頁面，要取標題帶 `Transcript:` 前綴的那一個**（另一個集數頁正文僅約 2,700 字元，很容易被誤判成付費牆）。Unhedged 的 feed 也會放姊妹節目的重播。
- **講者標記會被 Gemini 誤植，而完整度檢查抓不到（2026-08-06）。** 實例：完整度 1.01、狀態 OK，但中段起把主持人標成片頭廣告旁白者。`shows.json` 的主持人名單只是提示。**這是繼 503、0 字回傳之後的第四種失敗態，而且是唯一一種「所有機械檢查都顯示正常」的**。
- **改防護機制時要問「它在最壞情況下會怎麼失敗」。** 典型形態是正常路徑有效、極端路徑放大傷害（08-04 修 503 造成 08-06 的 Lite 被永久除名；08-03 修時序造成根因誤判）。**次數與完整清單一律以第 12 節登記簿的第一類 bullet 為準，不要在這裡另記一個數字**——本行過去寫死「連續兩次」，後來與登記簿脫鉤。
- **「收工後」快照要在**最後一次**改動之後取，否則快照鏈會出現空窗（2026-08-16）。** 08-16 那場的「收工後」快照取於 18:27，但 `08-16 ⑤`（第 5.5 步、回報第 16 項、成本模型訂正）是在那之後才寫進 `SKILL.md`——**於是當晚稽核時，SKILL.md 唯一的還原點是舊的**，若那時檔案遺失，⑤ 那一輪的工作就沒了。**而且症狀會偽裝成規格漂移**：對讀「brief 宣稱已同步 vs 快照查無實據」，看起來就像 SKILL.md 沒改。當晚由使用者貼回正本、以 `2026-08-16-233000` 補存還原。**「改了 SKILL.md 就存快照」這條規則本身沒問題，錯在把快照當成「收工的儀式」而不是「每次改動的收尾」。**
  **2026-08-17 第二場又碰到同一件事的另一面**：那場在沙箱裡取了四份「收工後」快照，**每一份都缺 `SKILL.md`**（沙箱看不到 `~/Documents`），而那天 `SKILL.md` 被整份覆寫了三次。真正完整的還原點是使用者在 Mac 上跑出來的 `2026-08-17-223331`。**所以「收工後快照」要分兩種看**：沙箱那份只證明 `.podfetch/` 的檔案有備份，**排程 prompt 的還原點只能由 Mac 產生**——改過 `SKILL.md` 的場次，收工訊息一定要請使用者在 Mac 補跑一次，並確認 `--list` 裡那筆真的有 `SKILL.md`。
- **`snapshot.sh` 曾經會安靜產生「空快照」（2026-08-16 修）。** 它的 `BASE` 原本寫死 `$HOME/.podfetch`、**沒有掛載點偵測**，所以在 Cowork 沙箱裡（`$HOME` 不是 `/Users/kenny`）每個檔案都印「（缺）」，然後**照樣建目錄、寫 MANIFEST、回報「快照完成」**——那筆會出現在 `--list` 裡，看起來就是一個正常的還原點。**而 repo 外的檔案沒有 git，快照是唯一還原點。** 已補上與 `healthcheck.py` 的 `resolve()` 同一套順序（先找 `/sessions/*/mnt`、再退回家目錄），並加了守衛：**一個檔都沒抄到就自刪目錄、印錯誤、回非 0**。`SKILL.md` 單獨缺仍屬正常（`~/Documents` 是 macOS TCC 保護區，沙箱看不到）。**教訓：兩支腳本讀同一組路徑，只有一支做了偵測——「另一支也需要嗎」這個問題當時沒有人問。**
- **shell 腳本裡 `$VAR` 後面緊接全形字元，在 Mac 上會炸（2026-08-17 ③）。** `snapshot.sh` 的最後一行寫 `echo "快照完成：$DEST（來源 BASE＝$BASE）"`，**macOS 的 bash 是 3.2.57**，它的識別字掃描會把 `（`（U+FF08，`EF BC 88`）那幾個高位元組吃進變數名，於是那個變數變成未設定，`set -u` 當場中止並印 `DEST?: unbound variable`（`?` 是終端機在渲染那串無效位元組）。**修法：一律寫 `${DEST}`**，大括號讓邊界明確，任何版本都對。
  **這個 bug 有三個性質值得記住**：①**只在 Mac 上出現**——Cowork 沙箱是 bash 5.1，解析完全正常，所以在沙箱跑一百次也測不到，**而 Mac 正是唯一能取得完整快照（含 `SKILL.md`）的地方**；②**失敗點在最後一行**，`mkdir`／`cp`／MANIFEST 全都已經完成，**所以快照其實是好的，壞的只有那句「快照完成」與離開碼**——看到這個錯誤先去 `--list` 確認，不要急著重跑；③**它是 `08-16 ⑥` 那一輪加掛載點偵測時引進的**，而那一列的驗收狀態一直掛著「守衛與偵測未被排程觸及，仍待實戰」——**8/16 之後每一次「請在 Mac 補跑一次完整快照」都是非 0 離開，只是沒有人回報**。**「待實戰」掛著沒人去踩，就等於沒有人發現它已經壞了。**
- **`update_scheduled_task` 的 `prompt` 是整份覆寫，不是局部編輯。** 送出前一定要先 Read 目前的 SKILL.md，以它為基礎改。2026-08-04 就這樣蓋掉了另一場維護的四項改動（見第 7 節）。**「我記得的版本」在多場維護之間一定會過期**，而 SKILL.md 不在 git 裡，沒有任何機制會擋。
- **brief 說「已同步進 SKILL.md」不等於真的同步。** 紀錄與實況會脫鉤，而紀錄看起來更可信。子代理比對時要專門檢查這一項。
- **`web_fetch` 對 RSS／XML 一律回 `[binary data]`**，不要指望直接讀 feedUrl。
- **FT 存取失效時不會報錯**，只會安靜退回付費牆。每次都要檢查正文長度（Unhedged 通常 5,000 字元以上）。
- **YouTube 字幕自 2026-08-02 起全面失效**，只當最後手段，每集最多試 5 分鐘。操作要領見第 8 節。
- **Word 報告絕對不能存進 repo**（那是 public repo），要寫到暫存輸出資料夾再用 `present_files` 交付。
- **前一晚的美東晚間集數必定順延到隔天那一版**，這是 03:00 時段的設計取捨，不是故障，不要去追。
- **brief 內部會自打架。** 邊做邊改時習慣在下文補新值卻漏改上文，2026-08-03 一次抓到四處。`healthcheck.py` 的「brief vs config」就是為此而寫，但它只涵蓋數值型設定，敘述性矛盾仍要靠子代理比對。

---

## 4B. 基礎設施完整參考（自 brief 第 6 節移入，2026-08-08）

brief 第 6 節只留每日執行需要的，完整版在這裡：

- **Repo（本機）**：`~/podcast-knowledge-digest`（家目錄下，不要放 `~/Documents`，macOS TCC 會擋背景程式）。
- **GitHub**：`GunDamnBoy/podcast-knowledge-digest`，Public，Pages（Source ＝ GitHub Actions）。
- **推送認證**：**本機推送走 SSH**——`.git/config` 的 `url = git@github.com:GunDamnBoy/podcast-knowledge-digest.git`（2026-08-31 實測；`~/kb-core` 同樣是 SSH）。**Pages 部署不經過它**：`.github/workflows/deploy.yml` 用 Actions 內建權限（`permissions:` ＋ `id-token: write` ＋ `actions/deploy-pages@v4`），**全檔沒有任何 `secrets.` 引用**，所以那一側也沒有需要輪替的 token。
  > **2026-08-31 訂正**：本行原本寫「remote URL 內嵌 fine-grained PAT（只授權此 repo、Contents 讀寫）」並附一條換 token 指令 `remote set-url origin https://<新PAT>@github.com/...`。**那條指令現在是有害的**——照著跑會把一個能用的 SSH remote 換成 HTTPS，然後要人去找一把這條路徑上根本沒在用的 PAT。
  > **`kb-core/README.md` 一直都是對的**：它開頭就寫著「零 PAT 架構」「推送用 SSH key」。所以這不是兩份文件各對一半，是**本檔單獨落後**——而落後的那一份正好是維護者查基礎設施時會來看的那一份。
  > **兩件事沒有查證，不要在這裡假裝知道**：（一）SSH 金鑰是哪一把、放在哪；（二）這個 remote 是什麼時候從 HTTPS＋PAT 換成 SSH 的，以及那把舊 PAT 有沒有被撤銷（**若還沒撤，那是一把沒人在用卻仍有寫入權的憑證**，值得去 GitHub 設定看一眼）。
  > **同一輪已 grep 全庫**：另一處 `AGENT_BRIEF.md` 第 6 節的指路寫「PAT 換發」，同日一併訂正；其餘命中全是 `PATH` 變數，非本主題。
  > **這一條是怎麼被發現的，比內容本身值得記**：同一場維護已經修了它正下方的「背景推送」那一行，**而且在訂正註記裡親手寫下「每次都只修被指出來的那一處」**——然後照樣沒有看它上面一行。是使用者貼回 `git push` 的輸出、`To github.com:...` 那個 `host:path` 形式（SSH 語法，HTTPS 會印完整網址）才露出來的。**第四處修完，第五處就在隔壁。**
- **背景推送**：launchd `com.kenny.kbpublish.podcast`（`StartInterval` 60）跑 `~/kb-core/tools/publish.py`，掃 `~/outbox/podcast/*.draft.json` → 寫進 repo 的 `data/` → commit → rebase → push，並把結果寫成 `~/outbox/podcast/<日期>.receipt.json`（歷史在同目錄的 `publish.log`，回執每輪會被覆寫）。**兩個邊界要記住，因為它們都製造過事故**：（一）**outbox 沒有草稿時是空輪次，整輪完全不碰 git**；（二）`systems/podcast.py` 的 `staged_paths` 只有 `["data"]`，**非 `data/` 的變更它不 stage、不 commit，而且會擋下整輪**（`exit 15 @ worktree-dirty`）。所以 repo 根目錄的檔案（本檔、`AGENT_BRIEF.md`、`index.html`）改完**一定要自己提交**，否則擋住的是隔天早上的日報。
  > **2026-08-31 訂正**：本行原本寫「`~/.dashpush/auto-push.sh`……launchd `com.kenny.dashpush` 每 180 秒觸發」。dashpush 在 2026-08-20 重建時就退場了（殘骸在 `chart-of-the-day/tools/_to_delete/dashpush-auto-push.sh`）。
  > **第 2 節第 9 條（不要跑 git 指令）在 08-30 就訂正過同一個錯，而且當場把散佈情況數了一遍**：`MAIN.md` 08-21 改、`AGENT_BRIEF` 第 6 節 08-23 改、維護流程那一份留到 08-30，結論是「同一個錯誤在三份文件裡各活了不同長度，**因為每次都只修被指出來的那一處**」。
  > **而本行就是沒有被指出來的第四處，又多活了一天。** 這次的觸發點同樣不是有人來讀它，是 `healthcheck.py` 抄了同一個錯、在 publish 卡住那兩小時四十分叫人不要動作（見第 7 節 2026-08-31）。**所以修這類錯誤時要當場 `grep -rn` 全庫，不要只修被指到的那一處**——08-30 那條註記自己就寫出了病因，然後同一場維護沒有照它做。
- **podfetch 排程**：launchd `com.kenny.podfetch` 每天 01:00；`~/.podfetch/fix-schedule.sh` 可把 plist 寫回 01:00（驗證／還原用，不是例行修復）。
- **電源設定**：見第 9 節（`pmset -c sleep 0` 主保障＋00:55 排定喚醒後備、AlDente 否決理由、FileVault 單點）。
- **每日轉檔**：`~/kb-core/tools/podcast_docx.py`（**實際在跑的那一支**，依 `launchd/kbdocx-podcast.sh` 確認；`scripts/podcast/json2docx.py` 是 08-08 的舊腳本） 把 `data/YYYY-MM-DD.json` 轉成 Word（python-docx；沙箱用 `pip install python-docx --break-system-packages`）。**內容以 JSON 為唯一來源**，不要讓 LLM 重寫一遍。

---

## 4C. podfetch 的四個不要改的設計（自 brief 第 2 節移入，2026-08-17 ②）

**為什麼移進來**：每日排程完整讀 brief 一次，而**執行者不執行 podfetch，只讀它的輸出**——視窗怎麼算、模型池怎麼排、503 怎麼冷卻，它一項都用不到。留在 brief 只是佔掉每天都要付的 context。**四條規則本身完全有效，動 `podfetch.py` 之前一定要先讀完這一節。** brief 第 2 節保留的是「品質指標怎麼讀」——那一段才是執行者每天要用的。

1. **視窗以 `last_run_utc` 為準，不是固定 26 小時。** 從上次成功執行時間往前推 30 分鐘重疊開始抓，上限 72 小時。**不要改回固定窗口**——舊版在漏跑一天時會產生無法察覺的缺口。
2. **字數檢查是防「安靜失效」的唯一機制。** Gemini 是 LLM 不是機械式辨識器，長音檔上可能改寫、壓縮或跳過整段而不報錯。腳本以該節目的語速基準估算期望值，低於 55% 就重試該段；仍不足則標 `DEGRADED` 並寫進 front matter 與 manifest。**不要拿掉。**

> **語速基準是 per-show 的**（2026-08-09 校準）：全域預設 `WORDS_PER_MIN = 200`，個別節目可在 `shows.json` 用 `"wpm"` 覆寫（目前 7 檔有覆寫，如 In Good Company 160、ILTB 140、Odd Lots 185）。
>
> **原本全域 165 太低，後果是幾乎每一集的完整度都顯示 1.2–1.3**——「超標」變成常態，這個數字因此不再有可讀性。**指標要讓「正常 ≈ 1.0」，偏離才有意義。** 校準後 15 檔節目全部落在 0.87–1.09。
>
> **但要分清楚它影響什麼（2026-08-09 訂正一個曾寫錯的因果）：** `wpm` 只決定 ① 完整度分母（使用者看到的那個倍數）、② 段級重試門檻（**上修 wpm 會讓門檻變嚴、更容易重試**）、③ `median_rate` 的後備值。**它不決定 `DEGRADED`。** status 只看 `warnings`，而 warnings 只有三個來源：字數低於 `MIN_RATIO`、段級語速偏離**本集中位數**（`REL_RATIO`／`HIGH_RATIO`，不看 wpm）、跳針剔除。**完整度 1.4 這種「超標」不會產生任何 warning。** 08-09 那兩集的 `DEGRADED` 來自段級相對檢查，與 wpm 無關。
>
> 新增節目時若語速明顯偏離 200，累積幾集後在 `shows.json` 補 `wpm`。

> **計字前會先剔除跳針**（`collapse_loops()`：連續重複 20 次以上的同一 token 壓成一次），剔除量單獨記在 `warnings` 裡並標明集中在第幾段。**沒有這一步，完整度指標在「跳針」這個失效態下會完全反向**——2026-08-08 實例：一行 28,122 個重複的「I」讓完整度顯示成 3.36，實際只有 1.10。

> `WORDS_PER_MIN` 這個常數在程式碼裡、不在 `config.json`，所以 `healthcheck.py` 的「brief vs config」檢查**涵蓋不到它**——改動時要自己回頭改本節。沿革：130（初版）→ 165（08-06）→ 200（08-09）。
3. **Lite 是主力，Flash 是備援（2026-08-05 反轉）。** 池子仍是 3 Lite ＋ 3 Flash，但**順序改成 Lite 在前**（`config.json` 的 `prefer_lite: true`）。

   | 模型 | RPD |
   |---|---|
   | Flash 3.5／3.6 | **10–20，會浮動** |
   | Flash 2.5 | 20 |
   | Flash-Lite 2.5 | 20 |
   | **Flash-Lite 3.1／3.5** | **500** |

   > **Flash 的 RPD 上限不是常數**：08-05 從 20 掉到 10，08-07 又顯示回 20。**不要把它當固定值來做設計判斷**——Lite 優先的理由是「500 遠大於 10–20」這個量級差，不是某個特定數字。

   新版 Flash 只剩 10 RPD，而重試會讓實際請求放大約 2.5 倍（08-05 實測：預估 17 個請求，實際打出 42 次，Flash 兩個模型雙雙超限而 Lite 只用 8 次）。**把最稀缺的資源排在最前面，等於每天開場就燒光它。** 實測 Lite 的完整度 1.14–1.16，與 Flash 沒有可辨識的品質差距。要改回品質優先就把 `prefer_lite` 設為 `false`。
4. **500／503 換模型，但只冷卻、不除名。** 過載是「這個模型現在忙」，不是額度問題。同一模型重試兩次仍 503 就丟 `ModelOverloaded`，該模型進 `OVERLOADED` 冷卻 `overload_cooldown_seconds` 秒（現為 300）後**自動回池**；`EXHAUSTED` 只放日額度用盡與 404 這類**永久性**失效。

   **這兩件事必須用不同的資料結構，共用一個集合就等於把語意也合併了。** 2026-08-04 把 503 也丟進 `EXHAUSTED`，結果 08-06 一次過載尖峰在 77 秒內把三個 500 RPD 的 Lite 全部永久除名，剩 10 RPD 的 Flash 扛完整場、燒到 22/10。**暫時性錯誤不可以產生永久性後果。**

   **輪換同時受次數與時間約束**：過載輪換上限 `max(4, len(pool) * 3)`，單集牆鐘上限 `episode_budget_seconds`（現為 3600 秒；**2026-08-10 修正語意**——舊版名為單集上限、實為每段每次重試各領一份 1200 秒且只在過載分支檢查，等於無上界；現在整集只設一次、每段開始前檢查）。另有**整場執行上限 `run_budget_seconds`（現為 6000 秒＝100 分鐘）**：01:00 起跑、02:40 後不再開始新的集數，未處理的標 FAILED——03:00 日報要讀 manifest，寧可缺集也不能讓日報讀到不完整的檔案。**光有次數上限不夠**——每次輪換都可能等一輪 300 秒冷卻，18 次就超過一小時。

   撞到任一上限就丟 `RuntimeError`，該例外會穿出分段迴圈，**使該集標為 `FAILED`**（不只是那一段）。這是可接受的：已完成的段落留在 `~/.podfetch/cache/`，該集不寫進 `seen`、`last_run_utc` 不推進，下次執行接續。日額度輪換不計入過載上限，兩者語意不同、用不同的計數器。經過見第 7 節。

---

## 5. 新增一檔節目的完整步驟

> **移出一檔節目走同一張表，但第 4 步反過來：`index.html` 的三組 CSS 一律保留不刪**（2026-08-16 立）。歷史日檔仍然帶著舊 `showKey`，刪掉色碼那些集數就會掉回預設藍。`healthcheck.py` 的 showKey 檢查早就寫明「歷史檔案含已移除的節目是正常的」，只比對**最新一份**——所以留著不會製造 FAIL。目前有三組屬於這種狀態：`capitalallocators`、`bg2`、`breakdowns`。
> **另外第 6 步有可能「不需要動」**：一進一出時總數不變，SKILL.md 的 `description` 就不必改。**這種時候要在登記簿明寫「因總數不變而未動」**——不然事後看，「不必動」與「忘了動」長得一模一樣。

1. `AGENT_BRIEF.md` 第 1 節：加進節目清單（A／B 類歸屬、官方逐字稿來源若有、排序位置）。**AppleID 只寫進下一步的 `shows.json`，不要抄進 brief**——2026-08-17 ② 刪掉了 brief 裡那份無機械把關的 AppleID 抄本，本步原本寫「含 AppleID」，照舊做會把它復活。
2. `~/.podfetch/shows.json`：加上 `appleId`／`name`／`hosts`（主持人名單會餵給轉錄 prompt，讓 Gemini 寫真名而不是 `Speaker 1`／`Speaker 2`）。
3. `~/.podfetch/config.json` 的 `show_priority`：插到適當位置（額度不足時先犧牲排在後面的）。
4. `index.html`：補 `.ep.s-<key>::before`、`.b-<key>`、`html[data-theme="dark"] .b-<key>` 三處。**撞色要三處分別檢查**——08-10 只查了色條就宣稱查過，結果徽章文字色與現役的 `unhedged` 只差 12（RGB 距離），等於同一個顏色。實務門檻：**與最近鄰的 RGB 距離至少 25，低於 20 一定要換。**
5. **`README.md` 兩處**：開頭的「偵測 N 檔節目」與「追蹤的節目」清單。**這一步 08-10 漏了**——數字改了、清單沒補，而 `healthcheck.py` 只比對 brief 與 `shows.json`，**看不到 README**。它是 Public repo 唯一會被外部讀者看到的檔案。
6. 排程 SKILL.md：節目數變動一定會動到 `description`（「N 檔財經 Podcast」）；流程或排序規則有變才動 prompt 本體。
7. brief 第 8 節加變更紀錄，`MAINTENANCE.md` 第 12 節登記簿補一列。
8. 跑 `healthcheck.py` 確認相關檢查都 PASS。**2026-08-20 起，第 1、2、4、5 步有機械把關**：`節目在文件裡`（第 1、5 步：現役節目的名稱在 brief 與 README 都要找得到）、`showKey CSS` 的「現役節目沒有色條」（第 4 步）、`shows.json 兩份`（`~/.podfetch` 與版控那份的對帳）。**仍然涵蓋不到的只剩撞色（第 4 步的第二半）與第 3、6、7 步。**
  > 這四項會補上，是因為這張清單**失敗過兩次而且是同一種**：08-10 加 TIP 漏了 README（第 12 節登記簿有紀錄），08-20 加 fwdguidance 漏了 brief 與 CSS。**清單沒有錯，錯的是「靠人記得走完清單」這件事本身。**

---

## 6. 目前已知待辦與觀察中的事項

> **已結案（2026-08-06 移出待辦）**：plist `Hour = 1` 與 `pmset` 那組電源設定都已驗證通過——08-04、08-05、08-06 連續三天日誌開頭都是 `[01:00:00]`。若哪天又偏晚，照第 4 節的三項排查（**plist 優先**），不要重新當成未驗證的新問題。
>
> 當初留的退路仍然有效，記在這裡備用：**若時序保證連續失效**，就別再靠它，改成讓日報容忍 podfetch 尚未跑完（短暫等待後重讀，或改用前一天的 manifest 並在回報中標明）。

- **🔴 檢查印出來的「建議」會跟著基礎設施一起過期，而沒有任何東西在看它（2026-08-31，未處理）。** `healthcheck.py` 的推送鏈 WARN 印了「dashpush 每 180 秒推一次，稍等再看」約 11 天，而 dashpush 08-20 就退場了；08-31 publish 卡住那兩小時四十分，它**主動叫維護者不要動作**。當天已改掉那一則（見第 7 節），**但同型的字串還有幾處沒有對帳機制**——訊息裡任何「稍等再看」「由某某負責」「在 Mac 上跑一次」都是一個因果宣稱，而**宣稱的對象換掉時不會有人回頭改字串**。
  **當場沒有加自動檢查，理由要記住**：想加的是「掃 kb-core 裡所有 `dashpush` 字串」，但 dashpush 在歷史紀錄裡有十幾處合法出現（`chart/CHANGELOG.md`、`push_kbcore.py` 註解、`launchd/*.plist` 的沿革），**字串比對必然對它們全部誤報**——`MODIFY.md` 那條「新增檢查前先問正常情況會不會也觸發」擋下了它。要判的差別是「宣稱它還在跑」與「記錄它退場了」，**那個差別目前機器判不出來**。可行的方向是反過來做：讓每則帶有執行者名稱的訊息去讀該執行者的實際來源（回執、plist），像 08-31 那次改法一樣，**而不是加一條掃字串的檢查**。
  **同型的已知殘留（08-31 複驗子代理具名，都在別套系統，未處理）**：`skills/maintain/convergence/MAIN.md:13`、`convergence/FILES.md:22`、`houseview/MAIN.md:12`（後者還叫人跑 `cat ~/.dashpush/repos.txt`，**那個驗證指令本身已失效**）、`chart/MAIN.md:48`（寫「三支 kbpublish」，實際五支）。**改它們要連同重新打包安裝技能**，否則 `check_skill_copy()` 會亮。
- **FT 的 syndication cookie 仍是脆弱依賴。** Chrome 路徑本身已於 2026-08-08 在無人值守排程中實測成功，但 cookie 會過期、失效時安靜退回付費牆，長期解法是登入真正的 FT 帳號。
- **YouTube 字幕退援路徑實質上已死**，目前只剩 podfetch、官方逐字稿、FT 三條路。若 podfetch 出問題，退援能力比看起來薄。
- **資料夾連線不保證跨工作階段留存**，排程要能自我修復（已寫進 SKILL.md）。
> **已結案（2026-08-08）**：變更紀錄膨脹問題已處理——歸檔到本檔第 11 節，brief 只留最近一場的條目，歸檔規則寫死在 brief 第 8 節末尾（每次維護開工時執行；**單位是「一場」不是「一天」，2026-08-17 ② 訂正**）。
- **Gemini 免費層額度會被 Google 無預警調整，而我們沒有任何偵測機制。** 08-05 就發生了（Flash 3.5／3.6 的 RPD 從 20 砍到 10），是使用者主動截圖 Console 才發現。`healthcheck.py` 檢查不到——它只比對 brief 與 `config.json`，兩邊都是我們自己寫的。**建議每隔一兩週看一次 AI Studio Console 的 Rate limits 頁**，或想辦法讓 podfetch 把實際回傳的額度資訊記進日誌。
- **重試放大率約 2.5 倍，值得盯著。** 08-05 預估 17 個請求實際打出 42 次。目前靠 Lite 的 500 RPD 吸收，但若哪天 Lite 也被砍，就得回頭檢視三種重試（503、字數不足、輸出截斷）的觸發條件。
- **GitHub Actions／Pages 部署端沒有任何主動監測。** 08-07 卡了至少兩小時，只能靠人去看 Actions 分頁。`healthcheck.py` 現在會偵測「線上落後本地」，但**偵測不到原因**——workflow 沒觸發、跑失敗、還是 Pages 佇列塞住，是三種不同的修法。
- **講者標記誤植目前只能靠人工校正把關**，機械檢查抓得到格式異常與統計異常，抓不到「合理人名張冠李戴」（見第 7 節）。這是目前最主要的品質風險。
- **補了檢查之後要問「它涵蓋到鏈的哪一段為止」。** 已經連續兩次是同一條鏈往下游挪一格再出事：08-03 是 push 沒跑，08-07 是 push 跑了但 deploy 沒跑。
- **在沙箱裡匯入 `podfetch.py` 做測試，會在沙箱家目錄建出假的 `~/.podfetch/logs/`。** 它的 `log()` 是模組層級行為，只要 import 就會建目錄。`healthcheck.py` 的 `resolve()` 已改為優先認掛載點，但**測試完仍要記得 `rm -rf /sessions/*/.podfetch`**，否則後續工具可能抓錯路徑。2026-08-08 就因此看到一整排假故障。
- **程式碼裡的常數完全在機械檢查的涵蓋範圍之外。** `healthcheck.py` 只比對 brief 與 `config.json`，而 `WORDS_PER_MIN`（現為 200，已改過三代 130→165→200）這種寫在 `podfetch.py` 裡的常數改了之後，brief 與警告字串都可能漏改——08-06 就發現「用 165 計算卻對使用者報 130」，而且那句話會寫進 front matter 與 manifest。**寫進使用者可見輸出的數字一律引用常數，不要寫死。** 值得考慮把這類常數移進 `config.json` 讓檢查涵蓋得到。
- **✅ 已結案（2026-08-16 當晚）：第 5.5 步確實在正本裡。** 稽核時 18:27 的「收工後」快照裡搜不到 `5.5`／`第 16 項`／`eff_tokens`，一度懷疑 `08-16 ⑤` 宣稱已同步但實際沒改。**使用者貼回正本後確認：第 5.5 步、回報第 16 項、成本模型訂正全部都在。****所以那不是規格漂移，是快照落後一輪**——真正的問題在下一條。
- **觀察點記分板積壓（2026-08-16 量測）。** 56 條裡 **47 條卡在「觀察中」（84%）**，只有 9 條曾被改判，**最舊一條停在 07-29、18 天沒動**；而新增是每天 3 條、連續 12 天沒斷。也就是「附加」一直有做、「改判」是斷續的（近 6 天有 4 天做了）。`healthcheck.py` 現在會把這三個數字印出來，**但刻意不報警**——積壓算不算問題要人看，讓它天天亮只會變成背景噪音。**盯的是趨勢：觀察中佔比若持續往上，就是回訪流於形式。**
- **brief 違反自己訂的分工規則（2026-08-16 稽核，未處理）。** 那條規則是「本檔只寫現在的規格與判斷規則，事故經過一律寫在 `MAINTENANCE.md` 第 7 節」（**在失效橫幅之後的第一段；本條原本寫「第 9 行」，08-21 加的 22 行橫幅把它推到第 34 行，2026-08-30 改成指內容不指行號**）。實測第 1–7 節有 79 行帶事故／沿革標記、**合計佔第 1–7 節字元的 43%**（這些行同時含規則與敘事，43% 是可切分的上界），全檔共 128 個日期標記；第 8 節另佔 9.3%（**此為 08-16 改動前的量測；記下這一筆之後第 8 節本身變厚，已升到約 12%——自我指涉的數字要標時點**）。**2026-08-30 複量：比例 42.3%，沒有惡化也沒有改善**（第 8 節回落到 9.1%、日期標記 114 個）——**所以真正惡化的從來不是這個比例，是絕對值**，見下一條。**規則是對的，沒有被執行。** 本輪刻意不動——明天 8/17 已經有六個變因要驗（本線四個 ⏳ ＋ 投顧線兩個），再疊上大面積下推就分不清是誰造成的。
- **🔴 `check_timestamps()` 看不見「時間戳回跳」與「內容重複」，而這兩種形態已經實際發生（2026-08-18，未處理）。** 08-18 的 20VC：子代理自行發現 `21:08→35:15` 與 `1:04:33` 之後回跳到 `40:00`，外加一段重複內容，**但 `timestampNote` 是空的**。
  **根因在程式的第一行**：`podfetch.py:676` 是 `secs = sorted({...})`——**集合去重＋排序，在任何判斷之前就把順序與重複同時丟掉**。所以回跳進到 `if` 之前已經變成遞增數列；更糟的是排序會**主動遮蔽**：回跳那批時間戳排完正好填進前面的空隙，讓時間軸看起來更密而非更疏。另一道大間隙檢查要求 `完整度 < 0.85`，而本集是 **1.13**，`and` 直接短路。**兩道檢查對這一集結構上都無效。** 另有一道未見於任何文件的靜默閘：`len(secs) < 5` 直接 return 空清單。
  **可能的上游成因有兩個，都是真缺陷，動手前要先分辨**：①`offset_timestamps()`（`podfetch.py:124`）**逐段**判斷 Gemini 是否已輸出絕對時間，判準是「該段第一個時間戳是否貼近該段絕對起點」——**它的 docstring 自己就寫著「同一次執行裡時而遵守時而不遵守」，而 :143–146 記過一次誤判造成「合併後時間軸倒退 20 分鐘」，正是今天的症狀**；②Gemini 在分段邊界重複轉錄同一段音檔，重複段落帶著原本的絕對時間戳，於是插回較早的位置。
  **修法順序：先加偵測、不要動 `offset_timestamps()`。** 那個啟發式一動就會影響全庫所有集數的時間軸，而 `08-04` 修 503 造成 `08-06` 事故就是同一形態。偵測器只是多印一則 note，最壞情況是誤報。
  **❌ 2026-08-18 實測：單調性檢查不能加，初判被推翻。** 全庫 91 集跑 `measure-ts-monotonic.py`（容忍值 5 秒）：**29 集有回跳，佔 32%**；把容忍值提高到 60 秒仍有 20 集、22%。**遠超過「報超過一成就是雜訊」那條自訂門檻。** 初判說「單調遞增在正確合併的逐字稿裡恆真」——**那是推論，而量測直接否定了它**。這是本檔反覆記的那條的又一次實例：**先修量測、再修規則；沒有量測就不要加檢查。**（若沒先量就加，這道檢查會對每三集報一集，重演 2026-08-07 講者檢查初版「九集報八集」與時間軸初版「72 集報 15 集」。）

  > **但明細裡有結構，而且結構本身就是診斷（2026-08-18）。** 回跳幅度不是連續分布，是三團：
  > | 幅度 | 集數 | 判讀 |
  > |---|---|---|
  > | **< 60 秒** | 8 | 良性——同分鐘內的排版差異，不是時間軸壞掉 |
  > | **≈ 1,200 秒**（1192–1200，八集擠在這裡） | 8 | **`segment_seconds` 正好是 1200。回跳一整個分段長度＝`offset_timestamps()` 漏做位移**，不是 Gemini 的雜訊 |
  > | 其餘（501–69,141 秒） | 13 | 混雜；最大那兩筆（69,141／64,345 秒）**溢出檢查本來就抓得到**，不是漏網 |
  >
  > **「≈ 一個 `segment_seconds` 的回跳」是可指名的失效簽章**，而它的分布說明了一件好事：**8 筆裡有 6 筆落在 08-02～08-05，只有 tip 08-17 那筆是近期的**。`offset_timestamps()` 的 `ABS_TS_TOLERANCE` 修法（08-08）**確實有效**——分段接縫回跳從每天數集降到十天一集。
  > **今天 20VC 的 1,473 秒不是 1200 的倍數**，所以它不是位移漏做，**比較可能是 Gemini 在分段邊界重複轉錄**（與子代理回報的「一段重複」一致）。**兩種成因確實是兩種，不要用同一個修法蓋過去。**
  >
  > **結論：不加通用的單調性檢查。** 若之後真要補，唯一有機會的形態是**「回跳幅度落在 `segment_seconds` 的整數倍 ±30 秒內」**——那一團在全庫是 8 集（9%），剛好在門檻內，而且語意明確、不是啟發式。**但它現在幾乎不再發生，補了也抓不到東西**；等它復發再說。`measure-ts-monotonic.py` 留在 repo 根目錄，復發時直接重跑。
- **✅ 已結案（2026-08-22）：帳本的「逾期未判」判準終於判得了。** 原本 `BRIEF.md` 第七節第四條把它列為當期失敗判準，而 `checks/podcast.py` 的 `_ledger` 在 `blind_to` 裡寫著判不了、要先給帳本加 `due`。實作走了第三條路：**新項目強制帶 `due`，歷史從 text 既有的「（到期 …）」回退解析**，一條歷史都沒改。新檢查是 `podcast.ledger_no_overdue`。**但洞還在**：61 條觀察中有 52 條沒有任何到期日，這條對它們完全是瞎的——**它報綠燈只代表「有到期日的那些沒逾期」**。要真正補起來，得有人回頭替那 52 條補上到期日或直接改判，而那件事這條檢查逼不出來。
- **🔴 帳本裡其實有兩種東西，而它們共用一個檔（2026-08-23 查證，這是積壓的真正形狀）。**
  逐條看過那 52 條無到期日的項目之後，性質跟原本以為的不一樣：**它們沒有一條是結構化的 claim**——45 條是敘述段落（「第一，…」「第二，…」，直接從當日 `postscript` 抄進來的），7 條只是文字裡出現「檢驗」兩字，**帶「檢驗方式：…（到期 YYYY-MM-DD）」格式的是 0 條**。全部落在 2026-07-29 至 08-18，也就是 08-19 升成 `ledger-v1`、08-22 強制帶 `due` 之前。
  **所以「補上到期日」這個做法是錯的。** 它們的自然時程差異極大——`obs-20260730-1` 的第一個檢核點是「今天晚間的 6 月核心 PCE」，隔天就該結案；而 `obs-20260729-1` 講的是九月的聯準會。**機械補 +3 個月會生出 52 個看起來合理、實際是編的到期日**，正是 `metrics-columns.md` 那句「填了看不出來是錯的」。
  **真正要做的是逐條研判**（每一條都要查當時的市場結果），那是一場獨立的工作、需要 web 檢索，不是維護順手能收的。在那之前 08-23 做了一件事：**把盲點變成每天看得到的數字**——`healthcheck` 的「觀察點記分板」那行現在會印「其中 N 條無到期日（逾期檢查對它們是瞎的）」。刻意**不報 WARN**，理由是 `healthcheck.py` 裡那條「一條永遠會響的檢查會讓人不再讀它，比沒有更糟」（**不在本檔**——這裡原本寫「本檔第 232 行」，而本檔那一行是表格分隔列）。
  **趨勢仍要盯**：佔比 08-16 是 47/56（84%）、08-22 是 61/71（86%）、08-23 是 64/74（86.5%）、**08-30 是 85/95（89.5%）**；分子一直沒動，**代表沒有人在回訪，只是分母一直長**。
  **⚠️ 那個 52 是對的，但它差點被一個「訂正」改錯（2026-08-30）。** 複驗子代理回報「52 是錯的、實際是 58」，主代理**當場照著改了兩份規格，之後才去讀程式**——而讀完發現**兩個數都是真的，只是定義不同**：
  | 定義 | 08-30 實測 | 誰在用 |
  |---|---|---|
  | 觀察中、**沒有 `due` 欄位** | **58** | 沒有人 |
  | 觀察中、**沒有 `due` 也沒有 text 回退** | **52** | **閘門**（`checks/podcast.py` 的 `_due_of()` 會解析 text 結尾的「（到期 YYYY-MM-DD）」） |

  差額 6 條就是靠 text 回退救回來的。**閘門真正瞎掉的是 52 條，所以三份文件記的 52 從頭到尾沒有錯**，錯的訂正已在同日撤回。
  **這一筆的教訓不是「子代理弄錯了」，是「那個數字沒有標定義」**——它只寫「52 條沒有到期日」，而「到期日」在這套系統裡有欄位與 text 兩個來源。**一個沒有標定義的數字，會主動招來錯的訂正**，而訂正看起來比原文更可信（它帶著新的量測）。**所以引用條數時一律寫明是哪一個定義**，並照 `MODIFY.md` 驗證第 2 項：**改任何指標或因果宣稱之前，先讀程式確認那個值實際接到哪條分支**——這次正是漏了那一步才差點寫錯。
  同理，本節上方那句「帶『檢驗方式：…（到期 …）』格式的是 0 條」也要標定義：08-30 實測 58 條無 `due` 欄位裡有 **6 條** text 含「到期」、**14 條**含「檢驗」，所以「0 條」只在「嚴格符合完整格式」這個定義下成立。
  **現況條數一律看 `healthcheck.py` 的「觀察點記分板」那一行**（它印的就是閘門那個定義），不要在文件裡各記一個會過期的數字。

- **🔴🔴 觀察中佔比仍在往上，而 2026-08-30 找到了機械成因：流程叫執行者不要改判。** 08-16 是 84%、08-22 是 86%、08-23 是 86.5%、**08-30 是 85/95（89.5%）**，最舊那一條仍是 2026-07-29、**32 天沒動**。這條先前三場都記成「沒有人在回訪」，**而沒有人問為什麼**。
  **答案在 `DIGEST-PROMPT.md` 第 5 步：它原本寫著「既有項目一條都不改」。** 而 `BRIEF.md` 第七節第四條把「有逾期未判的項目」列為**當期失敗判準** —— **兩句直接打架，而流程那一份才是每天被執行的那一份**。「怎麼判」的完整說明當時只在 `AGENT_BRIEF.md` 第 4 節（已降級、沒有人讀），**於是整套系統要求「不能有逾期未判」，卻沒有任何一份執行中的文件寫怎麼判。**
  **08-30 已訂正**：分界線改成欄位——`id`／`date`／`text`／`due` 開帳後不得改寫（那是 claim），`status`／`verdict`／`verdictDate` 是判決欄位、有明確結果就該動；回訪程序補回流程正本第 5 步。~~**⏳ 下一場的第一件事是看佔比有沒有掉頭**~~

  **✅ 2026-09-02 結案：掉頭了，而且成因確認就是那條訂正。** 佔比 **89.5%（85/95）→ 84.6%（88/104）**，而分子動了是真的動：08-18 之後中斷 13 天沒有任何改判，**08-31 判了 2 條、09-01 判了 3 條**（`obs-20260729-1`、`-20260730-2`、`-20260731-2`、`-20260804-1`、`obs-2026-08-27-1`）。**訂正生效的時間差是一天。**
  **但同一天暴露了第二個成因**：09-02 那一輪自己**改判 0 條** —— 執行者掃過 86 條、只對 1 條做外部查證就依「保守判定，不確定就維持觀察中」收手。那條規則是對的，**問題是它讓「空轉」與「盡力查了查不到」在輸出上一模一樣**。所以 09-02 在流程第 5 步加了最低要求：**每輪至少處置三條，處置不等於改判，但要填 `lastReviewed` 並在回報具名**。當場實跑三條驗過（見上一條 ⏳）。
  **⏳ 下一場看兩個數**：`lastReviewed` 是不是每天都有三條、以及佔比有沒有繼續往下。**前者比後者重要** —— 佔比會被新增條數稀釋，而 `lastReviewed` 直接量「有沒有人在看」。
  > **這一條的形狀值得單獨記**：三場維護、兩週時間，每一場都正確量到了「分子沒動」，**而沒有一場去問「執行者被要求做什麼」**。指標一直是對的，缺的是把指標接到指令上。**量到異常之後的下一個問題不是「多嚴重」，是「誰該做這件事，他被告知要做嗎」。**

- **🔴 成本基線要改由「在 Mac 上跑的維護者」補，排程那一側補不了（2026-08-23 實測，未處理）。** 08-22 把那四欄從「人工抄」改成「讀逐字稿量」，方向是對的 —— **但那支在 Cowork 排程裡跑不動**。三條路全部實測過：①沙箱沒有 `~/.claude/projects`，腳本 `exit 14`；②逐字稿確實在 Mac 的 `local-agent-mode-sessions/<id>/.claude/projects/…`、**目錄結構與腳本預期完全一致、只差 base dir**，但 `request_cowork_directory` 明文拒絕（「That directory is Cowork's internal session storage… intentionally not accessible」）；③Cowork 原生的 `session_info__read_transcript` 只回訊息內容、**不含 usage 欄位**。
  **⚠ 但不要把這件事寫成「排程不留 transcript」** —— 那個結論 08-14 寫過一次（見本節上方那張五次修正表）、**08-16 就被推翻**，理由是「五次都是從外面找，沒有人從排程自己的沙箱裡找過」。08-23 從排程自己這一側找了，結論要說得更準：**逐字稿存在，只是這一側搆不到**。`Glob` 實地看到它在 `~/Library/Application Support/Claude/local-agent-mode-sessions/<帳號>/<工作區>/local_<階段>/.claude/projects/<專案>/<uuid>.jsonl`，子代理在同層 `<uuid>/subagents/agent-*.jsonl`。
  **而且這條基線離復活只差一行**：`measure_session_tokens()` 裡那段被開頭的 `return "", "", ""` 擋掉的程式，glob 樣式正是 `local-agent-mode-sessions/*/*/*/.claude/projects` —— 08-23 用 `fnmatch` 對實際路徑驗過為 **True**。**在 Mac 上量得到**（08-23 已實跑）。~~把 early-return 拿掉~~ **那個做法後來被否決** —— 它會回報整場而不是日報那一輪。（沙箱驗不了這件事，所以本輪沒有動程式，只記在這裡。）
  **佐證這是環境差異而不是資料不存在**：`kb-core/metrics/usage.csv` 原本只有 `2026-08-22,broker-research` 一列 —— 那是唯一跑在別種執行環境的系統。
  **✅ 2026-08-23 在 Mac 上實跑成功，這條基線已經有第一筆量測值。** 做法（兩行）：
  ```bash
  BASE=$(ls -dt "$HOME/Library/Application Support/Claude/local-agent-mode-sessions"/*/*/local_*/.claude | head -1)
  CLAUDE_CONFIG_DIR="$BASE" python3 ~/kb-core/tools/usage_report.py podcast
  ```
  **但整場的數字不能直接用。** Cowork 一個工作階段可以同時裝著排程日報與事後的維護對話，08-23 就是同一個 `.jsonl` 前段 03:07 日報、後段 08:40 起維護。再跑一次帶 `--since <維護起點>`，兩者相減才是日報那一輪：

  | | 整場 | 維護 | **日報那一輪** |
  |---|---|---|---|
  | eff_tokens_k | 30,744 | 24,133 | **6,611** |
  | 主線輪數 | 331 | 242 | **89** |
  | 子代理 | 7 | 2 | **5** |
  | 子代理輪數 | 229 | 169 | **60** |
  | subagent_tokens_k | 7,607 | 5,126 | **2,481** |

  **🔴 而這一量就推翻了自述值，幅度大到不能忽略**：同日主代理自述 `eff_tokens_k=276`／`agent_turns=27`／`subagent_tokens_k=471`，實測是 **6,611／60／2,481** —— **eff 低估 24 倍、子代理 token 低估 5.3 倍**，只有「子代理 5 個」報對。**08-21 的 4913 與 08-22 的 235 同屬自述，一併作廢，不要拿來畫曲線。**
  第一個可用的效率指標：`subagent_tokens_k ÷ transcript_kb` = 2,481 ÷ 297 = **8.4**（08-23）。
  **🔴 自動化還不能開，而且理由比之前硬。** `healthcheck.py` 的 `measure_session_tokens()` 開頭那個 early-return **要留著**：底下那段程式的 glob 樣式其實對得上（08-23 在 Mac 上確認），**但它的挑選邏輯是「挑最大的那一份、整份掃完」**，遇到日報與維護共用同一個工作階段時會回報整場的 30,744、也就是真實成本的 **4.6 倍**，`subagents` 還會變成 7 —— **而且沒有任何徵兆**。那是 08-09「挑錯就等於量錯」的變形：這次挑對了檔案，算錯了範圍。
  ~~**要開它，先給 `usage_report.py` 補一個 `--until`。**~~ **✅ 2026-08-23 已補**（`--until` 與 `--until-receipt`，後者直接取 `~/outbox/<目錄>/<date>.receipt.json` 的 `at`，沙箱以合成 fixture 驗過八種情況）。**但前置條件滿足不等於可以開**——還缺的是讓 `measure_session_tokens()` 自己去算那個界線並套用；在它會之前，那個 early-return 仍然要留著，否則回報的是整場。
  **在修好之前四欄一律留空**，已寫進 `DIGEST-PROMPT.md`、`MAIN.md` 第 1 步、`metrics-columns.md`。**`healthcheck.py` 缺欄會出聲，那則 WARN 現在是常態不是待辦。**
  > **這一條的形狀值得記**：08-22 那次是「換成量測」，而**換過去的那個量測在這個執行環境不存在**，於是結果比換之前更糟 —— 換之前至少每天有一個（錯的）數字，換之後因為排程副本沒同步到新指示，執行者照舊版填了自述值，**而新指示的存在讓沒有人去懷疑那些值**。`eff_tokens_k` 因此有 4913／235／276 三個相差 20 倍的值。**加一個量測之前，先確認它在目標執行環境裡跑得動。**

- **⚠️ `shows.json` 的 `hosts` 過期沒有回饋路徑（2026-08-23 補上路徑並改了兩檔，但機制仍靠人）。** 規則端是齊備的：`DIGEST-PROMPT` 與 `preamble` 第四節都要求 subagent 依稿判定、不符要具名回報，而且**確實照做了** —— 但回報進了對話，沒有進 `shows.json`。**08-22 八集有兩集不符、08-23 四集又有兩集不符，其中 bloomberg 連兩天報同一個問題。** 08-23 已處理：latentspace 有官方稿直接證明 Alessio Fanelli 整集未發言，換成 Vibhu；bloomberg 只有兩天觀察，**只加不減**（補 Isabelle Lee、Scarlett Fu）。回饋路徑寫進 `anchors.shows_lifecycle.hosts_stale_touches`。**但那仍然是「維護時翻回報、手動改」，沒有任何機制逼它發生**——下一次名單爛掉還是只會出現在回報裡。
  代價是複利的：`hosts` 會餵進轉錄 prompt（見第 5 節），名單越舊，Gemini 越容易把不在場的人標上去 —— 也就是說**它會製造第 6 節另一條記過的「合理人名張冠李戴」**，而那條的原文是「`shows.json` 的主持人名單只是提示」，等於預設它不準且不打算修。**兩條合起來看才知道問題在哪：一條說名單不準沒關係，另一條說不準的名單會誘發錯誤標記。**
  修的時候注意 `shows.json` **有兩份**（kb-core 與 `~/.podfetch/`，後者是 symlink），`healthcheck` 有一條專門對帳但在沙箱固定跳過，**所以要在 Mac 上改**。另外 Bloomberg 值得考慮拆條目 —— 「Bloomberg Money」與 Surveillance 主節目明顯是兩套常設陣容，同一個 `hosts` 欄位服務不了兩個。
  **2026-08-24 續**：又一集不符（tip 名單五人、實際只有 Stig Brodersen 一人主持），**但這一種依「只加不減」不必改名單** —— 他本來就在名單內，一天的觀察不足以砍掉四個名字。同日補進了 08-22 就該加而未加的 unhedged／Katie Martin（她有 02:03 稿內自我介紹當證據，**隔了兩天才進檔**）。**連續三天，三次都是人工接的**：機制仍然不存在，這一條不要當成已處理。可操作的下一步是把「多了人」與「少了人」分開 —— 少了要補（有稿內證據就該進檔），多了先觀察（需要連續幾天），**現在兩者混在同一條規則裡，所以每次都要重新判一遍**。
  **2026-08-30 續：bloomberg 同一檔第三次不符（08-22／08-23／08-30），而「多了先觀察」已經觀察夠久了。**
  當天實際是 Tom Keene 與 Scarlett Fu；名單六人裡 **Jonathan Ferro、Paul Sweeney、Lisa Abramowicz、Isabelle Lee 四人未出現**，
  其中 **Isabelle Lee 正是 08-23 才依「只加不減」補進去的**——補進去之後一次都沒出現過。
  **「只加不減」在單一節目上重複套用，會讓名單單調成長而永遠不收斂**：六人名單裡有四人連三次沒出現，
  而它每天都被餵進轉錄 prompt，也就是每天都在提高「合理人名張冠李戴」的機率。
  **所以把 `MAINTENANCE`／`anchors` 兩處都寫過的「Bloomberg 值得考慮拆條目」從括號裡的建議升成具名待辦**：
  Bloomberg Money（週五中午，Scarlett Fu ＋ Tom Keene）與 Surveillance 主節目是兩套常設陣容，
  一個 `hosts` 欄位服務不了兩個，而**這正是四人長期掛在名單上卻不出現的結構原因**——
  不是名單過期，是名單在描述兩個不同的節目。**拆完之後「只加不減」才會收斂。**
  （08-30 那一集是 Surveillance 主節目時段但節目自報三次是 Bloomberg Money，
  所以拆條目之前要先確認 `showKey` 這一層分不分得出來——分不出來就只是把問題換個地方放。）
  **這一輪照舊沒有改 `shows.json`**（沙箱只看得到一份，改了會安靜製造漂移）。
  **2026-08-31：bloomberg 連續第四次不符，而這次是新的形態，值得單獨記。**
  該集標題自帶 `(Sponsored Content)`，實際是 Prudential 與 Bloomberg Media Studios 製作、
  置入 Bloomberg 播客的贊助單元《The Great Client Transfer》，主持人是 **Maggie Lake**——
  名單六人**一個都沒出現**，包括 08-23 才剛因為同一個問題補進去的 Isabelle Lee 與 Scarlett Fu。
  **所以「只加不減」在這一檔已經漲到六人而命中率是零**，這是拆條目之外的第二個訊號：
  **同一個 `showKey` 底下混進了「不是這個節目」的內容**，而拆 Money／Surveillance 兩套陣容
  也接不住贊助單元——它根本不屬於任何一套。`episode_admission` 那一層目前只擋
  預告與 trailer，**擋不掉業配單元**，而它在指標上完全正常（18 分、completeness 1.03、status OK）。

- **🔴 循環重複目前沒有任何機械檢查涵蓋得到（2026-08-23 首記，2026-08-30 擴大且升級，未修）。** **原本記為「句級跳針」，那個名字比現象窄**——真正的形態是「連續數行在同一句或少數幾句之間繞圈」，**週期 p 可以大於 1**。`podcast_dupmap.py` 對 08-23 的 mib 判「乾淨」，而該集 `[37:08]`–`[37:17]` 同一句連續出現 13 次、合計約 324 token —— **高於 `block_repeat_min_tokens`（150）**。**成因不是門檻，是聚合**：`block_repeats()` 的合併條件 `a == cur[0] + cur[2]` 要求重複的兩端同步前進，而跳針每遇到下一個副本開頭，`seen[g]` 回傳的仍是第一份的位置、`a` 因此回捲（實測 12 筆的 `first` 全部相同、`second` 每次 +27），於是 324 token 被拆成 12 個區塊（11 個 38 token ＋ 尾段 27），逐一低於下限被濾掉。
  **這個差別是可操作的**：照「門檻太高」去修會把下限調到 38，**那會讓每一句廣告台詞都觸發**（第 12 節記過兩次「初版對 9 集報 8 集」的同型錯誤）；真正的修法是把 `first` 相同的區塊加總或改成計次。`stutter_token_repeat`（同一 **token** 連續 20 次）也接不到，因為兩次重複之間隔著整句話。
  **要動之前先照 `MODIFY.md`「新增檢查」量一次誤報率** —— 全庫 91 集跑一次聚合後的分佈，確認正常情況不會也觸發。在那之前規則寫在 `preamble.md` 第九節，靠讀稿的人抓。

  **2026-08-30 續：一天三集，其中一集是還沒有人記過的形態，而且它讓「找同一句」這個描述本身失效。**
  當天 `podcast_dupmap.py` 對五集全判「乾淨」，實際有三集有內容缺失：**twentyvc 是 p＝2**——
  兩句 A、B 交替 20 次（`[06:25]`–`[08:59]`、約 504 詞），**相鄰兩行永遠不相同**，
  所以主代理當場另外寫的「同句連續重複」逐行檢查也是全盲的（實測 period=1 命中 0、period=2 命中 21）；
  mib 是 p＝1 但同一句連續 **443 次**（約 3,987 詞、蓋掉 7 分 22 秒）；allin 是 41 次與 15 次。
  **這一條的實害形狀是「規則指不出資料在哪」**：`preamble.md` 第九節明寫這一種「只有你讀得出來」，
  而它給的描述是「同一個短句連續重複 N 次」——subagent 拿著那句話去掃 twentyvc 會掃不到。
  四份文件（`anchors`／`preamble`／`DIGEST-PROMPT` 正副本）已於 08-30 全部改成「循環重複、週期 p ≥ 1」。

  **同日的合成量測給出一個不變量，它比原本的敘述硬**：`block_repeats()` 的
  **單一區塊固定是「句長 ＋ k − 1」token，與重複次數 N 完全無關**——L=27 時 13 次與 443 次
  都是 38 token，N 只增加區塊的**個數**（L=9 → 20、L=65 → 76）。
  **所以「324 token 被拆成 12 個區塊」這個寫法會誤導**：它讀起來像量級問題，
  而 3,987 token／443 次照樣一個區塊都碰不到 150。可否證的講法是上面那個不變量。
  **修法方向同時被驗證了**：按 `first` 聚合後 mib 8,398／twentyvc 344／allin 1,093 token，
  三者全部遠高於 150 —— **同一個修法同時蓋掉 p＝1 與 p＝2**，這一點值得寫進修法本身。
  **但誤報率仍然沒量**：08-30 只驗了合成資料與當日三集，那證明得了「抓得到」，
  證明不了「不誤報」，全庫 91 集那一步還是動手前的必要前置。

  **另外補一個沒有人記過的第二訊號**：`warnings_sources` 的「段級語速偏離本集中位數」
  （印成「第 N 段語速高達 …，疑似重複跳針」）**08-30 接得到 mib 與 twentyvc**，
  只接不到 allin（該集 status OK、warnings 為空）。**所以「沒有任何機械檢查涵蓋得到」
  這句話只對 allin 那種形態完全成立**，其餘兩種有一個免費的定位訊號可用，先前四份文件都沒提。
  > **這一條也是「理由寫錯」的實例。** 08-23 主代理在日報回報裡寫的是「規模低於 150 token 門檻」，是複驗子代理去讀了 `block_repeats()` 才推翻的。**對讀文件永遠查不出這種矛盾，只有回去讀程式或實跑工具才會發現** —— 與第 12 節 08-22 那一列記的第②處同型。

  **2026-09-02 續：形態不是一種尺度，是三種，而 08-30 驗證的那個修法只蓋得住中間那一種。**
  當天八集裡有三集有循環重複，`podcast_verify.py` 與 `podcast_dupmap.py` **全判「乾淨」**：

  | 尺度 | 09-02 實例 | 08-30 的「按 `first` 聚合」修法接得到嗎 |
  |---|---|---|
  | **行內** | bloomberg TV：**單行 27,888 詞**，9 詞短句重複約 3,100 次，蓋掉 `[13:55]`–`[20:00]` 共 6 分 05 秒 | **接不到**——聚合仍走 `tokens_with_lines` 的行邊界，而它整個在一行內 |
  | **行級**（08-30 記的那一種） | oddlots：同句連續 **232 行**、約 1,624 詞、蓋掉 10 分 03 秒 | 接得到 |
  | **區塊級** | dwarkesh Cotra：一組約 **21 行**的內容塊循環約 20 輪、約 20,000 詞 | 未驗證，`dupmap` 把它切成 31 個各約 997 token 的區塊、逐塊嚴重低估規模 |

  **三件事因此要修正先前的記載：**
  1. **「連續數行在同一句或少數幾句之間繞圈」這個描述涵蓋不到頭尾兩種尺度。** 行內那一種**沒有第二行**，區塊級那一種是 21 行的內容塊。四份文件已於 09-02 改成三尺度表。
  2. **段級語速 warning 作為第二訊號的可靠度被高估了。** 上一段寫「08-30 接得到 mib 與 twentyvc，只接不到 allin」，**09-02 的 oddlots 也接不到**——蓋掉 10 分鐘、`status` 仍是 **OK、`warnings` 為空**。所以它是排序訊號、不是守門員。
  3. **三種尺度各有一個便宜的偵測訊號，而最有效的那個先前沒有人記過**：行內用**行數 ÷ 總字數**（正常一行 10–40 詞；該集 197 行對 31,812 詞），區塊級用**末端時間戳是否溢出片長**（該集跑到 `[12:13:28]` 而片長 141 分）。**行內那個比例是三者中最快也最可靠的**，09-02 就是靠它發現的。

  **⏳ 修法要重新設計，不能直接套 08-30 那個。** 按 `first` 聚合仍是行級那一種的正解，但**行內那一種要在切 token 之前、以「單行字數」為判準先攔一次**（例如單行詞數超過該集中位數的 N 倍）。這是**兩個獨立訊號**，符合 `MODIFY.md`「新增檢查要求兩個訊號同時成立」那條；**但全庫 91 集的誤報率仍然一次都沒量過**，那一步照舊是動手前的必要前置。

- **⏳ `DIGEST-PROMPT.md` 從來沒有叫組檔者產出 `quality{}` 四個子欄位（2026-09-02 複驗確認，未修）。** 該檔全文 `quality` 只出現 3 次、**全部是 `anchors.quality.*` 的路徑引用**，第 4 步組檔段一句都沒提 `completeness`／`status`／`speakerNote`／`timestampNote`，只靠「日期檔的完整形狀照 `BRIEF.md` 第二節」轉指。**這條先前被 `SYNC-CHECKLIST` 記成「流程正本『一度』全文沒提過它們」，而那個「一度」暗示已修復 —— 實際從來沒修**（該字已於 09-02 改掉）。目前沒出事的原因是組檔者確實會去讀 `BRIEF` 第二節，**但那是靠轉指、不是靠指令**，與 `trackId` 當初的形態相同。**09-02 沒有一起修，理由是它不在當場核可的四批範圍內，而改它要再做一次排程 `SKILL.md` 整份覆寫**（本場已做過一次，同一場做兩次會讓「哪一版是對的」變難追）。下一場一併處理。

- **⏳ 帳本回訪的 `lastReviewed` 欄位還沒有機械檢查（2026-09-02 新增）。** 09-02 在 `DIGEST-PROMPT.md` 第 5 步加了「每一輪至少處置三條觀察中項目，處置不等於改判、但要填 `lastReviewed` 並在回報具名」，**當場實跑三條驗證欄位寫得進去**（`obs-20260729-2/-3/-4`，其中 `-4` 改判部分應驗）。**刻意沒有同時加 `checks/podcast.py` 的檢查**，兩個理由：一是這個欄位還沒有分佈可看，照 `MODIFY.md`「先修量測、再修規則」該先讓它跑幾天；二是新增檢查要過 `fixture`／`near_miss` 契約，寫反會擋住**所有** kb-core 推送，不值得在同一場裡疊上去。**所以這條目前的執行者是讀回報的人**——照 `BRIEF.md` 第七節自己的標準，這句話要寫出來而不是假設它有人在跑。下一場看 `lastReviewed` 有沒有真的每天被寫，再決定要不要補檢查。

- **帳本的哨兵回訪從來沒有接上，而 `BRIEF.md` 第八節一直寫著它會（2026-08-23 標明，未接）。** `sentinel/report.md` 印的是「⏭️ `sentinel.ledger_overdue` 這個系統還沒有訊號帳本，該項未執行」。**而且不只是接線沒接**：`checks/sentinel.py` 讀的是 **list 型**的 `p["ledger"]`、每筆帶 `judge.type == "manual"`／`judge.at` 與 **ISO datetime** 的 `due`，而 `observations.json` 是 `{items:[{status:"觀察中", due:"YYYY-MM-DD"}]}` —— **兩者形狀不同，接上去還要一層轉接。** `anchors.observations.source` 寫的是待辦語氣（「接上去它就從 SKIPPED 變成真的在看」），而 `BRIEF` 第八節寫成了既成事實。08-23 已在 `BRIEF` 就地標明。
  > **這是同一份文件裡的同型錯誤第二次**：第七節第五條在 08-22 才剛就地標明「這條沒有對應的檢查，目前靠寫的人不會寫錯」，**隔一節又寫了一句沒有執行者的機制，而且沒有標**。

- **`SendMessage` 在排程工作階段不可用（2026-08-23，已記未修）。** subagent 交件之後無法追問，要改只能重派一個新的。08-23 為了重挑五條金句多開一個 subagent，**多花 77K token**。而 `preamble.md` 第一節明文預期 subagent 會反駁派工單（「派工單說的話你可以質疑」）—— **設計上就預期會有來回，而來回的機制不存在**。目前的處置是「派工單一次給足」，寫在 `DIGEST-PROMPT` 第 3 步。
- **✅ 已量測（2026-08-23）：A 類官方稿的取得率是 62%，而漏掉的那幾集集中在一個可指名的時段。**
  從 `data/*.json` 的 `source` 欄位盤點全部 24 集 A 類：**15 集走第一層（62%）、9 集第二層（38%）**。
  但 9 集不是同一回事，拆開之後只剩 4 集是真的漏掉：
  · **有正當理由的 5 集**——08-06 unhedged（休刊轉播，FT 路徑不適用）、08-07 macrovoices（官方稿尚未上架，正常時滯）、08-11 acquired（272 分鐘 > 120 上限）、08-12 dwarkesh（同上）、08-14 macrovoices（官方稿有取到、以 podfetch 為主）。**這 5 集的 `source` 都寫明了為什麼**。
  · **🔴 沒有理由、就是沒去抓的 4 集**：**08-20 unhedged、08-20 gsx、08-22 macrovoices、08-22 unhedged**。`source` 只寫「podfetch 逐字稿…」，對官方稿為何沒用**一個字都沒有**。
  **時間點把成因釘死了**：08-15 之前每一集 A 類都有交代，**斷點正好是 08-20**——規格拆進 kb-core 的那天，A/B 分類表從每日必讀的文件裡消失。08-22 查出來、08-23 修好（當天唯一的 A 類 latentspace 確實抓到了官方稿）。**所以這是一個有頭有尾的區間，不是持續性缺陷。**
  **補抓值不值得**：只有 4 集，而且 gsx 的官方稿應該還在、macrovoices 落後一週以上（08-22 那集現在多半已上架）、unhedged 需要 Chrome 登入 FT。**建議補 gsx 與 macrovoices 兩集就好**，unhedged 那兩集的成本效益最差。補抓要改歷史日檔，屬於獨立一輪。 那一層在 08-22 之前**執行者根本不知道自己該抓**（必讀的三份文件裡沒有 A/B 分類），而 `anchors` 又寫著上游 02:20 會處理——**一個不存在的步驟**。現在責任已經寫回 digest 那一輪，但**歷史上有多少集其實該用官方稿卻用了 podfetch 稿，可以從 `data/*.json` 的 `source` 欄位回頭數**，還沒數。這決定了「補抓」值不值得做。
- **⚠️ `~/.podfetch/` 不是程式目錄了——08-22 宣稱「當天全部改掉了」，08-23 查證發現沒改完（現已補完）。**
  漏掉的殘留：`AGENT_BRIEF.md` 第 2 節 4 處（**那是失效橫幅明文保留的權威區**）、`MAINTENANCE.md` 第 1／2／3 節 4 處，**其中最糟的是維護流程第 0 步還叫人跑 `bash ~/.podfetch/snapshot.sh`** —— 那支 08-22 已退役、現在只會印成功訊息，而 `MAIN.md` 寫的是「不再需要快照」。**同一套維護文件裡兩條相反的第 0 步，撐了一整天。**
  另外 `healthcheck.py` 有一處路徑**雙重過期**（`~/Documents/Claude/Scheduled/podcast-digest-daily/SKILL.md`：目錄 08-21 搬過、任務名也改過），而旁邊的註解把成因寫成「沙箱看不到 `~/Documents/`」——**在 Mac 上跑一樣是空的**。同一個 repo 裡 `snapshot.sh:85` 就同時試兩個任務名，**一支腳本知道搬過家，另一支不知道**。該欄本身是退休欄位，08-23 連同其他三個退休欄位一起停止填值。
  **這一條的教訓不是路徑，是「宣稱改完」這件事**：08-22 的登記簿寫得斬釘截鐵，而查證只花了一次 grep。 `ls -la ~/.podfetch` 的結果是：`podfetch.py`／`healthcheck.py`／`json2docx.py`／`fix-schedule.sh`／`snapshot.sh` **一個都不在**；`config.json` 與 `shows.json` 是指向 `~/kb-core/scripts/podcast/` 的 symlink；剩下的是 `gemini.key`、`state.json`、`podfetch.log`、`logs/`、`cache/`、`metrics.csv`。launchd 也證實了：`com.kenny.podfetch` 跑的是 `/Users/macmini/kb-core/scripts/podcast/podfetch.py`。**所有 `~/.podfetch/<某支程式>` 的路徑都是過期的**——包括 `MAIN.md` 的硬規矩、第 1 步的 healthcheck 指令、`MODIFY.md` 的驗證清單，當天全部改掉了。**這種過期不會報錯，只會「找不到檔案」，而那跟「這台機器沒裝」看起來一樣。**
- **✅ 已結案（2026-08-22）：`snapshot.sh` 退役。** 它抄的八個目標裡有七個現在住在 kb-core、有 git，而 `com.kenny.kbcorepush` 每 300 秒自動 commit＋push；排程 `SKILL.md` 也不再是孤本（它是 `DIGEST-PROMPT.md` 的副本，正本在 kb-core）。**2026-08-04 那次「SKILL.md 被整份覆寫、無法還原」的情境不會再發生。** 而**唯一真的在 git 之外的檔案，它從來沒有抄過**：`~/.podfetch/metrics.csv`。腳本改成只出聲、不建立任何東西——理由是本檔第 12 節自己記過的「空快照比沒有快照更危險」，**一個照舊印「快照完成」的退役腳本，會讓人以為還原點存在**。
- **✅ 已結案（2026-08-22）：兩份 metrics.csv 合併成一份。** `healthcheck.py` 寫 `~/.podfetch/metrics.csv`，而 `DIGEST-PROMPT.md` 第 8 步叫每日排程把人工四欄寫進 `kb-core/scripts/podcast/metrics.csv`——**兩個寫入者、兩個檔案、沒有仲裁者**。**⚠ 這一條的第一版把後果寫錯了**，原文是「兩組欄位從來不在同一列」——而實際跑合併腳本的試跑才發現：**兩份都是 22 天、kb-core 那份 08-22 這一列 `transcript_kb` 與人工四欄同時都在**。真正的後果是另一件事：兩份都是 22 天、內容大致相同，但**已有 6 天在特定儲存格上漂移**（`output_json_kb` 差約 3%、`brief_kb`／`segments_done`／`podfetch_minutes` 只有 `~/.podfetch` 那份有），而衝突時沒有人仲裁，`healthcheck.py` 的回填與排序也只作用在它自己那一份。**基線有兩份，就沒有一份是權威的。****順帶抓到主代理自己的一個錯**：08-22 那一列的 `transcript_kb=688` 與 `speaker_flags=5` 是手填的，而 688 來自 `du -sk`（算磁碟區塊、含檔案系統開銷），healthcheck 算的是實際位元組 654——**同一欄兩把尺**。合併規則因此改成兩類欄位勝方相反：人工四欄以 kb-core 為準（唯一生產者），其餘一律以 healthcheck 量的為準（同一欄要同一把尺）。合併腳本是 `kb-core/tools/podcast_metrics_merge.py`（一次性，做完可刪），實體檔留在 kb-core，`~/.podfetch/metrics.csv` 改成 symlink。**healthcheck 是就地寫入（`open(path,"w")`）不是 temp+rename，所以 symlink 不會被換掉**——這點在沙箱裡用暫存目錄實測過。
- **`shows.json` 有兩份，而沙箱通常只看得到一份（2026-08-22 記）。** `kb-core/scripts/podcast/shows.json` 與 `~/.podfetch/shows.json`，`healthcheck.py` 有一條專門對帳，但在 Cowork 沙箱裡固定跳過（`! WARN shows.json 兩份　有一邊不在`）。**所以在沙箱裡改 `shows.json` 會安靜製造漂移**——08-22 本來要在它裡面加 A/B 分類欄位，就是為了這個理由改成指向 `AGENT_BRIEF.md` 第 1 節。要動它請在 Mac 上動，或先確認兩份都掛得到。
- **`healthcheck.py` 的 brief 一致性檢查只涵蓋數值**。敘述性的自相矛盾（例如退援順序寫兩套）目前只能靠子代理比對抓，值得再想有沒有機械化的辦法。
- **🔴 已重新開啟（2026-08-30）：brief 又撐破 `Read` 上限，而這次兩塊權威區在截斷點之後。** 實測 33,977 字元／**28,507 token、超出 14%、截斷於第 405 行**，而失效橫幅明文保留的四塊權威裡，**第 6 節（基礎設施備忘）與第 8 節（變更紀錄）都在截斷點之後讀不到**。08-30 把第 3／4 節的抄本刪掉、第 8 節歸檔，**單次 `Read` 回傳全檔、無截斷提示**（複驗實測）。**這裡刻意不記字元數**——08-30 初版記了「26,121 字元」，而同一場稍後又補了三條規則回去，複驗量到 26,578，**當場就過期了**。這是「同一場裡量了之後又加字，紀錄停在中途值」的**第四次**（08-17 ②、08-18 各一次）。**現況一律用 `Read` 一次全檔看有沒有截斷提示，那是唯一可靠的量測，字元數換算只是估計。****下方那個 ✅ 與它立的完成條件都沒有被執行過**——這是同一條警告第三次被同一個方式違反（08-18 一次、08-30 一次），**而它每一次都寫著「加完要量」**。餘裕現在約 5%，仍然不寬。以下是 08-17 的原始記載：
- **✅ 已結案（2026-08-17 ⑤，已於 08-30 失效）：brief 一次 `Read` 讀得完了，但餘裕不到 1%。** 原問題：`Read` 單次上限約 25,000 token，而 brief 是 29,112——第 0 步「完整讀過 `AGENT_BRIEF.md`」在機制上做不到單次完成，**只讀到截斷處就往下走會漏掉第 4 節資料格式、第 5 節發布流程與第 7 節合規，那三節每天都要用**。這與 `08-17 ①`「`Read` 上限是 token 不是行數」是同一個機制，當時只套用到逐字稿、沒有回頭套用到 brief 自己。
  **下推毛額 −6,353 字元、回填 +1,612、全場淨額 −4,740**（34,381 → 29,641）。毛額拆開是：第 8 節歸檔 −1,749、第 2 節「四個不要改的設計」→ 第 4C 節 −2,518、刪過期抄本 −771、第 5 批 −1,315（第 1 節 BG2／Business Breakdowns 移出說明、第 3 節三段沿革敘述、`check_speaker_labels()` 五種訊號列舉與時間戳溢出實例、`index.html` 補色程序 → 第 5 節、以及把本節自己的 CHANGELOG 條目壓短）。**結果：約 24,990 token，實測單次 `Read` 不再截斷；08-18 排程回報第 17 項「brief 讀 1 次，未截斷」，實戰確認。**
  **但這是刀鋒上的通過，餘裕不到 1%（約 10 token）**——**任何一次往 brief 加字都可能把它推回上限之上**。**⚠️ 這條警告立下的隔天（08-18）就被自己違反**：往第 3 節加了約 300 字元的邊界聲明，當場實測 **25,120 token、截斷於第 417 行**。**加字的人正是寫下這條警告的人，而他沒有在加完之後量一次。** 所以把它寫成條件：**往 brief 加字之後、收工之前，一定要 `Read` 一次全檔，看不到截斷提示才算完成**——這是唯一可靠的量測，字元數換算只是估計。當天的補救是同批壓縮第 3 節的講者與時間軸兩段、把實例數字移進本檔，並依歸檔規則把 08-17 的 CHANGELOG 條目搬到第 11 節。
  **另一個一直沒被執行的減壓閥**：brief 第 8 節的歸檔規則寫「每次維護**開工時**把不屬於本場的條目搬走」，但 08-18 這場是**收工前才發現沒做**。開工時做，本場就有 700 字元的餘裕可用；收工才做，等於整場都在超載狀態下加字。**歸檔要排在第 1 步，不是第 4 步。**所以：①排程回報**第 17 項改成哨兵**（brief 讀了幾次，1 次與 2 次都算正常、照實寫；連續幾天讀 2 次就代表又長回去了）；②第 0 步的完成條件保留「看到截斷提示就接著讀完」那條分支，**不因為現在讀得完就拿掉**——這是刻意的，門檻兩邊都要能正確運作。
  **一個每場都會重演的機制**：下推毛額 −6,353，但同場寫進第 8 節與第 3 節的新文字回填了 +1,612，**淨額只有 −4,740、回填吃掉四分之一**。估瘦身量時要先扣掉它，否則每次都會以為省得比實際多。**08-18 複驗抓到本條原本把「淨額」貼上「三批下推小計」的標籤、再把第 5 批重複加一次**，數字自相矛盾——**毛額、回填、淨額是三個數，任何一個單獨出現都要標明是哪一個。**

- **`FILES.md` 的「唯一一處雙寫」宣稱與實況不符（2026-08-17 ② 稽核，未處理）。** 該檔寫「除了任務卡，任何第二份都是漂移」，但實測排程 `SKILL.md` 第 1／2／4 步另有 **9 處**在複製 brief 的事實細節而非指向章節：假零集外部對照（近乎逐字約 1,400 字元）、兩種去重、退援四層＋120 分鐘、FT 入口、重播規則、`last_run_utc` 語意、iTunes 退援指示、觀察點收錄標準、紅線＋cache-buster。**每一處都有事故史背書**（尤其假零集那段是 08-15 事故的直接產物），**所以純刪會提高風險**——正解是**反轉 source of truth 的方向**：流程當下需要的分支判斷讓 `SKILL.md` 當權威、brief 那邊改成 pointer，而不是反過來。這是重構不是清理，應獨立一輪做，並同步改寫 `FILES.md`。
> **已結案（2026-08-16）**：08-15 留下的三條待辦都已處理，改動見第 12 節 `08-16 ①②`。三條原文與結果如下，**留著是因為它們示範了「先問正常情況會不會也觸發」怎麼落到實作**：
> - 「0 集既是合法結果、也是失效態的症狀，目前無法區分」→ 加了 `discover()` 的健全性對照。**當初的顧慮是「門檻要用該節目自己的最長間隔而不是固定天數」**，實作時改成更省的做法：不挑特定節目，而是取**全部 23 檔所有集數裡最新的那一筆**與 `stale_feed_hours`（72）比。這樣既不必為每檔維護一組間隔，也讓門檻的依據變成一句可驗證的話——「23 檔裡有每個交易日都發的 Bloomberg，全部同時安靜三天不會發生」。零額外請求。
> - 「`discover()` 回空仍無條件推進 `last_run_utc`」→ 0 集一律不推進。當初列的兩個選項（不推進／推進到視窗起點＋一小段）取了前者，因為後者要多一個「一小段是多長」的參數，而它沒有可驗證的依據。
> - 「BG2 與 Business Breakdowns 更新頻率明顯下降」→ 08-16 移出，換成 Prof G Markets（週一至五每日）與 Money Stuff（每週五），總數維持 23 檔。**當初寫「若持續數月無更新再考慮」，實際提前動手的理由是使用者要的是頻率與討論度，不是等它們正式停更。**

---

## 7. 事故與決策檔案

### 2026-08-31　守衛擋對了，而旁邊那則警告叫人不要動作

**經過。** 08-30 的維護改了 `AGENT_BRIEF.md`（12:39）與本檔（12:55），**沒有提交**。隔天 03:34，日報草稿寫進 outbox 之後，publish 回 `exit 15 @ worktree-dirty`，並且**每 60 秒重複同一則回執**，直到 06:13 人工提交後才 `exit 0 @ already-published`。卡住約兩小時四十分。

**第一件事：08-24 那道守衛第一次上場，判對了。** 08-24 加 `dirty_outside()` 時，登記簿寫的驗收條件是「下一次維護留髒檔時，回執是不是一次 `15 @ worktree-dirty` 就停」——這次正是那個情境，而它停了。**對照組就在一週前**：08-24 同樣的髒工作區，舊版每輪先 commit 再倒在 rebase，三小時累積 188 筆推不出去的本地 commit。這次三小時的產出是同一則講得出成因的回執。**「重試」與「卡死」在回執上長得一樣，差別在有沒有人替你把它們分開。**

**第二件事，也是這次真正的新東西：`healthcheck.py` 的推送鏈 WARN 在整段期間都印著「dashpush 每 180 秒推一次，稍等再看」。** dashpush 08-20 就退場了。於是這則警告在**最需要動作的那兩小時四十分**，叫維護者等一個不存在的推送者。它甚至不是沉默——**它主動給了錯的指示**，而錯的指示比沉默更難懷疑，因為它看起來像檢查已經幫你判過了。

當天把它改成讀 publish 自己的回執（`latest_receipt()`，四個分支：回執非 0 印 `exit`／`stage`／`detail`；缺 `exit` 欄位就說判不出來；`exit` 0 講清楚 publish 只在有草稿時碰 git；讀不到回執分三種成因）。**設計判準是兩個訊號的雜訊程度不一樣**：「local 領先 origin」單獨看必然有雜訊（剛發布完的幾十秒本來就領先），回執才是第二個獨立訊號。

**第三件事，是這次最該記的，因為它發生在修正本身裡。** 初版修完之後，複驗子代理抓到兩個我自己寫進去的錯：

1. 註解裡寫了「九支 launchd 裡沒有它」——而 `kb-core/launchd/README.md` **明文禁止**在別處寫那個數字（實際 18 份 plist／16 列表格），理由逐字寫著「2026-08-21 一天之內這個數字錯了三次，因為它是表格數量的第二份副本」。**我讓它錯了第四次，而且是在一份正在修正別人抄錯的檔案裡。**
2. 更難看的是 `exit 0` 那一支。我寫「push 由 kbpublish 每 60 秒做一次，稍等再看，超過兩分鐘仍領先就查 publish.log」——但 publish 沒有草稿時是空輪次、完全不碰 git，且只 stage `data/`，所以「local 領先 ＋ 回執 exit 0」最可能的成因正是非 `data/` 的本機 commit，而它要等下一份草稿（最快隔天 03:00）才被順帶推走。**我修掉了「叫人等一個不存在的推送者」，然後在隔壁分支寫了「叫人等一個這一輪不會動作的推送者」。** 叫人去查的 `publish.log`，等著他的是一萬多行「空輪次，不是失敗」。

**教訓：修一則錯誤訊息時，最容易犯的是同一則錯誤。** 因為你的注意力全在「舊的哪裡錯」，而不在「我寫的這一句在什麼條件下成立」。可操作的版本是——**訊息裡每一句叫人「等」或「再看」的話，都要能回答「等的那個東西這一輪會動嗎」**。兩個錯都不是主線抓到的，是複驗子代理讀程式讀出來的；`MODIFY.md` 那條「改完再叫一次子代理獨立比對」這次是真的擋下東西了。

順帶：`exit` 欄位缺席時初版會落進 `exit 0` 分支、替回執宣稱它沒有的值。**防禦性判斷若防到的結果是編一個值出來，那比不防更糟。**

---

### 2026-08-17　自 brief 下推的細節（規則留在 brief，經過移到這裡）

**下推的判準**：事故細節只要**還在替某條規則提供動機**就留在 brief——刪掉理由，規則就變成沒來由的規定，執行者遵守的機率會下降。只有「**維護者才用得到的 meta 紀律**」與「**逐項的事後數字**」搬過來。

**2026-08-17 稽核的一個訂正**：前一天估「brief 第 1–7 節有 43% 是事故敘事」，那是**行層級的上界**（一行只要含日期標記就整行計入，而那些行同時含規則與敘事）。實際切分九個候選區塊只有 **2,529 字元、佔全檔 6.8%**，其中**七塊經逐塊判斷後判定該留**（113 秒預告佔滿當天兩集、跳針 28,122 個 I 讓完整度反向、wpm 不決定 `DEGRADED`、503 併進 `EXHAUSTED` 的事故鏈、時間軸雙訊號的設計理由等——這些都是規則的動機本身）。**「文件裡有很多歷史」與「那些歷史該搬走」是兩件事。**

#### iTunes US 商店快取的完整實例（自 brief 第 1 節下推）

2026-08-15 實測（**台北 03:50 那次查詢**）：不帶 `cb` 時 All-In 與 Bloomberg 都停在 07-31（落後 15 天），帶了 `cb` 立刻拿到 All-In 08-13、Bloomberg 08-14。

**引用這類快取實例時要連查詢時點一起寫**——同一支 AppleID 在幾小時後就會多出新集數（All-In 那集 08-14T20:11Z 發布，早上再查就變成最新一筆），**沒有時點的數字日後無法核對**。這是維護者寫紀錄時的紀律，不是執行者的規則，所以放這裡。

#### 「寫完再壓縮」的逐集數字（自 brief 第 3 節下推）

- **08-11**：五集有三集初稿超出上界後重寫——Odd Lots 8,257→6,434、Acquired 14,255→6,583、Bloomberg 3,642→2,983。
- **08-12**：又有三集——ILTB 11,611→6,485、Compound 5,546→3,987、In Good Company 2,769→1,789。

**每重寫一輪，整份逐字稿就被重新計費一次。** 08-12 把規劃單位從字數換成段數之後，08-14 十二集零重寫、每集成本減半，08-17 三集同樣零重寫。

---

### 2026-08-15　日誌說「沒有新集數」，那句話本身是對的——錯的是它看到的資料

08-15 凌晨 01:00，podfetch 準時執行、正常結束、日誌兩行：`視窗：08-13 16:30 → 08-14 17:00 (UTC)`、`沒有新集數。`。前一天有 12 集，這天 0 集。實際上窗口內有 10 集。

**根因在一行沒人懷疑過的字串組裝。** `itunes_lookup()` 組出的網址是固定的：同一支 AppleID、同一個 limit，就永遠是同一個 URL。iTunes 把回應**綁在確切的查詢字串**上快取，那份快取當時停在 07-31、落後 15 天。HTTP 回 200、JSON 結構完全正常、`discover()` 逐集比對 `releaseDate` 之後合理地判定「都比視窗起點舊」，於是回空陣列。`discover()` 的錯誤處理只在 `itunes_lookup()` 丟例外時寫 `! <key> 查詢失敗`——沒有例外，就沒有任何一行異常。

**為什麼三段排查抓不到。** 第 2 格明寫「有『沒有新集數。』就是真的 0 集，一切正常」。那條規則在設計時是對的：0 集日確實常見（週末尤其）。但它預設了「查詢結果可信」，而這正是本次失效的那一層。**「日誌說正常」與「真的正常」在這個失效態下不可區分**，任何只讀日誌的排查都會判成綠燈。當天定案靠的是外部對照——拿同一支 AppleID 帶與不帶 `&cb=` 各查一次，All-In 分別回 07-31 與 08-13。

**唯一的外顯訊號是執行時間。** 前一天光是輪詢 23 檔就花了 9 秒（`01:00:00` → `01:00:09`），這天從開始到「沒有新集數」只有 1 秒。23 次跨太平洋的 HTTP 查詢不可能在 1 秒內跑完，但**沒有任何機制在看這個數字**。

**它還會製造永久缺口，而且缺口的大小取決於失效持續多久。** `discover()` 回空時，程式仍**無條件推進** `last_run_utc`（`if not eps:` 分支裡就寫在 `log()` 下一行）。下一次的視窗起點因此是「這次的結束時刻減 30 分鐘」，那被跳過的 24.5 小時再也不會被涵蓋。當天最後一集發布於 `16:17`、新起點是 `16:30`，**差 13 分鐘全部落在窗口外**。復原是把 `last_run_utc` 回捲到前一次的值再重跑；72 小時 lookback 上限給的緩衝是兩個排程週期，超過就真的救不回來。

**教訓一：對外部 API 的「同一個請求會拿到同一份答案」是一個假設，不是保證。** brief 早在 08-05 就為 Substack 的 `/api/v1/archive` 寫過「一律要帶 `&cb=`」，甚至在同一段裡註明「與 iTunes 的 US 商店快取是同一類問題」——**知道是同一類問題，卻只在其中一邊套用解法**。跨系統的同型缺陷要一次掃完，不要只補當時踩到的那一個。

**教訓二：文件裡沒被執行過的解法，不算解法。** brief 從 08-03 起就寫著「換 GB 或 AU 商店重查」並附上 `itunes.apple.com/gb/lookup` 的寫法，還註明「實測 GB／AU 是即時的」。08-15 實測，**路徑前綴形式在本環境回空白內容**——那條退路在需要它的那天是不存在的。正確寫法是查詢參數 `&country=GB`。這與 08-14 學到的「最先該驗證的是最根本、最不像會錯的那個前提」是同一件事的另一面：**寫進文件的退路，要在不需要它的時候先跑一次**。

**教訓三：靜默失效要留下可被機器看見的痕跡。** 這次能被抓到，是因為有人覺得「12 集 → 0 集」不合理而去外部對照。若當天恰好是週末、0 集本來就合理，這 24.5 小時會無聲消失，而且沒有任何一份紀錄會顯示它曾經存在。**0 集是一個合法結果，也是一個失效態的症狀，兩者目前無法區分**——已列入第 6 節待辦。

### 2026-08-14　修對層次的那一次，以及一個看了三天沒看見的盲區

**連續三天測同一條規則，前兩次失敗、第三次成功，差別在改的層次不同。**

| 日期 | 改了什麼 | 重寫比例 | 每集成本 |
|---|---|---|---|
| 08-11 | 把「先估篇幅再落筆」寫進規格 | 3/5 | 57.6 萬 |
| 08-12 | 加強措辭、寫進任務卡 | 3/5 | 54.9 萬 |
| 08-14 | **把規劃單位從字數換成段數** | **0/12** | **28.3 萬** |

**前兩次改的是「要求」，第三次改的是「執行者手上的工具」。** 08-12 已經記過這個判斷（規則失效時先問它依賴的量測可不可靠），08-14 是它的驗收：段數達標 11/12、平均 2.6 次工具呼叫、無一集超過 5 次、成本減半。**這條規則現在有效，接下來就不要再動它了**——三次修改同一條規則，前兩次都是因為在錯的層次施力。

順帶把 08-08 那句「新架構是線性成長，集數多時才顯優勢」也驗了：12 集這天固定開銷佔比 20.1% → 10.4%。

---

**同一天發現的盲區比上面那件事更值得記，因為它已經存在很久而且完全隱形。**

TIP837 的回報裡有一句「時間戳最大到 227 分遠超 78 分鐘片長，章節切分改用主題轉換判定」。掃全庫 72 集之後：

| 日期 | 節目 | 完整度 | status | 時間戳溢出 |
|---|---|---|---|---|
| 08-11 | 20VC | 0.91 | **OK** | **+1888%**（最大 1,192 分／片長 59 分） |
| 08-14 | TIP837 | 0.98 | **OK** | **+224%** |
| 08-08 | Bloomberg | 1.19 | OK | +65% |
| 08-05 | Pivot | 1.14 | OK | +61% |

**兩集的時間軸整個崩壞，完整度正常、status 是 OK，沒有任何機制看見。** 08-11 那天的回報只說「20VC 有 21 條整段錯置訊號」——**講者檢查抓到的是副作用，時間軸崩壞本身隱形。**

**它為什麼要緊**：時間戳決定摘譯的章節切分。08-14 那集是子代理自己發現異常才改用主題轉換分節的——**又一次「執行者靠自覺補上規格缺口」，這是這套系統反覆出現的形態，每次都代表規格缺一條路。**

**修法與 08-07 講者檢查同構**：獨立欄位 `timestamp_notes`，不進 `warnings`、不影響 `status`。理由一樣——時間軸壞掉不代表內容缺漏（那兩集完整度 0.91／0.98），併進去會用一個錯的理由把好集數標成降級。

**但第二條規則差點重演 08-07 的雜訊問題。** 初版只看「大間隙」（相鄰時間戳超過 90 秒），對 72 集報 15 集——因為**長篇獨白與真跳段長得一模一樣**（時間戳只在換人時出現）。那正是講者檢查初版「九集報八集」的同一種失敗。改成**「間隙佔比 >30% 且完整度 <0.85」兩個訊號同時成立**之後，全庫只報 7 集，而唯一被間隙條件抓到的（08-14 MIB，22 分鐘無戳、完整度 0.73）正是子代理獨立確認確有兩處跳段的那一集。

**這裡有一個可以推廣的教訓**：我第一版把「與完整度一起看」寫進了警告訊息的文字裡，叫讀的人自己去對照。**能寫成條件的判斷就不要寫成叮嚀**——叮嚀會被忽略，而且每天叮嚀一次就變成雜訊。

---

**第四件事，也是這五天最該記的一件：我 debug 了四次，從來沒驗證過最根本的那個前提。**

`eff_tokens_k` 從 08-09 加入起連續空白六天。修正史：

| 次數 | 假設 | 結果 |
|---|---|---|
| 08-10 | 路徑是 `~/.claude/projects` | 錯（那是 CLI 的路徑） |
| 08-10 | 路徑是 `$TMPDIR/claude-hostloop-plugins/*/projects` | 錯 |
| 08-14 | 路徑是 `~/Library/.../local-agent-mode-sessions/*/*/*/.claude/projects` | **路徑對了**，但 70 個檔案沒一個落在 02:30–06:00 |
| 08-14 | mtime 窗口不可靠（App 續讀會推後），改用內容指紋 | 還是沒中 |

第五次才做對的事：`grep -rl "<排程 prompt 的指紋>" ~/Library/Application\ Support/Claude --include="*.jsonl"`——**唯一命中的是維護對話本身**（指紋出現在裡面，只因為我在那裡貼過排程 prompt）。

**結論：`local-agent-mode-sessions/` 底下只有互動式對話，排程執行根本不在本機留 transcript。** 四次修正全都建立在「那個檔案存在」這個從沒被驗證的前提上。**我從第二層（路徑對不對）開始查，把第一層（東西存在嗎）當成已知。**

> **這個結論在 2026-08-16 被修正了一半，界線現在是清楚的。** 08-15 的用量分析發現**沙箱掛載點讀得到 transcript**（`$HOME/mnt/.claude/projects/<專案目錄>/<uuid>.jsonl`，子代理在 `<uuid>/subagents/agent-*.jsonl`），那份報告的所有數字都是直接解析而得、無一估算。它留下一個可驗證的檢查點：「排程執行是否同樣留在這個位置」。**08-16 對帳完畢，答案是：**
> - **每個工作階段都能讀到自己的 transcript**，就在自己的沙箱掛載點裡。08-15 那份報告做得到，是因為那天日報產出、事故排查、維護全在同一場對話裡。
> - **但讀不到別場的。** 08-16 實測：本場的 `.claude/projects/` 底下只有本場那一個 `.jsonl`；`/sessions/` 下另有 70 多個其他工作階段的掛載點，**全部 `Permission denied`**。
> - **08-14 那個「掃 `~/Library` 找不到」的結論也沒錯**，它掃的是 Mac 上的互動對話目錄，與沙箱掛載點是兩個地方。三個看似矛盾的觀察其實同時成立。
>
> **所以已經確定的是：「只有當事人讀得到自己的」，事後任何人都救不回來**（沙箱隨工作階段銷毀）。08-16 的 `eff_tokens_k` 就是這樣永久遺失的。
>
> **但還有一件事沒有被驗證，不要把它當成已知（2026-08-16 標記）。** 兩份用量分析都寫「排程執行不留 transcript」，**而兩者的證據都只證明了「從別場讀不到」**——沒有人驗證過「排程執行在**它自己的**沙箱裡有沒有 `.jsonl`」，因為那個沙箱從外面看不見。**這正是 08-14 那個五次失敗的同型錯誤：把「我找不到」當成「它不存在」。**
> **唯一能回答的辦法是讓排程執行自己試一次**：在收工前 `ls` 自己的 `$HOME/mnt/.claude/projects/`，找到就量測並把數字寫進回報，找不到就在回報寫「找不到」（**當時是三欄；2026-08-17 ① 加了 `subagent_tokens_k` 之後是四欄，現行欄位一律以第 12 節「指標基線」那段為準**）。**兩種結果都有價值**——找得到就此不必人工填；找不到才算真正確認「排程不留 transcript」，此後可以放心寫死人工填。
> **若確認找得到，自我量測有一個必須標明的偏差**：讀檔當下最後幾個回合還沒寫進去，數字是**下限**。
**修法不是刪掉欄位，而是換來源。** 資料一直都在——每天的 token 分析報告有，只是機器讀不到。改成維護時人工抄入，`healthcheck.py` 只負責不要洗掉人工值、沒填時提醒。回填五天之後基線終於完整：

| 日期 | 集數 | 逐字稿 | 加權 token | 每集 |
|---|---|---|---|---|
| 08-09 | 5 | 412 KB | 9,700K | 1,940K |
| 08-11 | 5 | 490 KB | 2,883K | 577K |
| 08-12 | 5 | 369 KB | 2,746K | 549K |
| 08-14 | 12 | 656 KB | 3,399K | **283K** |

**「量不到就刪掉」與「留著空欄位」都是錯的答案**——前者丟掉真實存在的資料，後者假裝在追蹤。第三條路是問「資料在哪裡？」而不是「程式為什麼讀不到？」

---

**第三件事：基線有洞，而且洞是靜默的。** `metrics.csv` 缺 08-13 那一列——指標只在「跑 healthcheck 的當天」寫入，沒跑維護的日子就沒有紀錄。而 08-13 其實有 3 集、**其中一集 DEGRADED**，完全沒進基線也沒被任何人看到。healthcheck 現在會掃 `data/` 與逐字稿目錄的聯集回填缺漏日期（本次補了四天）。**一份有洞的基線在做趨勢比較時會安靜地誤導人**——這正是這個檔案存在的目的所要避免的事。

### 2026-08-12　規則沒有失效，是它依賴的量測失效了

**08-11 加了「先估篇幅再落筆」，08-12 五集仍有三集重寫——比例完全沒變（3/5）。** 成本 274 萬對 288 萬持平，但那天的逐字稿少了 25%，換算下來**每 KB 成本反而上升**。表面看是規則沒被遵守。

**實際上是規則要求執行者做一件它做不到的事。** 掃全庫 13 個日檔 74 集，比對子代理自報的 `chars` 與正文實際字元數：

- 偏差中位數 **0%**
- 範圍 **−38% 到 +81%**
- **42% 的集數偏差 20% 以上**

**這個分布的形狀本身就是答案。** 中位數 0 代表沒有系統性偏差——不是子代理習慣性高估或低估，而是**每一集用的尺都不一樣**（有的把英數詞組算一個單位、有的算字元、有的算法不明）。**一把無偏但誤差 ±40% 的尺，比一把有偏但穩定的尺更沒用**：有偏可以校正，雜訊不行。

當天三個實例把後果講得很完整：Dwarkesh 自報 5,900 而實際 8,709、Pivot 自報 5,948 而實際 8,045——**兩集都自以為在區間內，實際超上界近三成**；In Good Company 自報 1,789 而實際 1,087——**自以為超標所以壓縮了兩輪，實際上它一直低於下界**。三集都在「照規則行事」，三集都做錯，而且錯的方向不同。

**修法有兩層，缺一不可。**

**第一層是把記錄面的尺固定住**：`chars` 改由組檔時的 python 機械計算並覆寫，子代理填 `0` 即可。這解決了「事後判斷有沒有超規格」的問題。

**但這不解決寫作當下的問題**——子代理手上沒有 python。所以**第二層是把規劃單位換成它量得到的東西：段數。** 全庫 1,546 個段落的中位數是 198 字元（四分位 150–251），換算後每個層級都有對應的目標段數。**段數是下筆前就數得準的，字元數不是。**

**這件事的一般形態值得記住：一條規則失效時，先問它依賴的量測可不可靠，再問執行者有沒有遵守。** 08-11 那次我把原因歸給「寫作紀律」並加強措辭，隔天比例一模一樣——**加強一條建立在壞量測上的規則，只會讓執行者更用力地做錯事。**

**同一天的第二件事：重派壓縮比原本還貴。** ILTB 那集因 API 中斷後重派，**重派那次 36.2 萬，比原本那次 20.8 萬還高**，即使它完全沒讀逐字稿、只讀 41 KB 的既有 JSON——壓縮改寫本身就是反覆讀寫的工作。兩次合計佔全場 20.8%。因此加了成本門檻：**超上界 50% 以內直接收下並註明，超過才重派**。理由是 brief 本來就允許「超上限、說明理由」這種降級——**花 36 萬 token 去消滅一個被允許的降級，是拿成本換一個規格上並不要求的整齊。**

### 2026-08-11　三天前那句無法驗證的宣稱，今天有答案了

**08-08 做完 token 架構改造時寫下「粗估降 60–75%」，並在 08-09 自己標註「那是估算不是量測，至今無法驗證」。今天它被驗證了：同樣五集，970 萬 → 288 萬，降 70%。** 而且今天的逐字稿還多 19%（490 KB vs 412 KB），子代理佔比從 84% 降到 62%。

**成立的是那個機制判斷，不是那個數字。** 08-09 的分析結論是「成本的驅動是回合數不是資料量」——今天 Acquired 一集 310 KB 逐字稿（第二大的 4.5 倍）只花了 1.8 倍成本，正好反過來佐證同一件事。**降幅來自 08-09 加的工具呼叫預算，不是 08-08 的架構本身**；08-08 只做到「把資料隔離出去」，那是必要不充分。

**但這個勝利同時暴露了一個設計衝突。** 今天五集有四集超出呼叫預算，主因是**三集初稿超出字數上界後重寫**（Acquired 14,255→6,583）。而每重寫一輪，整份逐字稿就被重新計費一次——**寫作規格（字數上界）與成本規格（呼叫預算）在長節目上互相打架，而任務卡沒有告訴子代理該怎麼調解。** 子代理選擇了「先滿足字數、再吃成本」，那是合理的優先序，但兩者本來不必二選一：**先列骨架、先分配字數，就能一次寫到位。** 這條已寫進規格。

**另外三件當天記下的事：**

**① 子代理自報的呼叫次數會低報，而且低報幅度不小。** 自報 4／7／6／9／11，系統實測 5／7／6／11／18。Acquired 差了 7 次。回報第 13 項現在拿這個數字當成本監控依據，**所以規格已改為明講「這是下限不是實數」**——一個會系統性偏低的指標，如果沒人知道它偏低，就會拿它做錯決策。

**② 記分板的種子是我自己污染的。** 08-10 回填時把後記段落照單全收，連「以上內容為節目摘譯與整理，非投資建議」這種免責文句都收進去了。結果 08-11 六條改判裡有四條在處理這些雜訊——**記分板的價值在於「哪些判斷後來被證明對了」，混進不可能有真假的條目就只是稀釋它。** 已刪 3 條純免責條目並加收錄標準：新增前先問「這句話三個月後有可能被證明是錯的嗎」。

**③ 又一次「執行者靠自覺補上規格缺口」。** Acquired 273 分鐘，子代理自主判斷不去抓官方稿（`web_fetch` 上限會靜默截斷、拼接不切實際），判斷完全正確——**但規格裡沒有這條路**。這是 08-10 記過的同一種形態：當回報裡出現「這是我的自主判斷」，那就是規格缺條款的訊號。已寫成明確規則（>120 分鐘的 A 類直接用 podfetch 稿）。

### 2026-08-10　三個規則同時失效，但只有一個看得出來

**08-10 只有 2 集，而且是同一支 113 秒的〈Our Town〉預告**——Bloomberg News 把它同時推進 Bloomberg Surveillance 與 Odd Lots 兩個 feed。逐字稿合計 5 KB，是有紀錄以來最薄的一天。

**表面問題是重複，真正的問題是三條規則同時在同一天沒接住，而且只有第一條會被人看見。**

**① 去重比錯了欄位。** 現行規則讀「前一份日檔的 `url` 與 `title`」。這兩集**同一天**（不是跨日）、`url` 因 feed 不同而不同、日檔 `title` 是兩個子代理各自擬的中譯所以也不同——**三個條件全部避開**。而 manifest 裡它們的原文 `title` 都是 `"Introducing: Our Town"`、`durationMs` 都是 `113000`，**唯一能認出同源的兩個欄位，剛好都不在比對範圍內**。

這不是規則寫壞，是**規則的立論範圍寫死了**：當時 brief 寫的是「窗口重疊的代價是**相鄰兩天**會重複命中同一集」（該句已於 08-10 改寫）——去重從一開始就只為「跨日」設計，同日同源不在它的世界觀裡。**當一條規則附了它自己的理由，那個理由就是它的邊界；邊界外的同類問題不會觸發任何警訊。**

**② 字數下界沒有例外條款。** 素材只有 301 字，下界是 1,200 字。主代理寫了 986／501 字並把理由塞進 `source` 欄位——處置正確，但**規格裡沒有這條路**。原本只規定「超上限要說明」，低於下界完全沒寫，回報清單十三項也沒有對應項。**制度上，寫太短與偷懶在事後完全分不出來。**

**③ 子代理門檻不存在。** SKILL.md 第 3 步「每集派一個子代理」是無條件的，唯一豁免是「拿不到全文」。主代理當天判斷 2.5 KB 的逐字稿不值得派——省了數十萬 token，但那是臨場判斷，**明天換一個執行者就不會這樣做**。

**共同形態**：三件事都是「**主代理靠自覺補上了規格的缺口**」。當天回報看起來很正常，正是因為它補得好。**如果那天執行者剛好沒想到，這三個缺口會以三種完全不同的症狀出現（重複集數、灌水、成本暴增），而且都不會被歸因到同一個原因。** 教訓：**看到執行者「偏離規格但做對了」時，那是規格缺條款的訊號，不是執行者發揮得好。**

**還查出一件更難堪的事，而且當天修了兩次都沒修好**：08-09 才加的 token 量測，`eff_tokens_k` 連續兩天都是空的。第一次歸因是路徑——`measure_session_tokens()` 讀 `~/.claude/projects`，那是 Claude Code CLI 的路徑，而每日排程跑在 Cowork。改成同時搜 `$TMPDIR/claude-hostloop-plugins/<hash>/projects/` 之後在 Mac 上實測，**還是空的**。

**這裡的教訓不是「猜錯路徑」，是「猜錯沒有症狀」。** 留空看起來像「還沒有資料」，不像「壞了」——所以它可以安靜地跑很多天，而 `metrics.csv` 那三個欄位一直在那裡，讓人以為基線正在累積。**一個回傳空值的量測，跟沒有量測是同一件事，但它看起來像有。**

修法因此改變方向：不再繼續猜路徑，而是**讓失敗自己出聲**。量測不到時「每日指標」改報 WARN 並印出區分得出三種原因的訊息（找不到根目錄／根目錄下沒有 `.jsonl`／有檔案但都不在 02:30–06:00 窗口，最後一種附上最近三筆 mtime）。**下一次執行就會直接告訴我們該往哪裡查，不必再猜。**

### 2026-08-09　成本的驅動因素：當時判為回合數（2026-08-16 已訂正）

> **⚠ 本節標題原為「成本的驅動因素是回合數，不是資料量」，那個結論已於 2026-08-16 被推翻**（250 個回合的重算顯示成本呈尖峰狀、不隨回合序號單調上升；真正的驅動是**每回合帶進多少新內容**，見第 12 節）。**下面的分析與數字保持原樣，那是當時的實測紀錄；只有結論的措辭改了。** 回合數仍是好用的代理指標——因為多數回合都會帶進新內容。

**08-09 是子代理架構的首次實跑，結論一半成立一半不成立。**

成立的部分：424 KB 逐字稿**一個字都沒進主對話**，主對話只佔全程 16%，其中檔案操作僅 4.8%、五張摘要卡合計 2.1%。隔離設計是有效的。

不成立的部分：**五個子代理吃掉 84%**，平均每集 160 萬，最貴的 All-In 一集 225 萬。

**真正的原因用一個量測就講完了**：All-In 那個子代理跑了 70 次工具呼叫，而該集逐字稿只有 1,201 行——**Read 的上限是 2,000 行，一次就讀得完**。所以那 70 次不是被迫分頁，是讀完之後又做了 68 件事。最可能是為了滿足「金句必須是逐字稿實際出現的發言」而反覆 grep 核對，**但全文早已在它的 context 裡，那些核對一次都不必要**。

**核心機制**：逐字稿一進 context，之後每一次工具呼叫都把那 120 KB 再送一遍。70 次 ≒ 8.2 MB 重讀。**舊架構是「一份大 context 被重讀很多次」，新架構變成「五份小 context 各自被重讀很多次」，並沒有自動變便宜。**

**但方向仍然對**：新架構的成本是線性的（五集分五個 context，彼此不加乘），舊架構十集塞同一個 context 是平方式成長。**集數越多，新架構的優勢才顯現**——08-09 只有五集，可能還沒過損益兩平點。

**教訓有兩層**：

1. **優化前要先確認成本模型。** 我當初假設「資料量大＝貴」，所以把資料隔離出去；實際的模型是「context × 回合數」，隔離資料只解決了一半。**真正的槓桿是壓回合數**，而那需要在任務卡裡明講，因為子代理不知道自己每多一次呼叫要付什麼代價。
2. **沒有量測就不要寫降幅。** 08-08 的 changelog 寫「粗估降 60–75%」，那是估算，而且**因為舊架構從未被量測過，它至今無法驗證**。`metrics.csv` 已補上 token 欄位，但基線要從今天才開始累積。**估算要標明是估算。**

### 2026-08-09　指標校準：讓「正常」等於 1.0

起因是 08-09 有兩集標 `DEGRADED`，去查完整度發現是 1.44 與 1.53。掃過**單 token 與片語級跳針都是 0**——不是跳針，是 All-In 那類四人交叉搶話的節目本來就講到 238 字/分。

> **這裡先訂正一個當天寫錯、隔一輪才發現的因果（08-09 ②）**：那兩集的 `DEGRADED` **不是**完整度超標造成的，wpm 也**不決定** status。`podfetch.py` 的 `status` 只看 `warnings`，而 warnings 只有三個來源：字數低於 `MIN_RATIO`、段級語速偏離**本集中位數**（`REL_RATIO`／`HIGH_RATIO`，不看 wpm）、跳針剔除——**完整度 1.4 這種「超標」在任何路徑都不會產生 warning**。那兩集的降級來自段級相對檢查。**上修 wpm 反而讓段級重試門檻變嚴。** 所以底下這段校準的價值在「讓顯示的完整度可讀」，不在「減少 DEGRADED」。教訓：**改指標前先確認它實際接到哪條分支**，文件互相對讀查不出這種矛盾。

17 檔節目的實測語速中位數約 **200** 字/分，範圍 122–218（排除跳針污染樣本）。**原本全域設 165 的後果是：幾乎每一集的完整度都顯示 1.2–1.3，「超標」變成常態。** 當一個指標天天報異常，它就不再是訊號——真出問題時沒人分得出來。

修法：全域上修到 200，並讓 `shows.json` 可用 `"wpm"` 逐檔覆寫（已為 7 檔設定，如 ILTB 140、In Good Company 160、Odd Lots 185）。校準後 15 檔全部落在 0.87–1.09。

**教訓：閾值型指標要定期用實際分布回頭校準。** 這與 08-07 講者檢查那次是同一個道理——初版對 9 集報 8 集，等於雜訊產生器。**設計檢查時要問「正常情況會不會也觸發」，而且要在累積足夠樣本後回頭再問一次。**

### 2026-08-08　token 成本結構：問題不在讀了多少，在讀進哪個 context

**背景**：每日排程是一條長對話，**每次工具呼叫都會重送整個 context**。實測成本結構：10 集逐字稿 617 KB（≈15–20 萬 token）讀進主 context，之後每次 Write／bash 呼叫都再付一次；brief 68.8 KB 同理；寫完 229 KB 的 JSON 後，同一份內容又被 LLM 重寫成 82 頁 Word。

**三個修法與其原理**：

1. **子代理分集**：token 計費的關鍵不是「讀了多少」而是「讀進哪個 context、之後還有幾次呼叫」。逐字稿在子代理 context 只被讀一次就結束（子代理只做 3–4 次工具呼叫），成品走檔案系統回主流程，主 context 只收摘要卡。**資料量沒變，重複計費的次數變了。**
2. **json2docx 腳本**：LLM 輸出比輸入貴數倍，「同一份內容寫兩次」是最貴的浪費。凡是機械轉換（JSON→docx）都該是腳本，LLM 只寫一次事實來源。
3. **brief 瘦身**：固定輸入（每天讀＋每次呼叫重送）的膨脹是複利型成本。變更紀錄歸檔規則寫死在 brief 第 8 節末尾——**每次維護開工時把不屬於本場的條目移到本檔第 11 節**，否則 08-06 那次「重構成效兩天被吃回去」會重演。

**品質取捨（要誠實記著）**：交叉觀察過去是主代理讀完全部逐字稿後寫的，現在改用每集摘要卡（5–8 條具名論點）合成。**主代理再也看不到任何一句原文**——若某天交叉觀察品質明顯變淺，第一個該懷疑的就是摘要卡的資訊密度，修法是加厚摘要卡格式，不是退回讀全文。

**風險（新架構未實測）**：子代理並行寫 `/tmp/ep-*.json` 的組檔正確性、A 類節目在子代理裡抓官方稿的成功率（子代理有 web_fetch，但 Chrome 類工具未驗證——FT 已例外由主代理處理）、失敗重派的行為。**明天 03:00 是第一次實跑，後天要看回報第 12 項。**

### 2026-08-08　指標在最需要它的時候是反向的

**現象**：兩集標 `DEGRADED`，完整度分別是 **3.36** 與 **1.93**——看起來像「內容超標」，實際上是**內容嚴重缺漏**。The Compound 有一行是 28,122 個重複的「I」，Hard Fork 有 9,521 個「let's」。

**`spoken_words()` 直接數空白分隔的 token，跳針全部計入。** 於是失效越嚴重、數字越漂亮。**這比沒有指標更糟**——`completeness` 正是日報用來判斷「這集能不能信」的依據，而它在唯一需要它的場合給出相反的答案。

`HIGH_RATIO` 的逐段檢查有把它們標成 `DEGRADED`（機制有作用），所以不是完全沒訊號；**但寫進 front matter 與 manifest 的數字是垃圾**，而下游讀的是那個數字。

**修法**：新增 `collapse_loops()`，計字前把連續重複 20 次以上的 token 壓成一次，並單獨在 `warnings` 記一筆剔除量。實測 3.36 → 1.10、1.93 → 1.10，與人工評估吻合。

**教訓：設計指標時要問「這個指標在它要偵測的失效態下，會不會失效得比被偵測的東西更嚴重」。** 字數在「內容太少」時是好指標，在「內容重複到爆」時方向完全相反——而後者恰恰是 LLM 轉錄最典型的失效之一。

### 2026-08-08　時間戳雙重偏移：prompt 自己製造了誘因

**現象**：合併後的時間軸會往回跳。第 2 段標成 40:00–59:52，實際是 20:00–39:52；同一集的第 3 段卻是對的。

**根因**：prompt 寫「Segment 2 of 4, covering approximately 20:00-40:00 of the full episode」——**等於告訴 Gemini 這段的絕對位置，它就照絕對時間輸出**，而 `offset_timestamps()` 又加了一次 20 分鐘。

**關鍵在於它不一致**：同一次執行、同一集，第 2 段用絕對、第 3 段用相對。**所以只改 prompt 擋不住**——LLM 對格式指示的遵守率不是 100%，凡是「靠它守規矩」的地方都要有機械式的兜底。

**修法**：兩層。(1) 拿掉 prompt 裡的絕對時間範圍（那是誘因），改為明確要求從 00:00 起算；(2) 合併前看該段第一個時間戳離 0 近還是離 offset 近，取近的那個解釋——兩種輸出都能自動歸位。

**連帶影響**：08-07 回報的「`Speaker 7` 連續 20 分鐘」其實是這個造成的假象，不是講者標記問題。**一個 bug 會在別的檢查裡偽裝成另一種 bug。**

### 2026-08-07　驗證鏈只驗到一半，最後一段斷了卻報綠燈

**現象**：08-07 的日報寫檔、commit、push 全部完成（`local == origin` 同雜湊 `b542611`），`healthcheck.py` 的「推送鏈」報 PASS，**但網站兩小時後仍停在 8/6**。

**根因不是任何元件壞掉，是驗證範圍畫錯了。** 完整的發布鏈有五段：

```
寫檔 → commit → push → GitHub Actions → Pages
       └────── healthcheck 只驗到這裡 ──────┘
```

08-03 的 `auto-push.sh` 事故之後，我們得到「靜默必須是可辨識的狀態」這個結論，也確實補了 `updatedLabel` 驗證——**但只補在日報的執行流程裡，沒有補進 healthcheck**。於是每天早上巡檢會看到全綠，而網站是舊的。

**修法**：`healthcheck.py` 新增「線上狀態」檢查，帶 cache-buster 抓線上 `index.json` 比對。沙箱連不到 GitHub（403 tunnel），此時顯示 WARN 並提示到 Mac 上跑，**不會誤報成 PASS**。

**教訓：補了一個檢查之後，要問「它涵蓋到鏈的哪一段為止」。** 這次是同一個教訓在同一條鏈上往下游挪一格又發生一次——第一次是 push 沒跑，第二次是 push 跑了但 deploy 沒跑。**每次只補到「上次出事的那一格」，下一格就會是下一次的事故。**

### 2026-08-07　講者標記誤植：機器檢查抓得到什麼、抓不到什麼

**現象**：九集全部 `status=OK`、完整度正常，卻有五集的講者標記需要人工校正。失敗型態各異：整段標錯人（All-In 開頭 13 分鐘的 Jason 被標成 Chamath）、廣告與電台台呼被歸給主持人（MiB）、拆成 Speaker 1/2/3/4 且中途翻轉（MacroVoices）、三位來賓合併成一個（20VC）、人名誤植但代名詞對不上（Bloomberg 的「Secretary Yellen」配 he/his）。

**為什麼危險**：錯的名字會直接變成**對真實人物的不實陳述**——跨節目交叉觀察寫「誰主張什麼」時尤其嚴重。而且它比缺名字難發現得多：`Speaker 2` 一眼就知道要查證，一個合理的人名不會引起任何懷疑。

**兩道修法**：

1. **轉錄 prompt 改為保守標記**。預設 `Speaker N`，只有明確依據才寫真名；廣告與台呼標 `Announcer`；標籤不得中途改名。核心那句話直接寫進 prompt：*a wrong name is far worse than no name*。
2. **`check_speaker_labels()` 做機械式的可疑訊號偵測**，寫進 manifest `warnings`。

**校準過程本身是重點。** 初版用「名字不在 `shows.json` 的主持人名單裡」當訊號，結果 9 集報 8 集——**因為來賓本來就不在名單上**。一個對 89% 的樣本都響的檢查等於沒有檢查。改成偵測「標籤格式異常」（把整句台詞或廣告文案當人名）後降到 5 集，每一則都對應實際問題。**設計檢查時要先問「正常情況會不會也觸發」**，否則做出來的是雜訊產生器。

**明確的能力邊界，寫下來免得以後誤以為有覆蓋**：抓不到「合理人名張冠李戴」。All-In 那個案例的標籤是真實存在的主持人、格式正常、佔比正常，機械上完全看不出問題。**這一類只能靠讀內容時的上下文校正**（誰被直呼名字、誰在問問題、開場說了誰不在）。

### 2026-08-06　暫時性錯誤產生永久性後果，把修法的效果整個抵銷

**現象**：`prefer_lite` 上線後的第一天，Console 顯示 Gemini 3.6 Flash **22/10 RPD**（超限、跳出「You have reached a rate limit」），而三個 500 RPD 的 Flash-Lite 只用了 8、3、少量。**看起來像 `prefer_lite` 沒生效。**

**但它生效了。** 日誌第 15 行的池子順序確實是 Lite 在前。真正發生的事在後面 77 秒：

```
[01:01:23] gemini-3.5-flash-lite    本次停用（暫時過載）→ 改用 3.1-flash-lite
[01:02:04] gemini-3.1-flash-lite    本次停用（暫時過載）→ 改用 flash-lite-latest
[01:02:40] gemini-flash-lite-latest 本次停用（暫時過載）→ 改用 flash-latest
```

一次過載尖峰，把三個高額度模型全部打下線。之後 13 段裡有 10 段落在 10 RPD 的 Flash 身上。

**根因是 08-04 修 503 時我自己寫的**：把 `ModelOverloaded`（503）和 `DailyQuotaForModel`、`ModelUnavailable`（404）併進同一個 handler，全部 `EXHAUSTED.add(m)`。而 `EXHAUSTED` 在程式裡的宣告就寫著「本次執行中已確定日額度用完的模型」——**我把暫時性錯誤丟進了一個語意上專屬於永久性失效的集合**。

**修法**：拆成兩個 handler。503 走新的 `OVERLOADED` 冷卻表（預設 300 秒後自動回池），404 與日額度維持永久除名。`current_model()` 同時檢查兩者；若池內全在冷卻，等最早到期的那個而不是判定失敗。

**修法本身又需要兩道護欄，而且第一道還不夠**（08-06 當天的回歸檢查抓到）：「只冷卻不除名」讓輪換失去自然終點，供應端持續過載時唯一的收束方式是把當天所有額度燒到 429。先加了輪換次數上限 `max(4, len(pool) * 3)`，但**次數不等於時間**——每次輪換都可能等一輪 300 秒冷卻，18 次就超過一小時，而窗口只有兩小時。因此再加上單集牆鐘上限 `episode_budget_seconds`（1200 秒）。**同一個修法連續兩輪都少考慮一個維度。**

**教訓有兩層，第二層才是重點**：

1. **暫時性錯誤與永久性失效需要不同的資料結構，不只是不同的處置。** 共用一個集合就等於把語意也合併了。
2. **這是「修法本身製造了新失效模式」的第二次**——08-04 修 503 造成 08-06 的 Lite 被除名；更早 08-03 修時序造成的誤判也是同一類。**改動防護機制時，要問「這個修法在最壞情況下會怎麼失敗」，而不只是「它能不能解決眼前這個症狀」。** 兩次都是修法在正常路徑上有效、在極端路徑上反而放大傷害。

**還有一個觀測面的教訓**：08-05 我看到 Lite 的 TPM 是 51.27K，讀成「輪換正常分流」；08-06 Lite 的 TPM 只有 1.15K，才看得出它是被踢出去而不是在工作。**單看「Lite 有在用」不足以證明它在承擔主要負載，要看相對比例。**

### 2026-08-05　整份取代蓋掉了另一場維護的成果

**這是流程事故，不是系統事故，但代價一樣真實。**

08-03 有一場「同步巡檢」改了排程 SKILL.md 四個地方（給 iTunes 子代理補上 US 商店快取規則、補上第 7 節合規入口、回報清單補「寫不進 repo」、三段排查補第 3 格）。**08-04 我用 `update_scheduled_task` 送出一份完整 prompt 時，把那四項全部蓋掉了**，而且完全沒有察覺——`prompt` 是整份取代，不是局部編輯。

**為什麼沒被發現**：brief 第 8 節那則變更紀錄寫著「已補上排程 SKILL.md ⋯」，讀 brief 的人會相信它已經在了。**紀錄與實況脫鉤，而紀錄看起來更可信。** 是 08-05 的子代理獨立比對才抓出來（問題 7、8）。

**教訓有三層**：

1. **`update_scheduled_task` 的 `prompt` 是整份覆寫。** 送出前必須先 Read 目前的 SKILL.md，以它為基礎修改，不能拿自己記憶中的版本重寫。維護 skill 已經寫了這條警語，我還是踩了——**因為我以為自己就是上一個改它的人**。
2. **多場維護在同一份無版本控制的檔案上工作時，「我記得的版本」一定會過期。** 排程 SKILL.md 不在 git 裡，沒有任何機制會阻止覆蓋。
3. **變更紀錄宣稱「已同步」不等於真的同步。** 子代理比對要特別檢查：brief 說補進 SKILL.md 的東西，SKILL.md 裡真的有嗎。這一項已加進維護 skill 的比對清單。

**修法**：四項全部還原，並與 08-05 的新增一起寫回。

### 2026-08-05　兩個修法都成立，但 Google 把地板抽掉了

**驗證結果**：08-05 的 podfetch `[01:00:00]` 準時，**7 集全部拿到逐字稿**（08-04 是 3/6 失敗），只有 ILTB 一集 DEGRADED。Flash-Lite 的 TPM 從 1.2K 跳到 51.27K，證明 503 輪換確實在分流。⚠︎ 重寫規則也走通了：Bloomberg 日圓那集補回 6,012 字，跨檔搬移與 `index.json` 同步都正確。healthcheck 0 FAIL。

**但同一份 Console 數據顯示 Flash 3.5／3.6 的 RPD 從 20 掉到 10。** 修法生效的同一天，它所依賴的前提被砍半。

**這暴露了一個先前沒看清的量級問題**：08-05 預估 17 個請求，實際打出 **42 次**——放大約 2.5 倍。來源是三種重試疊加（503 換模型前 2 次、字數不足重試、輸出截斷重試）。在 Flash 有 20 RPD 時這個放大率還撐得住，砍到 10 就撐不住了。

**修法**：模型池順序反轉為 Lite 優先。**踩到的坑是「改設定沒有用」**——`build_model_pool` 一律先 `take(heavy)` 再 `take(lite)`，池子順序與 `model_preference` 無關，必須改程式。新增 `prefer_lite` 開關讓它可切換回去。

**教訓：外部額度是會變的前提，不是常數。** brief 把實測額度寫成表格是對的，但沒有任何機制會在它變動時提醒——是使用者主動截圖才發現。`healthcheck.py` 也檢查不到（它只比對 brief 與 `config.json`，兩邊都是我們自己寫的）。**目前只能靠人定期看 Console。**

**另一個獨立的失效模式**：ILTB 那集第 1/4 段回傳 **0 字**，重試後仍空——不是 503、不是額度，是模型直接吐空。字數檢查抓到並標記 DEGRADED，機制照設計運作。這是第三種失敗態，值得記著。

### 2026-08-04　503 死守重試，把 Flash 額度燒光卻放著 Lite 不用

**現象**：08-04 首跑 6 集，3 集 FAILED，日誌結尾是 `HTTP 503 This model is currently experiencing high demand`。

**光看日誌會得到錯誤結論**——它長得像「Google 那邊當機，無能為力」。要看 AI Studio Console 的當日數據才知道不是：

| 模型 | RPD | TPM |
|---|---|---|
| Gemini 3.6 Flash | **21/20 超限** | 30.8K/250K（12%） |
| Gemini 3.5 Flash | **21/20 超限** | 37.7K/250K（15%） |
| Gemini 3.5 Flash Lite | 9/500 | 1.2K/250K |
| Gemini 3.1 Flash Lite | 1/500 | 17/250K |

**兩個 Flash 各 20 RPD 全部燒光（合計 42 次請求，遠超日誌估計的 15 次），而有 500 RPD 的兩個 Lite 只用了 9 次和 1 次。** 多出來的請求就是重試——重試同樣計入 RPD。

**根因在程式碼**：舊版 `transcribe_one` 只有 429 日額度會丟 `DailyQuotaForModel` 觸發模型輪換；500／503 走的是 `time.sleep(20 * (retry + 1))` 然後 `continue`，**在同一個模型上把 5 次重試耗完（約 200 秒）後直接 `RuntimeError`，整集判定 FAILED**。於是變成一邊燒 Flash 的稀缺額度、一邊放著 491 次 Lite 額度不用。

**當時的修法**：新增 `ModelOverloaded`，503 重試兩次就丟出，接進既有的輪換路徑（與 404／日額度同一個 handler）。那 3 集幾乎確定換個模型就會成功。

> **⚠︎ 這個修法在兩天後被推翻。** 「與 404／日額度同一個 handler」正是 08-06 事故的根因——503 因此讓模型永久除名。現行做法是 503 走獨立的 `OVERLOADED` 冷卻表，見上一篇。**本段保留的是當時的判斷，不是現行規格。**

**教訓有兩層**：

1. **「暫時性錯誤」與「額度耗盡」需要不同的處置，混在一起會兩邊都做錯。** 對前者該換資源，對後者才該退讓。
2. **只看應用層日誌會誤判成外部故障。** 日誌說「模型忙」是真的，但「我們把重試打在最稀缺的資源上」只有從供應商端的用量數據才看得出來。**排查配額類問題時，應用層日誌不足以定案，要去看 Console 的實際用量。**

### 2026-08-04　去重會把自己補回來的集數擋掉

3 集轉錄失敗時，日報照規則寫成 `⚠︎ 全文摘譯待補` 的佔位。而 podfetch 失敗時**刻意不推進 `last_run_utc`**，下次執行必定重抓同一集——這是正確的設計。

但去重規則比對 `url`／`title`，會把佔位當成「已收錄」而略過，**補回來的完整逐字稿因此永遠進不了網站，而且毫無徵兆**。兩個各自正確的機制組合出一個必然發生的漏洞。

**修法**：`source` 以 `⚠︎` 開頭的集數不算已收錄，拿到全文要重寫並跨檔搬移。同時明訂 `⚠︎` 是機器讀的標記，不可改寫成別的格式。`healthcheck.py` 也加了「待補全文」檢查主動列出這些集數。

**教訓：兩個都對的規則放在一起可能是錯的。** 這類漏洞在單獨檢視任一機制時都看不出來，只有推演「A 發生之後 B 會怎樣」才會浮現。

### 2026-08-03　把「修改後的狀態」誤讀成「修改前的證據」

**這裡原本有一篇「podfetch 延後補跑」的事故紀錄，已於同日第四次巡檢刪除，因為它的根因判定是錯的。** 保留這則短紀錄，是因為它示範的判斷失誤本身值得記住。

**現象**：08-03 的 podfetch 日誌開頭是 `[07:00:01]` 而非 `[01:00:0x]`。

**當時的判定**：執行 `PlistBuddy -c "Print :StartCalendarInterval"` 印出 `Hour = 1`，據此認定「plist 一直都對」，把根因歸給機器睡眠導致 launchd 延後補跑。

**實情**：當天 07:00 的 plist 就是 `Hour = 7`——那也是當時 brief 第 6 節白紙黑字記載的設計值——是**當日稍晚才由使用者改成 1**。調查時讀到的 `Hour = 1` 是修改後的狀態，卻被當成修改前的證據。`[07:00:01]` 這種「整點加一秒」正是 `StartCalendarInterval` 準時觸發的特徵，本來就指向設定值。

**教訓**：在一個「正在被修改」的系統上除錯，**現況不等於事發時的狀況**。查任何設定前先問一句「這個值今天有沒有被人動過」；若動過，它只能證明現在，不能證明當時。旁證要從不會被回溯改寫的東西上找——日誌時間戳、檔案 mtime、當時版本的文件。

**`pmset` 那組電源設定與這件事無關，獨立保留**：01:00 的工作在會睡眠的 Mac 上本來就需要喚醒與不睡眠保障，那部分的決策是對的，見第 9 節。

### 2026-08-03　`auto-push.sh` 靜默失效

8/2 18:20 腳本被改成只含 `REPO="$HOME/advisory-knowledge-hub"` 的單一 repo 版，本 repo 從此完全脫離自動推送。**整個失效過程沒有任何外顯徵兆**——launchd 回報 exit 0、`push.log` 沒有新行（原版在「無變更」時直接 `exit 0` 且不留紀錄）、`data/` 檔案照常寫入、排程任務照常回報成功，只有網站悄悄停在舊版。發現方式是比對 `.git/logs/HEAD` 最後一次 commit 的時間戳與檔案 mtime。

修復後的腳本改為多 repo 迴圈、以 `continue` 而非 `exit` 跳過個別 repo、**無變更時也寫入 log**，並在推送成功後記錄 HEAD 短雜湊。

**教訓：「靜默」必須是可辨識的狀態，不能與正常運作無法區分。** 這條原則後來也用在別處——日報的 `notifyOnCompletion`、podfetch 的字數檢查、FT 的正文長度檢查，全都是同一件事的不同版本。也因為這次，`AGENT_BRIEF.md` 第 5 節的上線驗證必須比對 `updatedLabel` 而不能只看 `days[0].date`（事故當天 `days[0].date` 早就是當天日期，光看它會被騙過去）。

### 2026-08-03　執行時段從 09:00 改到 03:00

**動機**：一是希望起床就看得到成果；二是與 `advisory-dashboard-daily` 拉開 token 用量（那條線 cron 設 07:30，實際帶 jitter 約 07:35 觸發）。

**代價是結構性的**：台北 03:00 ＝ 美東前一天 15:00（EDT），而主要節目全部集中在台北 04:00–06:30 落地（Bloomberg TV 04:01、Bloomberg Money 04:25、MiB 05:00、All-In 06:23）。這些在 03:00 那一刻還沒發布，所以每天的日報固定收不到前一晚的美東晚間集數。All-In 因此大約在發布後 21 小時才進日報，而不是原本的 3 小時。podfetch 在 01:00 跑，看到的更早（美東 13:00 為止），13:00–15:00 EDT 之間發布的同樣順延一版。

**但沒有任何集數會遺失**：podfetch 視窗以 `last_run_utc` 為起點，日報又會去重，順延的集數下一版必定收得到。

### 2026-08-02　YouTube 字幕全面失效，改建 podfetch 管線

當天 YouTube 轉錄稿在本機 Chrome 上全面失效——面板可開啟但內容永遠停在載入中，`timedtext` 回傳空字串，InnerTube `get_transcript` 回 `FAILED_PRECONDITION`。在兩支不相干影片上重現，確認為環境層級故障，當日六集全部沒有逐字稿。

**事故暴露的真正問題是架構**：全文取得被綁死在「Chrome 開著 ＋ YouTube 已登入 ＋ YouTube 沒改版」三個條件上，任一失效就整天報廢，而且是安靜失效。

**修法**：改用 podcast 原始 MP3 ＋ Gemini API 轉錄。iTunes Lookup 的每個 `podcastEpisode` 都帶 `episodeUrl`（直接 MP3 網址），20 檔全部確認可取得——**這套系統從一開始就不需要 YouTube**。副作用是拿到了 YouTube 自動字幕從來給不了的維度：`shows.json` 預先寫入主持人名單，轉錄 prompt 要求 Gemini 用真名，跨節目交叉觀察因此能具體到人。

### 2026-08-02　Gemini 免費層：瓶頸是 RPD 不是 TPM

> **以下是 2026-08-02 當時的實測值，不是現行額度。** Google 已於 08-05 把 Flash 3.5／3.6 的 RPD 從 20 砍到 10（Lite 仍為 500，倍數因此從 25 倍變成 50 倍），模型池順序也已反轉為 Lite 優先。**現行數字見本檔第 4C 節第 3 項與本節 08-05 那篇**（2026-08-17 ② 之前在 brief 第 2 節，已下推）。 保留這篇是因為「瓶頸是 RPD 不是 TPM」這個判斷本身仍然成立，而且它是後續所有設計的起點。

從 AI Studio Console 實測（同一專案內）：

| 模型 | RPM | TPM | RPD |
|---|---|---|---|
| Flash（2.5／3／3.5／3.6） | 5 | 250K | **20** |
| Flash-Lite 2.5 | 10 | 250K | 20 |
| **Flash-Lite 3.1／3.5** | 15 | 250K | **500** |

一般 Flash 每個模型每天只有 20 個請求，而 TPM 上限 250K 實際只用到 12%（約 30K）。**這推翻了直覺——正確策略是「少而大」的請求，不是「多而小」。** 因此 MP3 一律走 Files API（上傳免費、不計入 RPD，也繞開 inline base64 的 20MB 上限），並組成多模型輪替池。

新版 Flash-Lite 的日額度是一般 Flash 的 **25 倍**，是整個設計裡最重要的一個數字。**不要把 Lite 名額拿掉**，那等於自願放棄 25 倍額度。

**但段長最後不是由 RPD 決定，而是由輸出上限決定。** Lite 有 500 RPD 之後 RPD 就不再是瓶頸，真正咬人的是 `maxOutputTokens`：30 分鐘音檔的逐字稿約 6,000 字≒8,000+ token，很容易撞天花板。所以段長收斂到 20 分鐘（約 4,000 字），425 分鐘的一天約 22 個請求。

其他實測結論：`gemini-flash-latest` 目前對應 Gemini 3.6 Flash，屬 20 RPD 那一組；`gemini-2.5-flash` 已對新專案關閉（回 404）；`gemini-2 flash`／`gemini-3.1 pro`／`gemini-2.5 pro` 在免費層是 0/0。初次測試撞 rate limit 的兩個原因：自動挑到 preview 模型（免費額度比穩定版嚴格得多），以及三個請求並行瞬間撞 TPM。

**`max_output_tokens` 必須明確指定，這是最容易重蹈的坑。** 預設約 8,192，而 Gemini 3.x 的 thinking token 也算進輸出預算，實測導致多段被腰斬（一致卡在 5,600–6,200 字，最慘一段只吐出 6 個字）。同時以 `thinkingConfig.thinkingBudget = 0` 關掉 thinking——逐字轉錄不需要推理。

**模型池會在執行中「瘦身」，這是正常的。** `state.json` 的 `model_pool` 記的是當次跑完後仍可用的模型，不是設定值。08-02 的實際軌跡：`gemini-3-flash-preview`（429）→ `gemini-2.5-flash`（404）→ `gemini-2.5-flash-image`（額度滿）→ `gemini-flash-latest`（跑一段後用盡）→ 最後三個 Flash-Lite 完成全部六集。**看到 `model_pool` 只剩 Lite 不代表設定壞了**，代表溢流機制正常運作。

**額度用完時的行為**：429 會照 Google 回傳的 `retryDelay` 等待；判定為日額度耗盡則丟 `QuotaExhausted` 停止本次執行，**已完成的段落留在 `~/.podfetch/cache/`，下次直接沿用不重跑**，未完成的集數不寫進 `seen`、`last_run_utc` 也不推進，下次視窗仍涵蓋得到。若要加量再考慮付費，約 US$0.12／小時音檔。

### 2026-08-02　切段改用純 Python

原本用 ffmpeg，但這台 Mac 沒有 Homebrew——為了一個切檔動作要求安裝套件管理器不合理。改為解析 MPEG Layer III 的 frame 標頭、在 frame 邊界切檔，不重新編碼，因此零外部相依。第一個 frame 若是 Xing／Info／VBRI 標頭會跳過：它記的是整個原檔的長度，複製到第一段會讓解碼器誤判時長。若系統剛好有 ffmpeg 則優先使用（順便降到 16kHz 單聲道 32kbps，上傳量小很多），失敗時自動退回內建切檔器。

早期的 9 MiB 單段上限是 inline base64 時代的產物（膨脹約 1.34 倍須低於 20MB 請求上限），改走 Files API 後已放寬到 `max_chunk_mb: 48`。

### 2026-08-02　固定窗口造成無法察覺的缺口

podfetch 舊版用固定 26 小時窗口，漏跑一天就會產生缺口——7/31 22:53 到 8/1 15:24 之間整段掉出去，而且完全沒有徵兆。新版改為**以 `last_run_utc` 為起點**往前推 30 分鐘重疊，上限 72 小時。**不要改回固定窗口。**

（日報那一側仍是 26 小時窗口，因為它有去重機制兜底；兩者的窗口邏輯不同是刻意的。）

---

## 8. YouTube 字幕操作要領（2026-08-02 起已知失效，僅存查）

真的掉進這條退援路徑時才需要看。每集最多試 5 分鐘。

**先找到影片**：用 `https://www.youtube.com/results?search_query=%22<完整標題>%22`（加引號精確比對）比逐頁捲頻道 `/videos` 快得多。用 `read_page` 帶 `ref_id` 取得結果的 `href` 拿到 `watch?v=` 連結，並核對片長與 Apple 的 `trackTimeMillis` 是否吻合。

**取字幕的實際可行順序**：

1. `navigate` 到 `watch` 頁，等 4 秒
2. `find`「`...更多內容`」按鈕並點擊，展開影片說明，等 3 秒
3. 用 `computer` 的 `scroll` 往下捲約 10–12 格，直到出現標題「**字幕記錄**」與藍色按鈕「**顯示轉錄稿**」。先 `screenshot` 確認位置
4. **用座標點擊**那顆「顯示轉錄稿」按鈕，等 6 秒
5. 按 `Home` 回頁面頂端，等 3 秒，再 `get_page_text`
6. 「字幕記錄／搜尋轉錄稿」之後、到片尾台詞之前的整段（含時間戳）即為逐字稿；移除時間戳行後合併

**已知的坑**：

- 用 `find` 拿到的「顯示轉錄稿」ref 直接點**經常沒有反應**（頁面上有兩個同名節點，且影片播放會讓描述自動收合）。要捲到該區塊、用座標點才穩。
- 影片右上角的「⋯」選單**沒有**轉錄稿選項，只有下載與檢舉，別浪費時間。
- 想用 `javascript_tool` 讀 `ytInitialPlayerResponse.captions` 再 fetch 字幕檔：**行不通**。YouTube 有 Trusted Types，`DOMParser` 會被擋；改用 `&fmt=json3` 回傳空字串（baseUrl 需要 POT token）。乖乖走 UI。
- 純音訊型 Podcast 影片（如 Bloomberg Surveillance）一樣有自動字幕，流程相同。

**該集根本不在 YouTube 上**也確實會發生。2026-07-31 補跑時，Bloomberg 的 *Reacting to PCE, GDP, and Kevin Warsh*（42:23）全站搜尋無結果，官方播放清單當日只有四則且顯示「已隱藏無法播放的影片」。此時不要硬湊內容，照 brief 第 2 節退援順序最後一層處理。

---

## 9. 執行環境：機器與電源（2026-08-03 確立）

第 4 節提過 `pmset` 電源設定不存在於任何設定檔裡。本節把整個環境依賴一次寫清楚——這些同樣不存在於設定檔中，換機或重灌不會遷移。

**決策**：把這台舊 MacBook Pro 當常時開機的伺服器——放在家裡、全天開機、插著電、**不裝任何第三方電源管理軟體**。

### 每天需要醒著的時間窗口

| 時間（台北） | 事件 | 需要什麼 |
|---|---|---|
| **01:00** | **`com.kenny.podfetch`** | Mac 醒著，約 30–45 分鐘（六集） |
| **03:00** | **`podcast-daily-300`** | Mac 醒著、Claude 桌面版開著 |
| 07:35 | `advisory-dashboard-daily`（另一條線） | Mac 醒著、Chrome 開著 |
| 至 11:30 | 該線跑完 | 同上，持續 |

**是從 01:00 到 11:30 的連續十個半小時，不是「時間到醒一下」。** 排定喚醒只解決 01:00 那個起點；喚醒後若閒置，macOS 會再睡回去，03:00 就接不到。這個風險是結構性的、與任何一次具體事故無關——`sleep 0` 就是為它而設。

### 已套用的設定

```bash
sudo pmset -c sleep 0          # 插電時永不睡眠（螢幕仍可關）
sudo pmset -c disksleep 0
sudo pmset -c womp 1           # 允許網路喚醒
sudo pmset repeat wakeorpoweron MTWRFSU 00:55:00   # 保險：萬一仍睡著
```

另加一項：

```bash
sudo pmset -b sleep 30    # 電池模式改 30 分鐘
```

電池模式原為 `sleep 1`，代表電源瞬斷切到電池後一分鐘就睡——**這會直接打斷正在跑的 01:00 轉錄或 03:00 日報**。30 分鐘可撐過短暫停電。

**2026-08-03 實測驗證通過**：AC Power 為 `sleep 0`、`disksleep 0`、`womp 1`、`displaysleep 10`；Battery Power 為 `sleep 30`；`pmset -g ps` 顯示 `AC Power`；`pmset -g sched` 顯示 `wakepoweron at 0:55AM every day`。

**`autorestart` 無法驗證，不要當成保障。** `sudo pmset -c autorestart 1` 執行未報錯，但 `pmset -g custom` 的 AC Power 清單**不會列出這個鍵**，無從確認是否生效。Apple Silicon 機種已移除此鍵（預設即復電自動開機），Intel 機種應會列出。要確認機型跑 `uname -m`（`arm64` ＝ Apple Silicon、`x86_64` ＝ Intel）。**工作假設一律採保守版：停電當天需人工介入，該日產出視為會缺。**

`sleep 0` 是主要保障，`repeat wakeorpoweron` 是後備。**兩者都要留**：只有後備而沒有主要保障時，機器會在 00:55 醒來、閒置後又睡回去，03:00 的日報就接不到。這是結構性風險，不繫於任何一次具體事故。

### 為什麼不用 AlDente（或任何充電管理軟體）

AlDente 有三個功能會讓 Mac **在插著電時改用電池供電**：Discharge、Sailing Mode、Calibration Mode。電源來源一變成電池，macOS 就改套用 `pmset -b` 並積極睡眠，`-c sleep 0` 完全不生效。

否決的真正理由不是「它會出錯」，而是**它的失效方式是安靜的**——與第 7 節 `auto-push.sh` 那起事故同一種模式：失效狀態與正常運作在現場無法區分。代價是電池長期停在 100%；這台是舊機且定位為固定式伺服器，判斷為可接受。macOS 內建的「最佳化電池充電」可保持開啟（只延後充電、不放電，無害）。

### 其他前提

- **蓋子必須打開**，除非接外接螢幕。闔蓋且無外接螢幕時一定會睡。
- **Claude 桌面版加進登入項目**；投顧那條還需要 Chrome 維持開啟與登入。
- **避免半夜自動重開機**，建議自動更新設成「僅下載、手動安裝」。
- **若啟用 FileVault，重開機需人工輸入密碼**，`autorestart` 救不回無人值守情境。已知單點，尚未解決。
- 全天開機要注意散熱，不要塞在密閉空間。

### 驗證

```bash
pmset -g custom     # AC Power 段的 sleep 應為 0
pmset -g sched      # 應有 repeat wakeorpoweron 00:55:00
pmset -g ps         # 應顯示 "AC Power"
```

`healthcheck.py` 的「podfetch 時刻」若持續 PASS（日誌開頭是 `[01:`），代表這套設定有效。

---

## 10. 不要做的事

- 不要憑印象編造節目內容，也不要編造引述。沒有逐字稿就不寫金句。
- 0 集時不要產生空檔案、不要動 `index.json`、不要產 Word 報告。
- 不要把 Word 報告寫進 repo。
- 不要跑 git 指令。
- 不要把 `~/podcast-transcripts` 移進 repo（Public repo ＋ 付費來源逐字稿 ＝ 著作權問題），就算加 `.gitignore` 也不要。
- 不要只改 brief 或只改排程 SKILL.md 其中一邊。
- 不要把事故經過寫進 brief——寫進本檔第 7 節。

---

## 11. 變更紀錄歸檔（自 `AGENT_BRIEF.md` 第 8 節移入）

### 2026-08-24

- **`publish.py` 在 rebase 之前加了工作區檢查（4a），回 `15 @ worktree-dirty`。** 原本 `git add` 只涵蓋各系統的 `staged_paths`（podcast 是 `data`），**repo 根目錄那些已追蹤、被改過、沒被提交的檔案 publish 永遠 stage 不到也 commit 不掉**，於是 rebase 每輪倒在同一處。檢查刻意放在 `add`／`commit` **之前**——放之後照樣每輪長一筆垃圾 commit，換了退出碼而堆積沒停。用 `CONFLICT(15)` 不用 `ENVIRONMENT(14)`：`result.py` 區分兩者的軸線是「重跑會不會好」，而這一種永遠不會好。解析部分抽成純函式 `dirty_outside()` 才驗得動，`/tmp` 另建 bare origin ＋ 工作 repo 實測十個邊界（改名、含空白路徑、多路徑系統）全通過。
- **`kbdocx.podcast` 由只釘 04:00 改為 04／07／10／13 四個時刻。** 它讀的回執是「那一刻」的狀態、不是那一天的結局——回執每輪被覆寫，發布晚成功時會從非 0 翻成 0，而一天只跑一次就永遠看不到翻過去的那一刻。**「一天只跑一次」被當成了「一天只問一次」，那是兩件事。** 腳本的冪等守衛（日檔沒比 Word 新就跳過）讓多問幾次幾乎免費。**plist 是版控副本，改完要在 Mac 上 `cp` 到 `~/Library/LaunchAgents/` 再 bootout／bootstrap 才生效**；`publish.py` 則是直接從 `~/kb-core/` 執行，改完即時生效——**同一場裡兩種生效方式，不要混為一談。**
- **`shows.json` 的 `unhedged` 補進 Katie Martin**（08-22 就具名回報過、有 02:03 稿內自我介紹當證據，屬於**該加而未加**，隔了兩天）。依 anchors 的「證據不足時寧可加不要減」，未動其餘兩位。08-24 的 tip 不符（名單五人、實際只有 Stig Brodersen）**不改名單**——實際主持人本來就在名單內，一天的觀察不足以砍掉四個名字。
- **流程正本補上「`14` 連續三輪同一個 `stage` 就當 15 處理」。** 原文只寫「會自己重試」，而 `14` 的語意是「重跑**可能**會好」——**分不出來的時候，重試與卡死長得一模一樣**，那天 188 輪沒有人有理由介入。

### 2026-08-23

- **第 6 節（權威區）兩處硬錯訂正。** 背景推送寫的是 `com.kenny.dashpush` 每 180 秒 —— **那支 08-20 重建時就退場了**，實際是 `com.kenny.kbpublish.podcast` 每 60 秒掃 `~/outbox/podcast/`；連線資料夾寫「三個」且含 `~/.podfetch`，**實際要四個**（`podcast-transcripts`／`kb-core`／`outbox`／`podcast-knowledge-digest`），照舊版連會少連兩個每天都要用的。`MAIN.md` 在 08-21 就訂正過推送者，本檔沒跟著改，**留了三天**。
- **第 0 節排程 taskId 由 `podcast-digest-daily` 改為 `podcast-daily-300`**（實查 `list_scheduled_tasks`），Word 轉出的路徑由 `~/.podfetch/json2docx.py` 改為**實際在跑的** `~/kb-core/tools/podcast_docx.py`（依 `launchd/kbdocx-podcast.sh` 確認；`scripts/podcast/json2docx.py` 是 08-08 的舊腳本，沒有在跑）。
- **開頭那段「執行流程去 SKILL.md 第 3／5.5／6 步找」整段改寫成只指檔案、不指章節號。** 實查：第 3 步沒有那三個門檻、**沒有第 5.5 步**、第 6 步是交草稿不是回報清單，`anchors.fetch_limits.subagent_threshold_kb` 全庫零讀者（**2026-09-02 已除役**）—— **章節號、內容、讀者三個都斷了**。章節號會漂，檔名不會。
- **第 2 節四處程式路徑改到 kb-core**（`podfetch.py`／`healthcheck.py`／兩行指令）。08-22 的登記簿寫「`~/.podfetch/<某支程式>` 當天全部改掉了」，**而第 2 節是漏網的四處**——它是失效橫幅明文保留的權威區，過期在這裡的代價比在別處高。設定那一列同時註明 `~/.podfetch/` 底下的 `config.json`／`shows.json` 是指回 kb-core 的 symlink。
- **第 5 節的 Word 轉檔路徑改成 `~/kb-core/tools/podcast_docx.py`**——依 `launchd/kbdocx-podcast.sh` 確認那才是實際在跑的；`scripts/podcast/json2docx.py` 是 08-08 的舊腳本。**08-23 稍早只把目錄從 `~/.podfetch/` 改掉、沒查哪一支真的在跑**，等於修對了一半。

- **本節連續三場（08-21／08-22／08-23）沒有被加條目，也沒有歸檔。** 08-23 補上並把 08-18 的條目歸檔到 `MAINTENANCE.md` 第 11 節。**「不要刪這段」的維護規則寫在本節末尾，而它被漏掉的方式正是沒有人讀到那裡。**

### 2026-08-18

- **第 3 節補上 `check_timestamps()` 的邊界聲明。** 該檢查只認得「時間戳溢出」與「大間隙＋低完整度」兩種形態；**時間戳回跳、內容重複、相異時間戳少於 5 個的集數都在涵蓋範圍外**，而 08-18 的 20VC 正好踩在前兩種上（子代理自行發現兩處跳躍與一段重複，`timestampNote` 卻是空的）。**原本的寫法讓「空值」讀起來像「檢查過沒問題」**，現已寫明空值只代表那兩種沒觸發。同節的講者與時間軸兩段一併壓縮，把實例數字移進 `MAINTENANCE.md`——**加這段邊界聲明當下把本檔推回 25,000 token 上限之上（實測 25,120、截斷於第 417 行），壓縮是同一批的必要配套**。
  **這是「餘裕不到 1%」這條警告立下的隔天就被自己違反**：加字前沒有先量。往本檔加字一律先問「加完還讀得完嗎」，見 `MAINTENANCE.md` 第 6 節。
- **原本要補的「時間戳單調性檢查」，實測後決定不加。** 全庫 91 集有 32% 出現回跳，遠超過雜訊門檻——**初判「單調遞增在正確合併的逐字稿裡恆真」被量測推翻**。規格面因此維持現狀：`timestampNote` 為空**不代表時間軸沒問題**，讀到倒退或重複照樣改用主題轉換切章節。詳見 `MAINTENANCE.md` 第 6 節。


**為什麼歸檔**：brief 每天被排程完整讀一次，變更紀錄一度膨脹到佔全檔 68%（46.6 KB），而執行者不需要歷史。第 8 節只留**最近一場**維護的條目，其餘依日期由新到舊收在這裡。維護時要查「當初為什麼這樣改」，先看第 7 節的事故檔案，再看這裡的逐日紀錄。

> **歸檔的單位是「一場維護」，不是「一個日曆日」（2026-08-17 ② 訂正）。** 原本寫「最近一次維護**當天**」，而一天跑兩場以上已經是常態（08-16 有 ①–⑥、08-17 有 ①–⑤；**場次數一律以第 12 節登記簿的列數為準**，不要在別處另記）——**同日的前一場條目會因為日期相同而躲過歸檔，第 8 節於是只增不減**。判準改成：**開工時，把不屬於本場的條目全部搬過來**，同日的前幾場用「（同日第 N 批 · 主題）」分隔，格式見下方 2026-08-16 那一節。

### 2026-08-17

**（同日第二批～第五批 · 六槓桿稽核與 brief 瘦身，2026-08-17 晚）**

- **以 `writing-for-agents` 六槓桿稽核 brief 與排程 `SKILL.md`，分五批改（brief 34,381 → 29,641 字元）。** 刪五處會過期的抄本（舊的工具呼叫預算數值、三處「宣告舊規則作廢卻把它完整寫進 context」的句子、23 組 AppleID 抄本、開場白）；修 `snapshot.sh` 在 Mac bash 3.2 上的解析錯誤；brief 第 8 節歸檔；**把 podfetch 內部設計與沿革敘述下推到本檔第 4C／7／11 節**——**判準是逐句問「每日執行者會不會用到」**，執行者不執行 podfetch，只讀它的輸出。`config.json` 現行值刻意留著：它是 `healthcheck.py`「brief vs config」唯一的比對對象。
  **`SKILL.md` 同步**：第 0／1／2 步各補一條可驗證的完成條件、回報新增第 17 項（brief 讀了幾次），並引進「**進場**」一詞取代原本反覆展開的成本說明。brief 第 3 節另引進「**雙訊號**」（`SKILL.md` 端未引進）。
  **為什麼要瘦**：**brief 一次 `Read` 讀不完就會被截斷**（上限約 25,000 token），而第 0 步要求整份讀完。

**（同日第一批 · Read 上限機制訂正＋成本正規化欄位）**

- **`Read` 的真正上限是 token 不是行數——這條規則的機制寫錯了。** 排程 `SKILL.md` 任務卡原本寫「逐字稿最長約 1,200 行，一次讀得完（Read 上限 2,000 行）」，08-17 實測**兩個前提都不成立**：Market Huddle 是 1,346 行（已破「1,200 行」），而真正的約束是 **`Read` 每次回應約 25,000 token**——英文逐字稿約 4 字元／token，**超過約 75 KB 就一次讀不完**。實測 lennys 75 KB→1 次、tip 125 KB→2 次、markethuddle 158 KB→3 次，與 ⌈KB÷75⌉ 完全吻合。
  **後果不只是寫錯，是把結構性限制寫成了執行端的錯**：規則說「分段讀只會讓成本翻倍」、回報第 13 項要求點名超過 5 次的集數、用量分析也把 Market Huddle 讀三次列為浪費——**但那兩集在結構上就不可能一次讀完**。已改為由主代理在 `ls -l` 時算出 R＝⌈KB÷75⌉ 寫進任務卡，預算改為 R+1 理想／R+3 上限，回報門檻同步改成相對值。**這與 08-11「先估篇幅」失效是同一形態：規則本身沒錯，錯在它依賴的東西**（那次是壞掉的字數量測，這次是錯的機制理解）。
- **`metrics.csv` 新增 `subagent_tokens_k` 欄。** 08-17 表面每集 985K、是 08-14 的 3.5 倍，看起來像大幅退化；拆開後 **2.1 倍來自逐字稿變長**（116 KB/集 vs 55）、其餘來自**固定成本攤到 3 集而非 12 集**，**08-17 的子代理每 KB ＝ 1582÷349 ＝ 4.53，這是第一個實測值**；**08-14 沒有這一欄，無法直接比較**——用 `eff_tokens_k` 反推要先減掉一個估來的固定成本，而**減多少會決定結論方向**（不扣是 5.18 vs 4.53、看起來反而變好 13%；扣 600K 才得到「等於沒變」），所以不做這個比較。**真正的比較從 08-17 之後才開始有基礎。**`eff_tokens_k` 是整場工作階段，混了固定開銷與一次性維護動作（當天「分析用量」本身就佔 14.2%），**除以集數得到的數字不可比**——08-15 也踩過同一個坑。探測腳本本來就印子代理的加權值，先前只是沒存。**要看效率就看 `subagent_tokens_k ÷ transcript_kb`。**
- **`snapshot.sh` 的時間戳改用台北時區。** 原本用執行環境本地時間，Mac 上是台北、沙箱裡是 UTC，於是沙箱取的快照名早 8 小時、`--list` 排序錯亂（8/17 早上那筆排到 8/16 晚上前面）。**昨天補掛載點偵測時只修了路徑、沒修時間。**
- **文件層修正六處**（前一日稽核清單的 🟡🟢）：FT 入口的驗證狀態不再宣稱「已驗證」（**現行入口版本尚未在無人值守排程中跑過**，08-15 那次是事故日人工維護）、`avoid_preview_models` 補進「布林值抓不到」的但書（需人工比對的是四項不是三項）、第 8 節重播通則的節號由第 1 節改為第 4 節、「6 處中 3 處」逐條歸屬後訂正為 5 處＋1 處舊帳、三處「講一條已作廢的舊規則只為了說它作廢」改寫或刪除、任務卡補上 `published` 的日期書寫格式。
- **事故敘事下推：只搬了兩塊、約 500 字元，並訂正了前一天的估計。** 前一日估「第 1–7 節有 43% 是事故敘事」，那是**行層級的上界**；實際切分九個候選區塊只有 **2,529 字元**——**佔改動前全檔的 6.8%；若與 43% 對齊分母（第 1–7 節）則是 8.1%**（**那次訂正一口氣換了兩個分母：範圍由第 1–7 節改成全檔、單位由行改成字元，而只說明了後者**），**其中七塊逐塊判斷後判定該留**——113 秒預告佔滿當天兩集、跳針 28,122 個「I」讓完整度反向、wpm 不決定 `DEGRADED`、503 併進 `EXHAUSTED` 的事故鏈、時間軸雙訊號的設計理由，**這些細節本身就是規則的動機，刪了規則就變成沒來由的規定**。只把「維護者才用得到的 meta 紀律」與「逐項的事後數字」移進第 7 節。

### 2026-08-16

- **0 集這條路徑補上兩道防線，兩道都只在 `discover()` 回空時才作用。** ①**不再推進 `last_run_utc`**——08-15 就是靠這個無條件推進把一次暫時性失效變成永久缺口（差 13 分鐘、10 集全落在窗口外）。代價是真 0 集日下次會重複查一次，成本可忽略。②**健全性對照**：`discover()` 順手回傳「全部 23 檔所有集數裡最新的一筆時間」（用已抓回的資料，**零額外請求**），超過 `stale_feed_hours`（72）就在日誌印 `!!` 警告。離線模擬四種情境：事故日 15.2 天觸發、真 0 集日 0.6 天通過、長週末 1.9 天通過、查無集數觸發。**門檻的依據是「清單裡有每個交易日都發的 Bloomberg」，不是隨手取的天數。**
- **換掉兩檔更新頻率已經掉下來的節目，總數維持 23 檔。** 移出 **BG2**（實際約每季一集：06-11／03-15／2025-12-23）與 **Business Breakdowns**（約每兩個月：07-27／05-29）；補進 **Prof G Markets（1744631325）**——Vox Media，Scott Galloway 與 Ed Elson，**週一至週五每日一集**，08-05 至 08-14 逐集核實；以及 **Money Stuff（1739582836）**——Bloomberg 的 Matt Levine 與 Katie Greifeld，**每週五一集**、約 30 分鐘。**兩檔都用帶 `&cb=` 的 lookup 核過實際節奏，不是憑印象選的。**
- **移出節目時 `index.html` 的 CSS 一律保留不刪。** BG2 與 Business Breakdowns 的歷史集數仍在站上，刪了色碼舊日檔就掉色。第 4 節已改寫成「目前定義 26 組，其中三組（`capitalallocators`／`bg2`／`breakdowns`）是已移出但歷史資料仍在」，並把這條寫成通則。新色 `profg` `#991b1b`／`#fb7185`、`moneystuff` `#312e81`／`#818cf8`，三處分別查過撞色，最近鄰 RGB 距離 37.7–54.0，全數高於門檻 25。
- **重播規則從 FT 專屬一般化成全庫通則。** 原本只有 Unhedged 那一格寫「feed 可能放姊妹節目重播」，而 Money Stuff 會發 `Re-Run:` 前綴的重播（08-14 那集是 3 月訪談 Boaz Weinstein）。現在 SKILL.md 第 2 步獨立成一點：主代理負責認出重播、任務卡的 `published` 填**原始播出日**、`source` 寫明是重播，回報第 14 項要列出來。**這是內容規格，brief 第 4 節（全庫通則）與任務卡兩邊都改了。**
- **`stale_feed_hours` 補進第 2 節的 `key: 值` 清單與 `healthcheck.py` 的 `CONFIG_KEYS`**，機械檢查現在涵蓋 12 個設定值（原 11）。**新增 config 鍵時這兩處要一起補**——只加進 `config.json` 的話，brief 與實況脫鉤時沒有任何東西會出聲。
- **回歸複驗抓到 6 處，其中 5 處是今天這輪改動自己製造的、1 處是舊帳，全數已修。**（原寫「3 處」，2026-08-17 逐條歸屬後訂正——下方「其餘五處」的 ①②③④ 加上「最要緊的一處」都源於當天，只有 ⑤ 是舊帳。） 最要緊的一處是**「假零集要回捲 `last_run_utc`」這句舊指示，在同一段落裡與新行為直接打架**——0 集已經不推進它了，回捲反而會把視窗起點往回多推一格。brief 第 2 節與 SKILL.md 第 1 步、回報第 1 項都已改成「直接重跑即可，唯一還要回捲的是『抓到一些、漏掉另一些』那種情形」。其餘五處：①第 2 節「看 `last_run_utc` 是否推進到今天」這個判準已隨新行為失效（真 0 集的週末會被讀成 podfetch 掛了），已改成「一律看日誌」；②`healthcheck.py` 的 `last_run_utc` 新鮮度門檻同理，30 小時 → 96 小時並改寫訊息，否則每個 0 集的週末都會誤報 WARN；③重播規則只寫進 SKILL.md、brief 端仍只有 Money Stuff 那一格的個別敘述，**而第 8 節卻已經宣稱「兩邊都改了」**，現已在第 4 節立為全庫通則；④本節下方「`capitalallocators` 會走預設藍」與新寫的「26 組全部有 CSS」矛盾；⑤`MAINTENANCE.md` 第 4 節的 CSS 組數還是舊的 24。
  **這輪的形態值得記**：三處新問題全是**「改了行為，但沒回頭找哪些敘述以舊行為為前提」**——`last_run_utc` 的語意從「上次執行時刻」變成「下次視窗起點」，而三份檔案裡有四個地方在用舊語意讀它。**改一個欄位的語意時，要搜的是所有讀它的地方，不是所有寫它的地方。**
- **SKILL.md 新增第 5.5 步：排程執行在收工前量一次自己的 token，結果寫進回報第 16 項。** 這一步不是為了省事，是為了**結案一個懸了六天的問題**：`eff_tokens_k` 三欄自 08-10 起五次嘗試自動化全部失敗，結論寫成「排程不留 transcript」——**但五次都是從別的工作階段去找**。08-16 實測 `/sessions/` 底下 70 多個其他工作階段全部 `Permission denied`，證明的只是「從外面讀不到」；**沒有人從排程執行自己的沙箱裡找過**，而那個沙箱隨執行結束銷毀，事後誰都救不回來（08-16 那三欄就是這樣永久遺失的）。**找得到就此不必人工填；連續印「找不到」才算真正確認，此後可以把人工填法寫死。** 成本是一次 bash 呼叫。
  **兩個防呆一併寫進去**：①自我量測的數字是**下限**（最後幾個回合尚未落檔），回報必須標明，不要讓它看起來像精算值；②**找不到就寫「找不到」，不准換路徑猜第二次**——08-14 那次五連猜正是這樣來的。
- **成本模型訂正：驅動成本的是「每回合帶進多少新內容」，不是回合序號。** 08-15 從一段 14 個回合推論出「成本隨回合數接近平方成長」，08-16 拉到 250 個回合重算後**不成立**——分段平均呈尖峰狀而非單調上升。拆成分後：**快取寫 ×2 佔 54.2%（尖峰狀）**、快取讀 ×0.1 佔 29.0%（穩定上升但被權重壓住）、輸出 ×5 佔 16.8%。SKILL.md 開頭那段已改寫，**三條核心紀律在新模型下理由更強**（它們擋的正是最貴的 54.2%），詳見 `MAINTENANCE.md` 第 12 節。

**（同日第二批 · 稽核與修補，2026-08-16 晚）**

- **`snapshot.sh` 補上掛載點偵測與「空快照」守衛。** 它的 `BASE` 原本寫死 `$HOME/.podfetch`、沒有偵測，所以在 Cowork 沙箱裡每個檔案都印「（缺）」，**然後照樣建目錄、寫 MANIFEST、回報「快照完成」**——那筆會出現在 `--list` 裡，看起來就是一個正常的還原點。**而 repo 外的檔案沒有 git，快照是唯一還原點。** 現在改用與 `healthcheck.py` 的 `resolve()` 同一套順序（先找 `/sessions/*/mnt`、再退回家目錄），並加了守衛：**一個檔都沒抄到就自刪目錄、印錯誤、回非 0**。`SKILL.md` 單獨缺仍屬正常。**兩支腳本讀同一組路徑，只有一支做了偵測——「另一支也需要嗎」這個問題當時沒有人問。**
- **`healthcheck.py` 新增 `check_observations()`。** brief 第 4 節說「每天要動三個檔」，先前只有前兩個有機械把關——回訪沒做、只附加不改判、整步跳過，都不會有任何東西出聲。現在比對 `observations.json` 的 `updated` 與最新日檔日期，落後就 WARN；並印出積壓數字（**實測 56 條裡 47 條卡在「觀察中」、最舊停在 07-29**）。**積壓只印不報警**——算不算問題要人看，天天亮就會變成背景噪音。**這個檢查刻意只用單一訊號**，理由寫在函式註解裡：它比的是兩個 ISO 日期字串、沒有雜訊來源，而「兩個獨立訊號」那條規則針對的是啟發式訊號；**0 集日不產日檔，所以真 0 集不會誤報**，六個分支已逐一驗過。
- **第 1 節同源去重補兩處。** ①「同源推送 `meta`」那一欄**指定由主代理在第 4 步組檔時補**——子代理讀不到本檔、任務卡的 `meta` 模板也只有兩欄，先前這條**規則存在但沒有執行者**；②「**走 iTunes 退援路徑時同源去重做不了**」補進本檔，它原本只寫在排程 `SKILL.md`，而本檔第 5 行申報的 SKILL-only 規則只有三條、且都是執行流程，這一條是內容規格。

> **本批已全數完成。** 第 1 節第 ① 點對應的 `SKILL.md` 第 4 步改動由使用者當晚貼上（維護端讀不到排程正本，`~/Documents` 是 macOS TCC 保護區），改動後的快照為 `2026-08-16-234103`、改動前為 `2026-08-16-233000`，兩份差 5 行、即那兩處。**第 5.5 步當晚已確認確實在正本裡**——原先的疑慮是快照落後一輪造成的，見 `MAINTENANCE.md` 第 4／6 節。

### 2026-08-15

- **iTunes 查詢沒有 cache-buster，造成一整天的「假零集」——這是本系統第一次出現「主路徑安靜漏抓當天全部集數」。** podfetch 01:00 準時執行、**1 秒內結束**、日誌寫「沒有新集數。」，而窗口內實際有 10 集。根因：`itunes_lookup()` 的網址是固定字串，iTunes 把回應綁在**確切的查詢字串**上快取，US 商店回傳停在 07-31（落後 15 天），HTTP 仍是 200，`discover()` 因此一集都沒收到、也不寫任何錯誤行。**修法是在網址加 `&cb=<unix 時間戳>`。** brief 第 1 節已同步 URL 範本、`&cb=` 的必要性、以及「換商店要用 `&country=GB`，路徑前綴 `/gb/`／`/au/` 在本環境回空白內容」這條訂正——**舊版寫的 `/gb/` 是錯的，照著做只會拿到空白**。
- **這個失效態的可觀測特徵要記住**：唯一的外顯訊號是**執行時間從 9 秒掉到 1 秒**（23 檔網路查詢不可能 1 秒跑完），而 brief 第 2 節「三段排查」的第 2 格會直接把「沒有新集數。」判成一切正常。**「日誌說正常」與「真的正常」在這個失效態下分不開，只能靠外部對照**——當天是拿同一支 AppleID 帶與不帶 `cb` 各查一次才定案。
- **而且它會製造永久缺口。** `discover()` 回空時仍**無條件推進** `last_run_utc`，下次窗口起點因此跳過那 24.5 小時。當天最後一集發布於 16:17、新起點是 16:30，**差 13 分鐘全部落在窗口外**。復原方式是把 `last_run_utc` 回捲再重跑，10 集全數救回。
- **FT 入口第二次寫錯，這次連「已更正的版本」也是錯的。** 08-08 把入口從 `ft.com/unhedged` 改成 `ft.com/the-economics-show`，但後者只列 The Economics Show 自己的集數、**一樣不含 Unhedged**。可用的入口是 FT 站內搜尋 `ft.com/search?q="<原文標題>"`，而且**每集有兩個頁面，要取標題帶 `Transcript:` 前綴的那一個**——另一個集數頁正文僅約 2,700 字元，很容易被誤判成「付費牆擋住」而白白退層。brief 第 1、2 節已同步。
- **FT 全文改由子代理抓，不再進主 context。** 舊寫法是「約 45K 字元會進主 context 一次，這是工具限制下的已接受成本」；現在主代理只做定位與驗證（用 `javascript_tool` 回傳長度與付費牆檢查，輸出僅數十字元），再把 URL 交給子代理 `get_page_text`。**23,924 字元完全沒有進主對話**，那條「已接受成本」不必再接受。
- **三處與實況脫鉤的敘述一併訂正**：①ILTB 語速兩處打架（brief 第 1 節寫 122，第 2 節與 `shows.json` 都是 140）；②檔名慣例把窗口寫成「台北 01:00 至 03:00」，實際**兩端都由 podfetch 決定**（實測 00:30 → 隔日 01:00、24.5 小時，終點是 podfetch 的執行時刻而非日報的）；③brief 開頭宣稱「讀了這份就能完整重現整套系統」，但三條成本紀律只存在於 SKILL.md，已改為明確指路而非默默不提。

### 2026-08-14

- **「用段數規劃」成立，而且效果比預期大。** 連續三天測同一條規則：08-11 寫進措辭（3/5 重寫）、08-12 加強措辭（3/5 重寫）、08-12 晚間把規劃單位從字數換成段數 → **08-14 十二集零重寫**，段數達標 11/12（唯一超標的 GS Exchanges 是官方逐字稿密度高的允許情形）。**每集成本 54.9 萬 → 28.3 萬，減半**；子代理平均 2.6 次工具呼叫、無一集超過 5 次。**這條規則現在有效，沒有新證據就不要再動它**——連續三次修改同一條規則已經證明，前兩次都因為改錯了層次而白費。
- **順帶驗證「新架構是線性成長、集數多才顯優勢」**（08-08 寫下、一直沒機會測）：12 集這天，固定開銷（指令與工具清單）佔比從 08-12 的 20.1% 降到 **10.4%**，攤薄效果清楚。
- **新增時間軸檢查 `check_timestamps()`，這是一個存在很久卻從沒人看見的盲區。** 08-11 的 20VC 最大時間戳跑到 1,192 分鐘（片長 59 分，**溢出 1888%**）、08-14 的 TIP837 跑到 253 分（片長 78 分，**溢出 224%**）——兩集完整度分別是 0.91 與 0.98、status 都是 OK，**三天來沒有任何機制看見它**。訊號寫進**獨立的 `timestamp_notes`／`timestampNotes`，不影響 `status`**（時間軸壞掉不等於內容缺漏，併進 `warnings` 會用錯的理由標降級——08-07 講者檢查踩過這個坑）。詳見第 2 節（維度定義）與第 3 節（兩條門檻）。
  **第二條規則刻意設成「兩個訊號同時成立」**：只看大間隙會對 72 集報 15 集，因為長篇獨白與真跳段長得一樣；加上「完整度 <0.85」之後全庫只報 7 集，而唯一被抓到的間隙案例（08-14 MIB，22 分鐘無戳、完整度 0.73）正是子代理獨立確認確有兩處跳段的那一集。**「與完整度一起看」這件事應該由程式做，不是寫進訊息叫人自己判斷。**
- **回歸複驗（子代理）抓到 9 處，全修。** 最嚴重的兩處都是**已經記過的同型錯誤又犯一次**：①08-12 的變更紀錄以 changelog 體例留在 `MAINTENANCE.md` 第 7 節、沒進第 11 節——**這正是 08-09 抓過的 08-08 同型錯，防它的規則就寫在本節末尾**；②第 12 節那段列舉又對不上（08-12 那列已翻 ✅ 卻沒進第三類 bullet），**連續五輪、四種排列**，所以把「改字與改表必須同一次做完」改寫成三步檢查清單。其餘七處：`podfetch.py` 新註解裡的例子數字（1,114／56 應為 1,192／59）與三份文件不符、`_ANY_TS` 不吃縮排時間戳（同檔其他正則都吃，帶縮排的稿會靜默回空）、`TOKEN_NOTE` 被回填日覆寫（今天的量測失敗原因指向 08-13）、檔頭設計清單沒有時間軸那條、README 只寫兩種檢查、brief 第 3 節有句子被新區塊擠斷、`TS_DARK_GAP=90` 的定義沒寫進 brief。
- **token 量測的路徑終於用 `find` 定位出來，不再猜。** 真實位置是 `~/Library/Application Support/Claude/local-agent-mode-sessions/<帳號>/<工作區>/local_<階段>/.claude/projects/<專案>/<uuid>.jsonl`，子代理在同層的 `<uuid>/subagents/agent-*.jsonl`——**掃描邏輯本來就對得上，錯的只有根目錄那一行 glob**。前兩次分別猜成 `~/.claude/projects` 與 `$TMPDIR/claude-hostloop-plugins/`，`eff_tokens_k` 因此連續五天空白。**猜兩次不中就該停下來去量，而不是猜第三次。**（此欄仍只在 Mac 上跑 healthcheck 時才有值，沙箱看不到 `~/Library`。）
  **最後的結論是這三欄根本量不到，已改為人工填入。** 用 `grep` 掃遍 `~/Library/Application Support/Claude` 之後確認：**`local-agent-mode-sessions/` 底下只有互動式對話，排程執行不在本機留 transcript。** 五天四次修正（兩次猜路徑、一次改判別法、一次加指紋）**全都建立在一個從沒被驗證的前提上——那個檔案存在**。
  **但資料一直都在**，每天的 token 分析報告有，只是機器讀不到。所以改成**維護時人工從當日報告抄進 `metrics.csv`**（加權總量千位／子代理數／子代理總回合數），`healthcheck.py` 只負責「不要把人工填的值洗掉」並在沒填時提醒。已回填 08-09／08-10／08-11／08-12／08-14 五天，**整條優化曲線終於在同一張表上**：每集成本 1,940K → 577K → 549K → **283K**。
  **教訓**：debug 一條鏈時，最先該驗證的是**最根本、最不像會錯的那個前提**——我從第二層（路徑）開始查，把第一層（檔案存在嗎）當成已知。
  **路徑修好之後露出的第二層問題也值得記，那是同一種毛病**：原本用「mtime 落在 02:30–06:00」判斷哪一個 transcript 是排程執行，實測 70 個檔案**沒有一個**落在窗口內——**mtime 是「最後一次寫入」，桌面 App 之後重開或續讀同一個 session 都會把它往後推**。已改用**內容指紋**（排程 prompt 開頭那句只會出現在排程執行的 transcript 裡）＋**檔內 `timestamp`** 判日期，mtime 窗口留作退援並在訊息中標明走了哪一條。**時間是外部條件，內容才是「這個檔案是什麼」的證據。**
- **`skill_kb` 這一欄從來沒有被填過任何一天**（15 列全空）——它讀 `~/Documents/`，而沙箱看不到那裡，維護幾乎都在沙箱跑。**刻意不改成從別處猜**：空值至少誠實，填一個猜來的大小會讓這一欄看起來有在追蹤。要它有值就得在 Mac 的終端機直接跑 healthcheck。
- **`healthcheck.py` 會回填 `metrics.csv` 缺漏的日子。** 指標原本只在「跑 healthcheck 的當天」寫入，**沒跑維護的日子就沒有那一列**——08-13 就這樣缺了，而那天其實有 3 集、其中一集 DEGRADED，完全沒進基線。現在掃 `data/` 與逐字稿目錄的聯集回填（本次補了 07-29／07-30／07-31／08-13）。**回填列的 `brief_kb`／`skill_kb` 一律留空**，那兩個是「當下的檔案大小」，回填舊日期時填今天的值會是假的。

### 2026-08-12

- **找到 08-11 那條「先估篇幅再落筆」失效的根因：尺是浮動的。** 08-12 五集仍有三集重寫（與前一天同樣是 3/5），成本 274 萬 vs 288 萬持平，但逐字稿少了 25%——**每 KB 成本反而上升**。實測全庫 13 個日檔 74 集，子代理自報的 `chars` 與正文實際字元數：**偏差中位數 0%，範圍 −38% 到 +81%，42% 的集數偏差 20% 以上**。這不是系統性偏差而是**雜訊**——一把無偏但誤差 ±40% 的尺，比一把有偏但穩定的尺更沒用。當天實例：Dwarkesh 自報 5,900／實際 8,709、Pivot 自報 5,948／實際 8,045（兩集都自以為在區間內，Pivot 實際超上界 23.8%、Dwarkesh 34.0%），而 In Good Company 自報 1,789／實際 1,087（自以為超標所以壓縮了兩輪，實際一直低於下界）。**在一把會浮動的尺上「先估篇幅」是不可能執行的。**
- **`chars` 改由組檔時機械計算並覆寫**（第 4 節）。子代理現在填 `0` 即可，不必也不應該自己數。**歷史 13 個日檔已一併重算，74 集中 42 集的值有變。**
- **寫作規格改用「段數」規劃，不用字數**（第 3 節）。字數的唯一定義寫死為「`sections[].paragraphs[]` 串接後的字元數」，同時給每個層級對應的目標段數（6–9／10–15／13–20／20–33），依據是全庫 1,546 個段落的中位數 **198 字元**（四分位 150–251）。**段數是子代理下筆時數得準的，字元數不是**——把規劃單位換成它量得到的東西，規則才可能被執行。
- **回歸複驗（子代理）抓到 8 處，全修**：最嚴重的是**「用段數規劃」只改了 SKILL.md、brief 第 3 節仍留著被廢掉的「查出目標字數／分配每節字數」**——一組兩份漏改一邊，而且留下的正是本輪判定為「要求執行者做一件它做不到的事」的那條；任務卡自己打架（一處說「填估計值」、兩處說「填 0」）；MAINTENANCE 第 12 節的 08-11 列未結案（仍寫「四項修正待 08-12 驗證」，而 08-12 已證偽其中一項）、第二類 bullet 未納入 08-11、「連續三輪」未更新；另有歸檔範圍、回報第 14 項單位、50% 邊界重疊、150–250 vs 150–251、超上界百分比概括等五處。
- **`index.html` 卡片顯示的「摘譯 N 字」語意已改變**（顯示的就是 `chars`）：74 集中 42 集的數字變了，且它的意義從「子代理自報字數，每集一把尺」變成「正文字元數」。**沒有改 `index.html`（那是紅線）**，但記在這裡以免下次有人比對舊截圖時困惑。
- **中斷重派加成本門檻**（排程 SKILL.md 第 3 步）：已留下有效 JSON 但超上界時，**超出 50% 以內直接收下並在 `source` 註明，超過才重派壓縮**。08-12 的 ILTB 中斷後重派，**重派那次 36.2 萬比原本那次 20.8 萬還貴**，即使它完全沒讀逐字稿——壓縮改寫本身就是反覆讀寫的工作，兩次合計佔當天全場 20.8%。**超上界是本檔已經允許、只要說明理由的降級；花 36 萬 token 去消滅一個被允許的降級並不划算。**（那次超了 79%，依新規則仍該重派，當天處置正確。）

### 2026-08-11

- **token 優化終於有量測答案：同樣五集，970 萬 → 288 萬，降 70%，而且逐字稿還多 19%（490 KB vs 412 KB）。** 這個問題從 08-08 寫下「粗估降 60–75%」以來一直無法驗證，現在實測落在同一區間。子代理佔比從 84% 降到 62%。**「成本的驅動是回合數不是資料量」這個判斷成立**，工具呼叫預算是有效的那一刀。
- **集層級時限與整場硬停通過實測，餘裕很大。** Acquired 273 分鐘、14 段、45,237 字，從 01:06:57 跑到 01:18:41 只用 **11 分 44 秒**（單集上限 60 分）；整場 18 分 41 秒（整場上限 100 分）。**沒有誤殺，也證明 3600／6000 不算保守過頭。**
- **任務卡加「先估篇幅再落筆」（第 3 節同步）。** 08-11 四集超出呼叫預算，主因是三集初稿超上界後重寫（Odd Lots 8,257→6,434、Acquired 14,255→6,583、Bloomberg 3,642→2,983）——**每重寫一輪，整份逐字稿就被重新計費一次**。寫作規格與成本預算在長節目上會互相打架，解法是先列骨架並分配字數，不是事後刪。**理想工具呼叫數仍是 2 次（Read → Write）**，「想骨架」是中間的思考步驟、不佔呼叫。
- **補齊 `acquired`／`bg2`／`lennys` 三組 CSS，現役 23 檔全部齊備（`index.html` 共 24 組）。** 08-11 healthcheck 第一次真的 WARN——Acquired 卡片走了預設藍。三處撞色分別檢查，色條最近鄰 Δ47–64、深色最近鄰 Δ61 以上。
- **記分板加收錄標準，並清掉 3 條純免責條目（44 → 41）。** 08-10 回填種子時把後記段落照單全收，連「以上內容非投資建議」都進了記分板，**08-11 六條改判裡有四條在清這些雜訊**。新規則：只收可驗證的預測或檢驗點，免責聲明、來源標註、管線自檢一律不收。
- **回報第 13 項註明呼叫次數是自報下限。** 08-11 實測：自報 4／7／6／9／11，系統實際 5／7／6／11／18——Acquired 差了 7 次。**這個數字現在是成本監控的依據，但它會低報**，判斷時要記得。
- **回歸複驗（子代理）抓到 8 處，全修**：登記簿那兩列的 ⚠️ 沒跟著內文翻正（**只改字沒改表，與前兩輪方向相反、形態相同，已是連續第三輪**）、逐場登記表被一個空行切成兩張且 08-10 ② 錯位、`obs-20260810-3` 這條純管線自檢被判「應驗」而 brief 正好拿同句型當反例、任務卡「3 次工具呼叫」只枚舉得出 2 次真呼叫、120 分鐘規則沒進到子代理任務卡與第 1 節、收錄標準 brief 四類 SKILL 三類、README 未同步、observations 的 `note` 鍵未進格式範例。
- **超長節目不取官方稿寫進規格（第 2 節）**：片長超過 120 分鐘的 A 類節目直接用 podfetch 稿。`web_fetch` 約 104,700 字元的單次上限會靜默截斷，兩小時以上的官方稿必然超出數倍。08-11 的 Acquired 是子代理自主做了這個判斷、判斷正確，**只是規格裡當時沒有這條路**——又一次「執行者靠自覺補上規格缺口」。

### 2026-08-10（第三場，程式碼審查與知識庫新功能）

- **修掉 `episode_budget_seconds` 的語意謊言。** 它名為單集上限，實際上設在「每段、每次重試」裡、且只在過載分支檢查——乾淨跑永遠不會觸發（所以從沒被發現），極端路徑無上界：2 小時集理論最壞 4 小時。現在整集只設一次、每段開始前檢查，值同步加大到 3600（2 小時集乾淨跑要 10–20 分，1200 會把會成功的集殺成 FAILED）。**另加整場執行上限 `run_budget_seconds: 6000`**：02:40 後不再開始新集數——03:00 日報要讀 manifest，寧可缺集也不能讓它讀到不完整的檔案。
- **快取段落加品質門檻。** 舊版只驗「>200 bytes」，被截斷的壞段寫進 cache 後，下次接續會被無條件吸收，**該段內容永久缺失且無徵兆**。現在讀 cache 套用與現場相同的 0.55× 門檻；**只丟棄一次**（`.redo` 標記），重轉後仍不足就照收並記 warning——否則語速慢於基準的節目會每天重轉同一段燒 RPD。
- **模型探測結果 72 小時內沿用。** 探測發真實請求、計入 RPD——每天 6–10 個、佔全日 25–30%，其中 3 個以上打在只有 10 RPD 的 Flash 池上，而結果早就存進 state 只是從不回讀。防線：執行中有模型 404 就標 `force_reprobe`，隔天強制重測全池。
- **config.json 讀取失敗改為中止。** 舊行為靜默退回程式內建預設值——而內建的 `segment_seconds` 預設是 1800，正是被回滾過的值。「用錯參數跑完」比「不跑」難發現得多；不跑會被 healthcheck 的日誌檢查抓到。內建預設值也全部對齊 config 現值。
- **healthcheck 三處修正**：推送鏈補 packed-refs fallback（git gc 之後 loose refs 會消失，否則變成每天假 WARN）；01:00 判定改掃全檔的「視窗：」行（維護時手動跑過一次就會把排程執行擠出第一行，誤報 FAIL 還把人引去查 plist）；`podfetch_requests` 改名 **`segments_done`**——它數的是完成段數不是 API 請求數，08-05 實測請求數是段數的 2.5 倍，舊名會讓人拿它估 RPD 而系統性低估。
- **網站四個新功能**：①**觀察點記分板**（`#/obs`）——每天觀察後記的可驗證觀察點進 `data/observations.json` 追蹤（已回填 41 條種子），日報每天回訪改判，讓知識庫從「摘要」變成「有記憶的判斷追蹤」；②**來賓索引**（`#/guests`）——按來賓彙整歷次出現，新 schema 加結構化 `guests` 欄位，舊集數退回解析自由文字；③**品質與主持人顯示**——新 `quality` 欄位（完整度／status／講者警示）進閱讀器 meta 表格，`hosts` 欄位存在至今從未顯示過，一併補上；④**主題標籤**——新 `topics` 欄位（15 個受控詞表、每集 1–3 個），先累積資料，跨日主題頁之後再建。
- **前端搜尋效能**：歷史日檔（不可變）拿掉 `no-cache`，只有今天那份保留；搜尋文本快取（`__txt`），不再每次按鍵重建。天數多了才會顯現的差距，趁小先修。
- schema 三個新欄位（`guests`／`topics`／`quality`）與觀察點格式已寫進第 4 節；任務卡同步（詞表兩邊一致）；回報清單擴到 15 項（第 15 項＝記分板本日動了什麼）。`snapshot.sh` 時間戳改到秒級並防同秒覆蓋（分鐘級時同一分鐘兩次快照會靜默互蓋——對一個因覆寫事故而誕生的備份工具而言不可接受）。
- **回歸複驗（子代理）抓到 9 處，全修**：①`run_deadline` 起算點原在模型探測**之後**——重新探測的那一天（最慢的一天）門檻會往後漂十幾分鐘，已移到程式啟動即起算；②第 4 節「每天只需要動兩個檔」與記分板每日回寫直接矛盾（**照 brief 字面可以合理略過回訪**，記分板的價值會被規格自己關掉），已改「三個檔」並補目錄樹；③「看日誌第一行」的判準只修了機器端，brief／MAINTENANCE／SKILL 五處人工指示全留舊，已全改為掃「視窗：」行；④觀察點種子檔補上 `verdict`／`verdictDate` 欄位（brief 格式範例與實檔曾不一致——「補欄位」與「改欄位」是兩種不同動作）；⑤README 補兩個新頁與 observations.json；⑥nav chip 原本只在 manifest 載入成功後才綁事件，失敗時看起來正常但點不動，改為無條件綁定＋來賓頁防呆；⑦healthcheck 01:00 判定的分支順序調整（全檔掃描結果優先於第一行格式）；⑧snapshot.sh 改動補進本紀錄；⑨`segments_done` 說明補「快取段不計入」。

### 2026-08-10

- **新增 We Study Billionaires（TIP），節目數 22 → 23。** 加它的唯一理由是**週末補檔**：週一那一版涵蓋美東週六中午到週日中午，現有 22 檔在這個窗口幾乎沒有供給（08-10 只有 2 集，還是同一支預告）。TIP 一週兩檔、**其中一檔固定在週日**（實查 08-09／08-02／07-26 皆為週日發布，UTC 00:00 ＝ 美東週六 20:00，**落在窗口內**）。六處都已補：brief 第 1 節、`shows.json`、`config.json` 的 `show_priority`（插在 `iltb` 之後）、`index.html` 三組 CSS、`README.md` 的節目清單、排程 SKILL.md 的 `description`。**語速未校準**，先沿用全域 200，累積幾集後再決定要不要補 `wpm`。
  **配色第一版選錯，已換掉。** 初版 `#a16207` 只檢查了色條就宣稱查過撞色——實際上徽章文字色與現役的 `unhedged`（`#a1560a`）**RGB 距離只有 12**，等於同一個顏色；深色模式也與 All-In 撞在同一片金色。**「補的時候三處都要補」那句話，撞色檢查也要三處分別做。** 現用 `#701a75`（梅紫），最近鄰是 `pivot` Δ60／深色 `nopriors` Δ26。
- **`healthcheck.py` 的 token 量測改成「失敗要出聲」。** 08-10 修了路徑之後在 Mac 上實測，`eff_tokens_k` **仍然是空的**——第二次猜路徑又猜錯。**真正的問題不是猜錯，是猜錯這件事沒有任何症狀**：留空看起來像「還沒有資料」而不是「壞了」，所以它安靜地跑了兩天。現在量測失敗會讓「每日指標」變成 WARN 並印出原因（找不到根目錄／根目錄下沒有 `.jsonl`／有檔案但都不在 02:30–06:00，後者會附上最近三筆 mtime）。**加了量測就要順手驗證它回傳非空值，這是第二次踩同一個坑。**
- **新增「同日同源去重」（第 1 節）。** 08-10 當天兩集是**同一支 113 秒的〈Our Town〉預告**，分別被 Bloomberg Surveillance 與 Odd Lots 推送——**同一天、不同 url、不同中譯標題**，三個條件全部避開了現行去重。而唯一能認出同源的兩個欄位（manifest 原文 `title` 與 `durationMs`）**剛好都不在比對範圍內**。新規則比對這兩個欄位、兩者都相同才合併，保留 `show_priority` 較前者並在其 `meta` 加「同源推送」。**失敗方向刻意選在漏判那一側**：多留一集只是重複，誤刪會弄丟內容。
- **補上「低於字數下界」的例外與回報義務（第 3 節）。** 原本規格只規定超上限要說明，**低於下界完全沒有條款**——08-10 那兩集逐字稿只有 301／303 字，下界卻是 1,200 字，主代理寫了 986／501 字並自行把理由塞進 `source` 欄位。**依賴自覺的紀律不是紀律**，現在明訂「素材撐不起下界時以素材為準」，同時列為回報第 14 項。
- **子代理改為有門檻：逐字稿小於 10 KB 的集數由主代理直接處理**（排程 SKILL.md 第 3 步）。08-10 兩份逐字稿各 2.5 KB，派子代理的固定開銷（整包指令送進新 context）遠大於處理內容本身。當天主代理是臨場判斷不派，**寫進規格才會穩定生效**。
- **修 SKILL.md 的退援順序（第 2 步）。** 原本寫「官方稿 → podfetch → FT → YouTube」，把 FT 排成獨立的第 3 層；實際上 **FT 流程取得的就是 Unhedged 的官方逐字稿（第 1 層）**，失效時退回 podfetch 而非跳到 YouTube。同一份文件裡順序宣告與操作指示互相打架。
- **統一「26 小時」的說法（第 1 節）。** 原本第 1 節寫「固定 26 小時」、第 2 節的設計原則又寫「以 `last_run_utc` 為準，不是固定窗口」——**字面互相否定**。實測 08-10 的窗口是 24.5 小時，兩種描述都不吻合。現在一律以 `last_run_utc` 為準，26 小時只當說明用的近似值。
- **修 token 量測讀錯路徑。** `healthcheck.py` 的 `measure_session_tokens()` 只找 `~/.claude/projects`，那是 Claude Code CLI 的路徑，**而每日排程跑在 Cowork**，transcript 在 `$TMPDIR/claude-hostloop-plugins/<hash>/projects/` 底下。結果 `eff_tokens_k` 連續兩天都是空的——**加了量測卻沒量到，比沒加更糟，因為 `metrics.csv` 看起來已經在收集基線了。** 現在同時搜尋多個候選根目錄（用 glob，因為 hash 會變）。

### 2026-08-09

- **子代理任務卡加上工具呼叫預算，這是目前最有效的成本控制點。** 08-09 首次實跑，五個子代理吃掉全程 84%，最貴一集 225 萬——**不是因為逐字稿大，是因為它跑了 70 次工具呼叫**。實測所有逐字稿最長 1,201 行，Read 上限 2,000 行，**每一份都在一次呼叫內讀得完**，所以那 70 次不是被迫分頁，是讀完之後又做了 68 件事（多半是為了核對金句而反覆 grep／重讀）。**逐字稿一進 context，之後每一次呼叫都把那 120 KB 再送一遍。** 任務卡現在明訂：Read 一次不帶 offset → Write 一次，**目標 2 次、上限 5 次**，並明講「全文已在你的 context 裡，核對不要再讀第二次」。摘要卡末行回報實際次數，回報清單第 13 項收集，超過 5 次要點名。
- **`metrics.csv` 補上 `eff_tokens_k`／`subagents`／`agent_turns` 三欄。** 08-08 寫的「粗估降 60–75%」**是估算不是量測**，至今無法驗證，因為舊架構從未被量測過。`healthcheck.py` 現在會解析當日排程 session 的 transcript 算加權 token（重讀 0.1x、寫入 2x、產出 5x）。**選 session 時只認 02:30–06:00 之間結束的**——維護用的互動對話往往比排程執行大好幾倍，挑錯就等於量錯。沙箱讀不到實機路徑時留空、不報錯。
- **語速基準改為 per-show 並上修全域值**（`WORDS_PER_MIN` 165 → 200，`shows.json` 可用 `"wpm"` 覆寫，已為 7 檔設定）。起因是查 08-09 兩集的完整度（1.44／1.53），掃過**單 token 與片語級跳針都是 0**——不是跳針，只是 All-In 那類四人搶話的節目本來就講到 238 字/分（**那兩集的 `DEGRADED` 另有原因，見下一條**）。17 檔實測中位數約 200，**原本的 165 讓幾乎每集完整度都顯示 1.2–1.3，「超標」變成常態，這個數字失去可讀性**。校準後 15 檔全部落在 0.87–1.09。
- **回歸複驗抓到六處**（子代理獨立比對）。**其中兩處是我當天改動的副作用**（`metrics.csv` 擴欄後舊列錯位、把 wpm 校準寫成一個程式上不成立的因果），**兩處是 08-08 歸檔留下的**（規則被跟著搬走、08-08 紀錄根本沒進第 11 節），**其餘是既有缺陷**：
  - **`metrics.csv` 表頭擴為 15 欄，但 08-02～08-08 的舊列仍是 12 欄**，整份檔案無法按欄名解析——**而它存在的唯一目的就是當可解析的基線**。`healthcheck.py` 現在重寫時會把短列補空欄。
  - **歸檔時把規則跟著紀錄一起搬走了。** 「brief 與 SKILL.md 是一組兩份、改任一邊都要同步」這條規則原在本節末尾，08-08 隨變更紀錄被移進 `MAINTENANCE.md`，**於是 brief 整整一天沒有任何一句要求雙邊同步**——而 08-04 的整份覆寫事故正是這類漏抄造成的。已寫回本節並標明不要刪。**歸檔時要分辨哪些是紀錄、哪些是規則。**
  - **08-08 的三段變更紀錄根本沒進 `MAINTENANCE.md` 第 11 節**，而是以 changelog 體例留在第 7 節（事故檔案）裡，使本節「已歸檔至第 11 節」成為假陳述。已移正。
  - **`healthcheck.py` 的「brief vs config」會靜默跳過抓不到的鍵，卻仍回報「N 個都對得上」。** `episode_budget_seconds` 在 brief 寫成「`key`（現為 1200 秒）」，正則吃不到，等於從未被檢查。現在改為 WARN 列出哪些鍵找不到可比對的數值，brief 也補上 `key: 值` 形式。**「檢查數」與「實查數」脫鉤是最會讓人安心的一種失效。**
  - 順帶修掉：`podfetch.py` 常數上方兩代前的舊註解（還寫著「原本設 130」與已過期的 Market Huddle 168）、brief 第 6 節指向 `MAINTENANCE.md` 第 4 節（實際在 4B）、`healthcheck.py` 與 brief 都還宣稱它「不寫任何檔案」（它會寫 `metrics.csv`）。
- **訂正一個當天自己寫錯的因果：`wpm` 不決定 `DEGRADED`。** 初稿寫「基準過低導致 `DEGRADED` 失去訊號價值」，讀 `podfetch.py` 才發現 status 只看 `warnings`，而完整度「超標」在任何路徑都不會產生 warning——那兩集的降級來自段級相對檢查。**上修 wpm 反而讓段級重試門檻變嚴。** 教訓：**改指標前先確認它接到哪條分支**，文件與文件互相對讀查不出這種矛盾，只有讀程式才會發現。

### 2026-08-08（第三次，版本控制與指標基線）

- **建立 `~/.podfetch/snapshot.sh`，補上 repo 外檔案的回溯能力。** repo 內的 brief／MAINTENANCE／index.html 有 git，但**改動最頻繁的檔案全在 repo 外且零版本紀錄**——`podfetch.py` 這一週至少改 8 次、排程 `SKILL.md` 至少 9 次而且**被整份覆寫過一次**（08-04），當時無法還原、只能靠子代理事後比對發現。腳本存八個檔案＋`MANIFEST.txt`（SHA 與大小），支援 `--list`／`--diff`，還原就是 `cp` 回去。**`gemini.key` 與 `state.json` 刻意排除。** 維護流程第 0 步改為「開工先快照」（`MAINTENANCE.md` 第 2 節）。
- **`healthcheck.py` 每次執行自動累積每日指標到 `~/.podfetch/metrics.csv`**（12 欄：集數、狀態分布、逐字稿 KB、產出 JSON KB、brief KB、SKILL KB、podfetch 請求數、音檔分鐘、講者訊號數）。**動機是今天的 token 優化只能「估算」降幅**——從來沒有量測過任何一天，沒有基線的優化只能自稱有效。已回填 08-02 起的歷史；**08-08 是舊架構最後一天，也是新架構的對照基準**（10 集／逐字稿 617 KB／產出 229 KB／29 個請求）。同日重跑覆寫該列。
- **新增 `MAINTENANCE.md` 第 12 節「版本登記簿」**：13 場維護的速查表——動了哪些檔案、想解決什麼、**後來成立嗎**。最後一欄標出「修法本身製造新問題」的案例，動防護機制前要先看。（建立當時是三次，現況以第 12 節那張表為準。）第 11 節仍是敘事體的來龍去脈，兩者互補。

### 2026-08-08（第二次，token 優化）

- **每日排程改為「子代理分集處理」架構，這是本系統成本結構的根本改變。** 主對話是一條長對話，每次工具呼叫都重送整個 context——過去 10 集逐字稿（617 KB ≈ 15–20 萬 token）讀進主 context 後被重複計費數十次。現在：主代理只讀 manifest 與 brief，**每集派一個子代理**（同一則訊息並行派出）在各自 context 讀逐字稿、把該集 JSON 寫進 `/tmp/ep-NN.json`、只回傳十行摘要卡；主代理用摘要卡寫交叉觀察、用 python 合併暫存檔組出 `data/` 兩檔。**逐字稿與完整集數 JSON 從此不進主對話。** 交叉觀察改以摘要卡合成（每卡含 5–8 條具名論點），品質取捨已評估。
- **Word 報告改由 `~/.podfetch/json2docx.py` 從 JSON 機械轉出**。過去同一份內容 LLM 寫兩次（JSON 229 KB ＋ 82 頁 docx），現在第二次是免費的腳本轉檔。已用 08-08 真實資料驗證：10 集、528 段落、81K 字元，跨節目觀察與金句齊全。
- **本檔瘦身**：變更紀錄只留最近兩天、其餘歸檔至 `MAINTENANCE.md` 第 11 節（歸檔規則寫在第 8 節末尾）；第 6 節維護專用內容（PAT 換發、電源設定完整指令、launchd 細節）移至 `MAINTENANCE.md` 第 4B／9 節，只留每日執行需要的。68.8 KB → 44.6 KB（規格本體 20.9 KB；當日紀錄 22.7 KB 會在下次維護歸檔）。
- **三項合計的降幅：當時只能粗估 60–75%，而那是估算不是量測**——沒有任何一天的實際數字可比。08-09 首次實跑後回頭檢視，這個數字**至今仍無法驗證**，因為舊架構從未被量測過。`metrics.csv` 已於 08-09 補上 `eff_tokens_k` 欄位，往後才會有真基線。**寫下未經量測的降幅是這次的教訓：估算要標明是估算。**


### 2026-08-08

- **修掉一個讓完整度指標「反向」的缺陷。** Gemini 在長音檔上會整行跳針（08-08 實例：The Compound 一行 28,122 個重複的「I」、Hard Fork 9,521 個「let's」），而 `spoken_words()` 直接數 token，跳針全部計入——於是**內容嚴重缺漏的失效態顯示成「超標 336%」**。`completeness` 是日報用來判斷該集能不能信的依據，這個數字在最需要它的時候是錯的。新增 `collapse_loops()`：計字前把「連續重複 20 次以上的同一 token」壓成一次，並在 `warnings` 單獨記一筆剔除了多少字。**實測還原成 1.10 與 1.10，與人工評估吻合。**
- **修掉時間戳的雙重偏移。** prompt 原本告訴 Gemini「本段涵蓋整集的 20:00–40:00」，它就照絕對時間輸出，而 `offset_timestamps()` 又加一次 → 第 2 段標成 40:00–59:52、實際是 20:00–39:52。**而且同一集裡時而遵守時而不遵守**（第 3 段是對的），所以合併後時間軸會往回跳。**光改 prompt 擋不住**，已加機械式偵測：看該段第一個時間戳離 0 近還是離 offset 近，取近的那個解釋；同時把 prompt 裡那句誘因拿掉、改為明確要求從 00:00 起算。08-08 的「`Speaker 7` 連續 20 分鐘」就是這個造成的假象——**一個 bug 會在別的檢查裡偽裝成另一種 bug**。
- **修正 FT 的入口網址**（第 1 節）。`ft.com/unhedged` 是 Unhedged **電子報存檔、完全不含 podcast 集數**，過去一直寫錯；正確入口是 `ft.com/the-economics-show`。08-08 是 FT 路徑第一次在無人值守排程中跑完並成功（45,181 字元、無付費牆）。同時記下 **Unhedged 的 feed 會放姊妹節目的重播**，遇到要標明原始播出日。
- 補 `hardfork` CSS，`index.html` 現為 20 組；**補色時要檢查撞色**——第一版讓它用到 `markethuddle` 的橘紅。現役 22 檔中仍缺 `bg2`／`acquired`／`lennys` 三組。
- **`speakerNotes` 上線第一天就發揮作用**：10 集有 8 集報警，其中 Pivot 的標籤在中段廣告後整體對調，照標籤直接引用會把 Kara Swisher 的話掛到希拉蕊．柯林頓頭上。**這正是這個欄位存在的理由**，也再次確認機械檢查只能提示可疑、判斷仍要靠讀內容。
- **回歸檢查抓到八處，含我這次改動製造的三個瑕疵**（子代理獨立比對）：
  - **時間戳偵測的判定線太鬆**。原本用「離 off 比離 0 近」，判定線落在 `off/2`——第 2 段 `off=1200` 時門檻只有 **10:00**，只要段首有十分鐘廣告或音樂，整段就會被誤判成絕對時間而跳過位移，時間軸倒退 20 分鐘。改為要求**貼近** off（`ABS_TS_TOLERANCE = 180` 秒）。
  - **跳過位移的分支直接 `return text`，不再正規化格式**。模型若寫 `[105:30]` 這種三位數分鐘，下游 `SPEAKER_LINE` 匹配不到，會誤報成「裸時間戳」。改為把 offset 歸零後照樣走一次格式化。
  - **`collapse_loops()` 把跳針從段級語速也扣掉了，等於默默關掉 `HIGH_RATIO` 對單 token 跳針的偵測**。新增 `loop_segs` 記錄哪幾段跳針，並寫進警告文字——**位置資訊不能因為換了偵測方式就丟掉**。
  - 順帶修掉：`transcribe_one()` 殘留未使用的 `start_s`／`end_s` 參數與死掉的 `hhmm()`；`episode_budget_seconds` 補進 `healthcheck.py` 的 `CONFIG_KEYS`（現檢查 10 個）。
  - **`healthcheck.py` 的 `resolve()` 改為先找掛載點、再找家目錄。** 在沙箱裡把 `podfetch.py` 匯入測試時，它的 `log()` 會在沙箱家目錄建出一個空的 `~/.podfetch/logs/`，原本的順序會抓到那個假目錄，然後報出一整排假故障（讀不到 `shows.json`、執行時刻變成測試當下的時間）。**驗證工具本身被測試行為污染，這是最難察覺的一種假警報。**
  - 文件面：第 3 節仍寫「四種訊號寫進 `warnings`」（實際是五種、且 08-07 已拆到 `speaker_notes`）；跳針字數 28,127／9,527 應為 **28,122／9,521**（第 3 節自己要求「數字一律不四捨五入」）；「08-07 的 Speaker 7」應為 08-08；第 2 節的 front matter 欄位清單漏了 `showKey`／`segments`／`words_per_minute`；**跳針剔除只寫在變更紀錄、沒進第 2 節規格本體**（執行者照 SKILL.md 只會讀第 2 節）；`MAINTENANCE.md` 的 CSS 組數與健康檢查清單未跟上；**「FT 從未在無人值守實測過」這句過期敘述同時存在於四個地方**，今天才真正跑通。

### 2026-08-07

- **`healthcheck.py` 新增「線上狀態」檢查**。08-07 網站停在前一天，而推送鏈檢查報 PASS——**因為它只比對 `local == origin`，而完整的鏈是「寫檔 → commit → push → Actions → Pages」**。前三段對不代表最後一段有跑。新檢查帶 cache-buster 抓線上 `index.json`，比對 `updatedLabel` 與 `days[0].date`；連不到網路時顯示 WARN 並提示到 Mac 上跑，不會誤報綠燈。**這是 08-03 auto-push 事故的同一個教訓往下游挪了一格。**
- **轉錄 prompt 改為保守標記講者**：預設 `Speaker N`，**只有拿到明確依據（自我介紹、被直呼、主持人點名）才寫真名**；廣告與台呼標 `Announcer`，不再歸給主持人；同一段內標籤不得中途改名或重新編號；並明講「錯的名字遠比沒有名字糟——它是對真實人物的不實陳述，而且事後極難發現」。
- **新增 `check_speaker_labels()`**，把四種可疑訊號寫進 manifest `warnings`：標籤格式異常（把台詞或廣告文案當人名）、全集只有一個講者、泛稱佔比 ≥40% 卻混著真名、單一講者連續獨佔 ≥15 分鐘。**用 08-07 的九集實測校準過**：初版對 9 集報 8 集（等於沒有訊號，因為「名字不在主持人名單裡」對每一位來賓都會響），改用「格式異常」而非「不在名單」後降到 5 集，每一則都對應實際發生的問題。
  - **明確的能力邊界**：抓不到「合理人名張冠李戴」。08-07 的 All-In 把開頭 13 分鐘的 Jason Calacanis 整段標成 Chamath Palihapitiya——標籤本身完全合理，只是錯的。這一類只能靠讀內容時的上下文校正。
- **修正兩個官方稿來源的取法**（第 1 節）：GS Exchanges 的 slug **不是節目標題的直譯**，拼錯會回空頁而不是 404、看起來像「沒有官方稿」；MacroVoices 官方稿**通常落後一週以上**，當集退回 podfetch 是正常的，不是失效。
- **額度表改為「10–20，會浮動」**。08-05 從 20 掉到 10、08-07 又回到 20。**Lite 優先的理由是「500 遠大於 10–20」這個量級差，不是某個特定數字**，不要拿浮動的值去做設計判斷。
- **字數規格補上來源密度的例外**：官方逐字稿沒有廣告與口頭贅語，同片長的實質內容多三到五成，超出層級上限一成左右是合理的——**不要為了壓進區間而砍掉整段論點**。
- 補 `gsx`／`nopriors` 兩組 CSS，`index.html` 現為 19 組；現役 22 檔中仍缺 `bg2`／`hardfork`／`acquired`／`lennys` 四組。
- **回歸檢查抓到我這次改動的三個瑕疵，一併修掉**（子代理獨立比對）：
  - **講者訊號原本會直接把 `status` 打成 `DEGRADED`**，因為它和完整度訊號共用同一個 `warnings` list。實測 08-07 九集會有五集誤降，而**唯一真正標錯人的 All-In 反而零訊號**——降級的分佈與實際品質相反，而且 `DEGRADED` 的定義是「完整度不足」，寫進網站 `source` 的降級理由會是假的。已拆成獨立的 `speaker_notes`／`speakerNotes` 欄位，不影響 `status`。
  - **`SPEAKER_LINE` 正則只認 `[MM:SS]`**，而合併後超過一小時的行是 `[H:MM:SS]`——一小時以上的節目後半整段不進統計。已改為兩種都吃。
  - **泛稱分類用行首錨定**，`*Speaker 3` 會被歸進「真名」，同一個標籤在不同訊號裡身分互相矛盾。已改為子字串比對。
  - 順帶新增第五個訊號「**裸時間戳**」：有時間戳但整行沒有講者標記。08-07 的 MiB 有 **71%（167／234 行）**是這樣，而完整度 1.00、狀態 OK——正是使用者回報的「1:00–1:19 整段無標記」，先前完全看不出來。
- 同步 `README.md`（原本宣稱「逐字稿標的是真實姓名而非講者 A」，與保守標記直接矛盾）、`MAINTENANCE.md` 的 CSS 組數與健康檢查項目清單、`config.json` 的額度註解。

### 2026-08-06（第二次，回歸複驗）

- **修掉一個會寫進逐字稿的錯數字**：`WORDS_PER_MIN` 早已改為 **165**，但字數不足的警告字串仍寫死「130 字/分基準」——**那句會進 front matter 與 manifest**，等於用 165 計算卻對使用者報 130。已改為引用常數本身。第 2 節與程式碼註解也一併更正。**`healthcheck.py` 抓不到這一類**：`WORDS_PER_MIN` 是程式常數、不在 `config.json` 裡，「brief vs config」涵蓋不到。
- **輪換上限補上時間維度**（`episode_budget_seconds: 1200`）。昨天只加了次數上限，但**每次輪換都可能等一輪 300 秒冷卻，18 次就超過一小時**，而 podfetch 01:00 到日報 03:00 只有兩小時窗口。20 分鐘的話五集全撞上也只花 100 分鐘。
- **修正規格與程式碼不符**：昨天寫「撞到就讓該段失敗」，實際上 `RuntimeError` 會穿出分段迴圈使**整集** `FAILED`。已據實改寫，並說明為何可接受（快取保留、`last_run_utc` 不推進、下次接續）。
- **拆開計數器**：`rotations` 原本同時計過載輪換與額度輪換，卻只有過載分支檢查上限。改為 `overload_rotations` 只計過載，額度輪換天然受池子大小限制。
- **`ModelOverloaded` 的 docstring 還留著被推翻的舊語意**（「走與 404／日額度相同的輪換路徑」），註解與實作互相矛盾，已改寫並加上「不要再併回去」的警語。
- **`MAINTENANCE.md` 第 6 節清掉兩項已完成的待辦**：plist 與 `pmset` 已連續三天（08-04／05／06）驗證通過，卻還掛在待辦且措辭是未來式。FT 那條也更新為反映封鎖清單。
- 順手修掉變更紀錄的四處小錯：根因日期記成 08-05（應為 08-04）、「八處」底下只有七條、分層表邊界在紀錄裡還是舊的重疊寫法、「本節設定清單」實指第 2 節。

### 2026-08-06

- **修掉 08-04 我自己引進的 bug：503 不再讓模型永久除名。**（`ModelOverloaded` 是 08-04 加的，當時錯在把它接進 404／日額度那個 handler；08-05 的改動是 `prefer_lite` 與額度表，與此無關。） 新增 `OVERLOADED` 冷卻表（`config.json` 的 `overload_cooldown_seconds: 300`），500／503 改為冷卻 5 分鐘後自動回池；`EXHAUSTED` 回歸原設計，只放日額度用盡與 404。**08-06 的實況是：三個 500 RPD 的 Lite 在 77 秒內被一次過載尖峰全數永久除名，剩下 10 RPD 的 Flash 扛完 13 段裡的 10 段，把 3.6 Flash 燒到 22/10。** `prefer_lite` 本身生效了，是冷卻語意錯誤把它的效果整個抵銷掉。經過見 `MAINTENANCE.md` 第 7 節。
- **摘譯字數改為依節目長度分層**（第 3 節）：未滿 15 分 1,200–1,800／15–30 分 2,000–3,000／30–55 分 2,500–4,000／55 分以上或 10 個以上獨立主題 4,000–6,500（邊界含下界、不含上界）。**原本一律 2,000–3,000 每天都被違反**——57–65 分鐘的節目壓到 3,000 字必然砍掉整段內容，執行者只能超規格後在回報裡解釋。**天天被違反的規格不是規格。** 上限與「不要灌水」的紀律仍在，超出所屬層級才需要說明。
- **`ft.com` 已進入本環境 `web_fetch` 的封鎖清單**（HTTP 403 `URL is on blocklist`）。第 2 節退援第 3 層改為明寫「只能走 Chrome」，並標註**這條路徑從未在無人值守的排程中實測過**，卡住就退回 podfetch 那層並在回報中明講，不要空等。
- **回歸檢查抓到的其餘七處一併修掉**（子代理獨立比對）：
  - **第 2 節「四個不要改的設計」第 4 項還寫著舊語意**（「走與 404／日額度相同的輪換路徑」）——那正是這次事故的根因寫法。**規格與同一份檔案裡的變更紀錄互相牴觸，而且規格那半段跟程式碼不符**，已改寫。
  - **第 0 節仍寫死「約 2,000–3,000 字」**，與第 3 節的分層表直接打架，而且它在整份文件最前面、執行者會先撞到被廢掉的那條。`README.md` 同一句也一併改。
  - **我這次的改動製造了一個新問題**：503 改成「只冷卻不除名」後，`while True` 沒有輪換上限，供應端持續過載時本段會在池內無限輪轉，**唯一的自然終點是把當天所有額度燒到 429**。已加上 `len(pool) * 3` 的上限，撞到就讓該段失敗。
  - 日誌的「改用 X」原本只排除 `EXHAUSTED`、沒排除冷卻中的模型，會指錯下一個模型——**08-06 的根因正是靠這串日誌反推出來的**，誤導會直接影響下次排查。
  - `overload_cooldown_seconds` 補進第 2 節的設定清單與 `healthcheck.py` 的 `CONFIG_KEYS`。
  - 分層表的邊界值重疊（15／30／55 各有兩個答案），已改為「含下界、不含上界」。
  - `MAINTENANCE.md` 第 7 節 08-04 那篇的修法已被推翻卻沒有時效標註，已比照 08-02 額度表加註。
- **新增「講者標記誤植」的寫作紀律**（第 3 節）。08-06 實例：完整度 1.01、狀態 OK，但 Gemini 從中段起把主持人標成片頭廣告旁白者，而開場已說明該主持人請假。**這是字數檢查抓不到的失敗態**——`shows.json` 的主持人名單只是提示、不保證歸屬正確。金句與立場歸屬要交叉驗證，判定不了就寫節目名。SKILL.md 的主動回報清單也補上這一項（現為十項）。

### 2026-08-05

- **Google 把新版 Flash 的免費層 RPD 從 20 砍到 10**（3.5／3.6；2.5 Flash 仍是 20，Flash-Lite 3.1／3.5 仍是 500）。第 2 節額度表已更新。
- **模型池順序反轉為 Lite 優先**（`config.json` 新增 `prefer_lite: true`，`podfetch.py` 的 `build_model_pool` 依此決定 `take()` 順序）。**注意：光改 `model_preference` 沒有用**——舊版一律先 `take(heavy)` 再 `take(lite)`，池子順序與偏好清單無關，必須改程式。08-05 實測預估 17 個請求實際打出 42 次（重試放大約 2.5 倍），Flash 兩個模型雙雙超限而 Lite 只用 8 次；Lite 完整度 1.14–1.16 與 Flash 無可辨識差距。
- **補兩個會安靜失敗的官方稿陷阱**（第 1 節）：Substack `/api/v1/archive` 回過期快取（Dwarkesh、Latent Space 分別停在 6/08 與 7/08，要帶 cache-buster）；`web_fetch` 約 104,700 字元上限會截斷長逐字稿且不報錯（Latent Space 只取到 73%，末段以 podfetch 補齊）。**兩者失效的樣子都是「今天沒有官方稿」，於是安靜退回機器轉錄。**
- 補 `dwarkesh`／`iltb`／`pivot` 三組 CSS，`index.html` 現為 17 組。
- **還原被覆寫的排程 SKILL.md 四項改動**（08-03 第五次巡檢加的：iTunes 子代理的 US 商店快取規則、第 7 節合規入口、回報清單的「寫不進 repo」、三段排查第 3 格）。**08-04 我用 `update_scheduled_task` 送出完整 prompt 時把它們蓋掉了**——`prompt` 是整份取代不是局部編輯，而我以為自己就是上一個改它的人。經過與教訓見 `MAINTENANCE.md` 第 7 節，維護 skill 已加入對應的檢查步驟。
- **修掉子代理比對抓到的其餘不一致**：`config.json` 的 `_comment_quota` 仍寫舊的 20 RPD（與同檔 `_comment_pool` 打架）；`MAINTENANCE.md` 第 7 節 08-02 那篇的額度表沒標明是當時值、讀起來像現行規格；MAINTENANCE 多處仍寫 20 檔／9 組 CSS；「三個不要改的設計」下面有四項、「三個已知陷阱」下面有五條；本節變更紀錄未由新到舊排序；Masters in Business 被誤接在 Compound 的說明結尾而不在 B 類清單裡。

### 2026-08-04（首次實跑後的修正）

- **`pmset` 那套設定驗證成功**：08-04 的 podfetch 日誌開頭是 `[01:00:00]`，準時執行。08-03 的延後補跑問題確認解決。
- **修掉一個會靜默吃掉集數的去重缺陷（本次最重要）。** 08-04 有 3 集因 Gemini HTTP 503 轉錄失敗，日報照規則寫成 `⚠︎ 全文摘譯待補` 的佔位。但 podfetch 失敗時不推進 `last_run_utc`，下次執行必定重抓同一集——**而去重規則會比對 `url`／`title` 把它當成「已收錄」而略過，補回來的完整逐字稿因此永遠進不了網站，且毫無徵兆**。這是設計本身保證會發生的組合，不是偶發。已在第 1 節加入例外：`source` 以 `⚠︎` 開頭的集數不算已收錄，拿到全文要重寫並跨檔搬移。同時明訂 **`⚠︎` 是機器讀的標記，不可改寫成別的格式**。
- **`healthcheck.py` 兩處誤報，都是這次實跑才暴露的**：(1) 缺 CSS 原本判 FAIL，但 brief 明訂那只是視覺不一致、功能正常，改為 WARN；(2) showKey 命名檢查原本拿全部歷史資料比對 `shows.json`，於是移除節目後會產生**永久性假警報**（歷史檔案裡的 `capitalallocators` 永遠都在），改為只檢查最新一份，並把僅存在於歷史的鍵值單獨列為預期狀態。
- **新增「待補全文」檢查**：列出最新一份裡 `⚠︎` 開頭的集數，提醒下次要重寫。
- 補上 `oddlots` 與 `capitalallocators` 的 CSS（前者是現役節目卻一直沒定義，08-04 第一次出現；後者為了讓歷史資料正常顯示），`index.html` 現為 14 組。
- **`podfetch.py` 新增 `ModelOverloaded`：500／503 重試兩次就換模型。** 從 AI Studio Console 的當日數據反推才看清根因——兩個 Flash 的 RPD 都爆到 21/20，而 Flash-Lite 只用了 9/500 與 1/500，503 才是主要錯誤（約 22 次）。**舊版只有 429 日額度會觸發輪換，503 則在同一模型上把 5 次重試燒完再判定整集失敗**；由於重試也計入 RPD，等於一邊燒掉 Flash 額度、一邊放著 491 次 Lite 額度不用。順帶確認 TPM 只用到 12–15%，brief 原本「瓶頸是 RPD 不是 TPM」的判斷成立。
- 08-04 那兩集 Capital Allocators 佔位已從 `data/2026-08-04.json` 移除（空殼內容，且節目本次下架不會再補），該日 6 集改為 4 集，`index.json` 同步。

### 2026-08-03（第五次，同步巡檢）

- **撤銷「podfetch 延後補跑」這個根因判定，`MAINTENANCE.md` 該篇事故紀錄已刪除。** 該篇宣稱 08-03 日誌是 `[07:00:01]` 的原因是機器睡眠、且「plist 一直都是 `Hour = 1`」。實情相反：**當天 plist 就是 `Hour = 7`**（那也是重整前本檔第 6 節記載的設計值），是當日稍晚才由使用者改成 1；調查者讀到的 `Hour = 1` 是修改後的狀態，卻被當成修改前的證據。`[07:00:01]` 這種「整點加一秒」正是 `StartCalendarInterval` 準時觸發的特徵，不是喚醒補跑。**`pmset` 那組電源設定獨立保留**——01:00 的工作在會睡眠的 Mac 上確實需要它，那部分的決策沒有問題（第 6 節、`MAINTENANCE.md` 第 9 節）。
- **時間戳排查順序改為 plist 優先**（原為 `pmset -g custom` → `pmset -g sched` → plist）。plist 最便宜也最確定，且「整點加一秒」的特徵直接指向設定值。同時加上一條通則：**先查到的那一項若已經被人改過，很容易把「修改後的狀態」誤讀成「修改前的證據」**，所以三項都要看完。
- **補上排程 SKILL.md 給 iTunes 子代理的指示裡缺的「US 商店快取」規則。** 子代理讀不到本檔，而該規則只寫在本檔第 1 節，實測會導致退援路徑漏抓 All-In（US 商店回報最新只到 7/18，GB 商店為 7/31）。同時把「怎麼換國別商店」寫成具體網址格式，不再只說「改用 GB／AU」。
- **SKILL.md 補上指向第 7 節「合規與語氣」的入口**。第三次重整時漏了，導致來源標註、付費來源只做重點濃縮、非投資建議這些會影響交付內容的規則沒有任何入口。
- **修掉四處內部矛盾**：窗口接縫敘述與 `last_run_utc` 自癒機制對撞（已釐清為「只有走 iTunes 退援路徑時 26 小時才是硬邊界」）、檔名慣例的舉例仍停留在 09:00 時代、Unhedged 同時被放在退援第 1 層與第 3 層（已說明 FT 流程就是它取得官方稿的方式）、Masters in Business 在 A 類表格說「新集數改走 B」但 B 類清單裡沒有它。
- **釐清 `limit=8` 與「limit 越小快取越舊」的拉扯**：`limit=8` 是起手式，發現回傳過舊就換國別商店重查。
- **SKILL.md 的主動回報清單由七項增為八項**，補上「寫不進 repo `data/`」——該情境的處置本來就寫在本檔第 5 節，但沒進封閉列舉的回報清單，等於會安靜地不被講出來。
- **補上時刻漂移守衛的死角**：守衛原本掛在「今天的日誌」上，但若 podfetch 的時刻漂到 03:00 之後，日報執行當下今天的日誌必然不存在，會直接落進「podfetch 失效」分支，守衛永遠不會觸發。已在三段排查加入第 3 格：今天的日誌不存在時，**先讀 `logs/` 裡最新那一份的開頭時間戳**來區分「時刻漂移」與「真失效」，兩者的修法完全不同。

### 2026-08-03（第四次，節目異動）

- **移除 Capital Allocators，加入 Business Breakdowns（1559120677）、In Good Company（1614211565）、The Compound and Friends（1456467014）。節目數 20 → 22。** Capital Allocators 本身完全正常（812 集，當天還發了新集），移除是取捨不是汰換：它談的是機構配置者與 LP-GP 關係，在這份清單裡最不直接可用。
- **補上的是一個真正的缺口：個股／公司基本面拆解。** 原本 20 檔裡沒有任何一檔在做這件事——ILTB 是投資人訪談，不是公司拆解。Business Breakdowns 每集拆一家公司的商業模式、單位經濟、護城河、估值與風險，來賓多是實際持有部位的 buy-side。
- 三檔都已加進 `shows.json`、`config.json` 的 `show_priority`，`index.html` 也補了三組 CSS（現為 12 組）。
- **驗證候選時踩到 brief 早就警告過的坑，值得記一筆**：Business Breakdowns 的節目層 `releaseDate` 顯示 2026-05-29，看起來停更兩個月；拉集數層才發現最新是 07-27。**節目層那個日期就是第 1 節說的「舊資料，不要誤判」**，差點因此把一檔活著的節目判死。挑選節目時一律要看 `entity=podcastEpisode` 的結果。
- **另外發現 iTunes 的 `search` 端點透過 `web_fetch` 回空字串，`lookup` 正常。** 找新節目的 AppleID 不能靠 `search`，要用 WebSearch 或直接從 Apple Podcasts 網址取 id。已補進第 1 節的已知陷阱。

### 2026-08-03（第三次，結構重整）

- **本檔與 `MAINTENANCE.md` 重新分工**：brief 只留規格與判斷規則，事故經過／誤判過程／被否決的選項全部搬到 `MAINTENANCE.md` 第 7 節「事故與決策檔案」，YouTube 操作要領搬到第 8 節。**動機是本檔已膨脹到 42 KB 而排程每天要完整讀一次**，其中很大一部分是執行者根本不需要知道的歷史；而且同一件事在兩處各寫一份，反而讓「哪個是現行規格」變得不清楚。
- **排程 SKILL.md 改為流程骨架**：只留執行順序與分支判斷，事實細節指向本檔章節，不再重抄。**這是為了根除「漏抄型」同步 bug**——08-03 第二次巡檢抓到的四處缺漏全部屬於這一類，只要兩份文件都在描述同一批事實，這種 bug 就會無限重演。保留了「讀不到 brief 時仍能自我修復連線」的最小集合，不會因為瘦身而失去韌性。
- **新增 `~/.podfetch/healthcheck.py`**：把每次維護都要手動重跑的機械式檢查集中成一支唯讀腳本（JSON 可解析與排序、`days` 與檔案雙向對應、`episodeCount` 相符、showKey 三處 CSS 與 `shows.json` 對齊、podfetch 日誌時間戳與收束狀態、逐字稿降級統計、推送鏈、**brief 引用的 `config.json` 數值是否與實際一致**）。最後一項專門抓 08-03 那類「下文改了、上文漏改」的內部矛盾。
- **開啟排程的 `notifyOnCompletion`**：0 集日不動 `index.json`、不產 Word 報告，網站毫無變化，先前完全無法區分「今天沒東西」與「排程根本沒跑」。這是這套系統反覆中招的靜默失效模式，補上通知作為最低限度的心跳。
- **修正去重的措辭**：原寫「前一天的 json」，但 0 集日不產檔，昨天的檔案可能不存在。改為「`data/` 裡日期最大的那一份」。
- **重構後的回歸檢查抓到九處問題，一併修掉**（子代理獨立比對）：
  - **給 iTunes 子代理的指示缺了完成任務所需的定義。** SKILL.md 把「窗口判定」外包給子代理，但子代理讀不到 brief，而 26 小時窗口、`releaseDate` 是 UTC、台北 ＝ UTC+8、Bloomberg 要用 `limit=20` 全都只寫在 brief 裡。**瘦身的代價就在這裡：指向章節對主代理有效，對子代理無效**，凡是外包出去的步驟，定義必須隨指示一起傳。
  - 「窗口邊緣又拿不到全文的集數不收錄」原本沒進 SKILL.md 的主動回報清單（那是封閉列舉的六項），被排除的集數不會出現在 manifest 狀態裡，等於靜默掉一集。已補為第 4 項。
  - **本檔與 `MAINTENANCE.md` 對「日誌時間戳不對」的第一步互相牴觸**：brief 寫「不要建議去改 plist」，MAINTENANCE 寫「兩種成因都要驗、先驗 plist」。前者把一次性的查證結果固化成永久前提，是錯的——已改為三項依序全查（順序於第四次修訂再調整為 plist 優先）。
  - **`pmset -c sleep 0` 這個主要保障原本完全不在 brief**，只有後備的 `repeat wakeorpoweron`。實際需要的是 01:00 到中午的連續清醒，只靠排定喚醒會在閒置後睡回去、03:00 接不到。同時刪掉「唯一一個不在設定檔裡的依賴」這句——蓋子、登入項目、充電管理軟體等前提同樣不在設定檔裡。
  - `~/.podfetch/cache/` 補進元件表（額度中斷時的續跑機制，排錯時看不到會困惑）。
  - `config.json` 的 `show_priority` 與 `model_preference` 補進說明，並註明 `healthcheck.py` 只檢查數值型、這兩個清單型的檢查不到。
  - `MAINTENANCE.md` 第 7 節一處章節引用指錯（「第 5 節」實指 brief 第 5 節），以及 07:30／07:35 的不一致（cron 設 07:30，帶 jitter 實際約 07:35）。
  - **`README.md` 整份過期且是 Public repo 的門面**：仍寫「每個平日早上 08:30」「其餘節目讀 YouTube 官方頻道字幕」，檔案結構也沒有 `MAINTENANCE.md`。已改寫為現行實況。brief 第 4 節的目錄結構圖原本也沒列 `README.md`，一併補上——**兩份文件的檔案清單互不涵蓋對方，是這次才發現的盲點**。

### 2026-08-03（第二次，維護巡檢）

- **修正 podfetch 與日報的時序倒置**：podfetch 實際跑在 07:00 而非 01:00，日報 cron 改 03:00 後會每天搶先執行、誤判成「podfetch 失效」而掉進已死的 YouTube 退援。修法是把 plist 的 `StartCalendarInterval` 從 `Hour = 7` 改為 `Hour = 1`。
- **第 2 節加入「時刻漂移」守衛**：排查時要順手確認日誌開頭時間戳是 `[01:`，不是就在當日回報中明講。
- **`pmset` 電源設定登記為基礎設施依賴**（第 6 節）。它與 plist 時刻是兩件獨立的事：plist 決定「幾點跑」，`pmset` 決定「那個時刻機器醒不醒著」，兩者都要對。
- **補齊 `index.html` 四組節目徽章**（`latentspace`／`lex`／`mib`／`twentyvc`），這四個 showKey 早已出現在資料裡卻一直走預設藍。同時要求 showKey 一律沿用 `shows.json` 的鍵值。
- **統一 YouTube 嘗試上限為 5 分鐘**（當時的 YouTube 操作章節寫 8 分鐘，與其他三處牴觸；該章節已於第三次重整搬到 `MAINTENANCE.md` 第 8 節）。
- **修掉本檔內部自打架的過期數值**：20 分鐘段／Files API（原寫 25 分鐘／base64）、`max_chunk_mb: 48`（原寫 9 MiB）、每日請求數（該數值已不列在本檔，改由 `config.json` 註解與 `MAINTENANCE.md` 第 7 節記載）、「每天（含週末）」（原寫「每個平日」）、26 小時（原殘留「26／74 小時」）。
- **補上排程 SKILL.md 缺的四條規則**：A 類節目仍須抓官方稿（原本的「不要開 Chrome」會讓官方稿永遠拿不到）、podfetch 主路徑同樣要去重、寫不進 repo 時的退援、YouTube 登入前提。

### 2026-08-03（第一次）

- **執行時段從 09:00 改到 03:00**，確立「前一晚美東晚間集數順延一版」的取捨。
- **資料夾連線不保證跨工作階段留存**：排程執行中讀不到就用 `request_cowork_directory` 自我修復。
- **確立「當天沒有目錄 ≠ podfetch 掛了」的三段排查。**
- **iTunes lookup 已知陷阱**（US 商店快取、RSS 回 `[binary data]`）。
- **驗證上線必須帶 cache-buster**，且要確認 `updatedLabel`。

### 2026-08-02

- **改以本機 podfetch 管線為主要全文來源**（第 2 節）：01:00 抓音檔經 Gemini API 轉錄，產出帶講者姓名的逐字稿。跨節目交叉觀察因此可以具體到人。
- **YouTube 字幕全面失效**，降為最後手段。

---

## 12. 版本登記簿（速查表）

第 11 節是敘事體，回答「為什麼」；這一節是速查表，回答「**動了哪些檔案、能還原到哪裡、當時想解決什麼、後來成立嗎**」。找歷史先看這裡定位，再回第 11 節讀細節。

### 2026-09-02（四批：擋發布的欄位、循環重複三尺度、gsx 入口、文件清債）

| 動的檔案 | 想解決什麼 | 後來成立嗎 |
|---|---|---|
| **A 批**：`kb-core/podcast/BRIEF.md`（第二節加 `lowerBoundException` 欄位＋第八節限定 claim 欄位；為回到預算砍掉第七節第五條的鑑識段與第二節 latentspace 那筆來歷）、`kb-core/scripts/podcast/DIGEST-PROMPT.md`（第 4 步補寫者、第 5 步補回訪最低要求）、排程 `SKILL.md` 整份覆寫 | 兩個「規則指不出資料在哪」：①`lowerBoundException` **全庫只有讀者沒有寫者**，真觸發下界例外會 FAIL 擋掉整輪，而三份文件都只說「在回報裡講」；②`BRIEF` 第八節「開帳之後不得改寫」沒限定欄位，**正是 08-30 訂正時被指認為打架對象卻沒回頭改的那一句** | **待觀察。** ①至今沒有集數真的觸發過下界例外，所以寫者對不對要等它第一次觸發才知道——**驗收條件：下一次有集數低於下界時，回執是 `exit 0` 而不是 `chars_in_tier` FAIL**；②`lastReviewed` **當場實跑三條驗過欄位寫得進去**（`obs-20260729-2/-3/-4`），且 claim 四欄以程式斷言比對過一個字沒動 |
| **B 批**：`kb-core/scripts/podcast/preamble.md` 第九節（改成三尺度表）、`kb-core/podcast/anchors.json`（`block_repeat_blind_to_stutter` 補行內與區塊級、`completeness_high` 補第三種成因）、`DIGEST-PROMPT.md` 第 1 步、本檔第 6 節 | 08-30 記的形態只有「行級」一種，而 09-02 一天就撞到另外兩種：**行內**（單行 27,888 詞、9 詞短句 ×3,100）與**區塊級**（21 行內容塊循環 20 輪）。三種工具全判「乾淨」，其中 oddlots 連 manifest 都判 OK、warnings 為空 | **部分成立，且推翻了一條先前的記載。** 三種尺度的偵測訊號是這一場實跑出來的（行內那個「行數 ÷ 總字數」就是當天發現它的方法），**但「段級語速 warning 是第二訊號」這條被 oddlots 推翻**——蓋掉 10 分鐘而 warning 為空，已改成「排序用、不是守門員」。**修法仍未實作**，且 08-30 驗證的「按 `first` 聚合」對行內那一種無效 |
| **C 批**：`AGENT_BRIEF.md` 第 1 節 gsx 那一列、`DIGEST-PROMPT.md` 回報清單新增三項必答 | gsx 是 A 類，但該列開的兩條取 slug 的路（列表頁、WebSearch）09-02 實測都不通，**而該列半個字沒改**；且它沒有 FT 那條「走通與否都要在回報寫一句」的累積紀律，於是失效不會累積成證據、只會每天重撞 | **待觀察。驗收條件：下一次 gsx 有新集數時，回報裡有沒有那一句。** 有的話這條紀律成立；沒有的話代表「寫進回報清單」這個做法本身接不上，要換成別的接法 |
| **D 批**：`skills/maintain/podcast/` 的 `MAIN.md`（刪快照那段自相矛盾、補正副本辨識法）／`MODIFY.md`（補 08-30 漏量與 09-02 兩列）／`FILES.md`（雙寫兩處→三處）／`SYNC-CHECKLIST.md`（mtime 錨點改綁登記簿、拿掉行數判準）、`kb-core/checks/podcast.py`（「八成」改成帶時點的三筆）、`anchors.json`（`subagent_threshold_kb` 除役） | 六處文件自打架或過期數字，其中三處是「判斷兌現了、沒有人回頭刪那一段」的同型 | **成立（純文字，當場可驗）。** `anchors.json` 與 `checks/podcast.py` 改完各驗一次可解析／可載入 |

> **本場自己犯的一個錯，記在這裡因為它正是本節存在的理由**：整份覆寫排程 `SKILL.md` 時，我在副本裡多加了一行 09-02 的主持人不符紀錄，**而正本當下還沒有那一行** —— 等於在同步的當下製造了一個新的正副本漂移。發現得快（同一步就補回正本），但它證明了 `MODIFY.md` 那條「先 Read 目前檔案、以它為基礎修改」**擋得住「拿記憶重寫」，擋不住「覆寫時順手加東西」**。**規矩要補一句：整份覆寫時不要順手改內容，要改就先改正本再重讀。**

### 2026-08-31（一批：推送鏈警告改讀回執）

| 動的檔案 | 想解決什麼 | 後來成立嗎 |
|---|---|---|
| `kb-core/scripts/podcast/healthcheck.py`（新增 `latest_receipt()`，`check_push()` 的領先分支改成四支） | 推送鏈 WARN 印「dashpush 每 180 秒推一次，稍等再看」，而 dashpush 08-20 已退場。08-31 publish 卡在 `exit 15 @ worktree-dirty` 兩小時四十分期間，這則警告**主動給了錯的指示**——在最需要動作的時候叫人等一個不存在的推送者 | **✅ 成立，四支全部驗過。** 四個分支各測一次＋`latest_receipt()` 實測非空；healthcheck 重跑 8 PASS／5 WARN／**0 FAIL**，與改動前相同。**`exit 0` 那一支原本只有推理與程式證據，同一場結尾就被實地驗掉了**：提交本檔（`0e779e5`）之後 local 領先 origin 而回執仍是 `exit 0`，healthcheck 印出的正是那一支，「非 `data/` 的 commit 沒有排程會即時推走、要等下一份草稿」與實況完全相符。**順帶也證明了舊訊息在這個情境下同樣是錯的**——「dashpush 每 180 秒推一次，稍等再看」在這裡等不到任何東西 |
| 同上（複驗後的第二輪修正） | 初版自己犯了兩個同型錯：註解抄了被 `launchd/README.md` 明文禁止複製的 plist 數量；`exit 0` 分支把「叫人等錯的東西」複製了一份 | **成立，但要記的是它怎麼被抓到的**——兩個都不是主線發現的，是 `MODIFY.md` 那條「改完再叫一次子代理獨立比對」抓的。**這是那條規則第一次真的擋下東西**（前幾次都是回報「沒有新的不同步」） |
| 本檔第 4B 節「背景推送」＋第 6 節新增一條待辦 | 同一個 dashpush 錯誤的**第四份副本**（第 2 節第 9 條 08-30 已訂正並數過前三處，唯獨漏了第 4B 節這一處） | **成立。** 順帶補上兩個從來沒寫進文件的邊界：空輪次不碰 git、`staged_paths` 只有 `data/`。**教訓寫進 08-30 那條註記的旁邊**：它自己就寫著病因是「每次都只修被指出來的那一處」，而同一場維護沒有照它 grep 全庫 |

**上一場（08-24）的結案**：

- **`publish.py` 的 `dirty_outside()`：待觀察 → ✅ 成立。** 08-24 寫的驗收條件是「下一次維護留髒檔時，回執是不是一次 `15 @ worktree-dirty` 就停」。08-31 正是那個情境（08-30 留下兩個未提交的非 `data/` 檔），**它停了**，兩小時四十分只產出同一則講得出成因的回執——對照 08-24 舊版同樣情境累積的 188 筆本地 commit。
- **`kbdocx.podcast` 04／07／10／13：待觀察且尚未生效 → ✅ 成立，且已在 Mac 上生效。** 08-24 寫「第一個能驗的日子是下一次發布晚於 04:00 的那天」——08-31 就是（06:13 才發布成功）。實際軌跡：04:00 依回執 `exit 15` **正確拒絕**、07:00 轉出 `podcast-2026-08-31.docx`（2 集、53,331 bytes）、10:00 判定日檔未動而跳過。**三個時刻的行為各不相同且都正確**，且 04:00 那則訊息只存在於新版腳本，等於同時證明 plist 已重裝。
- 另外兩列維持原判：`shows.json` 補 Katie Martin 仍是「部分成立」（機制沒建，而**08-31 又多了一筆證據**——bloomberg 第四次 hosts 不符，見第 6 節）；`DIGEST-PROMPT`／`SKILL.md` 的 `exit 14` 叮嚀仍是待觀察，今天沒有觸發 `14`。

**三個 bullet 的重讀**：

- **該進來的**：**「檢查印出來的建議本身會過期，而且沒有任何東西在看它」**——這次的根因不是程式邏輯錯，是一句**寫死在訊息裡的因果宣稱**跟著基礎設施一起爛掉。`healthcheck.py` 還有幾則同型的字串（例如各處「稍等再看」「請在 Mac 上跑一次」），目前沒有機制在對帳。**已列入第 6 節待辦，不當場加檢查**——理由見下一點。
- **該移類的**：無。但要記一件事——本場一度想加「掃描 kb-core 裡所有 dashpush 字串」的檢查，**沒有加**，因為 `MODIFY.md` 說新增檢查前先問「正常情況會不會也觸發」，而 dashpush 在歷史紀錄裡有十幾處合法出現（`chart/CHANGELOG.md`、`push_kbcore.py` 註解等），**單靠字串比對必然對它們全部誤報**。這一條要修的是「宣稱它還在跑」與「記錄它退場了」的差別，而那個差別機器目前判不出來。
- **共同點仍然成立，但這次是它的鏡像**：前幾場記的是「正常路徑有效、極端路徑放大傷害」。這次的守衛（`dirty_outside()`）正是為極端路徑寫的，而它**做對了**——真正出問題的是旁邊那則**在極端路徑上給出正常路徑建議**的訊息。**守衛擋得住狀態，擋不住敘述。**

### 2026-08-24（三批：發布防護／Word 補跑／hosts 回檔）

| 動的檔案 | 想解決什麼 | 後來成立嗎 |
|---|---|---|
| `kb-core/tools/publish.py`（新增 `dirty_outside()` ＋ 4a 區塊） | publish 的 `git add` 只涵蓋 `staged_paths`，對 repo 根目錄的未提交變更是瞎的，失敗形態是每 60 秒重試一次且**每輪先 commit 一次**——08-24 累積 188 輪、188 筆推不出去的本地 commit（187 筆只改 `index.json` 一個時間戳） | **待觀察。** `/tmp` 實測十個解析邊界全過，且實測證明它擋下的狀態原本就會在三行後的 rebase 倒下（**所以不可能比現況更嚴**）。真正要看的是下一次維護留髒檔時，回執是不是一次 `15 @ worktree-dirty` 就停 |
| `kb-core/launchd/com.kenny.kbdocx.podcast.plist`（04:00 → 04／07／10／13）＋ `kbdocx-podcast.sh`（訊息與註解） | 04:00 讀到的回執是那一刻的狀態不是那一天的結局；08-24 publish 06:43 才成功，Word 是人工補的 | **待觀察，且尚未生效**——plist 是版控副本，要在 Mac 上重裝。第一個能驗的日子是下一次發布晚於 04:00 的那天 |
| `kb-core/scripts/podcast/shows.json`（`unhedged` 補 Katie Martin） | 連續三天回報 hosts 不符而名單一個字沒動，缺的是「把回報接回檔案」那一段 | **部分成立。** 這次是人工接的，**接回去的機制仍然不存在**——第 6 節那條缺口只縮小、沒有關閉 |
| `kb-core/scripts/podcast/DIGEST-PROMPT.md` ＋ 排程 `SKILL.md`（整份覆寫） | `14` 只寫「會自己重試」，讓執行者沒有理由介入 | **待觀察。** 這是叮嚀不是條件——`MAIN.md` 說「能寫成條件的就寫成條件」，而**這一條寫不成條件**（執行者手上只有回執，看不到歷史回執），所以它天生比另外三批弱。已補上唯一有歷史的出處（`grep '回執：' publish.log`），但要不要看仍靠自覺 |
| `healthcheck.py` 新增 `check_skill_copy()`＋`_skill_copy_dir()`；`skills/maintain/podcast/{MAIN,MODIFY}.md` 補兩條硬規矩 | 維護技能有兩份而**沒有任何東西在維持它們相同**，不同步的樣子是「維護照常進行，只是照著過期的規則做」 | **當場成立。** 六個情境全驗（相同→PASS／內容不同→WARN 具名／副本缺檔→WARN／副本多檔→WARN／找不到→**SKIP 不是 PASS**／kb-core 不在→SKIP）。重新打包安裝後實測 27 個檔全同。**選 WARN 不選 FAIL 是刻意的**：任何一場維護只要改了正本、還沒重新打包，這條就會亮 —— 那是常態而且正是要的提醒，把常態印成 FAIL 會訓練人忽略 FAIL |

**三個 bullet 的重讀**：

- **該進來的，已在同一場關掉**：`kb-core → 技能副本` 的複製原本沒有任何維持機制（本場子代理實測七個檔落後，其中 `MAIN.md` 第 1 步第 3 項**方向相反**：副本叫人把回報數字抄進 `metrics.csv`，正本 08-23 明令不要；`SYNC-CHECKLIST.md` 副本只有正本的四成，**而那份正是用來查漂移的清單**）。這是「用來查漂移的工具自己漂了」的第二次，所以沒有留成待辦 —— 當場加了 `healthcheck.check_skill_copy()`，並重新打包安裝，實測副本已與正本 27 個檔全同。
- **該移類的**：第 6 節的 hosts 缺口從「未處理」改成「部分處理」——資料補了，機制沒建。
- **共同點仍然成立**：這三批擋的都是「正常路徑有效、極端路徑放大傷害」。188 筆 commit 那一筆尤其典型——**正常那天它一輪一筆、完全正確；失敗那天它一分鐘一筆、跑了三小時。**

### 回溯機制（2026-08-08 建立，**2026-08-22 退役**）

> **這一整節已經不適用。** `snapshot.sh` 於 2026-08-22 退役，跑它不會建立任何快照。
> 理由是它保護的東西已經不在它以為的地方——八個目標有七個現在住在 `~/kb-core`、有 git、
> 且 `com.kenny.kbcorepush` 每 300 秒自動推；唯一在 git 之外的 `~/.podfetch/metrics.csv`
> 已合併進 kb-core 並改成 symlink。**要還原或看歷史：`cd ~/kb-core && git log -- scripts/podcast/`。**
> 下面保留原文，因為它記錄了當初為什麼需要快照，以及「空快照比沒有快照更危險」那條判斷——
> **那條判斷正是退役的理由本身**：一個照舊印「快照完成」的腳本會讓人以為還原點存在。



**repo 內的 `AGENT_BRIEF.md`／`MAINTENANCE.md`／`README.md`／`index.html`／`data/` 有 git。** 但改動最頻繁的檔案全在 repo 外、**在 2026-08-08 之前完全沒有版本紀錄**——`podfetch.py` 這一週至少改了 8 次，排程 `SKILL.md` 至少 9 次而且**被整份覆寫過一次**（08-04），當時無法還原、只能靠子代理事後比對發現。

```bash
bash ~/.podfetch/snapshot.sh "開工前"      # 每次維護開工與收工各存一份
bash ~/.podfetch/snapshot.sh --list        # 列出所有快照
bash ~/.podfetch/snapshot.sh --diff A B    # 比對兩份
cp ~/.podfetch/snapshots/<時間戳>/podfetch.py ~/.podfetch/   # 還原
```

存 `podfetch.py`／`healthcheck.py`／`json2docx.py`／`snapshot.sh`／`fix-schedule.sh`／`config.json`／`shows.json`／`SKILL.md`，附 `MANIFEST.txt` 記 SHA 與大小。**`gemini.key` 與 `state.json` 刻意排除**（金鑰不入快照；執行狀態不是設定，存它只是雜訊）。

> **在 Cowork 沙箱裡跑這支腳本會缺 `SKILL.md`**（沙箱看不到 `~/Documents/`），要在 Mac 的終端機跑才完整。`snapshots/2026-08-08-1852/` 就是這樣產生的不完整快照，內附說明檔。

### 指標基線（`~/.podfetch/metrics.csv`，每次跑 healthcheck 自動追加）

**動機**：08-08 做了一次大幅 token 優化，卻只能「估算」降幅，因為從來沒量測過任何一天。**沒有基線，任何優化都只能自稱有效。** 同一天重跑會覆寫該日那列。

欄位（16 欄，2026-08-09 由 12 擴為 15、2026-08-17 ① 再擴為 16）：`date`／`episodes`／`ok`／`degraded`／`failed`／`transcript_kb`／`output_json_kb`／`brief_kb`／`skill_kb`／`segments_done`（08-10 前叫 `podfetch_requests`，但它數的是完成段數不是 API 請求數——實測請求是段數的 2.5 倍，舊名會讓人拿它估 RPD 而低估）／`podfetch_minutes`／`speaker_flags`／**`eff_tokens_k`／`subagents`／`agent_turns`／`subagent_tokens_k`**

**擴欄時舊列會少欄。** 表頭改成 15 欄而歷史列還是 12 欄，整份檔案就無法按欄名解析——而這份檔案存在的唯一目的就是當可解析的基線。`healthcheck.py` 現在每次重寫時會把短列補空欄到與表頭等長（2026-08-09 由子代理抓到）。

> **08-15 那一列不可與其他日子直接比較（2026-08-16 註記）。** 它記的是 `31891/15/186`——每集 319 萬，是 08-14（28.3 萬）的十倍。**那不是成本回頭惡化，是事故當天的維護成本混進了產出成本**：那天要做假零集判定、外部對照、`state.json` 回捲、重跑 podfetch，再補完 10 集的日報。**15 個子代理裡只有 10 個是日報本業**（3,492K／68 回合），另外 5 個是集數盤點與文件稽核的一次性成本（5,166K），**比整個日報摘譯還貴**。
> **可比的數字是摘譯部分的每集 349K**（3,492K ÷ 10 集），與 08-14 的 283K 同一量級——**段數規劃那條紀律在事故日仍然有效**（10 集零重寫、子代理平均 2.4 次呼叫）。畫成本趨勢時用這個數，有效序列是 08-09 194 萬 → 08-11 57.6 萬 → 08-12 54.9 萬 → 08-14 28.3 萬 → 08-15 34.9 萬（摘譯部分）。
> **`eff_tokens_k` 於 08-16 由 28425 改為 31891。** 同一場工作階段量了兩次，中間只隔 14 個回合就多出 3,466K（+12%）——**那 14 回合的輸出只佔新增成本的 6%，其餘 94% 是重讀對話本身**。填表一律用**最後一次量測**，並記住這一欄的語意是「整場工作階段」，不是「日報產出」。

> **成本模型：驅動成本的是「每回合帶進多少新內容」，不是回合序號（2026-08-16 訂正）。**
> **這一條在 24 小時內被推翻過一次，經過本身就是教訓。** 08-15 的用量分析從**一段 14 個回合**的觀察（平均 248K，是當時全場平均的 1.8 倍）推論出「成本隨回合數接近平方成長」。08-16 把樣本拉到 250 個回合重算，**結論不成立**——每 50 回合的平均是 46K → 173K → 45K → 78K → 142K → 66K，**尖峰狀，不是單調上升**。
>
> 把每回合拆成三種成分才看得懂：
>
> | 成分 | 加權 | 佔比 | 行為 |
> |---|---|---|---|
> | 快取寫 ×2 | 13,224K | **54.2%** | **尖峰狀**——新內容進場時才發生 |
> | 快取讀 ×0.1 | 7,089K | 29.0% | 穩定上升（每回合 15K → 48K，約 3 倍） |
> | 輸出 ×5 | 4,106K | 16.8% | 與寫作量相關 |
>
> - **對話變長確實會墊高地板**（context 從 164K 升到 486K），**但它被 ×0.1 壓住，只佔 29%。**
> - **主導的是快取寫（54.2%），由「這一回合有多少新內容進場」決定。** 兩個尖峰段分別對應「派發 15 個子代理」與「大量檔案讀寫」。
> - **空轉的回合其實很便宜**（101–150 段每回合只有 45K）。所以「回合數」是個好用的代理指標，只因為多數回合都會帶進新內容——**它不是機制本身**。
>
> **現行四條紀律全部仍然成立，但其中一條的理由要改**：「一件事做完就開新對話」不是因為平方成長，而是因為長對話墊高每回合地板（3 倍），且新任務會把舊任務的紀錄一起帶進快取。另外三條（逐字稿不進主對話、子代理呼叫預算、先估篇幅不要重寫）在新模型下**理由更強**——它們擋的正是最貴的那 54.2%。
>
> **方法論教訓：拿一段 14 回合的觀察去推「成長曲線的形狀」，樣本不足以支撐那個結論。** 要判斷曲線形狀，最少要看完整區間的分段平均；要判斷成本來源，要拆成分而不是看分組——**只看分組（回覆／子代理／bash）會完全看不出「尖峰狀」這個性質**。

已回填 08-02 起的歷史。**08-08 是舊架構的最後一天，也是新架構的對照基準**：10 集、逐字稿 617 KB、產出 JSON 229 KB、brief 45 KB、podfetch 29 個請求、470 分鐘音檔、8 集有講者訊號。08-09 起是子代理架構的第一筆，但 08-08 那列沒有 `eff_tokens_k`（欄位是隔天才加的），**所以新舊架構仍然沒有可直接相減的 token 基線**——只能從 08-09 起往後累積。

### 逐場登記

| 日期 | 主題 | 動到的檔案 | 想解決什麼 | 後來成立嗎 |
|---|---|---|---|---|
| 08-02 | 建 podfetch 管線 | `podfetch.py`（新）、`config.json`、`shows.json`、brief | YouTube 字幕全面失效，全文取得綁死在 Chrome＋登入＋沒改版三個條件上 | ✅ 沿用至今；副產品是講者姓名 |
| 08-03 ①② | 改 03:00、三段排查、cache-buster | brief、SKILL.md | 起床就看得到；與投顧線拉開 token | ✅ 但代價是美東晚間集數順延一版 |
| 08-03 ③ | **規格與事故史分家** | brief（42→32 KB）、`MAINTENANCE.md`、`healthcheck.py`（新） | brief 每天被完整讀一次，塞滿執行者用不到的歷史 | ⚠️ 方向對，但變更紀錄兩天內把成效吃回去，08-08 才用歸檔規則根治 |
| 08-03 ④ | 節目異動 22 檔 | brief、`shows.json`、`config.json`、`index.html` | 移除 Capital Allocators，補個股基本面拆解的缺口 | ✅ |
| 08-03 ⑤ | 同步巡檢 | brief、SKILL.md | **撤銷 08-03 的根因誤判**（plist 當時就是 `Hour = 7`） | ✅ 教訓：讀到的現況不等於事發時狀況 |
| 08-04 | 503 換模型 | `podfetch.py` | 503 死守重試把 Flash 額度燒光，3 集全失敗 | ❌ **修法本身造成 08-06 事故**——503 併進 `EXHAUSTED` 讓 Lite 被永久除名 |
| 08-05 | Lite 優先 | `podfetch.py`、`config.json`、brief | Flash RPD 被砍半，稀缺資源排最前面等於開場燒光 | ✅ 但當天被 08-04 的冷卻語意抵銷，08-06 才真正生效 |
| 08-06 ① | **503 改冷卻不除名** | `podfetch.py`、`config.json` | 一次過載尖峰 77 秒內把三個 500 RPD 的 Lite 全部永久除名 | ✅ 08-07 實測 Lite 承擔 82% 請求 |
| 08-06 ② | 回歸複驗 | `podfetch.py`、`healthcheck.py`、brief | 修法沒有輪換上限，最壞會把當天額度全燒光 | ✅ 加了次數＋時間雙上限 |
| 08-07 | 講者保守標記＋線上檢查 | `podfetch.py`、`healthcheck.py`、brief、SKILL.md | 五檔節目講者誤植；推送鏈 PASS 但網站沒更新 | ✅ 08-08 抓到 Pivot 標籤對調（會把主持人的話掛到前國務卿頭上） |
| 08-08 ① | 跳針與時間戳 | `podfetch.py`、brief | 完整度指標在跳針失效態下**反向**（3.36 實為 1.10） | ✅ 實測還原吻合人工評估 |
| 08-08 ② | **token 優化** | SKILL.md（重寫）、`json2docx.py`（新）、brief（68.8→45 KB） | 逐字稿在主 context 被重複計費數十次；同一份內容 LLM 寫兩次 | ✅ **架構成立，降幅已於 08-11 證實**。08-09 實測：逐字稿確實一個字都沒進主對話（檔案操作僅 4.8%），但**五個子代理吃掉全程 84%**——成本從「一份大 context 被重讀很多次」變成「五份小 context 各自被重讀很多次」。好處是改為線性成長，集數多時才顯優勢。**08-11 實測：同樣五集 970 萬 → 288 萬，降 70%，落在當初粗估的 60–75% 區間內。** 但降幅主要來自 08-09 加的呼叫預算，本列的架構隔離是必要不充分 |
| 08-08 ③ | 版本控制與基線 | `snapshot.sh`（新）、`healthcheck.py`、本節 | repo 外的檔案零版本紀錄；優化降幅只能估算 | ✅ 快照機制此後每場都用到（08-10 靠它確認只有 SKILL.md 有變）；基線於 08-11 首次派上用場 |
| 08-08 ④ | FT 入口「更正」（**2026-08-15 事後補登**） | brief、SKILL.md | `ft.com/unhedged` 是電子報存檔、不含 podcast 集數 | ❌ **改成了另一個同樣不含 Unhedged 的頁面**（`ft.com/the-economics-show` 只列姊妹節目自己的集數），08-15 實際去跑才發現，正解是站內搜尋＋選 `Transcript:` 頁。**當時完全沒登記在本表**，只在 brief 第 1 節留下「（2026-08-08 更正）」五個字——**沒進登記簿的改動，等於沒有人會回頭驗收它** |
| 08-09 ① | 呼叫預算、token 量測、語速校準 | SKILL.md（任務卡）、`healthcheck.py`、`podfetch.py`、`shows.json`、brief、本檔 | 子代理回合數失控（單集 70 次呼叫）；降幅無法量測；完整度基準過低使 1.2–1.3 變常態 | ✅ **呼叫預算 08-11 受測並證實有效**（與 08-08 ② 合計降 70%）；⚠️ 但 **token 量測根本沒生效**，路徑寫成 CLI 的 `~/.claude/projects`，連兩天空值，08-10 才修 |
| 08-09 ② | 回歸複驗 | brief、`podfetch.py`、`healthcheck.py`、本檔 | 上一輪自己寫錯的因果與三處歸檔／擴欄後遺症 | ⚠️ **又是修法本身留下問題**：把 wpm 校準說成「修好 DEGRADED」（程式上不成立）、擴欄後歷史列錯位、歸檔時把「兩份一組要同步」這條規則一起搬走 |
| 08-10 ① | 同源去重、下界例外、子代理門檻、退援層號、26 小時統一、token 量測出聲 | brief、SKILL.md、`healthcheck.py`、本檔 | 同一支音檔被兩個 feed 推送而佔滿當日；規格缺「低於下界」與「低量日」條款；FT 被誤列成獨立的退援層；26 小時與 `last_run_utc` 字面互斥；token 量測讀錯路徑 | ✅ 08-11 起未再出現同源重複、退援層號混用或低於下界未回報；**前三個缺口共同形態是「主代理靠自覺補上規格的缺口」**，見第 7 節 |
| 08-10 ② | **新增 TIP（22→23 檔）** | brief、`shows.json`、`config.json`、`index.html`、`README.md`、SKILL.md（description）、本檔 | 週一那一版涵蓋美東週末，現有節目在該窗口幾乎零供給 | ⚠️ 節目本身待驗（08-11 起看它是否真的落進窗口、語速未校準）；**流程面已知失誤兩處**：README 節目清單漏補、撞色只查了三處中的一處，兩者都不在 `healthcheck.py` 涵蓋範圍，已補進第 5 節步驟表 |
| 08-10 ③ | **程式碼審查＋知識庫新功能** | `podfetch.py`、`config.json`、`healthcheck.py`、`snapshot.sh`、`index.html`、`data/observations.json`（新）、brief、SKILL.md、本檔 | 單集時限實為無上界（2 小時集最壞 4 小時）；壞快取永久固化；探測每天燒 Flash 池 30%；config 壞損靜默用錯參數；知識庫沒有記憶（觀察點寫完就丟、來賓查不到、品質看不到） | ✅ 08-11 實測通過（273 分鐘集只跑 11 分 44 秒，遠低於 60 分上限；無誤殺）。**時限語意與快取門檻都是防護機制**，改前已按第 12 節慣例逐項寫下最壞情況 |
| 08-10 ④ | **維護 skill 本身改版** | podcast-maintain skill（經 `save_skill` 覆寫；**它不在 snapshot.sh 涵蓋範圍**，版本只存在 Cowork 帳號端）、本檔 | skill 裡的過期清單直接造成了 08-10 ② 的 README 漏補（「四處」實為八步）；「連續三次」寫死的次數已腐化；缺這兩天新增的七個同步比對點與三條驗證教訓 | ✅ 08-11 起連續多場依新版流程執行，未再出現「照過期清單漏步驟」。**設計原則改為「指向單一事實來源，不再複製清單」**——skill 指向第 5 節步驟表與第 12 節登記簿，而不是自己再抄一份 |
| 08-11 | **首次量測驗收＋四項修正** | brief、SKILL.md、`index.html`、`data/observations.json`、本檔 | 呼叫預算與字數上界互相打架（三集寫完再壓縮）；acquired 無 CSS 實際撞到；記分板種子含免責雜訊；自報呼叫次數低報；超長節目不取官方稿無規格依據 | ⚠️ **驗收那半成立、修正那半有一項被證偽**。成立：08-08＋08-09 的 token 優化實測降 70%（970 萬→288 萬）、集層級時限實測 11 分 44 秒遠低於 60 分上限。**證偽：「先估篇幅再落筆」隔天重寫比例一模一樣（3/5）**——根因是它依賴的字數量測本身不可靠，08-12 改為以段數規劃 |
| 08-12 | **量測面的根因修正** | brief、SKILL.md、`data/*.json`（13 檔重算）、本檔 | 「先估篇幅」連兩天失效（3/5 集重寫），根因是子代理自報字數的誤差範圍 −38%～+81%；中斷重派比原本還貴 | ✅ **08-14 驗收通過**：12 集零重寫（前兩天都是 3/5）、段數達標 11/12、每集成本減半。**這是第一次修的是「規則依賴的量測」而不是規則本身，也是第一次一次就成功** |
| 08-14 | **段數規劃驗收＋時間軸檢查** | `podfetch.py`、`healthcheck.py`、brief、SKILL.md、本檔 | 驗收 08-12 的段數改動；時間戳崩壞三天無人看見；metrics.csv 缺沒跑維護的日子 | ✅ **驗收通過**：12 集零重寫、每集成本減半（54.9→28.3 萬）。**時間軸檢查已於 08-15 首次實戰命中**——MIB 那集 39% 片長落在大間隙且完整度 0.73，雙訊號同時成立，子代理獨立確認確有兩處跳段，全日 10 集無誤報；metrics 回填亦正常 |
| 08-15 | **iTunes cache-buster＋文件同步** | `podfetch.py`、`state.json`（回捲）、brief、SKILL.md、本檔 | 主路徑安靜漏抓當天全部 10 集（假零集）；FT 入口二度寫錯；子代理比對出 12 處文件不同步 | ✅ cache-buster 那半當天修完即救回全部 10 集，**08-16 的排程執行再度確認**：23 檔輪詢耗時 **10 秒**（事故當天是 1 秒），且視窗起點 `08-14 23:43Z` 早於前一次終點 `08-15 00:13Z`、有 30 分鐘重疊無缺口——**手動回捲與隔天排程正確銜接**。**這是第一次「主路徑」而非退援路徑失效**，唯一外顯訊號只有執行時間 9 秒→1 秒。⏳ **FT 入口那半是第三個版本，尚未在無人值守排程中跑過**（`ft.com/search?q="<原文標題>"` → 取標題帶 `Transcript:` 前綴的頁面）——**前兩個版本都是「更正過但沒驗證過」，這個版本目前也還在同一個狀態**，下次 Unhedged 有新集數時要在回報中確認走通了沒 |
| 08-16 ① | **0 集兩道防線** | `podfetch.py`、`config.json`（`stale_feed_hours`）、`healthcheck.py`（`CONFIG_KEYS`）、brief、SKILL.md、本檔 | 08-15 事故的兩個獨立成因：`discover()` 回空仍無條件推進 `last_run_utc`（暫時性失效變永久缺口）；「0 集」與「查詢壞了」在日誌上完全無法區分 | ⏳ **待實戰**。離線模擬四情境全對（事故日 15.2 天觸發／真 0 集 0.6 天通過／長週末 1.9 天通過／查無集數觸發）。**動防護機制前照慣例先問最壞情況**：健全性對照零額外請求、只印警告不改流程；不推進 `last_run_utc` 的最壞代價是真 0 集日重複查一次。**兩者都不會在極端路徑放大傷害，這是刻意設計的** |
| 08-16 ② | **換掉兩檔低頻節目（仍 23 檔）** | brief、`shows.json`、`config.json`、`index.html`、`README.md`、本檔 | BG2 掉到約每季一集、Business Breakdowns 約每兩個月，brief 仍把兩者當常態節目（第 6 節 08-15 已列為觀察中） | ⏳ **待實戰**。移出 BG2／Business Breakdowns，補進 Prof G Markets（1744631325，週一至五每日）與 Money Stuff（1739582836，每週五）；**兩檔的節奏都用帶 `&cb=` 的 lookup 逐集核實過，不是憑印象**。**流程面新增一條通則：移出節目時 `index.html` 的 CSS 一律保留**（歷史集數還在站上，刪了會掉色），已寫進第 5 節與 brief 第 4 節。SKILL.md 的 `description` 因總數不變而未動——**這是第 5 節第 6 步第一次不需要動，要留意「不必動」與「忘了動」在事後看起來一樣**。**2026-08-18 部分結案：Prof G 那半 ✅。** 08-18 它確實有集數落進窗口，節目運作正常；當天那集是 `Prof G Markets Is Taking A Break — You Should Too`——**兩週暑假（8/31 週一回歸），不是停播**，選它的理由（週一至五每日）沒有被推翻。**代價是 8/18–8/30 兩週日更供給少一檔**，若那段期間出現集數偏少的日子，先想到這裡、不要往 podfetch 失效的方向查。Money Stuff 那半仍待驗（每週五一集） |
| 08-16 ③ | 重播規則一般化 | brief（第 4 節通則）、SKILL.md（第 2 步、任務卡、回報第 14 項） | 「feed 放重播」原本只寫在 Unhedged 那一格，而新加的 Money Stuff 會發 `Re-Run:` 前綴的重播 | ⏳ **待實戰**。**第一版只改了 SKILL.md、brief 端沒立通則，卻已在 brief 第 8 節宣稱「兩邊都改了」**，由複驗抓出後補上——見 `08-16 ④` |
| 08-16 ⑤ | **排程自我量測探測＋成本模型訂正** | SKILL.md（新增第 5.5 步、回報第 16 項、開頭成本段）、brief、`metrics.csv`（08-15 改 31891）、本檔 | ①`eff_tokens_k` 五次自動化失敗後結論成「排程不留 transcript」，**但五次都是從別場去找**，沒人驗證過排程能不能讀自己的；②08-15 從 14 個回合推論的「成本平方成長」被 250 回合的重算推翻 | ✅（原 ⏳，2026-08-17 結案）這一步的設計重點是**兩種結果都有價值**：找得到就不必人工填，連續找不到才算真正結案。**已寫死兩個防呆**：數字是下限要標明、找不到不准換路徑猜第二次（08-14 五連猜的教訓）。成本模型部分已即時訂正三處文件。**✅ 同步狀態已於 2026-08-16 當晚結案**：稽核時 18:27 快照裡查無第 5.5 步，一度懷疑宣稱不實；使用者貼回正本後確認**三項改動全部都在**（第 5.5 步、回報第 16 項、成本模型訂正）。**問題出在快照時機不是規格**——「收工後」快照取於 18:27、⑤ 在那之後才寫，已以 `2026-08-16-233000` 補存。**✅ 2026-08-17 結案**：首次執行即 `PROBE=找到`，回報第 16 項印出 `eff_tokens_k=2954／subagents=3／agent_turns=9`，已填進 `metrics.csv`。**「排程確實會留下自己的 transcript，只是只有它自己讀得到」**——先前五次從別場工作階段找不到而推論「排程不留 transcript」，那個結論被推翻。**這是登記簿上第二筆「一次就成功」的修法**（前一筆是 08-12 的段數規劃），共同點都是**先修量測、再修規則** |
| 08-16 ④ | 回歸複驗 | brief、SKILL.md、`healthcheck.py`、本檔 | 上面三列自己製造的新問題 | ⚠️ **又是修法本身留下問題，這次是第六次。** 6 處中 **5 處**源於今天（2026-08-17 訂正，原寫 3 處）：`last_run_utc` 的語意從「上次執行時刻」改成「下次視窗起點」之後，**brief、SKILL.md、`healthcheck.py` 共四個地方仍在用舊語意讀它**（最嚴重的是「假零集要回捲」與新行為在同一段落裡打架，照做會把視窗起點多推一格）；重播規則宣稱同步但只同步了一半；`capitalallocators` 走預設藍那句與新寫的 26 組矛盾。另 1 處是舊帳：`MAINTENANCE.md` 第 4 節的 CSS 組數還是 24。（**原寫「6 處中 3 處源於今天／另 2 處是舊帳」，兩個數字都不對**——2026-08-17 逐條歸屬後為 5 處源於今天、1 處舊帳，brief 第 8 節同步訂正。）**教訓：改一個欄位的語意時，要搜的是所有「讀」它的地方，不是所有「寫」它的地方。** |
| 08-16 ⑥ | **`snapshot.sh` 掛載點偵測＋記分板機械把關** | `snapshot.sh`、`healthcheck.py`（`check_observations`）、brief 第 1 節、本檔第 4／6 節、SKILL.md 第 4 步（**待使用者貼上**） | ①`snapshot.sh` 在沙箱裡會**安靜產生空快照**，而它是 repo 外檔案的唯一還原點；②`observations.json` 每天要動卻零機械把關；③「同源推送 `meta`」規則存在但沒有執行者；④「退援路徑做不了同源去重」只寫在 SKILL.md | ⏳→部分結案（2026-08-17）**動防護機制前照慣例先問最壞情況**：`snapshot.sh` 的守衛只在「一個檔都沒抄到」時觸發（Mac 上除非 `.podfetch` 整個空掉，否則不可能），觸發時是自刪＋非 0 退出，不會蓋掉任何既有快照；`check_observations` **刻意只用單一訊號**（兩個 ISO 日期字串比對，無雜訊來源），並在註解裡寫明為什麼不套「兩個獨立訊號」那條規則——**0 集日不產日檔所以不會誤報**，六個分支已用暫存假資料逐一驗過（含 0 集日）。積壓數字只印不報警。**本場未動 brief 的 43% 事故敘事**，理由是明天已有六個變因要驗。**⚠ 本場的修法自己也留下三個問題，當場被複驗子代理抓出並修掉**——①`check_observations()` 原本用 `elif` 鏈，**非法 status 會把「落後 15 天」整條蓋掉**（次要的資料衛生檢查排在主要目的前面）；②`data/` 零日檔時回 PASS，等於什麼都沒驗到卻報綠燈；③函式 docstring 寫「回訪沒做、只附加不改判、整步跳過都會出聲」，**實際只擋住第一種**（實測當天新增 3 條、改判 0 條，照樣 PASS）。**第七次了，而且這次是在同一場之內被抓到的**——差別在於複驗被指定去讀程式分支，而不是只對讀文件。**另補一件本場才發現的事**：SKILL.md 的還原點當時是舊的（見上一列與第 4 節新增的坑），已請使用者貼回正本並以 `2026-08-16-233000` 補存；SKILL.md 第 4 步的兩處修改由使用者當晚貼上，**改動後另存 `2026-08-16-234103`（兩份差 5 行、即那兩處）**——這次刻意在改動之後才取，就是上一條坑講的那件事。快照鏈恢復完整，本批全數完成。**⏳→部分結案（2026-08-17）**：`check_observations` **已在正式環境驗證**——觀察中 47→50、已判 9→9，精準對上回報的「無改判，新增 3 條」；`snapshot.sh` 的守衛與偵測未被排程觸及（排程不跑它），同源 `meta` 因當天無同源合併而未觸發，兩者仍待實戰。**❌ 2026-08-17 ③ 結案：`snapshot.sh` 那半是失敗的。** 本輪為了印出解析結果而新增的 `（來源 BASE＝$BASE）`，在 macOS 的 bash 3.2 上把全形括號吃進變數名，`set -u` 中止——**8/16 之後每一次在 Mac 上跑 `snapshot.sh` 都是非 0 離開，整整一天沒有人回報**。失敗點在最後一行，所有實際工作都已完成，**所以快照本身一直是好的，壞的只有「快照完成」那句與離開碼**——這也正是它能潛伏一天的原因。**兩個教訓**：①**改一支只在 Mac 上跑的腳本，就必須在 Mac 上跑一次**，沙箱是 bash 5.1、測不到 3.2 的解析差異，而本輪的驗證只做到「沙箱裡跑得動」；②**這一列的 ⏳ 掛了一整天沒有人去踩**——`⏳` 的規矩是「下一場維護的第一件事就是回來結掉它」，本輪之所以是使用者手動撞到而不是維護流程發現，就是因為那條規矩沒被執行 |
| 08-17 ① | **Read 上限的機制訂正＋成本正規化欄位** | SKILL.md（第 3 步、任務卡預算、回報第 13 項、`published` 格式）、`metrics.csv`＋`healthcheck.py`（新欄）、`snapshot.sh`（時區）、brief 第 1／2／3／8 節、本檔第 7／11／12 節 | ①任務卡宣稱「逐字稿一次讀得完（Read 上限 2,000 行）」，實際約束是**每次回應約 25,000 token**，超過約 75 KB 就讀不完——**規則把結構性限制寫成了執行端的浪費**；②`eff_tokens_k ÷ 集數` 不可比（08-17 表面 985K／集、是 08-14 的 3.5 倍，實為集數少×集數長×分析成本混入）；③`snapshot.sh` 時間戳用執行環境時區，沙箱取的快照早 8 小時、`--list` 排序錯亂 | ⏳ **待實戰。** **動手前照慣例問最壞情況**：R＝⌈KB÷75⌉ 若算少，上限 R+3 仍留了餘裕；算多只是預算寬鬆、不會逼出額外呼叫。新欄 `subagent_tokens_k` 走既有的「舊列自動補欄」路徑，已實測歷史列正確補齊。**本場自己也踩了一次**：把 08-17 條目插進 brief 第 8 節時位置落在 08-16 區塊**內部**，歸檔切片因此把它一起搬走，長度斷言（5,000–9,000 字元）通過但語意是錯的——**斷言擋得住手滑，擋不住錨點選錯層級**，已當場修回。**改動後的獨立複驗又抓到七處，全部當場修掉，其中三處值得記**：①**「子代理每 KB 4.53 vs 4.27、等於沒變」這個結論靠一個未揭露的減數撐著**——08-14 沒有 `subagent_tokens_k`，4.27 是拿 `eff_tokens_k` 減掉「約 600K 固定成本」反推的，**而那個 600K 全庫查無記載；不扣的話是 5.18 vs 4.53、結論反向**。已改為只陳述 08-17 的實測值並言明無法比較。②**新增的 `subagent_tokens_k` 沒有蒐集出口**——唯一提示人工填的 WARN 只列三欄，且第 5.5 步探測成功後 `eff_tokens_k` 必有值、那則 WARN 從此不再觸發，**新欄會安靜地永遠留空**。已改為四欄一起判、缺任一欄就出聲。**「加了量測就要順手驗證它會被填」是第三次踩同一個坑。**③`snapshot.sh` 的 `TZ` 修法在 tzdata 缺席時**靜默退回 UTC 且離開碼 0**，症狀與修法前完全相同；已加 `date +%z` 核對守衛。**另記一筆待觀察**：R＝⌈KB÷75⌉ 的「1 次」那一格是靠 lennys 恰好 75 KB（75÷75＝1.0）驗的，**刀鋒上的巧合，等於沒真正測過** |
| 08-17 ② | **以 `writing-for-agents` 六槓桿稽核 brief 與排程 prompt；第一批只刪過期抄本** | brief 第 1／2／3（第 5 行）／8 節、SKILL.md（開頭 token 紀律段、第 1 步 iTunes 退援指示）、本檔第 12 節 | ①brief 第 5 行的工具呼叫預算仍是 `08-17 ①` 之前的「目標 2 次／上限 5 次」，**且與同檔第 8 節當日條目直接打架**——`08-16 ④` 記過的「改語意要搜所有**讀**它的地方」**第二次犯**；②三處「為了宣告舊規則作廢而把它完整寫進 context」的 negation；③brief 自承非權威、healthcheck 讀不到的 23 組 AppleID 抄本 | ⏳ **待實戰**（首次驗證是 8/18 03:00 那一場）。**五處全是刪除、彼此無依賴**，最壞後果是少一句話——這是刻意選的第一批形狀。**動手前照慣例問最壞情況**：刪 AppleID 清單前先讀 `healthcheck.py` 確認它只 regex 比對「N 檔」、完全不讀那份清單，並在 `SKILL.md` 第 1 步退援指示就地補上「AppleID 讀 `shows.json`」，**因為那是全系統唯一真的需要 AppleID 的路徑**；刪 FT 層號訂正括號前先確認正解「第 1 層的特例」在下一段完整存在。改動前後量測：五個錨點皆唯一，**刪除量 −771 字元**，逐處事後斷言全過。**但 brief 的淨效果是 +357 字元（34,381 → 34,738 字元／526 行）**——刪掉 771，為了記錄這件事而寫的第 8 節條目卻佔了 881。**這一批的目的本來就不是瘦身**（瘦身是下一批的第 8 節歸檔與第 2 節下推），但方向相反這件事要講明，否則下次看會以為第一批已經往目標推進了。**這也是「自我指涉的數字要標時點」的又一次實例**：同一場裡 brief 字元數動了兩次，複驗抓到本列原本寫的是稽核當下的值。**`08-17 ①` 的錨點層級教訓當場派上用場**——本場的 CHANGELOG 條目一樣要插進既有的 08-17 區塊，改用「區塊最後一行尾端＋空行＋下一個 blockquote 起首」三段錨定，確保落在 bullet 層而不是區塊內部。**本場的主要產出其實不是這五處刪除，而是稽核本身**：**實測 brief 一次 `Read` 讀不完（29,112 token > 25,000 上限）**，因此 SKILL.md 第 0 步「完整讀過 `AGENT_BRIEF.md`」這條完成條件**在機制上無法單次成立**——與 `08-17 ①` 是同一個機制，當時只套用到逐字稿、沒有回頭套用到 brief 自己。**這把 brief 瘦身從整潔問題升級為正確性問題**，門檻可計算（需降到約 30,000 字元以下），列為下一批。另發現 `FILES.md` 宣稱「唯一一處雙寫是任務卡」，實測 SKILL.md 第 1／2／4 步另有 9 處在複製 brief 的事實細節；**但每一處都有事故史背書，純刪會提高風險，正解是反轉 source of truth 方向而非清理**，不列入刪除批次 |
| 08-17 ③ | **修 `snapshot.sh` 在 Mac 上的解析錯誤** | `snapshot.sh`（末行的完成訊息）、本檔第 4 節與第 12 節 | `08-16 ⑥` 引進的 `echo "⋯$DEST（來源 BASE＝$BASE）"` 在 macOS bash 3.2.57 上把 `（`（U+FF08）的高位元組吃進變數名，`set -u` 中止並印 `DEST?: unbound variable`；**還原點腳本每次非 0 離開，而 Mac 是唯一取得完整快照（含 `SKILL.md`）的地方** | ✅ **2026-08-17 當晚在 Mac 實跑驗收通過**：印出「快照完成：/Users/kenny/.podfetch/snapshots/2026-08-17-223331（來源 BASE＝/Users/kenny/.podfetch）」，八個檔案全數抄到、含 `SKILL.md`（37,351 bytes）。**這份同時是今天 SKILL.md 三次改動之後的第一份完整還原點**——先前四份都是沙箱取的、一律缺 `SKILL.md`。**原為 ⏳**（沙箱是 bash 5.1，**複現不了 3.2 的解析差異，所以機制是推論、不是驗證過的**——這正是 `08-08 ④` FT 入口「更正過但沒驗證過」的同一形態，所以當下刻意不寫 ✅，等實跑）。**修法刻意選成不依賴那個推論**：`${DEST}` 的大括號在任何 bash 版本都明確界定變數名邊界，**診斷精不精確都不影響修法正確性**——這是「機制存疑時，挑一個兩種機制都能修好的改法」。改法是 `${DEST}`／`${BASE}` 加大括號，**不依賴機制推論是否正確**：大括號讓變數名邊界明確，任何 bash 版本都對。**動手前照慣例問最壞情況**：這一行只是最後的確認訊息，改壞的最壞後果是訊息印得不對，不影響任何檔案複製或 MANIFEST。已全檔掃過 `snapshot.sh` 與 `fix-schedule.sh` 的所有「`$VAR` 緊接非 ASCII」寫法，**確認只有這一處**。**驗收條件寫死**：在 Mac 跑 `bash ~/.podfetch/snapshot.sh "測試"`，看到「快照完成：⋯」印出來且 `echo $?` 為 0。**複驗另抓到一件**：原本寫進註解當守衛的 `grep -P` 在 macOS 上不存在（BSD grep 無 `-P`），**守衛在它唯一該防的機器上跑不起來就等於沒有守衛**，已改成 `perl -ne` |
| 08-17 ④ | **依六槓桿稽核結果改 brief 與排程 prompt（第 3、4 批）** | `AGENT_BRIEF.md` 第 2 節（下推）／第 3 節（雙訊號）／第 5 行、`MAINTENANCE.md` 新設 `## 4C.` 節＋第 4／6／11／12 節、`README.md`、排程 `SKILL.md`（整份覆寫） | ①brief 第 2 節「四個不要改的設計」是 podfetch 內部設計，**每日執行者不執行 podfetch、只讀它的輸出**，卻每天被完整讀一次；②排程第 0／1／2 步沒有完成條件，而第 1 步（假零集）是全流程最容易安靜失敗的一步；③第 6 步開頭的「簡短講」與其後十六項必講清單互相打架；④成本說明在三處各展開一次，缺一個可反覆使用的詞；⑤`README.md` 寫「講者 1／2」，`podfetch.py:945` 實際產生的是 `Speaker 1`／`Speaker 2` | ⏳ **待實戰**（首次驗證是 8/18 03:00）。**下推的判準**：逐句問「每日執行者會不會用到」——移走視窗算法／模型池 RPD／503 冷卻語意，**留下 `status` 三種值、`DEGRADED` 成因、跳針處理、三種旗標的分工**，brief 原地改成「品質指標怎麼讀」三條。**`config.json` 現行值刻意不動**：先讀 `healthcheck.py` 確認 `CONFIG_KEYS` 的 12 個鍵全部只命中 brief 那兩行，移走會讓「brief vs config」落到 `missing` 分支、退化成一則不擋任何事的 WARN——**那是這套系統唯一擋得住「改了 config 卻漏改文件」的機械檢查**。新增：第 0／1／2 步各一條可驗證的完成條件（第 0 步就地寫明 brief 會截斷、要接著讀完）、回報第 17 項（brief 讀了幾次）、「進場」一詞（**「雙訊號」只進了 brief 第 3 節，`SKILL.md` 端沒引進——08-18 複驗抓出本列與 brief 第 8 節都宣稱了兩個，實際只做了一個半**）。**✅ 2026-08-18 結案**：08-18 排程回報逐項對上——第 17 項「brief 讀 1 次，未截斷」（下推目標達成）、第 3 項五集全 `OK` 且未把語速基準當成 `DEGRADED` 成因（「品質指標怎麼讀」三條有效）、第 0／1／2 步的完成條件未造成任何卡關。**⚠ 本場自己留下的問題由複驗抓出，全部當場修掉**：①**brief 字元數在同一場內動了三次，兩處記錄都停在中途值**（30,755 vs 實測 30,941）——`MAINTENANCE.md` 第 6 節那條「自我指涉的數字要標時點」**第二次被自己違反**；②**第 3、4 批一度完全沒有登記簿列**（本列就是補的），正是 `08-08 ④` 記的「沒進登記簿的改動，等於沒有人會回頭驗收它」；③第 11 節 `### 2026-08-17` 漏標「（同日第 N 批）」，而那個格式正是本場自己訂的；④四處斷鏈與過期說法（第 7 節額度表指向已下推的 brief 第 2 節、`metrics.csv` 欄數仍寫 15、歸檔單位「當日」「兩天前」兩種舊說法、`Speaker A` 從來不是程式產生的值）。**教訓：一場裡連改四批，「記錄本場」這件事本身會落後於改動——量測與登記要排在每一批的收尾，不是整場的收尾。** |
| 08-17 ⑤ ✅ | **brief 瘦到單次 `Read` 讀得完（第 5 批）＋排程 prompt 三處訂正** | `AGENT_BRIEF.md` 第 1／3／4／8 節、排程 `SKILL.md`（整份覆寫）、本檔第 6 節與第 12 節 | `08-17 ④` 之後 brief 仍是 25,912 token、上限 25,000，**第 0 步的完成條件在機制上仍不成立**；另有兩處數字過期（`SKILL.md` 兩處寫 brief 約 27,000 token）與一處同型缺陷（第 16 項寫「照抄三個數字」，而 `metrics.csv` 的人工欄位已是四欄） | ✅ **當場實測結案**：再砍 1,315 字元（BG2／Business Breakdowns 移出說明、第 3 節三段沿革敘述、`check_speaker_labels()` 五種訊號列舉與時間戳溢出實例、`index.html` 補色程序 → 第 5 節、本檔 CHANGELOG 條目壓短），**34,381 → 29,641 字元、約 24,990 token，Read 全檔 492 行無截斷提示**。**砍的判準與 `08-17 ④` 同一條**：逐句問「每日執行者會不會用到」——留下規則與判準，把「當初為什麼這樣改」的敘述指向本檔。**但這是刀鋒上的通過（餘裕約 10 token）**，所以刻意做了兩件事：①第 0 步的截斷分支**不因為現在讀得完就拿掉**，門檻兩邊都要能正確運作；②**回報第 17 項改成哨兵**——連續幾天讀 2 次就代表 brief 又長回上限之上。**順手補掉第 16 項的同型缺陷**（`subagent_tokens_k` 是第四個人工欄位，但第 16 項只叫執行者抄三個數字，而 `healthcheck.py` 已改成四欄一起判——**這是「加了量測要驗證它會被填」的第五次，而且這次是在下游的執行指令上**；探測腳本也一併改成直接印出第四個數字，不要讓執行者從括號裡的敘述去換算）。**✅ 2026-08-18 實戰結案**：回報第 17 項寫「brief 讀 1 次，未截斷」——**這是本列唯一寫死的驗收條件，一次通過**；第 16 項四個數字齊全（`eff_tokens_k 2919`／`subagents 4`／`agent_turns 9`／**`subagent_tokens_k 1257`**），新欄第一次由排程自己填出來。**但 `transcript_kb` 仍是空的**：`healthcheck.py` 一直在 Cowork 沙箱裡跑，而該工作階段只掛載了 `podcast-knowledge-digest` 與 `.podfetch`、沒有 `~/podcast-transcripts`，於是分母永遠缺席——**`subagent_tokens_k ÷ transcript_kb` 這個判準立了兩天，一次都還沒算出來過**。**這是同一個坑的第六次，而且形態又換了一層**：前五次是「加了量測沒驗證它會被填」，這次是「量測會被填，但**只在某些執行環境**」。修法不在程式：**healthcheck 要在 Mac 上跑，或該工作階段要連上三個資料夾**。**✅ 2026-08-18 解決**：在 Mac 上跑 healthcheck，`transcripts=/Users/kenny/podcast-transcripts` 有掛到，`transcript_kb` 首次填出 221 KB。 |
| 08-18 | **驗收 08-17 五批＋六處文件訂正＋`check_timestamps()` 邊界聲明** | `AGENT_BRIEF.md` 第 3／8 節、`MAINTENANCE.md` 第 4／6／7／11／12 節、`healthcheck.py`（第 539 行「三個值」→「這四個值」）、`metrics.csv`（08-18 四欄）、`measure-ts-monotonic.py`（新，只讀量測腳本） | ①08-17 那場留下六處自我記錄與實況不符（宣稱引進「雙訊號」實際只做一半、「08-17 有三場」實為五場、skill 端狀態過期、瘦身數字把淨額貼成毛額標籤、第 7 節「三個數字」）；②**08-18 的 20VC 有兩處時間戳跳躍與一段重複，`timestampNote` 卻是空的**——子代理自行發現，機械檢查看不見 | ⏳ **待實戰**。**驗收結果先記**：08-17 ④⑤ 全數通過（第 17 項「brief 讀 1 次，未截斷」、第 16 項四個數字齊全、第 13 項 R 相對門檻正常），已結成 ✅。**`check_timestamps()` 的缺口先只補文件、不動程式**：讀 `podfetch.py:676` 確認 `sorted({...})` 在任何判斷之前就把**順序與重複同時丟掉**，所以回跳與重複是結構性看不見；20VC 完整度 1.13 又讓大間隙那條的 `ratio < 0.85` 永遠短路——**兩道檢查對這一集都無效，空值是程式的正確行為**。已在 brief 第 3 節寫明「空值只代表那兩種形態沒觸發」。**補單調性檢查要先實測誤報率才動手**（`MODIFY.md`「新增檢查」：先問正常情況會不會也觸發）。**✅ 已實測，結論是不加**：全庫 91 集有 29 集回跳（32%），容忍值放寬到 60 秒仍有 22%——**初判「單調遞增應該恆真」被量測直接推翻**，若照初判動手就會做出一道每三集報一集的檢查。**量測本身反而挖出更有用的東西**：回跳幅度分三團，其中八集擠在 ≈1,200 秒＝正好一個 `segment_seconds`，那是 `offset_timestamps()` 漏做位移的簽章，而且八集裡六集落在 08-02～08-05——**證明 08-08 的 `ABS_TS_TOLERANCE` 修法有效**。今天 20VC 的 1,473 秒不是分段倍數，成因不同（疑為 Gemini 分段邊界重複轉錄）。量測腳本 `measure-ts-monotonic.py` 留在 repo 根目錄備用。**⚠ 本場自己也踩了一次**：加那段邊界聲明當下把 brief 推回 25,000 token 之上（實測 25,120），**而「餘裕不到 1%」這條警告是前一天自己寫的**；補救是同批壓縮＋把 08-17 的 CHANGELOG 依規則歸檔——**歸檔本來就該在開工時做，那樣本場一開始就有 700 字元餘裕**。**另記一筆判斷失誤**：先前僅憑回報第 14 項「（節目停更公告）」五個字就推論 Prof G Markets 停播、準備把 `08-16 ②` 判為 ❌，讀日檔原文標題才發現是**兩週暑假、8/31 回歸**；已寫進第 4 節。**⚠ 本場的驗證清單沒跑完，這一點要記著**：沙箱 VM 整場啟動失敗，`MODIFY.md` 驗證第 2 項（獨立子代理照 `SYNC-CHECKLIST.md` 複驗改動後的狀態）**一次都沒跑**——第 2 步的漂移比對是在**改動之前**做的，改完之後沒有人再對讀一次。**而本場修的六處不一致，全部源自 08-17 那場「自認改完了」**。所以**下一場開工的第一件事是複驗 08-18 這批**，不是只結掉本列的 ⏳ |
| 08-22 | **依 08-22 排程實跑的六批修正**（官方稿層／複製定位器／收斂規則／hosts／帳本逾期／文件指向） | `kb-core/podcast/anchors.json`、`kb-core/podcast/BRIEF.md`、`kb-core/scripts/podcast/DIGEST-PROMPT.md`（＋排程 `SKILL.md` 整份覆寫）、`kb-core/scripts/podcast/preamble.md`、`kb-core/kbcore/transcript.py`、`kb-core/checks/podcast.py`、**`kb-core/tools/podcast_dupmap.py`（新）**、`kb-core/skills/maintain/podcast/{MAIN,FILES,MODIFY}.md`、本檔第 6／12 節 | 08-22 那一輪 8 集全數發布 `exit 0`，但過程暴露六件事：①`anchors.schedule.official_transcripts` 寫著 02:20，**而那個步驟從來沒有被實作出來**，於是「A 類抓不到官方稿」與「根本沒去抓」在輸出上完全一樣，當天 macrovoices 與 unhedged 兩檔 A 類就是這樣全退第二層；②整段複製只給 token 位置，subagent 定位不了，主代理臨時寫腳本補、**寫在 `/tmp`**；③那支臨時腳本跨分段比較時間戳，把 macrovoices 誤判成「內容只到 28:44」**並把結論寫進派工單**，靠 subagent 逐行核對才推翻；④`preamble` 的「靠合併不靠刪」對**字數**超標完全無效，bloomberg 主集照它收斂一次仍超上界 6%；⑤`hosts` 直接抄 `shows.json`，八集有兩集名單過期而機械檢查看不到；⑥`MAIN.md`／`FILES.md`／`MODIFY.md` 仍寫「規格在 `AGENT_BRIEF.md`」，而 brief 早在 08-21 就被降級 | ✅⚠️ **2026-08-23 實戰結案。** **先把批號對齊，因為本列自己數過兩次都對不上**：主題欄列的是六批，而同列後段又寫「本場加開第七批」——**實際是七批**，而「用量：量它，不要估它」那一節是**同場稍晚（11:59）另外加的第八件事，不在任何一批裡**。以七批計：**四批成立（①④⑤⑥）、一批不完整（②）、一批本場未判（③跨段時間戳誤判，08-23 沒有出現跨段素材，無從驗證）、一批成立但被第八件事抵銷（⑦文件指向）**，**加上那第八件事是 ❌**。<br>**成立的四批**：①A 類官方稿的責任寫回 digest 之後，08-23 唯一的 A 類 latentspace **確實抓到了官方稿**（`&cb=` 帶了、`simile` 對得上 manifest、末端時間戳 01:09:24 對照片長 69.6 分鐘未截斷）——**這是 `official_transcripts` 改成 null 之後第一次有人真的去抓**；④收斂規則分開段數與字數之後，**四集全部一次寫對、零收斂**（08-22 是主代理自己在組檔那一步跑了 6 輪）；⑤`hosts` 依稿判定確實執行，四集抓出兩集名單過期並具名回報；⑥文件指向改到 kb-core 之後，本場開工讀規格沒有再讀到降級的那一份。②`podcast_dupmap.py` 有跑、有進派工單，twentyvc 那一段原樣貼上、subagent 據此把該區間當缺漏處理。<br>**不完整的那一批（②）**：dupmap **認不得句級跳針**——08-23 的 mib 同一句連續 13 次、合計 324 token（**高於下限**）卻被判「乾淨」，成因在 `block_repeats()` 的合併條件而不是門檻。已寫進 `blind_to` 與`anchors.block_repeat_blind_to_stutter`，修法待量測（見第 6 節）。<br>**❌ 反而製造新問題的那一批**：同場稍晚（11:59）加進 `DIGEST-PROMPT.md` 的「用量：量它，不要估它」**整段沒有貼進排程副本**，而 `FILES.md` 在 06:41 寫下的「實測只差 front matter——目前同步」沒有回頭改。於是 08-22 與 08-23 兩輪都照舊版指示把自述值抄進 `metrics.csv`，`eff_tokens_k` 因此出現 **4913／235／276 三個相差 20 倍的值**。**而且那支腳本在 Cowork 側根本跑不動**（三條路 08-23 全部實測，見第 6 節），所以就算當時貼了也一樣拿不到數字——**換成量測之前沒有確認那個量測在目標環境存在**。<br>**原始的逐項驗證結果（08-22 當場記的）**：①`official_transcripts` 改回 `null` 並具名寫出查證方式（repo 只有 `deploy.yml`／`sentinel.yml`、launchd 九支無、全庫 grep 零引用），A 類清單**不抄過來**、改指向 `AGENT_BRIEF.md` 第 1 節（該節是 08-21 失效橫幅明文保留的權威）；②新增 `podcast_dupmap.py`，**不另寫偵測邏輯**、沿用 `kbcore.transcript.significant_repeats`，實跑 08-22 與 08-21 兩天皆非空，且**它自己抓出 macrovoices 第 2 段乾淨**——正是③那個誤判的反例；③為了做 token→時間戳對應，在 `kbcore/transcript.py` 新增 `tokens_with_lines()` 並讓 `tokens()` 呼叫它，**改動前先存全庫 26 份逐字稿的長度與雜湊當基準**，改完逐檔比對：25 份完全一致，1 份差 2 個 token，追下去是**新版修掉了舊版的真缺陷**（連續兩個講者標籤時，第二個因前一次匹配的 `\s*` 吃掉換行而失去 `^` 錨點，於是殘留成內容 token）；`tokens()` 只被 `block_repeats()` 使用，影響封閉；④`preamble` 第一之二節改寫成**先分辨超的是段數還是字數**的兩欄表，並把「一次收斂」明文延伸到組檔階段（當天主代理自己在那一步跑了 6 輪逐段修剪）；⑤`hosts` 改成 subagent 依稿判定、名單只當候選；⑥三份維護文件的指向全部改到 kb-core，`FILES.md` 的「brief 篇幅是硬限制」整段改寫——**每日排程已不再完整讀 brief，那條限制解除了，但拆檔把失效形態從「截斷」換成「只改其中一份」**。**帳本那批的做法值得記**：`checks/podcast.py` 的 `_ledger` 早把修法寫在 `blind_to`（「要先給帳本加 `due`，而歷史不改寫」），實作時找到第三條路——**新項目強制帶 `due`，歷史從 text 既有的「（到期 …）」回退解析**，於是一條歷史都不必改就有了即時涵蓋。新檢查 `podcast.ledger_no_overdue` 照 `MODIFY.md`「兩個獨立訊號」設計：逾期只 WARN、**超過 `overdue_grace_days`（7）才 FAIL**，理由是一觸即 FAIL 會擋住當天發布，而被擋住的是當天的內容——**一條三個月前的觀察沒回訪，代價由今天的讀者付，那個賠率不對**。三個級別已用真實帳本實跑驗過（今天 PASS／逾期 5 天 WARN／逾期 20 天 FAIL）。**已知的洞照 `blind_to` 寫死**：61 條觀察中有 52 條沒有任何到期日，這條對它們是瞎的——**綠燈不等於帳本乾淨**。**⚠ 本場沒跑完的驗證，照實記**：`MODIFY.md` 驗證第 4 項（收工快照）與第 5 項（線上 cache-buster）在 Cowork 沙箱裡做不到，需在 Mac 補跑；而 **08-18 那一列交代的「下一場開工第一件事是複驗 08-18 這批」**，本場**沒有做**——本場做的是複驗 08-22 自己的改動。那一列仍是 ⏳。**⚠ 複驗（`MODIFY.md` 驗證第 2 項，08-18 那場漏掉的就是這一步）抓到六處，全部當場修掉，其中三處是本場自己製造的**：①**本場宣稱「新項目一律要帶 `due`」，而當天新增的三條實際上沒有帶**——規則從第一天起就沒被執行，綠燈完全靠歷史回退解析撐著。**這正是本檔反覆記的「紀錄與實況脫鉤，而紀錄看起來更可信」**，而且這次寫紀錄與該執行的是同一個人、同一場。已補上三條的 `due`。②**覆蓋率門檻的依據寫錯了**：本場初版寫「四集是 29／19／5／4%，中間有整整一個數量級的空隙」，**而那個 5% 來自臨時腳本、不是正式工具**；複驗用 `podcast_dupmap.py` 重跑，compound 實際是 9%，距門檻只有 1 個百分點，空隙的說法不成立。已改成不假裝門檻是資料切出來的：10% 與 25% 是判斷，樣本只有一天四集，本來就切不出門檻。**這是「理由寫錯」的又一次，而它只有在複驗被指定去讀程式、實跑工具時才抓得到——對讀文件永遠查不出來。**③**兩條新規則互相打架**：anchors 要求「不要在派工單裡預告下界例外」，DIGEST 要求把 dupmap 那一段「原樣貼上」，而工具印的字串正是「可自行判定下界例外」——照做必然違反第一條。已把那句話從逐集輸出移到總結行並標明「這一行不要貼進派工單」。另三處是既有的：④`char_definition` 的「一個英文單字算它自己的字元數」沒傳進 `preamble.md`，而 preamble 是唯一會用到它的讀者；⑤`kbcore/transcript.py` 的 `TS_RE` 重構後成死碼，而 `tokens_with_lines` 的等價性說明仍拿它立論——**已改成不寫「等價」，寫「等價再加一個修正」，否則下一個人會以為 26 份比對全綠**；⑥**`BRIEF.md` 第七節第五條（`index.json` 的 `days[0]`）沒有任何機械檢查，也沒標示判不了**——實況是 `publish.py` 組檔時保證了它、`checks/sentinel.py` 是另一套件的事後心跳，兩者都不是那條判準的執行者，所以它目前靠「寫的人不會寫錯」，而第七節存在的前提正好是不要依賴那個。已在 BRIEF 就地標明。**⚠ 還有一處只記不修**：`AGENT_BRIEF.md` 第 1 節說 AppleID「唯一來源是 `~/.podfetch/shows.json`」，anchors 的 `_not_here` 說「唯一的家是 `scripts/podcast/shows.json`」——**兩份各自宣稱唯一**。實況是兩份都存在、`healthcheck.py` 有一條專門對帳但在沙箱裡固定跳過，**所以在沙箱裡改 `shows.json` 會安靜製造漂移**。已寫進 anchors 與本檔第 6 節，但兩份合併成一份要在 Mac 上做，不在本場範圍。**⚠ 交付步驟時又踩到同一類缺陷，而且是連續兩次。** 我請使用者在 Mac 上跑 `bash ~/.podfetch/snapshot.sh`——**那個路徑不存在**。`ls -la ~/.podfetch` 才發現：它已經不是程式目錄了，五支腳本一個都不在，`config.json` 與 `shows.json` 是指向 kb-core 的 symlink。而 `MAIN.md` 的硬規矩、第 1 步的 healthcheck 指令、`MODIFY.md` 的驗證第 1 項全都寫著那個路徑。**這種過期不會報錯，只會「找不到檔案」，而那跟「這台機器沒裝」看起來一樣。** 於是本場加開第七批：①`~/.podfetch` 的真實角色寫進 MAIN.md 開頭與本檔第 6 節；②三份維護文件裡所有過期路徑改成 `~/kb-core/scripts/podcast/`；③**`snapshot.sh` 退役**——它抄的八個目標有七個現在有 git，而唯一沒有的 `~/.podfetch/metrics.csv` 它從來沒抄過；④**兩份 metrics.csv 合併**（`tools/podcast_metrics_merge.py`，一次性、預設試跑、自動備份），實體檔留 kb-core、`~/.podfetch/` 那份改 symlink，並訂正 `healthcheck.py` 的 docstring 與 `DIGEST-PROMPT.md` 第 8 步。**還有一件差點釀成靜默失效的**：`push_kbcore.py` 的第三道閘門會跑 `selftest()`，而本場新加的 `podcast.ledger_no_overdue` **原本沒過**——`fixture` 與 `near_miss` 的方向我寫反了（`fixture` 要能觸發檢查、`near_miss` 要剛好通過，兩者相反）。**沒發現的話，kb-core 會從那一刻起安靜地停止推送，而每一輪的日誌都只說「不推」。**已修正並補登 `checks.lock`（80 條）。**這一批全部在沙箱用暫存目錄實測過**：snapshot.sh 退役後只出聲不建目錄且 exit 0、合併腳本的試跑與 --apply、symlink 在就地覆寫後仍是 symlink 且兩邊同步。**但 `--apply` 本身要在 Mac 上跑，因為那兩份檔案只在 Mac 上同時存在。****✅ 2026-08-22 07:22 在 Mac 實跑結案，三個驗收條件全中**：`metrics.csv 共 22 天；加權 token 235K（子代理 715K／8 個）`——人工四欄在合併後仍在；`逐字稿 654 KB`——手量的 688 已被 healthcheck 自己的尺取代；`ls -l` 顯示 `~/.podfetch/metrics.csv` 在 healthcheck 就地覆寫之後仍是 symlink。**試跑本身抓到兩個主代理的錯，兩個都不是讀文件讀得出來的**：①合併規則原本寫「衝突時 kb 勝」，會把主代理用 `du -sk` 手量的 `transcript_kb=688`與 `speaker_flags=5` 留下來——`du -sk` 算磁碟區塊含檔案系統開銷，healthcheck 算實際位元組（654／4），**同一欄兩把尺**，而這份檔案存在的唯一目的就是可比較。規則改成兩類欄位勝方相反：人工四欄以 kb-core 為準（唯一生產者），其餘一律以 healthcheck 量的為準。②主代理在四個地方寫了「這四欄與 `transcript_kb` 從來不在同一列」——**那句是錯的**，kb-core 那份 08-22 兩者都在；真正的後果是兩份各由不同寫入者維護、沒有仲裁者，實測 6 天已在特定儲存格漂移。四處全部照實訂正。**同一場 Mac 實跑另外證實了 `MAIN.md` 新加的那條註記**：沙箱裡固定 WARN 的 `shows.json 兩份`／`節目在文件裡`／`podfetch` 三條，在 Mac 上全部 PASS（21 檔執行檔與版控一致、21 檔在 brief 與 README 都找得到、01:00 準時執行）——**那三條在沙箱裡等於沒跑，而輸出看起來只是「跳過」**。線上狀態同時也 PASS（`8/22 03:32，days[0]=2026-08-22`），所以交付清單裡的網站確認由它一併完成。Mac 全盤結果：`PASS 17　WARN 1　FAIL 0　SKIP 1`。 |
| 08-23 | **依 08-23 排程實跑的七批修正**（正本／副本同步鏈與成本基線／金句比對基準／日期檔欄位／句級跳針／AGENT_BRIEF 權威區／查漂移清單自己／待辦與登記簿） | `kb-core/scripts/podcast/DIGEST-PROMPT.md`（＋排程 `SKILL.md` 整份覆寫）、`kb-core/scripts/podcast/preamble.md`、`kb-core/scripts/podcast/metrics-columns.md`、`kb-core/scripts/podcast/metrics.csv`、`kb-core/podcast/BRIEF.md`、`kb-core/podcast/anchors.json`、`kb-core/checks/podcast.py`、`kb-core/skills/maintain/podcast/{MAIN,FILES,SYNC-CHECKLIST}.md`、**`kb-core/scripts/podcast/healthcheck.py`**、`AGENT_BRIEF.md` 開頭與第 0／5／6／**8** 節、本檔第 6／**11**／12 節 | 08-23 那一輪 4 集全數發布 `exit 0`，但複驗抓到七件事，**其中最重的一件是前一場自己種下的**：①**排程副本缺了正本整整一節**（第 199–217 行的用量指示），而 `FILES.md` 寫著「目前同步」——那句是 06:41 量的、正本 11:59 才改，**同步是在改動之前量的**；②`quotes[].original` 的比對基準四份規格全無記載，而 `quote_misses()` 寫死了 podfetch 稿，**latentspace 走第一層用官方稿寫，五條全 MISS**；③`DIGEST-PROMPT` 第 1 步兩行指令用 `~/.venvs/kb/bin/python`，**那個 venv 在沙箱不存在**，而同一份檔案第 201 行自己就寫著「在 Mac 上跑一定失敗」；④`BRIEF` 第二節的 `quotes[]` 欄位寫成 `speaker`（實檔是 `by`），且 **`trackId` 與 `minutes` 兩個必要欄位不在清單裡**；⑤`podcast_dupmap.py` 認不得句級跳針；⑥`AGENT_BRIEF` 第 6 節（**權威區**）留著已退場的 `com.kenny.dashpush` 與一份少了兩個資料夾的連線清單；⑦`SYNC-CHECKLIST.md` 自己沒跟上 08-22 拆檔 | ⏳ **待實戰**（首次驗證是 8/24 03:00）。**七批各自收尾**：①正本改完**整份覆寫排程副本並驗過偏移**（副本 283 行＝正本 278＋5，三個錨點行號一致）；`FILES.md` 那句「目前同步」**改成寫「怎麼驗」而不是「驗過了」**——同步是一個時點的事實，而那是一份長期文件。②③④⑤⑥⑦逐項改在對應的權威檔，不重抄。**最有價值的產出不是那七處修改，是查證出一件結構性的事**：`tools/usage_report.py` **在 Cowork 排程裡跑不動**，三條路全部實測（沙箱無 `~/.claude/projects` → `exit 14`；逐字稿在 Mac 但 `request_cowork_directory` **明文拒絕掛載工作階段儲存區**；`session_info__read_transcript` 不回 usage 欄位），佐證是 `metrics/usage.csv` 至今只有 `2026-08-22,broker-research` 一列。**所以那四欄改成一律留空**，並把 08-23 那一列的自述值抽掉（08-21／08-22 保留不改寫歷史，但已標明不可比）。**這比修好那七處重要**，因為它把一個「看起來已經解決的問題」改回「未解決且知道卡在哪」。**⚠ 本場自己也踩了一次已經記過的坑**：往 `BRIEF.md` 加了 937 字元之後當場量到 **5,119 token、超出 5,000 預算**，同批壓縮第八節一次收斂回 4,826 並更新 front matter 的量測時點——**「加字之後量一次」這條是 08-18 立的，那次沒做到，這次做到了**。**⚠ 一處判斷錯誤被複驗推翻**：主代理在日報回報裡把 mib 的跳針成因寫成「規模低於 150 token 門檻」，而複驗子代理去讀 `block_repeats()` 才發現**實際是 324 token、高於門檻**，成因在合併條件。**差別是可操作的**：照「門檻太高」修會把下限調到 38，那會讓每一句廣告台詞都觸發。**這是「理由寫錯」第三次，三次都只有在複驗被指定去讀程式或實跑工具時才抓得到。****⚠ 複驗（`MODIFY.md` 驗證第 2 項）抓到 8 處，全部當場修掉，其中 6 處是本場自己製造的**：①**登記簿表格被插壞** —— 08-23 那一列插進了 08-22 儲存格的中間，把它切成兩半，後半四段變成表格外的正文、緊貼在 08-23 底下，**下一場會把「成立的四批」讀成 08-23 的成績單**。這正是 `08-17 ①` 記過的「錨點選錯層級，斷言擋得住手滑擋不住它」。②**宣稱訂正了 `checks/podcast.py` 檔頭的「這十條」，字沒改**（只在新寫的註解裡說訂正了），而 `SYNC-CHECKLIST` 已經把它寫成戰績。③`healthcheck.py` 同一個函式兩個分支互相打架（elif 仍叫人抄、else 叫人不要抄），改寫只做了一個。④新 WARN 把 276 算進 08-21／08-22，而那是 08-23 的、已被本場抽掉。⑤`BRIEF.md` front matter 插入新句時把「~3,025 token」那半句切斷，**連帶讓「預算取實測的 1.65 倍」讀起來像還有六成餘裕，實際只剩 3.5%**。⑥`preamble` 寫死「五條全 True」，而金句是 2–5 條。另兩處是既有的：⑦`FILES.md` 引用的節名已被本場改掉、且立論方向與新結論相反；⑧`AGENT_BRIEF` 的 `json2docx` 取代指到不在跑的那一支（實際是 `tools/podcast_docx.py`，依 `launchd/kbdocx-podcast.sh` 確認）——**取代只換了目錄、沒查哪一支真的在跑**。**⚠⚠ 複驗還推翻了本場一個已經寫進四份文件的結論。** 本場原本寫「`usage_report.py` 量不到，所以那四欄永遠留空」，而 `MAINTENANCE` 第 6 節上方那張五次修正表記著：**「排程不留 transcript」這個結論 08-14 寫過一次、08-16 就被推翻**，理由是「五次都是從外面找」。回頭查證：**逐字稿是存在的**（`Glob` 實地看到 `local-agent-mode-sessions/<帳號>/<工作區>/local_<階段>/.claude/projects/`），而 `healthcheck.py:703-704` 那段被 `:692` early-return 擋掉的程式，**glob 樣式正好對得上**（`fnmatch` 驗過為 True）。所以正確的說法是**「排程那一側搆不到，Mac 那一側量得到」**，不是「量不到」。四份文件已全部改成後者。**這是本場最接近重蹈覆轍的一次** —— 差一點就把一個被推翻過的結論寫回去第三次。**✅ 兩件 Mac 待辦當天就收掉了，而且兩件都翻出新東西。**①**線上 cache-buster**：用 `web_fetch` 帶 `?cb=` 取 `data/index.json`，`updatedLabel=8/23 03:41`、`days[0]=2026-08-23`、4 集，`publish.verify_needs` 兩條都成立 —— **沙箱連不到 Pages 的是 python 的 urllib，不是 `web_fetch`**，所以這一項其實不必進 Mac 待辦。②**成本基線**：在 Mac 上跑 `usage_report.py` 成功，**日報那一輪 6,611K／5 個子代理／60 輪／子代理 2,481K**（整場 30,744K 扣掉維護 24,133K）。**但「拿掉 early-return」這個原訂做法被否決了** —— 查證發現它會回報整場的 30,744、是真實成本的 4.6 倍且無徵兆（挑對檔案、算錯範圍），改成把量測指令寫進 `TOKEN_NOTE`，並把「補 `usage_report.py --until`」列為開啟自動化的前置條件。詳見第 6 節。**這一項的教訓是第三次同型**：**「拿掉一行就好」的判斷同樣需要先查證** —— 前置的指紋字串 `MARK` 早已因 prompt 改寫而失效，只拿掉 return 會兩條路都不中、安靜留空。**⚠ 仍沒做完的**：**08-18 那一列交代的「下一場開工第一件事是複驗 08-18 這批」，連續第二場沒有做**——08-22 做的是複驗自己、08-23 做的也是複驗自己。那一列仍是 ⏳。 |
| 08-23 ② | **清掉 08-22／08-23 兩場留下的殘留，並把三件「沒有人知道」的事量出來** | `AGENT_BRIEF.md` 第 2 節與第 5 節、`MAINTENANCE.md` 第 1／2／3／6 節、`kb-core/scripts/podcast/healthcheck.py`、`kb-core/scripts/podcast/shows.json`、`kb-core/podcast/anchors.json`、`kb-core/tools/usage_report.py`、`podcast-knowledge-digest/README.md` | 08-23 ① 收工後清點剩餘待辦，發現八項；其中三項的共同形狀是**「文件宣稱已處理，實際沒有」** | ⏳ **待實戰**（首次驗證是 8/24 03:00，退休欄位那項 8/23 14:00 的看門狗就會驗到）。**逐項**：①**路徑殘留**——08-22 登記簿寫「`~/.podfetch/<某支程式>` 當天全部改掉了」，實際漏了 8 處，其中 4 處在 `AGENT_BRIEF` 第 2 節（**權威區**），最糟的是維護流程第 0 步還叫人跑已退役的 `snapshot.sh`，**與 `MAIN.md` 直接打架**。②**四個退休欄位每天還在自動填**（`brief_kb`／`skill_kb`／`segments_done`／`podfetch_minutes`），而 `metrics-columns.md` 08-21 就寫「一律留空」——**退休的定義寫在一份文件裡、填值的程式在另一份，沒有人對帳**；`brief_kb` 量的還是 `AGENT_BRIEF.md`，而規格 08-20 已搬家，**同一個欄名前後指兩份檔**。③**`usage_report.py` 補上 `--until` 與 `--until-receipt`**，界線取自回執的 `at`（那是那一輪真正落地的時刻）；沙箱用合成 fixture 驗過四種情況（不切／`--until`／`--until-receipt`／`--since`＋`--until`）全部正確，含時區換算。④**`shows.json` 的 `hosts`**：latentspace 有官方稿直接證明 Alessio Fanelli 整集未發言，換成 Vibhu；bloomberg 只有兩天觀察，**因此只加不減**（補 Isabelle Lee、Scarlett Fu），並把「hosts 過期」寫成 `anchors.shows_lifecycle` 的第三種異動——前兩種是有人主動做的，**這一種是自己爛掉的**。⑤**A 類官方稿取得率量出來了：62%（15/24）**，而漏掉的 9 集裡有 5 集 `source` 寫明了正當理由，**真正沒去抓的只有 4 集，全部落在 08-20 至 08-22**——正是規格拆進 kb-core、A/B 表從必讀文件消失的那個區間。**有頭有尾，不是持續性缺陷。** ⑥**帳本那 52 條的性質被查錯了**：原本記成「補到期日或改判」，實際上**沒有一條是結構化 claim**（45 條敘述段落、7 條只是提到「檢驗」、帶格式的 0 條），自然時程從「今天晚間」到「九月」都有，**機械補 +3 個月會生出 52 個編的到期日**。改成把盲點變成每天看得到的數字（記分板那行加印「其中 N 條無到期日」），**刻意不報 WARN**。**⚠ 本場最該記的一筆**：那句被 08-16 推翻過的「排程不留 transcript」，08-23 ① 已經在四份文件裡改掉，**而這一場又在另外兩處找到它**——`healthcheck.py` 的 `build_metric_row` 註解，以及 `usage_report.py` 的錯誤訊息（那一處還把方向講反了，寫「在 Mac 上跑一定失敗」，而正解正好是要在 Mac 上跑）。**⚠ 複驗（`MODIFY.md` 驗證第 2 項）抓到 9 處，全部當場修掉，其中最重的兩處是本批自己的**：①**登記簿表格又壞了，而且比上一次嚴重**——上一次修好了「儲存格被切半」，但**沒把列接回表格**：一個引言區塊夾在表格中間、兩側各一個空行，把整張表切成三段，於是 08-18／08-22／08-23／08-23 ② **四列全部落在表格外**，GFM 下根本不會渲染成表格。另有一列缺尾管。**同一個地方連兩次沒修乾淨**，這次連引言區塊一起移到表尾、08-17 的序號也從 ③④⑤①② 排回 ①②③④⑤，整張表 40 列全部驗過是 6 管。②**`usage_report.py` 新加的 `--until` 有五個缺陷**，複驗自建 fixture 跑了 11 次才逼出來：`broker-research`／`advisory` 因為 outbox 目錄名對不上 `SYSTEMS` 而**結構性不可用**；`--until` 完全沒有格式驗證，**傳台北位移或純日期會安靜不切、exit 0**；`fromisoformat` 在 `try` 之外，`Z` 結尾的回執在 Python 3.10 直接 traceback；naive 的 `at` 會吃 `$TZ`、在 Mac 與 CI 算出差 8 小時的界線；界線切光時錯誤訊息寫「挑錯檔了」。全部修掉並重驗八種情況。**其中「文件說純日期不接受、程式卻接受」那一項尤其該記**——我寫了守衛的說明卻沒寫守衛。另外七處是文件面的自相矛盾：待辦說 hosts「未處理、名單一個字都沒動」而同批剛改了兩檔；「補上 `--until` 之前不要拿掉 return」的前置條件已滿足卻沒更新（程式註解也有一份）；同一個 bullet 裡「拿掉 early-return」與「early-return 要留著」相距 19 行並存；「本檔第 232 行」指向表格分隔列（那句話在 `healthcheck.py`）；四處寫死的行號因為本批改了 `healthcheck.py` 而全部失效——**已全部改成指函式名，行號會漂、名稱不會**。**一個被推翻的結論會在六個地方各活一份，****而每一份看起來都像是獨立的證據。** |
| 08-24（**補登，08-30 回填**） | **`publish.py` 工作區檢查／`kbdocx` 改四時刻／`unhedged` 補 Katie Martin／`14` 連三輪當 15／技能副本檢查** | `kb-core/tools/publish.py`、`launchd/kbdocx-podcast.plist`、`kb-core/scripts/podcast/shows.json`、`kb-core/scripts/podcast/DIGEST-PROMPT.md`（＋排程 `SKILL.md`）、`kb-core/scripts/podcast/healthcheck.py`（`check_skill_copy()`）、`kb-core/skills/maintain/podcast/{MAIN,MODIFY}.md` | **這一場當天沒有進登記簿**，08-30 依 `AGENT_BRIEF` 第 8 節的 08-24 條目與檔案 mtime 回填。內容：①`publish.py` 在 rebase 之前加工作區檢查、回 `15 @ worktree-dirty`（那天 03:32–06:43 連 191 輪 `exit 14 @ rebase`、188 筆本地 commit 推不出去）；②`kbdocx.podcast` 由只釘 04:00 改為 04／07／10／13——**「一天只跑一次」被當成了「一天只問一次」**；③`shows.json` 的 `unhedged` 補進 Katie Martin（08-22 就具名回報過，隔兩天才進檔）；④流程正本補上「`14` 連續三輪同一個 `stage` 就當 15 處理」；⑤`healthcheck` 新增 `check_skill_copy()`，**這是系統裡第二組「正本↔副本」關係**。 | ⚠️ **補登本身就是這一列的第一個結論**：`MODIFY.md` 第 12 節那三步（結案上一列／新增本場／重讀三個 bullet）**在 08-24 一步都沒做**，於是 08-23 ② 至今沒被結案、08-24 的五項改動沒有任何一項被回頭驗過。**登記簿的價值全在「下一場回來結掉它」，而漏登的那一場不會留下任何徵兆**——它跟「那天沒有維護」長得一模一樣。⑤那一項的延伸代價 08-30 才顯現：`SYNC-CHECKLIST.md` 完全不知道有第二組副本關係（同日補上）。 |
| 08-30 | **依 08-30 排程實跑的五批修正**（正副本雙向漂移／循環重複形態／`AGENT_BRIEF` 瘦身／帳本數字定義／行號與範例） | `kb-core/scripts/podcast/DIGEST-PROMPT.md`（＋排程 `SKILL.md` 整份覆寫）、`kb-core/podcast/anchors.json`、`kb-core/podcast/BRIEF.md`、`kb-core/scripts/podcast/preamble.md`、`kb-core/checks/podcast.py`、`kb-core/skills/maintain/podcast/SYNC-CHECKLIST.md`、`AGENT_BRIEF.md` 第 1／3／4／8 節、`MAINTENANCE.md` 第 2／6／11／12 節 | 當日 5 集全數 `exit 0`、零收斂、金句 25/25 命中，但暴露五件事：①**正本與副本反向漂移**——兩邊各缺一塊，且用量那一節**結論相反**（正本「搆得到了」／副本「搆不到」），正本自己還有節標題與內文打架，而**今天這一輪就是照著副本那個過期結論走的**（回報裡寫了「沙箱搆不到」，那是錯的）；②**循環重複的形態定義太窄**——四份文件都寫「同一句連續 N 次」，而當天 twentyvc 是**兩句交替 20 次**，「找相鄰兩行相同」對它結構上是盲的，`dupmap` 與主代理臨時寫的檢查雙雙落空；③`AGENT_BRIEF` 又撐破 `Read` 上限（28,507 token／超 14%），**截斷點在第 405 行，而第 6、8 兩塊權威區在它之後**；④帳本那個「52」三份文件都沒標定義；⑤`checks/podcast.py` 一處寫死行號一週漂了 12 行（08-23 才訂過「改成指函式名」）。 | ⏳ **本場剛做完，沒有實戰資料。下一場維護的第一件事是回來結掉它**，五批各自的驗收點：①下一輪日報的回報裡不該再出現「量不到／搆不到」的說法；②下一次出現循環重複時，subagent 應能報出**週期**而不只是次數；③往 `AGENT_BRIEF` 加字之後有沒有量（餘裕只剩約 5%）；④帳本數字有沒有再被「訂正」成另一個定義；⑤全庫還有沒有其他寫死行號。**另外本場自己踩了一次值得記的**：複驗子代理回報「52 是錯的」，**主代理先改了兩份規格才去讀程式**，讀完發現原本的 52 是對的、當日撤回——`MODIFY.md` 驗證第 2 項那句「改因果宣稱前先讀程式確認接到哪條分支」，這次是漏做之後才補做的。 |

> **`subagent_tokens_k ÷ transcript_kb` 這個判準立了兩天，08-18 第一次算得出來——而它一算出來就顯示自己有缺陷（2026-08-18）。**
> | 日期 | 子代理 | 逐字稿 KB | `subagent_tokens_k` | 每 KB |
> |---|---|---|---|---|
> | 08-17 | 3 | 349 | 1,582 | **4.53** |
> | 08-18 | 4 | 216（221 減去主代理直接處理的 Prof G 4.6） | 1,257 | **5.82** |
>
> 表面看是退步 28%。**但兩天的形狀完全不同**：08-17 是 3 集／每集 116 KB，08-18 是 5 集／每集 43 KB。**每個子代理有一份固定開銷（整包指令進場一次），集數短的時候那份固定成本攤在更少的 KB 上**——所以「每 KB」這個比值**本身就被集數與集長污染**，跟 `eff_tokens_k ÷ 集數` 是同一類毛病，只是輕一點。
> **拿兩點解聯立**（`cost ＝ a×子代理數 ＋ b×KB`）得到 **a ≈ 129K／子代理、b ≈ 3.42K／KB**——這個模型同時解釋兩天，而且給出可否證的預測。**但 n＝2，兩個未知數配兩筆資料必然完美吻合，這不構成驗證。** 本檔記過「拿 14 個回合去推成長曲線的形狀，樣本不足以支撐那個結論」——**這裡是同一個陷阱的更極端版本**。
> **接下來三天照抄四欄，累到 n≥5 再回頭擬合**；在那之前，**比較效率一律附上「幾集／每集幾 KB」**，不要單獨引用每 KB 的值。**✅ 2026-08-18 補記：skill 端已更新。** 當時記為未處理的兩處（`podcast/FILES.md` 歸檔單位寫「當天」、`podcast/MAIN.md` 寫「把三個數字抄進 `metrics.csv`」）已由使用者當晚重新安裝新版 skill 解決，08-18 複驗確認 24 檔與交付版本 `diff -rq` 完全一致、無重複安裝。**`save_skill` 只換得掉 `SKILL.md`、動不到附屬檔**，所以改附屬檔的唯一辦法是**把整個 skill 目錄重新打包成 `.skill` 交付、由使用者按「Save skill」重裝**——這條路徑要記住，下次改 skill 附屬檔會再用到 |

**讀這張表的方式**——「後來成立嗎」那一欄有四種標記，**四種代表四件不同的事，不要混為一談**（2026-08-17 由散文收成表）：

| 標記 | 意思 | 該怎麼處理 |
|---|---|---|
| **✅** | 已有實戰資料證明成立 | 結案 |
| **⚠️** | 方向對，但成效未證實、或被後續吃回去 | 留著，下次維護回頭看 |
| **❌** | 修法本身造成了新的故障 | **動同類機制前必讀** |
| **⏳** | 本場剛做完、**還沒有實戰資料**（2026-08-16 起使用） | **不是 ✅ 的弱化版**——下一場維護的第一件事就是回來結掉它 |

**⏳ 這個標記是拿教訓換來的**：舊做法是先寫 ✅ 再等打臉，08-11 的「先估篇幅」就這樣自稱成立了一整天。**❌ 與 ⚠️ 那兩格是全表最有價值的部分。** 以下三類只收已經結案的列：

- **修法本身製造新問題**（動防護機制前一定要先看這幾筆）：`08-04`（❌，503 併進 `EXHAUSTED` → 08-06 事故）、`08-03 ①②`（✅，根因誤判，由 `08-03 ⑤` 撤銷）、`08-06 ①`（✅，當下缺輪換上限，由 `08-06 ②` 補）、`08-09 ②`（⚠️，文件層三種副作用）、`08-08 ④`（❌，FT 入口從 `ft.com/unhedged`「更正」成 `ft.com/the-economics-show`，而後者**同樣不含 Unhedged 集數**，直到 `08-15` 才發現——**「更正過」不等於「驗證過」**，兩個版本都沒有在當下實際跑一次；該列是 08-15 事後補登的）、`08-16 ④`（⚠️，`last_run_utc` 的語意改了，但四個「讀」它的地方沒跟著改，其中一句還與新行為寫在同一段落裡；**2026-08-16 稽核補記：同一天的第二次子代理複驗又抓到 10 處，其中 3 處源自 ④ 那一輪本身**——宣稱已同步進 SKILL.md 的第 5.5 步與成本模型改寫都查無實據，且 ④ 自己的「6 處中 3 處源於今天」逐條歸屬實為 5 處。**「複驗」這個動作本身也需要被複驗**）、`08-16 ⑥`（❌，為了印出掛載點解析結果而加的 `（來源 BASE＝$BASE）` 在 macOS bash 3.2 上讓 `snapshot.sh` 每次非 0 離開，**潛伏一天、由使用者手動撞到**，2026-08-17 ③ 修。**這一筆的形態是新的**：前六筆都是「修法在極端路徑放大傷害」或「文件層副作用」，這一筆是**修法只在另一個執行環境失敗**——沙箱 bash 5.1 測不出 3.2 的解析差異，而驗證只做到「沙箱裡跑得動」。**改一支只在 Mac 上跑的腳本，就必須在 Mac 上跑一次**）、`08-22`（❌，**把成本基線從「自述」換成「量測」，結果比換之前更糟**：新指示整段沒貼進排程副本，於是兩輪照舊版填了自述值，而**新指示的存在讓沒有人去懷疑那些值**；更根本的是那支 `usage_report.py` 在 Cowork 側**根本跑不動**，就算貼了也拿不到數字。**形態是新的**：前七筆是「修法在極端路徑放大傷害」「文件層副作用」「修法只在另一個執行環境失敗」，這一筆是**修法依賴一個在目標執行環境不存在的東西，而且沒有人去確認它存不存在** ——與 `08-16 ⑥` 的差別在於，那次是同一支腳本在另一個 bash 版本失敗、跑了就會看到，這次是**那一步從來沒有被執行過**，所以連失敗都沒發生，只有一欄安靜的錯誤數字。2026-08-23 查證並改成「一律留空」）。**八次。**
  > **`08-22` 這一筆的尾巴拖了八天，2026-08-30 才收乾淨。** 根本解在 **08-24 晚間**就做好了——
  > 量測改成由 `com.kenny.kbscan` **從外面掃**，輪次什麼都不用做，`usage.csv` 自 08-21 起每天都有 podcast 的列。
  > **但排程副本沒有同步到那個解法**，於是 08-25 到 08-30 每一輪都還在照「這一輪量不到」做事，
  > 08-30 的執行者甚至在回報裡寫了「沙箱搆不到 `.claude/projects`」——**而沙箱裡就掛著 `mnt/.claude/projects`**。
  > **同一個 ❌ 的形態因此出現了第二層**：第一層是「換成一個在目標環境不存在的量測」，
  > 第二層是「**修好之後沒有通知執行者**」。兩層都不會報錯，都只是讓一欄安靜地空著。
  > **所以修好一件事之後要問的下一個問題是「誰還在照舊版做事」**，而那個答案在副本裡，不在正本裡。
  > **這個計數與各列儲存格裡的次數不是同一把尺，不要互相核對。** `08-16 ⑥` 的儲存格自己寫過「第七次了」，指的是**該場之內修法留下問題**的次數；本 bullet 數的是**已結案、且修法本身造成新故障**的列數。兩者在 08-16 就已經對不上，**因為本 bullet 只收已結案的列，而當時 `08-16 ⑥` 還掛著 ⏳**。
- **方向對但成效未證實或被吃回去**：`08-03 ③`（⚠️，瘦身兩天內被變更紀錄吃回去）、`08-11`（⚠️，「先估篇幅再落筆」隔天實測完全無效——**方向對，但它依賴的量測是壞的**，08-12 才把單位換成段數）。
- **原本未證實、後來被實測證明成立**：`08-12`（段數規劃，08-14 一次驗收通過——**這是第一筆「一次就成功」**，2026-08-17 由「唯一一筆」改；**該次新增第二筆時只加了新字、沒回頭改這一行，是本節記載的第六輪同型錯誤**）、`08-14`（時間軸檢查，08-15 首次實戰命中：MIB 那集雙訊號同時成立、子代理獨立確認確有兩處跳段，當日 10 集零誤報——**「兩個獨立訊號同時成立」這個設計原則第二次被證明有效**）、`08-08 ②` 與 `08-09 ①` 這一組——架構隔離加上呼叫預算，**08-11 實測同樣五集 970 萬 → 288 萬（降 70%）**，落在當初粗估的 60–75% 區間內，兩列的標記已同步翻為 ✅。`08-09 ①` 的 token 量測那半仍未生效（路徑猜錯兩次，08-10 改為失敗會出聲），該列因此保留一個 ⚠️。**這是登記簿上第一筆從 ⚠️ 翻正的紀錄**：優化的方向判斷對了，但要等到有量測才知道對在哪一段。
  **`08-16 ⑤`（排程自我量測）於 2026-08-17 首次執行即成功，一併收進本類。** 它的價值不只在拿到數字，更在**推翻了一個已經寫成結論的推論**——五次從別場工作階段找不到 transcript，結論寫成「排程不留 transcript」；實際只是**沒有人從排程執行自己的沙箱裡找過**。**「查不到」與「不存在」是兩件事，而前者很容易被寫成後者。**這也是本表第二筆「一次就成功」（前一筆 `08-12`），兩筆的共同點都是**先修量測、再修規則**。**「一次就成功」在這裡指的是「當初列為待驗、隔天首次實跑即通過且無返工」**——`08-14` 形態上也符合，只是當時沒貼這個標籤，**所以這個計數是靠定義收窄得來的，不要拿它當客觀次數**。
  **`08-17 ③`（`snapshot.sh` 的 Mac 解析錯誤）當晚實跑即通過，形態上也屬本類，但刻意不算進「一次就成功」**——它是一行大括號，不是機制；跟 `08-12` 的段數規劃、`08-16 ⑤` 的自我量測放進同一個計數，只會讓那個計數更沒有意義。**它示範的是另一件事：機制存疑時，挑一個「兩種機制都修得好」的改法。** 大括號在任何 bash 版本都對，所以「bash 3.2 到底吃掉幾個位元組」這個診斷精不精確，都不影響修法成立。**這比「先修量測、再修規則」更前面一步——當量測本身做不到時（沙箱複現不了 bash 3.2），就讓修法不依賴那個量測。**
  **`08-22` 的四批於 2026-08-23 首次實戰通過，收進本類**：①A 類官方稿的責任寫回 digest 之後，當天唯一的 A 類**確實抓到了官方稿**（`official_transcripts` 改成 null 之後第一次有人真的去抓）；④收斂規則分開段數與字數之後，**四集全部一次寫對、零收斂**——對照 08-22 主代理自己在組檔那一步跑了 6 輪，**這一批的效果是可量的**；⑤`hosts` 依稿判定確實執行並抓出兩集名單過期；⑥文件指向改到 kb-core 之後不再讀到降級的那一份。**但同一列的②只成立一半**（dupmap 有跑、有進派工單，卻認不得句級跳針），**而那一列的第七批是 ❌**（見第一類）——**同一場的七批可以同時落在三個類別裡，所以標記要標在批而不是場。**

**這三類與 ⚠️ 標記不是一一對應的**，不要用「⚠️ 有幾列」去核對「發生幾次」——第一類有些列標的是 ✅（問題在後續那一列才被抓出來，本列的修法當下成立）。**改標記或加新列時，三個 bullet 都要重讀一次，而且改字與改表必須同一次做完。** 08-09 ②、08-10、08-11、08-12、08-14 **連續五輪**抓到這段列舉與表對不上：08-09 ②與 08-10 是只改表沒改字；08-11 反過來只改字沒改表（宣稱「第一筆翻正」時表上一格都沒動）；08-12 寫了新列與新敘事，卻沒回頭把 08-11 那列結案；08-14 把 08-12 那列翻成 ✅ 了，卻沒把它加進第三類。**五輪、四種排列，所以把它變成檢查清單而不是叮嚀——每次動這張表，依序做完三件事再收工：①把上一列結案（本場驗證了它嗎？）②新增本場那一列 ③三個 bullet 逐條重讀，看有沒有列該進來或該移類。**

**08-09 那次的形態和前三次不同，值得單獨記：** 前三次都是「程式的修法在極端路徑放大傷害」，這次是**文件層的三種副作用**——歸檔搬走了規則、擴欄留下錯位的舊列、以及最隱蔽的一種：**把改動的理由寫成一個程式上不成立的因果**。前兩者靠對讀文件抓得到，第三者不行，只有回去讀程式分支才會發現。**所以每次改「指標」或「門檻」，驗證時要指定子代理去確認那個值實際接到哪條分支。**

---

